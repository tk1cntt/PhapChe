[
  {
    "pattern": "HARDCODED_CONFIG",
    "file": ".gitignore",
    "line": "5-6",
    "issue": "Non-local environment files (e.g., `.env.production`, `.env.staging`, `.env.development`) are not ignored. These files often contain secrets (API keys, database credentials, etc.) and could be accidentally committed, exposing sensitive information in the repository history.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": ".planning/config.json",
    "line": "50-51",
    "issue": "Conflicting configuration: mode 'yolo' (suggesting minimal oversight/autonomous execution) is combined with an extensive validation workflow (plan_check, verifier, nyquist_validation, code_review, node_repair, ui_safety_gate all enabled). If 'yolo' mode internally skips or downgrades these validation steps, the enabled workflow flags become misleading no-ops. If it respects them, then 'yolo' is...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": ".planning/quick/260606-udu-verify-all-screens-with-screenshot-evide/validate-and-capture.cjs",
    "line": "56-61",
    "issue": "Hardcoded credentials (emails and passwords) are embedded directly in source code. If this file is committed to version control, these credentials become exposed to anyone with repository access. These should be loaded from environment variables or a secure secrets manager that is excluded from version control.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": ".planning/quick/260606-udu-verify-all-screens-with-screenshot-evide/validate-and-capture.cjs",
    "line": "136-139",
    "issue": "PrismaClient.$disconnect() is never called when database queries throw an exception. In the `getSamples()` function, the `catch` block at line 168 returns `samples` without calling `prisma.$disconnect()`, which can leave database connections open and eventually exhaust the connection pool. This is especially problematic in CI/CD environments where this script may run repeatedly.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": ".planning/quick/260606-udu-verify-all-screens-with-screenshot-evide/validate-and-capture.cjs",
    "line": "115-118",
    "issue": "PrismaClient is imported inside getSamples() with a dynamic `require()`. If the import fails (e.g., @prisma/client is not installed), the error is silently caught but the `prisma` variable referenced in the `finally` block suggestion above would be undefined. The current code doesn't disconnect at all on the catch path, but even if fixed, the error handling needs refactoring — the `prisma` vari...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": ".planning/quick/260607-jo3-verify-all-screens-with-content-screensh/validate-screens.cjs",
    "line": "18",
    "issue": "Routes `/admin/ops/[requestId]` and `/admin/templates/[templateId]` contain literal placeholders `[requestId]` and `[templateId]` that are never resolved/replaced with actual IDs. The `verifyRoute` function navigates directly to URLs like `/admin/ops/[requestId]`, which will almost certainly result in a 404 or routing error, making the test meaningless for these parameterized routes.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": ".planning/quick/260607-jo3-verify-all-screens-with-content-screensh/validate-screens.cjs",
    "line": "32-36",
    "issue": "Sensitive credentials (email and password) are hardcoded directly in the source code. This is a security risk, especially if the script is committed to version control. Credentials should be loaded from environment variables or a secrets manager.",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": ".planning/quick/260607-jo3-verify-all-screens-with-content-screensh/validate-screens.cjs",
    "line": "88-131",
    "issue": "The `loginWithBrowser` function has a flawed dual-path login logic: 1) It calls `apiSignIn` first but does NOT stop or early-return if the API succeeds — it always proceeds to the browser form login. 2) The API result is only used as a fallback when the browser-form login fails (still on `/sign-in`), which is the opposite of what the comment 'First try API to get token' suggests. 3) Even when A...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": ".planning/quick/260607-jo3-verify-all-screens-with-content-screensh/validate-screens.cjs",
    "line": "118-123",
    "issue": "The cookie domain is hardcoded to `'localhost'` regardless of the actual `BASE_URL`. If the application runs on a different host or port (e.g., `http://127.0.0.1:3000` or `https://staging.example.com`), the cookie injection will silently fail because the domain does not match the current URL's origin, making session injection ineffective.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": ".planning/quick/260607-k0w-update-vietnamese-diacritics/fix-vietnamese.cjs",
    "line": "7-42",
    "issue": "Replacement order is logically inverted: shorter phrases like 'Yeu cau' are listed AFTER longer ones like 'Ho so yeu cau', but the script processes replacements in array order. If 'Yeu cau' runs first, it converts 'Yeu cau' → 'Yêu cầu', which means 'Ho so yeu cau' no longer exists in the source and the longer phrase replacement never fires. The longer phrases should be listed AND processed befo...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_db-test.cjs",
    "line": "17",
    "issue": "Hardcoded password 'Demo@123456' exposed in source code. If this file is accidentally committed to version control, the credential is permanently visible in the repository history. Even for test scripts, secrets should be loaded from environment variables or a dedicated .env entry rather than hardcoded.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "_debug-500.cjs",
    "line": "3-34",
    "issue": "Playwright browser process leaks if an exception is thrown before browser.close(). The browser variable is scoped inside main(), and the outer catch handler at line 31 only logs and exits — it has no access to the browser instance to close it. Over time, leaked chromium processes can exhaust system resources.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "_debug-500.cjs",
    "line": "21-25",
    "issue": "The script does not verify that navigation to /admin/ops succeeded or that the page is actually showing the expected admin page. It simply logs the URL and body text without checking for error indicators (e.g., a 500 error page, an empty body, or a redirect back to sign-in). This significantly reduces the script's diagnostic value for its intended purpose of debugging a 500 error.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_debug-signin-all.cjs",
    "line": "53-58",
    "issue": "Hardcoded test credentials in source code. Passwords in plain text are a security risk if this file is committed to version control. They could be exposed in CI logs, leaked via source control, or accessed by unauthorized personnel.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "_debug-signin-all.cjs",
    "line": "3-5",
    "issue": "Browser instance leaks if an exception is thrown before `browser.close()` is called. Several operations (page.goto, page.locator().fill(), page.locator().click(), etc.) can throw, and the current code does not use try/finally to guarantee cleanup. This can leave Chromium processes running on the host.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "_debug-signin.cjs",
    "line": "32-40",
    "issue": "The `page.on('response', ...)` listener is registered after `page.locator(...).click()` on line 16, so any HTTP responses triggered by the sign-in request (including potential 4xx/5xx errors) will already have been received and will not be captured. Move the listener registration before the click to capture all relevant responses.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_debug-signin.cjs",
    "line": "11-13",
    "issue": "Hardcoded test credentials (`admin.demo@example.test` / `Demo@123456`) are embedded directly in the source code. This risks accidental exposure in version control, CI logs, or screenshots. Consider using environment variables or a dedicated config file that is `.gitignore`-d.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "_debug-signin.cjs",
    "line": "45-47",
    "issue": "If any step in `main()` throws (e.g., navigation timeout, element not found), the `catch` handler calls `process.exit(1)` without closing the Chromium browser. This leaves browser processes orphaned, which can accumulate and exhaust system resources. Use `try/finally` or move `browser.close()` into a `finally` block.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "_debug-signin2.cjs",
    "line": "31-34",
    "issue": "Race condition: console listener registered too late. The page.on('console', ...) listener is added on line 22, after page.goto() on line 12 and form submission on lines 14-17. Any console messages emitted during page load, form interaction, or the post-submit redirect will be silently missed. Additionally, the logs array is never printed, so even if messages were captured, they would not be vi...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_debug-signin2.cjs",
    "line": "20-23",
    "issue": "Brittle sign-in flow with fixed timeout. Using page.waitForTimeout(8000) after clicking submit is fragile. If the server or network is slower than 8 seconds, the script will read page.url() before the redirect completes, producing a false negative. Also, there is no verification that login actually succeeded. Consider using page.waitForURL() with a pattern and generous timeout (e.g., 15s) or pa...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_debug-signin3.cjs",
    "line": "29",
    "issue": "Fixed sleep (`waitForTimeout(5000)`) is fragile and does not guarantee sign-in completed. If the server is slow, the script may check logs before auth completes. Use a more deterministic wait: wait for a post-login URL, a specific element on the dashboard, or `page.waitForResponse()` for the auth API call.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "_fix-accounts.cjs",
    "line": "24-35",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_gen-scrypt.cjs",
    "line": "3",
    "issue": "**Hardcoded absolute path for .env file**: The path 'D:/PhapChe/.env.local' is specific to one developer's Windows machine. This will crash on any other machine (different OS, username, or project location). Use a relative path or the standard `dotenv` convention of looking in the current working directory.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "_gen-scrypt.cjs",
    "line": "43-47",
    "issue": "**No transactional safety**: The update loop at lines 40-44 updates accounts one-by-one without a transaction. If the script crashes or an update fails partway through, some accounts will have new scrypt hashes while others retain old hashes, leaving the credential store in an inconsistent/corrupted state. Wrap the loop in a Prisma interactive transaction so all updates succeed or none do.",
    "fix": ""
  },
  {
    "pattern": "MISSING_TEST",
    "file": "_quick-auth-test.cjs",
    "line": "24-31",
    "issue": "Repeated 'pageerror' listener overwrites previous listener without removal. Each iteration of the loop adds a new listener via page.on('pageerror', ...), but previous listeners are never removed. This means errors from earlier pages bleed into later pages' error reports, and the errors2 array from later iterations will contain stale errors from earlier navigations, making test results unreliable.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "_quick-auth-test.cjs",
    "line": "36",
    "issue": "The error handler at the bottom (test().catch(...)) logs the error but does NOT call process.exit(1). The line `process.exit(1)` appears to be present but is unreachable — it's inside the same catch block after browser.close() in the main flow. Additionally, the browser is NOT closed on error (browser.close() is only called on the happy path at line 27). If any step throws (e.g., goto timeout, ...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "_regen-pw.cjs",
    "line": "5-7",
    "issue": "PrismaClient is created inside main() and only disconnected on the success path. If an error occurs during the loop (e.g., a database connection issue), db.$disconnect() is never called, leaking the database connection. Move db instantiation outside main() or use a finally block.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "_regen-pw.cjs",
    "line": "22-27",
    "issue": "No transaction wrapping for the mass update. If the script fails partway through the loop (e.g., network error, query timeout), some accounts will have the new password while others retain the old one, leaving the system in an inconsistent state. Wrap all updates in a Prisma interactive transaction so they either all succeed or all roll back.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "_run-fix.ps1",
    "line": "10",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_test-better-auth.cjs",
    "line": "1",
    "issue": "Hardcoded absolute path to `.env.local` file. This ties the script to a specific developer's machine (`D:/PhapChe/`) and will fail on other environments. Additionally, using `.env.local` suggests this may be loading sensitive credentials for a local/production database. Consider using a relative path (e.g., `dotenv.config()`) or accepting the env file path via command-line argument or environme...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_test-better-auth.cjs",
    "line": "9-10",
    "issue": "Hardcoded admin email (`admin.demo@example.test`) and providerId (`credential`) directly in source code. If this script is committed to version control, it exposes account metadata and reveals the credential-based authentication mechanism. Consider loading these from environment variables with sensible defaults for testing only.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_test-better-auth.cjs",
    "line": "22",
    "issue": "Hardcoded plaintext password `'Demo@123456'` in source code. If this script is committed to version control, the password is exposed. If logging is enabled, the password (or bcrypt failure) could leak into logs. Remove the hardcoded password and load it from an environment variable.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "_test-better-pw.mjs",
    "line": "4",
    "issue": "Hardcoded absolute path to `.env.local` is machine-specific and breaks portability. If the file is missing, environment variables (e.g., DATABASE_URL) won't load, causing confusing connection errors. Use a relative path: `require('dotenv').config()` with default `.env` resolution, or make the path configurable via an environment variable / CLI argument.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "_test-better-pw.mjs",
    "line": "15-17",
    "issue": "`account.password` is assumed to be a non-null string matching the `salt:key` format. If the stored password is null, undefined, or in an unexpected format (e.g., missing the `:` separator), `hash.split(':')` will produce fewer than 2 elements, causing `keyHex` to be `undefined`. This leads to confusing downstream errors in `Buffer.from` and `console.log`. Add an explicit null/format guard with...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "_test-signin-direct.cjs",
    "line": "57-60",
    "issue": "PrismaClient disconnect may not run if main() throws before reaching line 44. An exception from any query (e.g., connection timeout) will skip `db.$disconnect()`, potentially leaking database connections until the Node process exits. Wrap the main logic in a try/finally block to guarantee disconnect always runs.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "check-pw.cjs",
    "line": "7",
    "issue": "Hardcoded plaintext password 'Demo@123456' in source code. If this file is committed to version control, the credential is permanently exposed in the repository history. Even if this is a test/utility script, hardcoded credentials should be avoided. Consider reading the password from an environment variable (e.g., process.env.TEST_PASSWORD) or a local .env file that is excluded from version con...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "check-pw.cjs",
    "line": "11",
    "issue": "Error handler calls process.exit(1) without disconnecting the Prisma client first. This leaves database connections open, which can exhaust the connection pool on repeated failures and cause resource leaks. Always disconnect before exiting.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "coverage/block-navigation.js",
    "line": "22-29",
    "issue": "`currentIndex` is declared but never initialized. When `toggleClass` is called before `currentIndex` is set (e.g., first invocation of `makeCurrent`), `missingCoverageElements.item(currentIndex)` receives `undefined`, which causes `.classList` to throw a `TypeError: Cannot read properties of undefined`. Initialize `currentIndex` to a valid value (e.g., `-1` or `0`) and guard against unset state.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "coverage/block-navigation.js",
    "line": "41-50",
    "issue": "When `missingCoverageElements.length` is `0`, `goToPrevious` sets `nextIndex` to `-1` (line `missingCoverageElements.length - 1`). This causes `makeCurrent(-1)` to call `missingCoverageElements.item(-1)` which returns `undefined`, leading to a `TypeError` when accessing `.classList`. Guard against empty element lists.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "coverage/clover.xml",
    "line": "42",
    "issue": "Uncovered statements in markitdown.ts lines 41, 99-102, 110 — These are error-handling and fallback paths in the document conversion logic. Line 41 (0 executions) and lines 99-102 (0 executions, with both conditionals also uncovered) represent critical error recovery paths. If these paths contain bugs, they would silently fail in production since no tests exercise them.",
    "fix": ""
  },
  {
    "pattern": "MISSING_TEST",
    "file": "coverage/clover.xml",
    "line": "50-51",
    "issue": "Uncovered conditional at markitdown.ts line 76 (truecount=1, falsecount=1) and uncovered statement at line 77 (0 executions). This branch handles a specific document conversion condition; the 'false' branch is never tested, meaning the behavior when this condition is not met is completely unverified.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "cr-init-45.cjs",
    "line": "1-3",
    "issue": "No error handling for execSync or JSON.parse failures. If gsd-tools exits with a non-zero code or returns invalid/non-JSON output, this script will crash with an unhandled exception (e.g., `Error: Command failed` or `SyntaxError: Unexpected token`). Wrap in try/catch to provide meaningful error output.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "create-demo-users.cjs",
    "line": "22-40",
    "issue": "No transactional integrity between user and account creation. If `db.user.create()` succeeds but `db.account.create()` fails (e.g., constraint violation, network error), the script leaves an orphaned user with no associated account. In a seeded script this may be acceptable on first run, but the lack of a rollback mechanism means partial state can persist. Consider wrapping both operations in a...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "debug-signin.cjs",
    "line": "16-20",
    "issue": "Static wait (waitForTimeout(5000)) masks race conditions. After clicking submit, the script blindly waits 5 seconds regardless of whether the sign-in succeeded, failed, or is still loading. A failed login that renders an error in <5s will go undetected. Instead, wait for a reliable signal such as URL change (page.waitForURL('**/dashboard**')), a specific post-login element, or a success/error t...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "e2e/helpers.ts",
    "line": "18",
    "issue": "Hardcoded locale-prefixed URL paths ('/vi/sign-in', '/vi/admin') are scattered across the file. This violates the project rule prohibiting business-related hardcoded strings. If the locale changes or the app needs to be tested in other locales, these must be updated in multiple places. Extract locale and path segments into constants.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "e2e/login-test.js",
    "line": "15-16",
    "issue": "**Hardcoded Credentials**: Both login test files contain hardcoded email and password in plain text, posing a security risk if committed. Use environment variables for credentials.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "e2e/login-test.js",
    "line": "21-24",
    "issue": "Using a fixed `waitForTimeout(3000)` after form submission introduces a race condition and flaky test behavior. If the server responds slower than 3s, the URL check runs prematurely. Use `page.waitForURL()` or `page.waitForNavigation()` to wait for the actual navigation to complete instead.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "e2e/simple-login.js",
    "line": "42-43",
    "issue": "`page.textContent('body')` returns a string. If the body is empty or the page is unavailable, the result may be null or undefined, causing a TypeError when calling `.includes()`. Add a null check before accessing.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "fix-accounts.cjs",
    "line": "23-33",
    "issue": "The merge/delete logic (lines 21-31) is not wrapped in a transaction. If this script is run concurrently (e.g., multiple instances for different shards), two runs could both see `existing === null`, both attempt to update to the same email accountId, or one could delete a record that the other is processing. Use `db.$transaction()` to make the read-check-update/delete cycle atomic.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "fix-accounts.cjs",
    "line": "25-33",
    "issue": "When both the old account and the existing email account have passwords (i.e., `acc.password` and `existing.password` are both truthy), the old account is silently deleted without any warning about the discarded password. This could silently lose a newer password hash if the old account had a more recent password change.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "fix-accts.cjs",
    "line": "27",
    "issue": "Potential unique constraint violation on update. If another credential account already has accountId set to user.email (e.g., a previous run partially completed), the update at line 26 will throw a Prisma unique constraint error (accountId + providerId). The script has no try/catch around individual updates, so the entire script aborts, leaving remaining accounts unprocessed.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "fix-accts.cjs",
    "line": "10-33",
    "issue": "No error isolation per iteration: if a single merge or delete operation throws (e.g., constraint violation, race condition), the entire script aborts and exits with process.exit(1). Remaining accounts are never processed. The for loop should catch per-iteration errors and continue.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "fix-accts.cjs",
    "line": "35-38",
    "issue": "Hardcoded plaintext credentials: 'admin.demo@example.test' and 'Demo@123456' are embedded in the source. If this script is committed to version control, the credentials leak. The bcrypt output ('PASS'/'FAIL') may also leak into CI logs. Consider reading these from environment variables or a separate config file excluded from git.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "hash-passwords.cjs",
    "line": "3-8",
    "issue": "**Hard-coded plaintext credentials in source**: Demo passwords and emails are embedded directly in the source file. If this script is accidentally committed to version control (or already has been), these credentials become permanently exposed in the repository history. Even for demo accounts, this is a security hygiene issue — these credentials could be used to access any environment where thi...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "init-plan-45.cjs",
    "line": "1-2",
    "issue": "No error handling for child process execution. execSync will throw on non-zero exit codes, spawn failures (e.g., Node not found, missing file), or if the script path doesn't exist. The uncaught error will crash with a full stack trace, potentially leaking internal filesystem paths.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "init-plan-45.cjs",
    "line": "2",
    "issue": "Hardcoded absolute Windows path 'D:/PhapChe/.claude/gsd-core/bin/gsd-tools.cjs' makes this script non-portable: it breaks on other machines, other OSes (Linux/macOS), or if the project is cloned to a different location. Consider resolving the path relative to __dirname or using an environment variable / configuration.",
    "fix": ""
  },
  {
    "pattern": "INCORRECT_IMPORT",
    "file": "layout/admin-org-activity.html",
    "line": "1089-1091",
    "issue": "CSS class typos: 'mini-badge.blue' and 'mini-badge.green' use '.' incorrectly in class attribute, causing the intended styling not to apply. Change those to space-separated 'mini-badge blue' and 'mini-badge green'.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "layout/admin-partner.html",
    "line": "580-592",
    "issue": "All action buttons (Edit, Delete, back links, triage actions, 'Quay lại danh sách tổ chức', critical action buttons, 'Lưu nháp', 'Tiếp tục', 'Quay lại dashboard', 'Xem hồ sơ nháp') have no onclick, type, form, or href attributes, making them completely inert and misleading. Add type='button' to prevent accidental form submission, wire up proper handlers, or use <a> tags with correct routes.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "layout/admin-partner.html",
    "line": "602-611",
    "issue": "All display data is hardcoded demo content (partner names, stats, table rows, activity items, etc.) without any template interpolation or data-binding. This results in stale, non-functional mock data in production. Replace with server-side variable bindings or client-side dynamic rendering.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "layout/admin-request.html",
    "line": "973-978",
    "issue": "**Missing JavaScript/interactivity**: The entire file is a purely static template with no `<script>` tag or JavaScript logic. All toolbar buttons (search, filter, sort, export, refresh, column toggle) and the 'Create request' button are non-functional. Table rows are hardcoded (lines 562-657) and cannot reflect real data from a backend. A production-ready page needs event handling, AJAX/fetch c...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "layout/admin-user-request-all-in-one.html",
    "line": "328-332",
    "issue": "The 'setSelect' function relies on opt.text matching with Vietnamese display text as keys, causing multiple issues: (1) multiple options with the same text can mismatch (e.g., placeholders), (2) text changes break silent failures, and (3) no robust fallback. Use opt.value or data-value attributes with stable IDs for binding instead.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "layout/admin-vault.html",
    "line": "849",
    "issue": "Missing ARIA attributes and keyboard accessibility: interactive elements lack appropriate roles, labels, focus styles, and keyboard handlers (tree pattner, tablist, checkbox, buttons, navigation). Add semantic ARIA roles, aria-label, tabindex, and keyboard handlers to ensure compliance with WCAG for screen reader and keyboard-only users.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "layout/login.html",
    "line": "300-303",
    "issue": "The form lacks CSRF protection. There is no hidden CSRF token input field, making the endpoint vulnerable to cross-site request forgery attacks if the backend expects one. Add a hidden input for the CSRF token.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "layout/user-create-request.html",
    "line": "989-999",
    "issue": "Service option 'radio buttons' are purely visual — each <label> contains a decorative <div class='radio'></div> but no actual <input type='radio'>. The selected state is statically assigned to one option via the CSS class 'selected'. Without JavaScript event handlers, clicking other options has no effect, giving users a false sense that they can change their selection.",
    "fix": ""
  },
  {
    "pattern": "MISSING_TEST",
    "file": "package.json",
    "line": "38-39",
    "issue": "Multiple dependencies in package.json use 'latest' version, which risks non-reproducible builds and breaking changes. This includes runtime dependencies (next, react, react-dom), build tooling (tailwindcss, postcss, autoprefixer), linting (eslint, eslint-config-next, typescript), and type definitions (@types/react, @types/react-dom). Pin all 'latest' dependencies to specific versions to ensure ...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "plan-index.cjs",
    "line": "2",
    "issue": "**Missing error handling for subprocess call.** If the `gsd-tools.cjs` command fails (e.g., non-zero exit, spawn failure, or the file is missing), `execSync` will throw an uncaught exception. Similarly, if the stdout is not valid JSON, `JSON.parse` will throw. This produces a cryptic crash rather than a meaningful error message about what went wrong. Consider wrapping both in try/catch and prov...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "prisma/generate-sample-files.ts",
    "line": "297-299",
    "issue": "Path traversal vulnerability: `storageKey` from the database is concatenated directly into the file path using `join(STORAGE_ROOT, storageKey)` without sanitization. If a malicious `storageKey` value (e.g., `../../../etc/passwd`) exists in the database, the script could write files outside the intended storage root directory. Validate that the resolved path stays within `STORAGE_ROOT` before wr...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "prisma/migrations/20260620184500_add_draft_and_request_fields/migration.sql",
    "line": "60",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "prisma/schema.sql",
    "line": "678-679",
    "issue": "Multiple columns across the schema lack foreign key constraints, leading to potential referential integrity issues. Add FK constraints for: senderId/recipientId (Message) → User; templateId (DocumentVersion) → DocumentTemplate; documentVersionId (Review, VaultItem) → DocumentVersion; workspaceId (LegalRequest) → Workspace; organizationId (AuditEvent, Message) → Organization; userId (FileAccessL...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "prisma/schema.sql",
    "line": "568-574",
    "issue": "Several tables store sensitive tokens as plain TEXT: Account (accessToken, refreshToken, idToken) and Session (token). If the database is compromised, these tokens could be used to hijack user sessions or impersonate users on third-party providers. Encrypt OAuth tokens at rest and hash session tokens with a one-way function.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "prisma/seed-messages.ts",
    "line": "71-75",
    "issue": "Missing transaction: The deletion of old messages and creation of new messages are not wrapped in a Prisma transaction. If any step fails after `deleteMany` (e.g., user creation, message creation, or request update), the database is left in an inconsistent state with all messages deleted and none recreated. Consider wrapping the entire seed logic in `prisma.$transaction()`.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "prisma/seed-timeline.ts",
    "line": "196-222",
    "issue": "Missing transaction for atomic writes: WorkflowTransition and its corresponding AuditEvent are created in separate non-transactional calls. If the AuditEvent creation fails (e.g., network error, constraint violation), the transition will be orphaned with no corresponding audit trail, leading to inconsistent data. Wrap both creates in a Prisma interactive transaction.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "prisma/seed-timeline.ts",
    "line": "238-264",
    "issue": "Same missing-transaction issue: `requestAssignment.create` and its corresponding `auditEvent.create` are not wrapped in a transaction. If the audit event creation fails, the assignment will be orphaned without an audit trail. This applies to both the specialist assignment block (lines ~180-212) and the reviewer assignment block (lines ~215-247).",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "prisma/seed-timeline.ts",
    "line": "279-305",
    "issue": "Same missing-transaction issue in reviewer assignment. The `requestAssignment.create` and `auditEvent.create` in the reviewer assignment block (lines ~240-263) are not wrapped in a transaction. If the audit event creation fails, the reviewer assignment will be orphaned.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "prisma/seed.ts",
    "line": "537-550",
    "issue": "Demo requests are created with `prisma.legalRequest.create` instead of `upsert`. On re-runs of the seed script, this will throw a unique constraint violation if `code` has a unique index (e.g., `DEMO-2026-001`). All other seed data uses `upsert` for idempotency — demo requests should too.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "prisma/seed/foundation.ts",
    "line": "107-112",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "prisma/seed/operations.ts",
    "line": "222-224",
    "issue": "Fragile hardcoded user index assumptions: `userIds[i % 3 + 7]`, `userIds[i % 2 + 3]`, `userIds[i % 2 + 5]`, and the message sender/recipient indices `7` and `3` all assume the user seed produces IDs in a fixed order (customers at indices 7-9, specialists at 3-4, reviewers at 5-6). If the user seed ordering changes, roles will be assigned incorrectly, producing invalid test data. Consider extrac...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "scripts/backfill-matter-type-id.ts",
    "line": "37-83",
    "issue": "Sequential async processing in loop: each request's lookup and update is independent of others. For large datasets, this will be extremely slow. Use `Promise.all` with batched concurrency (e.g., process in chunks of 10–50) to parallelize the independent operations while avoiding overwhelming the database.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "scripts/backfill-request-assignment.ts",
    "line": "33-77",
    "issue": "Partial updates on error: the outer try-catch for each `requestId` catches any update failure and breaks out of the inner loop immediately. This leaves remaining assignments of the same request unprocessed (inconsistent data), and the `errors` counter only increments once per request regardless of how many assignments failed.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "scripts/backfill-vault-item-file-id.ts",
    "line": "36-65",
    "issue": "Sequential async operations in a loop: each vault file triggers two independent Prisma calls (findFirst + update) one after another. For large datasets this is very slow. Consider batching with Promise.all (e.g., process in chunks of 50–100) to parallelize independent lookups and updates, or use a single bulk UPDATE … FROM … WHERE join query.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "scripts/capture-i18n-screenshots.mjs",
    "line": "56-57",
    "issue": "Fragile sign-in locators: The login function uses hardcoded placeholder values 'Email' and 'Mật khẩu' (Vietnamese for 'Password') to locate input fields. These placeholders are locale-specific and will fail if the sign-in page is rendered in a different language, or if the placeholders are changed in a future UI update. Use semantically stable selectors such as `input[type='email']`, `input[typ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "scripts/capture-i18n-screenshots.mjs",
    "line": "78-81",
    "issue": "No error page detection: Screenshots are taken regardless of HTTP status code or page content. Error pages (404, 500, custom error pages) are reported as successful captures, producing misleading results. Verify the page is not an error page before taking the screenshot, for example by checking for error-specific DOM elements or validating the URL did not redirect to an error route.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "scripts/create-demo-user.ts",
    "line": "7",
    "issue": "Logging the cookie header exposes sensitive authentication tokens (e.g., session cookies, JWTs). Even the first 100 characters may contain a complete token. Remove this log or, if needed for debugging, redact the value entirely and only log its presence/length.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "scripts/create-demo-user.ts",
    "line": "4-7",
    "issue": "The `headers()` call from `next/headers` throws if not executed within a Next.js request context. Since this is a standalone script (`scripts/create-demo-user.ts`), it will almost certainly crash here with an unhandled error before reaching the session retrieval. Wrap this in a try-catch with a clear error message, or use a different approach to obtain headers in a script context.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "scripts/generate-component-registry.mjs",
    "line": "45-47",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "scripts/generate-component-registry.mjs",
    "line": "49-51",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "scripts/generate-component-registry.mjs",
    "line": "42-43",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "scripts/migrate-existing-data.ts",
    "line": "19-24",
    "issue": "The entire migration is not wrapped in a database transaction. If any single update fails mid-way, the database is left in a partially migrated state with no way to roll back automatically. For example, if 50 MatterTypes are updated successfully but the 51st fails, those 50 updates are already committed and cannot be undone without manual intervention.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "scripts/recreate-user.ts",
    "line": "10-24",
    "issue": "Data integrity risk: `deleteMany` removes the credential account before `signUpEmail`, but if `signUpEmail` fails, the user is left in an inconsistent state with no credential account. The catch block only logs the error and does not re-throw, so the script exits with code 0 (success) even on failure. Consider wrapping the entire delete+recreate in a transaction, or at minimum re-throw the erro...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "scripts/screenshot-all.mjs",
    "line": "39",
    "issue": "Hardcoded sensitive session user ID in source code. The value `cmpxf18fd00014wj8qe9ndirk` appears to be a database ID used for authentication impersonation. If committed to a repository, this leaks a credential that could allow unauthorized access to the application.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "scripts/screenshot-all.mjs",
    "line": "59-62",
    "issue": "The dev server process is never killed if `chromium.launch()` or `browser.newContext()` fails. This leaves an orphaned Next.js process holding port 3001 indefinitely, which will cause subsequent runs to fail with port conflicts.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "scripts/seed-db.sh",
    "line": "15",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "scripts/seed-documents.ts",
    "line": "345-385",
    "issue": "Unhandled per-request errors abort the entire script. If any single `document.create` or `documentVersion.create` fails (e.g., constraint violation, DB error), the script crashes immediately, leaving partial data and no retry mechanism. Wrap each iteration in a try-catch so one failure doesn't block all subsequent requests.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "scripts/seed-organizations.js",
    "line": "25-27",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "scripts/seed-partner-requests.js",
    "line": "141-145",
    "issue": "Partial database seeding on failure: if any single LegalRequest create fails inside the for-loop (line 105-119), all previously created requests remain in the database. Since the script crashes at the first failure, the database will be in an inconsistent partial state with no rollback mechanism. Consider wrapping the loop in a transaction, using `prisma.$transaction`, or adding individual try-...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "scripts/test-dashboard.js",
    "line": "20-21",
    "issue": "Race condition: the setTimeout callback calls both req.destroy() and cb(new Error('timeout')), but if a response arrives concurrently, the success callback (cb(null, statusCode, body)) may also fire. This results in double callback invocation — once with a timeout error and once with success — which can corrupt downstream logic.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "scripts/transform-question-labels.mjs",
    "line": "137-141",
    "issue": "**High: Regex requires label to be followed by `required` or `type` — silently skips non-conforming labels.** The capturing group `(,\\\\s*(?:required|type):)` makes the regex fail to match any label that is not immediately followed by one of those two property names. If a question object has a different property order, a trailing comment, or an additional property between `label` and `required`/`...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "scripts/transform-question-labels.mjs",
    "line": "128-131",
    "issue": "**High: Fragile exact-string interface replacement silently fails on formatting differences.** The replacement uses an exact multiline string match including whitespace. If the source file uses different indentation (e.g., 4 spaces vs 2, tabs), has trailing whitespace, or has different line endings (CRLF vs LF), the match will silently fail and the interface will remain unchanged — yet the labe...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "scripts/verify-data-consistency.ts",
    "line": "55-61",
    "issue": "The `groupBy` `_count` query does not correctly count only `isCurrent=true` records. In Prisma, `_count: { isCurrent: true }` counts non-null values of `isCurrent`. Since `isCurrent` is `Boolean` (non-nullable per the schema), `COUNT('isCurrent')` equals `COUNT(*)` — counting ALL records in the group, not just those where `isCurrent` is `true`. This means the query detects (requestId, kind) pai...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/admin/dashboard/page.tsx",
    "line": "148-154",
    "issue": "Inaccurate `approvedTodayCount`: The query includes both `approved` AND `revision_required` statuses, but the variable name and the translation key `statApprovedToday` suggest it only counts approvals. This causes misleading statistics — revisions are labeled as 'approved today' in the UI.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/admin/dashboard/page.tsx",
    "line": "179-184",
    "issue": "Double-counting in reviewer stats total: `revision_required` items updated today are counted in both `approvedTodayCount` (line 115) and `revisionRequiredCount` (line 117). The total at line 127 (`pendingCount + approvedTodayCount + revisionRequiredCount`) will inflate the count for any revision_required items updated today.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/[locale]/admin/layout.tsx",
    "line": "72-74",
    "issue": "**Dashboard sub-route permission bypass**: Any route under `/admin/dashboard/*` (e.g., `/admin/dashboard/settings`, `/admin/dashboard/reports`) resolves to `routeKey === 'dashboard'`, which is unconditionally skipped by the check `routeKey !== 'dashboard'`. This means all dashboard sub-routes are accessible without `canAccessRoute` verification. If there are sensitive sub-routes under dashboard...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/admin/layout.tsx",
    "line": "98-103",
    "issue": "**Catch-all silently redirects all errors to sign-in**: Any non-redirect error (e.g., Prisma connection failure, database timeout, programming error) is caught and the user is redirected to `/sign-in`. This is confusing because the user sees an authentication prompt for a server error. It also hides the real error from monitoring/APM tools. Consider logging the error and redirecting to a generi...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/admin/organizations/[id]/page.tsx",
    "line": "65-67",
    "issue": "No error handling for database queries. All Prisma calls are executed inside a single `Promise.all` without try-catch. If any query fails (e.g., database connection error, timeout), the entire page crashes with an unhandled rejection, exposing internal error details to the user. Wrap the Promise.all in a try-catch and return a user-friendly error state.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/admin/organizations/[id]/page.tsx",
    "line": "336-337",
    "issue": "Incorrect SLA risk badge logic: `slaRiskRequests` is a global count across all workspaces, but `hasSlaRisk` uses it for each individual workspace. If ANY workspace in the organization has SLA risks, ALL workspaces will incorrectly show the 'SLA risk' badge. Compute per-workspace SLA risk counts instead.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/[locale]/admin/organizations/[id]/page.tsx",
    "line": "177-181",
    "issue": "Sensitive data exposure: user emails and names are included in `memberCards` sent to the client component without any role-based filtering. Combined with the missing authorization check, this leaks personal data (PII) of all workspace members. Consider filtering sensitive fields or ensuring proper authorization is enforced.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/app/[locale]/admin/organizations/page.tsx",
    "line": "111-121",
    "issue": "Race condition: `fetchData` has no `AbortController`. When the user rapidly changes pages, types in the search box, or clicks refresh, multiple concurrent requests race. A later request could resolve before an earlier one, causing stale/incorrect data to be displayed. Use an `AbortController` and abort the previous request before starting a new one.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/app/[locale]/admin/partner/[id]/partner-detail.css",
    "line": "126-132",
    "issue": "The hero section pseudo-element gradient (::before) has no z-index set. Since it uses `position: absolute` with `inset: 0 0 auto 0`, it sits at the default stacking context level. If child elements like `.request-hero-main` or `.hero-stat-grid` have background colors (they do — `#ffffff` on the parent), the gradient bar may render behind them and become invisible. Add `z-index: 1` (or a value a...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/admin/partner/[id]/partner-detail.css",
    "line": "91-103",
    "issue": "Buttons and interactive elements lack `:focus-visible`, `:active`, and `:disabled` states. This affects keyboard navigation users who rely on focus outlines for orientation, and users who need disabled state feedback. All interactive elements (`.back-link`, `.ghost-btn`, `.primary-btn`, `.danger-btn`, `.small-btn`, `.document-btn`, `.btn-retry`, `.upload-area`, form elements) should define thes...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/admin/requests/[id]/chat/page.tsx",
    "line": "210-224",
    "issue": "Optimistic state updates without error rollback: `handleAcceptAnnotation` updates `aiAnnotations` and `handleDismissAnnotation` filters out an annotation from `aiAnnotations` *before* the API call completes. If the PATCH/DELETE request fails, the UI shows incorrect state (annotation appears resolved or removed) while the server state is unchanged. The empty catch blocks silently swallow the err...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/admin/requests/[id]/chat/page.tsx",
    "line": "226-236",
    "issue": "Optimistic state updates without error rollback: `handleDismissAnnotation` removes the annotation from `aiAnnotations` before the DELETE request completes. Same issue as `handleAcceptAnnotation` — if the API call fails, the annotation disappears from the UI but still exists on the server. Add rollback logic in the catch block.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/[locale]/admin/requests/[id]/page.tsx",
    "line": "79-105",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/app/[locale]/admin/requests/page.tsx",
    "line": "33-35",
    "issue": "When `visibleTabs` changes (e.g., user roles are updated), the `activeTab` state may still hold a value for a tab that is no longer in `visibleTabs`. Since `effectiveTab = activeTab ?? firstTab`, a non-null `activeTab` pointing to a now-invisible tab will be used as-is, causing that tab's panel to render without a corresponding tab button in the nav bar. This creates a UI inconsistency where th...",
    "fix": ""
  },
  {
    "pattern": "TYPE_SAFETY",
    "file": "src/app/[locale]/cases/page.tsx",
    "line": "63-64",
    "issue": "**Excessive `as any` type assertions**: Multiple uses of `as any` on Prisma `where` clauses bypass TypeScript's type safety, hiding potential type mismatches (e.g., `overdueBase` passed to `getWorkspaceRequestWhere` and its result to `prisma.legalRequest.count`). Define proper types or use a type-safe overload instead of casting to `any`.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/cases/page.tsx",
    "line": "41",
    "issue": "**Workspace ID defaults to empty string**: When `activeWorkspaceId` is `null`/`undefined`, `wsId` becomes `''` (empty string), which may not be a valid workspace ID and could cause Prisma to return incorrect results or errors. Consider handling the missing workspace case explicitly (e.g., redirect or show an empty state).",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/create/page.tsx",
    "line": "31-56",
    "issue": "Database queries (`prisma.user.findUnique` and `prisma.workspace.findMany`) are not wrapped in try/catch. If either query fails (e.g., database connection error), the unhandled promise rejection will cause a 500 error with no user-friendly fallback. Consider wrapping these in error handling to provide a graceful degradation or meaningful error page.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/dashboard/page.tsx",
    "line": "47",
    "issue": "**Empty workspace fallback is dangerous**: When `activeWorkspaceId` is `null`/`undefined`, `wsId` becomes an empty string `''`. This empty string is used in all `getWorkspaceRequestWhere(wsId, userId)` calls and Prisma `where: { workspaceId: wsId, ... }` clauses. This may return unfiltered data across all workspaces or cause queries to behave unexpectedly. Instead, explicitly handle the missing...",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/app/[locale]/dashboard/page.tsx",
    "line": "60-79",
    "issue": "**Dead code — unused `requestsWhere` variable**: The 6th element `requestsWhere` (from the 5th `getWorkspaceRequestWhere(wsId, userId)` call) is destructured but never used anywhere in the component. The same where clause is fetched again inside the inline IIFE at index 7, resulting in a redundant database/function call. Remove the unused destructured variable and the extra `getWorkspaceRequest...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/dashboard/page.tsx",
    "line": "150-151",
    "issue": "**Nested ternary expression**: The `assigneeRole` computation uses a nested ternary (`a ? b : c ? d : e`), which is prohibited by the review checklist. Nested ternaries are hard to read and error-prone. Extract into a helper or use `if/else` logic.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/dashboard/page.tsx",
    "line": "379",
    "issue": "**`vaultDocs` uses truncated count (max 10)**: `recentDocuments` is fetched with `take: 10`, so `recentDocuments.length` will never exceed 10. This is not the actual total count of vault documents. Use a separate `prisma.vaultFile.count()` query (or add it to the `Promise.all`) to get the true total.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/layout.tsx",
    "line": "19",
    "issue": "Missing error handling for `getMessages()` — if this async call fails (e.g., missing or corrupted messages file, network issue in some setups), the entire layout will throw an unhandled rejection, resulting in a 500 error page with no user-friendly fallback. Wrap it in a try/catch and provide a graceful fallback (e.g., render an error boundary, show a localized error message, or redirect to an ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/[locale]/messages/page.tsx",
    "line": "62-64",
    "issue": "`activeWorkspaceId` falls back to empty string (`''`) in all Prisma `where` clauses (lines 69, 72, 79, 107). If `activeWorkspaceId` is null/undefined, queries with `workspaceId: ''` will match no records (since workspace IDs are unlikely to be empty strings), silently returning empty results. This masks the absence of an active workspace and produces a misleading empty UI instead of surfacing t...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/[locale]/page.tsx",
    "line": "4-5",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/partner/dashboard/page.tsx",
    "line": "23-48",
    "issue": "Missing error handling for all three Prisma database queries. If any of these queries fail (e.g., DB connection issue, timeout), the page will crash with an unhandled rejection, resulting in a 500 error with no user-friendly fallback. Wrap these queries in a try/catch block and provide a graceful error UI or redirect to an error page.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/[locale]/partner/settings/page.tsx",
    "line": "16-41",
    "issue": "Missing error handling for async operations: `auth.api.getSession`, `prisma.partnerMember.findFirst`, and `prisma.user.findUnique` all lack try/catch. If any of these fail (e.g., DB connection issues, auth service errors), the page will crash with an unhandled 500 error, potentially leaking stack traces to the client. Wrap these calls in a try/catch or use an error boundary to gracefully handle...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/[locale]/settings/page.tsx",
    "line": "13-19",
    "issue": "**Missing error handling for async database calls**: All four `prisma` calls (`findUnique`, `legalRequest.count`, `auditEvent.count`, `userPreferences.count`, `workspace.count`) are unprotected. If any query fails (e.g., database connection loss, timeout, or schema mismatch), the error will propagate unhandled through the server component, resulting in a 500 error page with no user-friendly fee...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/workspace/page.tsx",
    "line": "38-58",
    "issue": "Missing error handling: the entire async function lacks try/catch. If any Prisma query fails (network error, DB timeout, invalid where clause), the unhandled rejection will result in a raw 500 error without user-friendly feedback. Wrap the data-fetching logic in a try/catch and return or render an appropriate error state.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/[locale]/workspace/page.tsx",
    "line": "36",
    "issue": "Dangerous fallback to empty string: if both `workspace?.id` and `activeWorkspaceId` are null/undefined, `wsId` becomes `''`. All subsequent Prisma queries will use `{ workspaceId: '' }`, which may silently match unintended records or return empty results. Consider aborting early (e.g., redirect or show an error) when no valid workspace ID is available.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/audit/route.ts",
    "line": "33",
    "issue": "No upper bound on the `page` parameter. An attacker can request an extremely large page number (e.g., `page=999999999`), causing the database to compute a massive offset. This can lead to severe performance degradation or denial of service. Consider capping the maximum page number (e.g., `Math.min(1000, …)`) or using cursor-based pagination.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/admin/operations/route.ts",
    "line": "49-52",
    "issue": "The catch-all handler returns HTTP 500 for **every** error, including authentication/authorization failures thrown by `requireAppSession()` (e.g., expired or missing session). Those should propagate as 401/403 rather than being masked as 500, which misleads clients and monitoring systems.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/organizations/[id]/route.ts",
    "line": "231-244",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/admin/partner/requests/[id]/comments/route.ts",
    "line": "150-173",
    "issue": "Message creation and audit event are not wrapped in a transaction. If `auditEvent.create` fails (e.g., DB error), the comment is already persisted without an audit trail, violating data integrity. Use `prisma.$transaction` to make both operations atomic.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/admin/partner/requests/[id]/route.ts",
    "line": "57-59",
    "issue": "Unsafe type assertion `request as { matterTypeRef?: ... }` bypasses TypeScript type checking. If the `matterTypeRef` relation shape changes or the Prisma schema is refactored, this cast will silently produce `undefined` at runtime instead of a compile-time error. Consider using a properly typed approach — either define the return type explicitly or use a type guard / conditional narrowing based...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/app/api/admin/partner/requests/[id]/status/route.ts",
    "line": "87-104",
    "issue": "The status update (line 87) and audit event creation (line 92) are not wrapped in a transaction. If the audit insert fails, the request status has already been changed but the client receives an error, leading to an inconsistent state where the status change is persisted without any audit trail.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/admin/partner/requests/route.ts",
    "line": "90-93",
    "issue": "The 500 error response includes `detail: error?.message`, which can leak internal error details (e.g., database error messages, stack traces, or Prisma error internals) to API consumers. This is a security risk that can aid attackers in reconnaissance.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/partners/[id]/route.ts",
    "line": "176-183",
    "issue": "Fabricated `docSize` using `Math.random()` for vault_file audit events. The actual file size should be queried from the database, not randomly generated.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/partners/[id]/route.ts",
    "line": "225-232",
    "issue": "Multiple fabricated statistics in the response: `documents: Math.floor(activeRequests * 1.5)`, `workspaces: partner.engagements.length * 2`, and `pendingDocs: Math.floor(activeRequests * 0.5)` are all computed from arbitrary multipliers rather than actual database counts. This returns incorrect data to clients.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/partners/[id]/route.ts",
    "line": "255",
    "issue": "Fabricated `pendingDocs` in the capacity object using `Math.floor(activeRequests * 0.5)`. This should be derived from an actual database query instead of an arbitrary multiplier.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/admin/partners/[id]/route.ts",
    "line": "256-259",
    "issue": "The `slaRisks.requests` field uses `recentRequests.slice(0, 2)` to represent SLA-at-risk requests, but `recentRequests` is not filtered by SLA risk — it's just the 20 most recently updated requests. This means the displayed SLA risk requests are not the actual SLA-at-risk requests, misleading users.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/app/api/admin/partners/route.ts",
    "line": "41-47",
    "issue": "Fetching all `assignedRequests` rows (with `select: { id: true }`) solely to compute `partner.assignedRequests.length` is wasteful — it loads full rows from the database and consumes bandwidth. Use Prisma's `_count` in the `include` (like `members` and `engagements` above) to get the count without loading individual records.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/requests/[id]/assign/route.ts",
    "line": "85-103",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/requests/[id]/assign/route.ts",
    "line": "34-38",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/requests/[id]/chat/route.ts",
    "line": "231-232",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/requests/[id]/chat/route.ts",
    "line": "540-545",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/admin/requests/[id]/files/[fileId]/ai-review/route.ts",
    "line": "320-330",
    "issue": "**Data integrity: annotation deletion before creation without transaction**: Old AI annotations are deleted (lines 215-220) before the creation loop (lines 225-261). If the loop fails partway — e.g., due to a DB error or process crash — the document ends up with no old annotations and only partial new ones, leaving an inconsistent state. Consider wrapping deletion and creation in a Prisma inter...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/app/api/admin/requests/[id]/files/[fileId]/preview/route.ts",
    "line": "503-507",
    "issue": "**Error message leak in top-level catch**: The top-level error handler exposes `detail: msg` to the client. This can leak stack traces, database errors, or other sensitive internal information.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/requests/[id]/files/[fileId]/preview/route.ts",
    "line": "104-132",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/requests/[id]/files/[fileId]/preview/route.ts",
    "line": "474-484",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/admin/requests/[id]/files/annotations/[annotationId]/route.ts",
    "line": "70-74",
    "issue": "**Race condition in review status update**: The `count` query and subsequent `upsert` are not atomic (lines 70-88). If two concurrent PATCH requests change annotation statuses on the same file, they could both read stale `openCount` values and produce an inconsistent `documentReviewStatus`. Wrap the count + upsert in a Prisma `$transaction` with appropriate isolation level, or use `prisma.$tran...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/requests/[id]/files/annotations/[annotationId]/route.ts",
    "line": "147-157",
    "issue": "**Incomplete review status update on DELETE**: The DELETE handler only updates the current reviewer's `documentReviewStatus` when `openCount === 0`, and only transitions `has_issues` → `reviewed`. It misses two cases: (1) When `openCount > 0` after deletion, other reviewers' statuses may need to remain `has_issues` — but the current code does nothing, which is actually fine only if the status w...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/app/api/admin/requests/[id]/files/annotations/route.ts",
    "line": "66",
    "issue": "Error responses leak internal error details to clients via the `detail` field. In production, raw error messages can expose sensitive information such as database schema details, file paths, or internal logic. Never return raw error messages to the client.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/requests/[id]/files/annotations/route.ts",
    "line": "49",
    "issue": "Potential null reference: `a.author.name` is accessed without a null check. If the author relation is null (e.g., user was deleted), this will throw a runtime error and return a 500 response. The Prisma `include` can return null if the foreign key references a deleted record.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/requests/[id]/files/annotations/route.ts",
    "line": "137",
    "issue": "Potential null reference: `annotation.author.name` is accessed without a null check after POST create. Same issue as the GET handler — if the author relation resolves to null, this will throw a runtime error.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/app/api/admin/requests/[id]/files/review-status/route.ts",
    "line": "46",
    "issue": "Error responses expose raw error messages (`detail: msg`) to the client. This can leak sensitive information such as database connection strings, internal paths, or Prisma schema details. Production error responses should return generic messages only.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/admin/requests/[id]/files/review-status/route.ts",
    "line": "24",
    "issue": "`session.roles` is cast as `string[]` without a null/undefined guard. If `session.roles` is `undefined` or `null`, calling `.includes()` will throw a `TypeError` that bypasses the role check and returns a 500. This should be defensively handled.",
    "fix": ""
  },
  {
    "pattern": "TYPE_SAFETY",
    "file": "src/app/api/admin/requests/[id]/files/route.ts",
    "line": "90-92",
    "issue": "ID prefix mismatch: the code comment on line 82 states that Document items are prefixed with `doc_`, but the actual mapping uses `gen_`. This inconsistency will break any frontend consumer that relies on the documented `doc_` prefix to identify document-type items.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/admin/requests/[id]/files/route.ts",
    "line": "31",
    "issue": "Unsafe cast of `session.roles`: `session.roles` is cast to `string[]` without checking if it is null, undefined, or not an array. If `session.roles` is not a valid array, calling `.includes()` will throw a runtime TypeError, resulting in a 500 response instead of a proper 403.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/admin/requests/[id]/report/route.ts",
    "line": "53-57",
    "issue": "Inconsistent summary counts: `criticalIssues`, `warnings`, `suggestions`, `questions`, and `totalAnnotations` are computed from the full `annotations` array, but the report content (via `byFile`) uses `filteredAnnotations`. When `includeResolved` is `false`, the summary shows counts for ALL annotations while the content only displays open ones. These counts should be derived from `filteredAnnot...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/requests/[id]/report/route.ts",
    "line": "250-261",
    "issue": "Inconsistent summary counts in LLM path: the summary counts are computed from the unfiltered `annotations` array, while the LLM prompt (`annotationsText`) respects `includeResolved`. The summary should reflect what the LLM actually analyzed, not the full annotation set. Consider using the same filtered set for both.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/admin/requests/[id]/route.ts",
    "line": "18-25",
    "issue": "Missing workspace-level authorization: Users with any allowed role (including specialist and reviewer) can access any legal request across all workspaces. This violates data isolation — specialists and reviewers should typically be scoped to requests within their own workspaces. Consider adding a workspace filter (e.g., using the session user's workspace memberships) to the Prisma query.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/requests/[id]/route.ts",
    "line": "26-31",
    "issue": "Ambiguous lookup via OR condition: Using `findFirst` with `OR: [{ id }, { code }]` can return the wrong request if the provided `id` parameter happens to match another request's `code` field. This could lead to incorrect data exposure. If the intent is to support lookup by both database ID and human-readable code, prefer a two-step approach: first try matching by primary key, and only fall back...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/admin/requests/[id]/status/route.ts",
    "line": "55-56",
    "issue": "Sensitive information disclosure: the catch block returns String(error) in the 'detail' field of the response. This may expose stack traces, internal file paths, database queries, or other implementation details to the client.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/requests/[id]/timeline/route.ts",
    "line": "69-77",
    "issue": "Ambiguous ID resolution via `findFirst` with `OR: [{ id }, { code: id }]` can silently return the wrong request. If a user passes a value that matches another request's `code` field, they may access an unintended request's timeline, resulting in data leakage. Since the `code` field is typically user-facing and may not be globally unique, this is a real risk. Consider requiring explicit ID looku...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/requests/[id]/timeline/route.ts",
    "line": "69-81",
    "issue": "The query fetches `workspaceId` from the legal request but never validates that the current user has access to that workspace. A user with a valid role (e.g., `specialist` in workspace A) could access timeline data for requests belonging to workspace B by guessing or enumerating request IDs. This is a cross-workspace data leakage vulnerability. Either add a workspace membership check against th...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/admin/requests/delivery/route.ts",
    "line": "14-19",
    "issue": "Missing workspace-level access control. The endpoint checks for `coordinator_admin` role but does not filter requests by the session's workspace. A coordinator in workspace A can see all legal requests across all workspaces. Consider scoping the `where` clause to the session's workspace(s) or adding a workspace filter parameter.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/requests/route.ts",
    "line": "143-149",
    "issue": "Null pointer risk: `req.workspace` and `req.createdBy` can be null if the related records don't exist (e.g., workspaceId is null on the legal request, or createdBy relation is missing). Accessing `.name`, `.slug`, `.name`, `.email` on null will throw a runtime error, crashing the entire request with a 500. Add optional chaining and fallback values.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/app/api/admin/requests/triage/route.ts",
    "line": "80",
    "issue": "**Bug: Unstable fallback request code.** The fallback `code` uses `index` (the page-relative array position, 0 to pageSize−1), so the same request without a stored `code` will display a different identifier depending on which page it appears on. This breaks idempotent display and can confuse users. Use a stable identifier such as the request ID or `skip + index + 1`.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/users/[id]/route.ts",
    "line": "224-243",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/users/[id]/route.ts",
    "line": "396-401",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/admin/users/route.ts",
    "line": "144",
    "issue": "The `password` field is destructured from the request body but never used when creating the user. Users are created without a password, which is a security vulnerability — accounts can be created without authentication credentials. Either implement password hashing and storage, or remove the unused field from destructuring.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/admin/users/route.ts",
    "line": "163",
    "issue": "The `role` parameter from the request body is not validated against allowed roles. An arbitrary string can be stored in the membership role, which could break role-based access control. Validate `role` against a defined set of allowed roles before creating the membership.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/workspaces/[id]/route.ts",
    "line": "41-45",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/workspaces/[id]/route.ts",
    "line": "47-49",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/admin/workspaces/route.ts",
    "line": "36-40",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/admin/workspaces/route.ts",
    "line": "98-103",
    "issue": "**Race condition — check-then-create on slug uniqueness is not atomic**: Two concurrent requests with the same slug can both pass the `findUnique` check before either `create` executes, causing a database unique-constraint violation (Prisma error `P2002`) that results in an unhandled 500 error. Either catch the `P2002` error and return a friendly 409 response, or use a database-level atomic app...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/ai/analyze/route.ts",
    "line": "128-143",
    "issue": "**Fragile error handling**: The catch block matches `error.message` against exact strings like `'LLM_API_KEY_MISSING'` and `'LLM_RATE_LIMIT'`. If the skill executor changes its error messages (e.g., a prefix, localization, or rewording), these checks silently fall through to a generic 500, masking the real cause. Consider using custom error classes (e.g., `class LlmApiKeyMissingError extends Er...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/ai/init/route.ts",
    "line": "27-34",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/ai/init/route.ts",
    "line": "12-14",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/auth/session-role/route.ts",
    "line": "22-24",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/debug-session/route.ts",
    "line": "5-12",
    "issue": "This debug endpoint has no environment guard and will be accessible in production. It exposes internal session state, user membership details, and database query results that could aid an attacker in reconnaissance. Wrap the handler with a check such as `NODE_ENV !== 'development'` or a feature flag.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/debug-session/route.ts",
    "line": "55-61",
    "issue": "The error response returns HTTP 200 (default for `NextResponse.json()`). This is misleading — clients and monitoring tools will treat the error as a success. Use `{ status: 500 }` or an appropriate error status code.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/files/[id]/access-logs/route.ts",
    "line": "40-41",
    "issue": "**Bug: `parseInt` on non-numeric strings returns `NaN`**, which propagates into `storageServer.getAccessLogs` and the `meta.totalPages` calculation (`Math.ceil(result.total / NaN)` → `NaN`). For example, `?page=abc&pageSize=xyz` bypasses validation entirely. Use `isNaN` guards or `Number.isNaN` to validate, and fall back to defaults when parsing fails.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/files/[id]/access-logs/route.ts",
    "line": "55-69",
    "issue": "**Security: Any workspace member can view access logs of any file.** Access logs may contain sensitive data (e.g., IP addresses, timestamps of sensitive file access). The current check only verifies workspace membership, not file ownership or admin role. Consider restricting to the file owner, workspace admins, or users with explicit share permissions.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/files/[id]/download/route.ts",
    "line": "80-81",
    "issue": "If `storageServer.getDownloadUrl()` (line 79) throws, the entire download fails even though the file content was already successfully fetched and buffered. This call appears to be for access logging, but its name ('getDownloadUrl') suggests it may perform network requests or other side effects. The download should not be blocked by a logging failure. Wrap this call in a try/catch or use `.catch...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/files/[id]/route.ts",
    "line": "116-117",
    "issue": "The DELETE handler fetches the file record and performs its own role-based authorization check, then calls `storageServer.deleteFile(id, session.user.id)`. Inside `storage.service.ts`, `checkWorkspacePermission` is called again (line 240), which only checks if the user is a workspace member — not whether they have admin/coordinator role. This means the role check in the route handler is effecti...",
    "fix": ""
  },
  {
    "pattern": "INCORRECT_IMPORT",
    "file": "src/app/api/files/[id]/route.ts",
    "line": "11",
    "issue": "Imports for `FileNotFoundError` and `FilePermissionError` are missing from this file. If the error-matching is fixed to use `instanceof` checks (as recommended), these imports must be added from `@/lib/storage/types`.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/files/route.ts",
    "line": "70-72",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/intake/attach-file/route.ts",
    "line": "7-9",
    "issue": "Authentication errors from `requireAppSession()` (e.g., expired session, missing token) fall into the generic catch block and return HTTP 500. This is misleading — unauthenticated/unauthorized requests should return 401/403. The caller cannot distinguish a server error from an auth failure, and monitoring tools will be confused by 500s for expected auth rejections.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/intake/create-draft/route.ts",
    "line": "38-47",
    "issue": "**Bug: Unreliable ordering for workspace-specific MatterType.** Using `orderBy: { workspaceId: 'desc' }` with nullable `workspaceId` is database-dependent. In PostgreSQL, `NULL` sorts last by default in ascending order but behavior varies in descending order depending on `NULLS FIRST/LAST` settings. This can cause a global MatterType (workspaceId: null) to be selected over the workspace-specifi...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/intake/create-draft/route.ts",
    "line": "12",
    "issue": "**Bug: Non-null assertion on `activeWorkspaceId` without runtime guard.** If `session.activeWorkspaceId` is unexpectedly `null` or `undefined`, the `!` assertion silences TypeScript but passes `undefined` to the database at runtime, causing a confusing error downstream. Add an explicit runtime check with a clear error message.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/intake/create-draft/route.ts",
    "line": "9-14",
    "issue": "**Missing input validation.** The request body fields (`matterTypeKey`, `title`, `answers`) are destructured without any type or structure validation. `answers` is implicitly `any` and could be a string, array, or deeply nested malicious object — the `|| {}` fallback only handles `null`/`undefined`. Use a schema validation library (e.g., Zod) to validate and parse the request body, ensuring `an...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/intake/draft/save/route.ts",
    "line": "14-21",
    "issue": "Missing ownership validation for file references. The `files` array accepts arbitrary `vaultFileId` values without verifying the current user owns those vault files. An attacker could reference files belonging to other users, potentially gaining unauthorized access to their uploaded files.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/intake/submit/route.ts",
    "line": "208-214",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/intake/submit/route.ts",
    "line": "216-223",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/messages/[requestId]/read/route.ts",
    "line": "25-38",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/messages/[requestId]/route.ts",
    "line": "20-23",
    "issue": "`activeWorkspaceId ?? ''` falls back to an empty string when `activeWorkspaceId` is null/undefined. This could cause data isolation issues: if any messages exist with `workspaceId` set to an empty string, they may be inadvertently exposed to users without an active workspace. Instead, validate that `activeWorkspaceId` is present and return a 400 (Bad Request) if it is missing, ensuring proper w...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/messages/poll/route.ts",
    "line": "81",
    "issue": "**Bug: senderName is incorrectly assigned to senderId (a user ID) instead of the actual sender's name.** This will cause the frontend to display a raw UUID/ID instead of a human-readable name. The `senderName` field is available in the `Message` model via relation (e.g., `sender.name`), but the query does not include the `sender` relation, so the name cannot be populated. Either include the `se...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/messages/poll/route.ts",
    "line": "44",
    "issue": "**Bug: Unvalidated `since` parameter can produce an invalid Date.** If `since` is a malformed string (e.g., `'not-a-date'`), `new Date(since)` returns an invalid Date object (`Invalid Date`). Queries using `{ gte: invalidDate }` may silently return empty results or cause unexpected behavior. Validate the date before using it.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/messages/send/route.ts",
    "line": "47-57",
    "issue": "RecipientId can be null when assignedSpecialistId is null and sender is the creator. The `?? ''` fallback on line 40 coerces null to an empty string, which likely violates foreign key constraints in the database. Add an explicit null check and return a meaningful error if no recipient is available.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/messages/send/route.ts",
    "line": "34-41",
    "issue": "No workspace validation: The session's activeWorkspaceId is not compared with the legalRequest's workspaceId. A user could send messages to a request in a workspace they don't belong to. Add a check to ensure activeWorkspaceId matches legalRequest.workspaceId.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/partner/auth/login/route.ts",
    "line": "84-87",
    "issue": "`auth.api.getSession()` is called with the original request headers which do not contain a session cookie (the cookie from `signInEmail` was discarded). This call will likely return `null`/`undefined`, and the `session` variable is never used in the response anyway. This appears to be dead code that also won't work as intended.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/partner/dashboard/route.ts",
    "line": "75-79",
    "issue": "The `pendingAssignments` query does not filter by `isCurrent: true`, meaning it counts all historical RequestAssignment records for this partner rather than only the currently pending ones. This inflates the `pendingAssignments` stat and may mislead the partner dashboard. Add `isCurrent: true` to the where clause if the schema supports it, or use an appropriate status filter to only count pendi...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/partner/engagements/route.ts",
    "line": "44",
    "issue": "Unbounded pagination: The `take` parameter has no maximum limit. An attacker could pass `?take=1000000` to request a massive dataset, causing excessive memory consumption, database load, and potential denial of service. Enforce a reasonable maximum (e.g., 100).",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/partner/invite/[id]/route.ts",
    "line": "104-109",
    "issue": "Missing permission check: GET endpoint returns invite details without verifying `manage_members` permission. DELETE requires this permission, but GET allows any active partner member to view all invites for the partner, including sensitive fields like `email` and `role`. This is an inconsistent authorization model.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/partner/members/route.ts",
    "line": "22-34",
    "issue": "Insufficient authorization: any active partner member can list all members of the organization, including their email addresses. There is no check for an admin/owner role. This leaks sensitive PII (emails) to all members regardless of their role. Consider adding a role-based gate (e.g., only allow 'admin' or 'owner' roles to access this endpoint).",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/app/api/partner/requests/[id]/comments/route.ts",
    "line": "80-89",
    "issue": "`isInternal` is hardcoded to `false` in the GET response. If the Message model has an `isInternal` field, it should be read from the message (e.g., `msg.isInternal`). Otherwise, internal comments are indistinguishable from regular comments, undermining the feature entirely.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/partner/requests/[id]/comments/route.ts",
    "line": "145",
    "issue": "`recipientId` is set to the sender's own ID (`session.user.id`). This means the message is sent to oneself, which is likely incorrect. The recipient should probably be the request owner or another relevant party (e.g., the workspace admin). This could break downstream notification or filtering logic.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/partner/requests/[id]/comments/route.ts",
    "line": "66-70",
    "issue": "Neither GET nor POST handler wraps its async logic in a try/catch block. If `prisma.message.findMany()`, `prisma.$transaction()`, or any other database call throws (e.g., connection timeout, constraint violation), the error will propagate as an unhandled exception, resulting in a generic 500 response with no logging or user-friendly error message.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/partner/requests/[id]/documents/route.ts",
    "line": "128-158",
    "issue": "**Unhandled database errors in POST handler**: Both `prisma.vaultFile.create` and `prisma.auditEvent.create` are not wrapped in a try-catch. If either database operation fails (e.g., constraint violation, connection timeout), the server will return an unhandled 500 error with no meaningful error message to the client. Additionally, if `vaultFile.create` succeeds but `auditEvent.create` fails, t...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/partner/requests/[id]/documents/route.ts",
    "line": "116-117",
    "issue": "**Weak MIME type validation — client-spoofable**: The MIME type check at line 97 relies solely on `file.type`, which is provided by the browser based on the file extension. An attacker can rename a malicious `.exe` to `.pdf` and the browser will send `application/pdf`. This bypasses the allowlist entirely. Consider validating the file's magic bytes (e.g., using `file-bytes` or reading the first...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/partner/requests/[id]/route.ts",
    "line": "18-36",
    "issue": "Missing error handling for async database calls. Both Prisma queries (`findFirst` and `findUnique`) are not wrapped in try-catch, so any database error (e.g., connection failure, timeout) will result in an unhandled Promise rejection, returning a generic 500 error with no user-friendly message. Wrap the database operations in try-catch and return a proper error response.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/partner/requests/[id]/status/route.ts",
    "line": "47-58",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/partner/requests/[id]/status/route.ts",
    "line": "73-74",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/partner/requests/my-work/route.ts",
    "line": "24-25",
    "issue": "**NaN propagation breaks pagination**: `parseInt` on non-numeric strings (e.g., `?page=abc`) returns `NaN`. `Math.max(1, NaN)` and `Math.min(50, Math.max(5, NaN))` both yield `NaN`, which then propagates to `skip` and `take` in Prisma queries, causing a runtime error. Validate and fallback explicitly.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/partner/requests/my-work/route.ts",
    "line": "36-38",
    "issue": "**Status filter silently removes exclusion guard**: When `statusFilter` is provided (line 34), it overwrites the entire `where.status` object, discarding the `notIn: ['draft_intake', 'triage', 'cancelled', 'delivered', 'closed']` exclusion. This means a request like `?status=draft_intake` would return requests that should never appear in the workbench, potentially leaking sensitive draft/triage...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/partner/requests/my-work/route.ts",
    "line": "30-34",
    "issue": "**Soft-deleted records not filtered**: The `LegalRequest` model has a `deletedAt` field for soft deletion (line 307 in schema), but the `where` clause does not include `deletedAt: null`. This means soft-deleted requests will appear in the workbench list and stats, which is almost certainly unintended.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/partner/requests/pending-review/route.ts",
    "line": "57-61",
    "issue": "**Stats inconsistency for admins**: The stats queries (lines 45-48) always filter by `assignedReviewerId: session.userId`, but for admins the main request list has no such filter. An admin will see all pending requests in the `data` array, but the `stats` counts will only reflect their own personally assigned requests — leading to confusing UX where, e.g., `pending` reads 0 while the list shows...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/partner/requests/route.ts",
    "line": "10-11",
    "issue": "Missing try-catch: The entire handler has no error handling. Any Prisma error (connection failure, constraint violation, etc.) will throw an unhandled rejection, resulting in a generic 500 response with potentially sensitive error details leaked to the client. Wrap the body in try-catch and return a controlled error response.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/requests/route.ts",
    "line": "11-24",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/requests/route.ts",
    "line": "69-75",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/api/requests/route.ts",
    "line": "28-32",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/service-types/route.ts",
    "line": "21-26",
    "issue": "Missing error handling: The Prisma query has no try/catch wrapper. If the database is unreachable, the query times out, or any other database error occurs, the promise rejection will surface as an unhandled 500 error with no user-friendly message. Wrap the query in try/catch and return a meaningful error response.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/app/api/service-types/route.ts",
    "line": "10",
    "issue": "PrismaClient instantiation in module scope is problematic in Next.js serverless environments. During hot reloads in development, each module reload creates a new PrismaClient instance without closing the previous one, leading to connection pool exhaustion. Use the recommended global singleton pattern to reuse the client across hot reloads.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/settings/language/route.ts",
    "line": "47-52",
    "issue": "**Fragile string matching for auth error detection.** The check `message === 'UNAUTHENTICATED'` couples the error handling to the exact error message string thrown by `requireAppSession`. If the message changes (e.g., becomes `'Unauthenticated'` or `'UNAUTHENTICATED_ERROR'`) or the error type changes (e.g., a custom `AuthError` class), the 401 response will be silently missed and a generic 500 ...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/settings/language/route.ts",
    "line": "14-15",
    "issue": "**Malformed JSON body returns 500 instead of 400.** `request.json()` throws a `SyntaxError` when the request body is not valid JSON. This error is caught by the generic `catch` block and returned as a 500 Internal Server Error, which is incorrect for a client-side input error. Wrap `request.json()` in its own try-catch or check for `SyntaxError` in the catch block to return a proper 400 Bad Req...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/settings/notifications/route.ts",
    "line": "71-79",
    "issue": "The PUT endpoint silently ignores non-boolean field values (e.g., `{ emailOnReply: 'true' }`). The client receives a 200 success response even though their preferences were not applied. This can cause confusion and makes debugging difficult. Consider returning a 400 error when any field is provided but has an invalid type.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/api/settings/password/route.ts",
    "line": "38-41",
    "issue": "Using `findFirst` with `{ userId, password: { not: null } }` is non-deterministic when a user has multiple `Account` records with passwords. The `Account` model has only `@@unique([providerId, accountId])` — `userId` is not unique. If a user has both a credentials account and another provider account that also has a password set, `findFirst` may return either one unpredictably, potentially comp...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/app/api/settings/profile/route.ts",
    "line": "37-53",
    "issue": "**Race condition on email uniqueness check**: The email existence check (lines 37-48) and the user update (lines 51-63) are not atomic. Two concurrent requests with the same email can both pass the `findFirst` check, then both attempt the `update`, causing the second to fail with a Prisma unique constraint violation (P2002). The current catch block masks this as a generic 500 'UPDATE_FAILED'. C...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/app/api/swagger/route.ts",
    "line": "15",
    "issue": "Hardcoded fallback URL 'http://localhost:3000' will be used in production if NEXT_PUBLIC_APP_URL is not set. This causes the Swagger spec to advertise an incorrect server URL, which can break API client generation (e.g., OpenAPI Generator, SDKs). Consider either removing the fallback so the route errors loudly, or defaulting to a production-safe value derived from the request's origin at runtime.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/vault/[vaultFileId]/download/route.ts",
    "line": "36-38",
    "issue": "The catch block returns HTTP 401 ('Unauthorized') for all errors indiscriminately. This misclassifies genuine server errors (e.g., database connection failures, Prisma query errors) as authentication failures, making debugging difficult and misleading API consumers. Consider logging the error and returning a generic 500 response for unexpected errors.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/app/api/vault/route.ts",
    "line": "20-22",
    "issue": "**Silent Error Swallowing**: The `catch` block does not capture the error object (`catch {` without a variable) and provides no logging whatsoever. If `listFolders`, `listTags`, or `listFileClassifications` fail, the error is silently discarded. This makes production debugging extremely difficult — there will be no trace of what went wrong in logs or monitoring systems.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/workspace/invite/route.ts",
    "line": "7-16",
    "issue": "Missing inviter authorization check. Any authenticated user with an active workspace can add members regardless of their role. This allows privilege escalation: a 'customer'-role member could invite other users as 'admin'. Consider adding a role-based check (e.g., only 'admin' or 'owner' roles can invite) before proceeding with the invitation.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/workspace/invite/route.ts",
    "line": "18-20",
    "issue": "The 'role' parameter from the request body is passed directly to the database without validation against allowed roles. A malicious user could set role to 'admin', 'owner', or any other privileged value, leading to privilege escalation. Validate 'role' against a predefined list of allowed roles, and consider restricting which roles the inviter can assign (e.g., an admin should not be able to as...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/app/api/workspaces/route.ts",
    "line": "9-16",
    "issue": "This endpoint returns ALL workspaces without filtering by user membership. Any authenticated user can retrieve the full list of workspaces, including those they don't belong to — exposing workspace names, slugs, member counts, and creation dates. The query should be scoped to only return workspaces where the authenticated user has a membership record.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/app/globals.css",
    "line": "37-44",
    "issue": "CSS custom properties `--space-sm2` and `--text-sm2` are referenced in `.pill` (gap, font-size) and `.audit-note` styles but are never defined anywhere in the codebase. This means these properties will fall back to their initial/inherited values, silently breaking layout spacing and font sizing for these components.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/app/invite/[token]/page.tsx",
    "line": "223-232",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/auth.ts",
    "line": "25-35",
    "issue": "Wildcard trusted origins (`https://*.trycloudflare.com`, `https://*.ngrok-free.app`) combined with disabled CSRF protection allows any attacker on those domains to potentially make authenticated requests. Consider restricting trusted origins to specific tunnel URLs or enabling them only in development mode.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/LanguageSwitcher.tsx",
    "line": "23-52",
    "issue": "Dead code: query string and hash extraction logic (lines 32-51) will never execute. In Next.js App Router, usePathname() returns only the pathname (e.g., /en/about), never the query string (?...) or hash (#...). The entire extraction block is unreachable dead code, making queryString and hash always empty strings. This adds unnecessary complexity and maintenance burden.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/AdminAuditClient.tsx",
    "line": "40-57",
    "issue": "The component only reads URL parameters on mount (line 47-62) and never listens to `popstate` events. When the user navigates via browser back/forward, the component state and URL will diverge, causing incorrect data display. Add a `popstate` listener to re-sync state from the URL.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/AdminAuditTable.tsx",
    "line": "141-150",
    "issue": "parseMetadata incorrectly handles JSON arrays. When metadata is a JSON array, `typeof parsed === 'object'` is true (arrays are objects in JS), so it enters the object branch. `Object.keys()` then returns numeric indices like '0', '1', which produce nonsensical output such as '0: [object Object]' when the array contains objects. Add an `Array.isArray()` guard to handle arrays separately.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/AdminOperationsClient.tsx",
    "line": "275",
    "issue": "**Missing null-safe access on `data?.filters`.** On line where `data?.filters.workspaces` is accessed, if `data` is not null but `data.filters` is `undefined` or `null`, this will throw a runtime TypeError: 'Cannot read properties of undefined (reading 'workspaces')'. The API may return an `OpsAggregateDto` where `filters` is optional. Use optional chaining to guard against this.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/AdminOperationsStats.tsx",
    "line": "17-23",
    "issue": "Null safety: The `stats` prop is accessed without any null/undefined guard. If `stats` is `undefined` or `null` (e.g., during a loading state or if the parent passes an empty value), the component will throw a runtime error when trying to access `stats.openRequests`, `stats.nearSla`, etc. Consider adding a default fallback value or a null guard at the top of the component.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/AdminOperationsTable.tsx",
    "line": "93",
    "issue": "Potential runtime error: `req.id.slice(-8)` will throw a TypeError if `req.id` is `undefined` or `null`. The `??` operator only handles the `null`/`undefined` case for `req.code`, but the fallback path assumes `req.id` is always a string. Add a null check or safe fallback.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/admin/AdminOperationsTable.tsx",
    "line": "66",
    "issue": "Inner component `TableRow` is declared inside `AdminOperationsTable`, causing it to be re-created on every render. This breaks React's reconciliation, loses component state, and degrades performance. Move `TableRow` outside the parent component or convert it to a render function (e.g., `renderTableRow(req)`).",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/AdminOperationsWorkload.tsx",
    "line": "101-104",
    "issue": "No null/undefined guard for the `workload` prop. If a parent component omits this prop or passes `undefined`/`null`, accessing `workload.length` and `workload.map()` will throw a runtime `TypeError`. Add a default value or a guard clause.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/AdminRequestsClient.tsx",
    "line": "207-212",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/AdminRequestsClient.tsx",
    "line": "861-872",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/AdminRequestsTable.tsx",
    "line": "167",
    "issue": "**High**: Using array index (`rowIndex`) as the React `key` is an anti-pattern. When rows are reordered, filtered, or added/removed, React may incorrectly reuse DOM nodes, causing stale state or visual glitches. Use `row.id` (or `row.fullId`) as the stable key instead.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/AdminRequestsTable.tsx",
    "line": "99",
    "issue": "**Bug**: Partial `translations` overrides do not fall back to defaults. `translations || defaults` only uses defaults when `translations` is falsy. If a caller passes `{ code: 'Custom' }`, all other header labels become `undefined`. Use a spread merge instead: `{ ...defaults, ...translations }`.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/AdminToolbar.tsx",
    "line": "86-89",
    "issue": "The 'Trạng thái' (Status) button incorrectly calls the same `onFilter` handler as the filter button. This is likely a copy-paste bug — a status-specific action (e.g., `onStatusFilter`) should be used instead, or a separate prop should be added to differentiate between general filtering and status filtering.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/AdminVaultClient.tsx",
    "line": "118-123",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/AdminVaultFilesTable.tsx",
    "line": "53-58",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/AdminVaultFoldersPanel.tsx",
    "line": "35-38",
    "issue": "The create-folder button is rendered without an `onClick` handler. It is visible to users but does nothing when clicked, which is a functional defect. Either add an `onClick` prop to the component interface and wire it up, or hide the button if no handler is provided.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/AdminVaultTagsPanel.tsx",
    "line": "36-40",
    "issue": "The `tags` prop is not defensively guarded against `undefined` or `null`. If the parent passes `tags={undefined}`, calling `tags.filter(...)` on line 33 will throw a TypeError and crash the component. Consider defaulting to an empty array.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/AdminWorkspaceClient.tsx",
    "line": "69-78",
    "issue": "The permission denial card ('Ranh giới quyền truy cập') is rendered unconditionally, always appearing alongside the workspace table. This creates a contradictory UX: the card says 'Bạn không có quyền xem nội dung này' (You don't have permission to view this content) even when data is successfully loaded and displayed. This card should only be shown when the user is actually unauthorized (e.g., ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/AiAnnotationContent.tsx",
    "line": "32-38",
    "issue": "`parsed.raw` may be `undefined` when the parser returns `{ sections: [] }` without a `raw` property. This happens when section markers are found but all extracted section texts are empty (filtered out at line 76 of the parser). In that case, `{parsed.raw}` renders the literal string 'undefined' in the UI.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/AiIssuePopup.tsx",
    "line": "91-92",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/AiResultCard.tsx",
    "line": "251-254",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/admin/ApprovalPanel.tsx",
    "line": "55",
    "issue": "**Array Index as React Key**: Using `index` as the `key` prop in `approvals.map()` can cause rendering issues and performance degradation when the list order changes, items are added, or removed. Use a stable unique identifier (e.g., an `id` field) instead.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/AssignmentDialog.tsx",
    "line": "97-98",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/admin/ChatActivityPanel.tsx",
    "line": "167-194",
    "issue": "`handleSend` performs async state updates (`setMessages`, `setAiNotConfigured`, `setIsSending`, `composerRef.current?.focus()`) without checking whether the component is still mounted. If the user navigates away before the fetch completes, this will cause React state-update-on-unmounted warnings and potential memory leaks. Add an `isMounted` ref or use an AbortController to cancel the request o...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/admin/ChatActivityPanel.tsx",
    "line": "247-260",
    "issue": "`handleRetryLoad` does not check whether the component is still mounted before updating state (`setMessages`, `setIsLoading`). If the user navigates away during the retry fetch, this triggers React warnings for state updates on unmounted components.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/admin/DeliveryConsole.tsx",
    "line": "226-229",
    "issue": "Locale shadowing bug: `onAiClick` re-declares `const locale` from `window.location.pathname`, shadowing the `locale` from `useLocale()` (line 59). This uses fragile URL parsing (`split('/')[1]`) instead of the hook-provided locale. If the URL structure changes (e.g., base path prefix), this breaks. Additionally, the hardcoded fallback `'vi'` is a business value that should come from configuration.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/DeliveryDialog.tsx",
    "line": "159",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/admin/DocumentAnnotationPanel.tsx",
    "line": "48-52",
    "issue": "SEVERITY_CONFIG and the form select only support 'critical', 'warning', 'info', but the Annotation type also allows 'high', 'medium', 'low'. When the API returns those severities, they fall back to the 'info' icon/color (line 266), misleading users. The form select also can't display or edit those values correctly — handleEdit at line 237 casts the severity to only the three supported values, w...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/admin/DocumentPreviewTipTap.tsx",
    "line": "157-161",
    "issue": "Fragile asynchronous highlight: a fixed 50ms `setTimeout` is used to wait for the editor DOM before applying highlights. On slower devices or with large documents the DOM may not be ready, causing highlights to be silently missing. Prefer hooking into the editor's `onCreate` callback or using TipTap's `editor.on('create', ...)` lifecycle event.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/OrgActivityClient.tsx",
    "line": "128-129",
    "issue": "**Non-functional filtering controls**: `activeTab` and `searchQuery` state are set via UI interactions but never used to filter the displayed content. All tabs, workspace selects, partner selects, status selects, time range selects, and the search input are rendered but their values are never consumed to filter the feed, tables, or grids. This makes the filtering UI misleading — users will beli...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/admin/PartnerActivityClient.tsx",
    "line": "317-323",
    "issue": "Missing cleanup for the fetch call on component unmount. If the component unmounts while the fetch is in-flight, the subsequent `setState` calls (`setPartner`, `setLoading`, `setError`) will cause a React warning: 'Can't perform a React state update on an unmounted component'. Use an AbortController or a mounted flag to cancel/ignore the response.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/PartnerActivityClient.tsx",
    "line": "890-895",
    "issue": "Division by zero when `partner.capacity.openRequests.max` is 0. This produces `NaN%` as the width value, breaking the progress bar rendering. Add a guard: when max is 0, treat the bar as 0% or 100% depending on the intended semantics.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/admin/RequestTimeline.tsx",
    "line": "100-102",
    "issue": "Missing abort/cleanup on unmount and race condition: the useEffect triggers fetchTimeline but never cancels in-flight requests. On unmount, setState calls will cause React warnings; when requestId changes rapidly, stale responses may overwrite newer state. Use AbortController with a cleanup function.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/RequestTimeline.tsx",
    "line": "128",
    "issue": "Missing null check for `data.timeline`: if the API returns `data` but the `timeline` field is undefined or null, accessing `.length` throws a TypeError. Use optional chaining.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/ReviewDialog.tsx",
    "line": "36-63",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/components/admin/ReviewerDashboardClient.tsx",
    "line": "3",
    "issue": "**Unused import**: `useTranslations` is imported but never called anywhere in this component. This is dead code that should be removed to keep the bundle clean and avoid confusion.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/admin/ReviewerDashboardClient.tsx",
    "line": "99-100",
    "issue": "**Hardcoded non-localized text**: The greeting and message in the welcome banner are hardcoded in Vietnamese, bypassing the `translations` prop entirely. This breaks internationalization — users of other locales will always see Vietnamese text here. These strings should be moved into the `translations` interface (e.g., `t.greeting`, `t.pendingCount`) and rendered via the `t` prop.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/ReviewerDashboardClient.tsx",
    "line": "96",
    "issue": "**Null/undefined safety**: `userName` is destructured from props without a default value. If `userName` is `undefined` or `null`, calling `.split()` will throw a runtime error (`Cannot read properties of undefined`). Consider adding a default value or a null guard.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/admin/RolePills.tsx",
    "line": "29-38",
    "issue": "**Inline styles for static styling**: This entire component uses inline `style` objects for static (non-dynamic) styles, which violates the React best practice of avoiding inline styles. This harms maintainability, makes consistent theming difficult, and prevents CSS-based optimizations (e.g., critical CSS extraction). Extract all static styles into a CSS module, styled-components, or Tailwind ...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/admin/SpecialistDashboardClient.tsx",
    "line": "110-111",
    "issue": "Hardcoded Vietnamese strings in the banner greeting. 'Xin chào' and 'hồ sơ đang chờ xử lý' are hardcoded, breaking the i18n pattern used throughout the rest of the component. These should be passed via the translations prop.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/SpecialistDashboardClient.tsx",
    "line": "107",
    "issue": "No null-safety for `userName`. If `userName` is `undefined`, `null`, or an empty string, `userName.split(' ')` will throw a runtime error or produce garbled avatar text (e.g., empty string yields `'UN'`). Add a guard: `userName?.split(' ') ?? []` and provide a fallback avatar (e.g., `'?'`).",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/admin/TriagePanel.tsx",
    "line": "122-123",
    "issue": "Stats (draftCount, triageCount) are derived from the raw API response (`data?.data`), ignoring the current search filter. When a user types a search term, the stats still show the unfiltered counts from the server page, which is misleading. The stats should reflect the actual filtered dataset or come from the API as totals.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/TriagePanel.tsx",
    "line": "240-243",
    "issue": "The `onAiClick` handler re-derives locale from `window.location.pathname.split('/')[1]` instead of using the already-available `locale` from `useLocale()`. This: (1) shadows the outer `locale` variable, (2) is fragile if the URL structure changes (e.g., basePath), and (3) unnecessarily accesses `window` on the server side. Use the hook-provided `locale` directly.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/UserActivityClient.tsx",
    "line": "133-141",
    "issue": "The `initialUser` prop is declared but never used. `useQuery` sets `initialData: undefined`, so the component always shows a loading state even when pre-fetched data is available from the server. This defeats the purpose of SSR/SSG data hydration. Pass `initialUser` as `initialData` to `useQuery` to avoid the unnecessary loading flash.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/UserActivityClient.tsx",
    "line": "1082-1101",
    "issue": "The `activeTab` state is set when a tab is clicked, but none of the rendered content sections are filtered based on the active tab. All sections (activity feed, requests, org access, partners, SLA, timeline) are always shown regardless of which tab is selected. This makes the tab UI misleading and non-functional.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/admin/UserTable.tsx",
    "line": "84-108",
    "issue": "Hardcoded column index `idx < 6` for `borderRight` is fragile. If columns are added or removed, the border logic will silently break. Define the header columns as a structured array and derive the last-column index dynamically (e.g., `idx < columns.length - 1`), or extract the column definitions so the grid and border logic stay in sync automatically.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/UserToolbar.tsx",
    "line": "243-246",
    "issue": "Columns button has no onClick handler. The button is rendered but clicking it does nothing — it's either dead code or a missing feature. If this is intentional placeholder, consider hiding it or adding a disabled state; otherwise, add an `onColumns` prop and wire it up.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/admin/UsersPageClient.tsx",
    "line": "241",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/admin.css",
    "line": "87-95",
    "issue": "Missing stat-icon variants: 'blue' and 'orange'. AdminStatCard.tsx explicitly references variants 'blue', 'green', 'orange', 'red' (line 4), and the base stat-icon styles are defined in other CSS files. However, this admin.css file only defines `.stat-icon.green` and `.stat-icon.red` without the base `.stat-icon` class or the `.stat-icon.blue` and `.stat-icon.orange` variants. If this CSS file ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/admin/admin.css",
    "line": "79-95",
    "issue": "The .app min-width: 1450px rule at 1450px breakpoint creates a contradiction with the grid collapse breakpoints at 1200px (lines 68-73). Since .app forces a minimum width of 1450px, the @media (max-width: 1200px) rules for admin-grid-2 and admin-grid-3 will never be triggered — the viewport can never shrink below 1450px due to the enforced min-width on .app. This means the 1fr collapse layout f...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/auth/SignInForm.tsx",
    "line": "325",
    "issue": "The `remember` checkbox state is tracked but never passed to `authClient.signIn.email()`. The 'remember me' functionality is non-functional — the auth call uses only `email` and `password`, ignoring the `remember` state entirely.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/auth/SignInForm.tsx",
    "line": "332-341",
    "issue": "The `fetch('/api/auth/session-role')` call after sign-in has no abort controller or cleanup. If the component unmounts before the fetch completes, it will attempt to call `router.push()` on an unmounted component, causing a memory leak and potential React warning.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/auth/SignInForm.tsx",
    "line": "219-220",
    "issue": "`useSearchParams()` is used without a `<Suspense>` boundary wrapping this component. In Next.js App Router, `useSearchParams()` triggers client-side rendering deopt for the entire page unless wrapped in Suspense. This can cause the page to lose its static/server-rendered shell.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/create-request/CreateRequestForm.tsx",
    "line": "68-99",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/create-request/CreateRequestForm.tsx",
    "line": "391-395",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/create-request/CreateRequestForm.tsx",
    "line": "383-396",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/create-request/FileUploadZone.tsx",
    "line": "100-121",
    "issue": "Memory leak: `setInterval` and `setTimeout` in `handleFileAdd` are never cleaned up when the component unmounts or when a new upload starts. This can cause state updates on an unmounted component (React warning) and wasted CPU cycles. Store the interval/timeout IDs in refs and clear them in a `useEffect` cleanup, and also clear any existing interval before starting a new one.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/create-request/IntakeQuestionsForm.tsx",
    "line": "34",
    "issue": "**Hardcoded service mapping**: The mapping from `selectedService` to `matterTypeKey` is hardcoded only for `'trademark'` → `'trademark_registration'`. For all other services, `matterTypeKey` is assumed to equal `selectedService` directly. If other services also have a different `matterTypeKey` (e.g., `'copyright'` → `'copyright_registration'`), they will silently fail to find their questions in...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/create-request/IntakeQuestionsFormEnhanced.tsx",
    "line": "94-104",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/create-request/RequestForm.tsx",
    "line": "50",
    "issue": "Hardcoded email placeholder 'name@company.com' bypasses the i18n system. All other placeholder text uses translation keys (e.g., `t('contactPlaceholder')`), making this inconsistent and preventing localization of the email field placeholder.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/create-request/RequestForm.tsx",
    "line": "18-24",
    "issue": "The form is not functional: all form inputs are uncontrolled and lack onChange handlers, and the component has no wrapping <form> element or submit button, making it impossible to collect or submit data.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/create-request/ServiceCard.tsx",
    "line": "28-29",
    "issue": "Multiple unsafe property accesses and incorrect fallback operators in ServiceCard.tsx: service.title, service.description, tag.label, and service.estimatedTime lack null checks and use || instead of ?? for fallback, potentially causing runtime errors and silent fallback to vi for empty strings.",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/components/create-request/create-request.css",
    "line": "823-828",
    "issue": "Duplicate .domain-title rule. The first declaration (lines 164-169) sets margin-bottom: 6px, while this second declaration sets margin-bottom: 16px. The second will win due to cascade order, making the first one dead code. The first also has an associated .domain-subtitle block that references different spacing. Consolidate all .domain-title rules into a single declaration.",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/components/create-request/create-request.css",
    "line": "1021-1035",
    "issue": "Duplicate .upload-zone rules. The first definition (around lines 266-282) uses #0d9488 on hover, while this second definition (around lines 579-595) uses #087f78 on hover. The colors are very close but not identical, which means the two upload zones will render slightly differently depending on which step is active. Consolidate into a single block.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/dashboard/CasesTable.tsx",
    "line": "28-30",
    "issue": "No null/undefined guard for the `cases` prop. If `cases` is undefined, null, or not an array at runtime, accessing `cases.length` will throw a TypeError, crashing the component. Add a default empty array fallback or a guard clause.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/dashboard/DashboardClient.tsx",
    "line": "206-208",
    "issue": "**Hardcoded Vietnamese strings in ErrorBoundary fallbacks bypass i18n.** All five fallback divs contain the hardcoded string `'Không thể tải dữ liệu. Vui lòng thử lại.'` instead of using the `t()` translation function. This breaks internationalization — non-Vietnamese users will see Vietnamese error text. Use the `t()` function with a translation key like `t('error.loadFailed')` instead.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/dashboard/DeadlineSLA.tsx",
    "line": "20-34",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/dashboard/RecentCases.tsx",
    "line": "47-48",
    "issue": "Hardcoded Vietnamese strings in EmptyState break multilingual support. These static strings will always display in Vietnamese regardless of the user's locale setting, while the rest of the component uses next-intl translation keys. Replace with proper translation keys, e.g., t('emptyTitle') and t('emptyDescription').",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/dashboard/RecentCases.tsx",
    "line": "51",
    "issue": "Missing locale prefix in router.push for the empty state action. The hardcoded path '/create' does not include the locale segment, unlike the 'see all' link which uses `/${locale}/cases`. On non-default locales, this will navigate to the wrong path or cause a 404. Should be `/${locale}/create`.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/dashboard/StatCard.tsx",
    "line": "76-84",
    "issue": "The `StatsCardGrid` component destructures `data` without any null/undefined guard. If `data` is `null` or `undefined` (e.g., due to a race condition between loading state and data fetching, or if the parent guard is removed), accessing `data.totalRequests` will throw a TypeError and crash the component. Consider adding a defensive null check or a fallback default value.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/dashboard/ToolbarCard.tsx",
    "line": "29-40",
    "issue": "All interactive buttons (filter, status, refresh, export, columns) lack onClick handlers, making them non-functional. Wire each button to its corresponding action.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/layout/AdminLayout.tsx",
    "line": "207-211",
    "issue": "The catch block in handleLogout unconditionally redirects to '/vi/sign-in' regardless of whether the user is still authenticated. If signOut fails due to a network error, the user remains authenticated but gets redirected to sign-in. If the sign-in page has an auth guard that redirects authenticated users back to admin, this creates a redirect loop. Additionally, no user-facing error feedback i...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/layout/LanguageSwitcher.tsx",
    "line": "24-35",
    "issue": "Query parameters and hash fragments are silently discarded when switching locale. `usePathname()` returns only the pathname (e.g., `/en/products`), so any existing `?page=2&filter=active` or `#section` is lost after navigation. Use `window.location.search` and `window.location.hash` to preserve them.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/layout/UserLayout.tsx",
    "line": "78-85",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/messages/MessagesClient.tsx",
    "line": "158-170",
    "issue": "Optimistic send never replaces the temp message with the server response. After a successful API call, the temporary message (with `temp-*` ID) remains in state permanently. The server's real message ID, timestamp, and any server-enriched fields are lost. This causes a persistent local/server state mismatch and can break features relying on the real message ID (e.g., deduplication during pollin...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/messages/MessagesContainer.tsx",
    "line": "65-97",
    "issue": "**Optimistic update not rolled back on API failure.** When the API call fails, the optimistic message and thread preview update are never removed from state. The user will see a message that was never actually sent, and the thread preview shows stale data. Add a rollback in the catch block that removes the optimistic message and reverts the thread preview.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/messages/MessagesContainer.tsx",
    "line": "38-39",
    "issue": "**Messages never updated after initial load — no real-time or polling mechanism.** The `messages` state is initialized from `initialMessages` and only updated via the optimistic `handleSendMessage`. There is no WebSocket, SSE, or polling to fetch new messages from other participants. This means the chat will never show incoming messages after the page loads, breaking the core chat functionality.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/messages/messages.css",
    "line": "258-267",
    "issue": "`.composer-input` uses `outline: none`, which removes the browser's default focus indicator. Combined with only a `border-color` change on `:focus`, this makes it hard for keyboard users to identify which field has focus. The border color change alone may have insufficient contrast (3:1 minimum) against the unfocused border. Replace `outline: none` with a clear accessible focus style.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/messages/messages.css",
    "line": "32-41",
    "issue": "`.search-input` uses `outline: none` like the composer, removing the browser's default focus ring. Uses only border-color change for `:focus`, which is insufficient as a sole focus indicator for WCAG compliance.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/messages/messages.css",
    "line": "70-77",
    "issue": "`.thread` items are interactive (`cursor: pointer`, `:hover`) but lack `:focus-visible` styles. Keyboard users navigating through thread items will have no visible indication of which thread has focus. Add a `:focus-visible` style (e.g., ring or outline) to the `.thread` selector.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/messages/messages.css",
    "line": "308-317",
    "issue": "`.info-close` button lacks `:focus-visible` styles and has no visible focus ring. Combined with a transparent background, keyboard users won't be able to see when this close button has focus.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/messages/messages.css",
    "line": "393-412",
    "issue": "`.floating-chat-btn` lacks `:focus-visible` styling. As a fixed-position interactive element, it must be accessible to keyboard users. Without visible focus indicators, keyboard-only users cannot perceive when this button is focused.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/my-cases/CaseDetailClient.tsx",
    "line": "125-130",
    "issue": "Nested ternary expressions are prohibited by project rules. This reduces readability. Extract the SLA message logic into a helper variable or function.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/my-cases/MyCasesClient.tsx",
    "line": "118-126",
    "issue": "Unvalidated status filter silently hides all results when URL contains an unrecognized status value. If `selectedStatus` is not one of the keys in `statusMap` (e.g., a manipulated URL or a stale bookmark), `statusMap[selectedStatus]` returns `undefined`. Since `req.statusBadge` is never `undefined`, the comparison `req.statusBadge !== undefined` is always `true`, causing every row to be filtere...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/my-cases/MyCasesTable.tsx",
    "line": "61-65",
    "issue": "**Pagination total/items mismatch**: `total` is set to `totalRequests ?? requests.length` (line 60), while `paginatedRequests` is sliced from `requests` (line 64). If `totalRequests > requests.length` (e.g., server-side pagination with a stale local cache), the pagination will show extra pages that render empty rows. This creates a broken UX where users can navigate to blank pages.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/my-cases/MyCasesTable.tsx",
    "line": "142-146",
    "issue": "**Unsanitized `actionHref` renders directly into `<a href>`**: The `actionHref` field from the `MyCaseRow` interface is rendered directly into an `<a href={item.actionHref}>` (line 129). A malicious or compromised data source could inject a `javascript:` URL (e.g., `javascript:alert(document.cookie)`), enabling XSS when a user clicks the link. Validate or sanitize the URL to only allow safe pro...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/my-cases/MyCasesToolbar.tsx",
    "line": "43-47",
    "issue": "Search input fires `onSearch` on every keystroke without debouncing. If `onSearch` triggers API calls (e.g., filtering a large list), rapid typing will flood the backend with requests, causing race conditions or degraded performance. Consider debouncing (e.g., 300ms) before calling `onSearch`.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/my-cases/StatCard.tsx",
    "line": "16-32",
    "issue": "Runtime crash risk: If the `icon` prop receives an unexpected value (e.g., from an API response, dynamic data, or a typo), `iconMap[icon]` returns `undefined`, and rendering `<undefined />` will throw a React runtime error, crashing the entire component tree.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/my-cases/SummaryBanner.tsx",
    "line": "18-28",
    "issue": "The `workspaceSlug` prop is destructured but never used. The link hardcodes `/create` but should likely be `/${workspaceSlug}/create` to route to the correct workspace. This appears to be an incomplete implementation.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/partner/InviteMembersModal.tsx",
    "line": "37",
    "issue": "Hardcoded API endpoint `/api/partner/invite` violates the project rule prohibiting business-related hardcoded URL paths. Extract this to a shared constants file, route configuration, or environment variable to centralize endpoint management and simplify future refactoring.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/partner/PartnerDashboardClient.tsx",
    "line": "72-101",
    "issue": "useEffect lacks a cleanup function to abort in-flight fetch requests. If the component unmounts before the fetches resolve, `setState` will be called on an unmounted component, causing a React warning and potential memory leak. Use an AbortController and an `ignore` flag, or a mounted ref.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/partner/PartnerDashboardClient.tsx",
    "line": "50-54",
    "issue": "No user-facing error handling. When fetches fail, only `console.error` is called, leaving the UI in a loading/skeleton state forever (since `setLoading(false)` is in the `finally` block, the skeleton disappears but the UI shows empty data without any error indication). Add an error state and display a user-friendly error message with a retry option.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/partner/PartnerSettingsClient.tsx",
    "line": "69-85",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/components/partner/PartnerSettingsClient.tsx",
    "line": "65-67",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/partner/RoleChangeDialog.tsx",
    "line": "74-76",
    "issue": "The permission change description is misleading for demotions. The ternary `newRole === 'viewer' ? 'mất quyền...' : 'có thêm quyền...'` assumes any non-viewer change is a promotion. For example, changing from admin → specialist would display 'có thêm quyền quản lý' (gains rights), but the user is actually losing admin privileges. This can confuse users and lead to accidental role changes.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/partners/ui/CommentList.tsx",
    "line": "29-36",
    "issue": "Missing error state for fetch failure. The `.catch` silently sets an empty array, making it impossible for users to distinguish between 'no comments exist' and 'failed to load comments.' Consider adding an `error` state variable and displaying a user-friendly error message with a retry button.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/partners/ui/DocumentList.tsx",
    "line": "128",
    "issue": "Hardcoded Vietnamese string in the delete confirmation dialog. Business-related strings should use the `t()` translation function from next-intl to support internationalization and avoid hardcoded UI text.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/partners/ui/DocumentUpload.tsx",
    "line": "22-56",
    "issue": "Missing cleanup on unmount during upload. If the component unmounts while `isUploading` is true, the `finally` block will call `setIsUploading(false)` on an unmounted component, triggering React warnings about state updates on unmounted components. Use an `AbortController` and a cleanup flag to prevent this.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/partners/ui/StatusUpdateForm.tsx",
    "line": "32-34",
    "issue": "**Inconsistent `allowAllStatuses` prop behavior**: When `allowAllStatuses` is true but `isAdmin` is false, the dropdown on line 68 shows all statuses from `REQUEST_STATUS_LABELS`, but the `isAllowedStatus` check on line 70 still restricts to `PARTNER_ALLOWED_STATUSES`. This means a user can select a non-partner status in the dropdown but can never submit it — the submit button stays disabled. T...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/partners/ui/StatusUpdateForm.tsx",
    "line": "83-93",
    "issue": "**Initial select value may not be in option list**: If `currentStatus` is not present in the derived `statusList` (e.g., a partner viewing a request in `triage` or `draft_intake` status), the `<select>` element's value will not match any `<option>`. React-controlled selects will still set `value={status}` to the non-matching value, but the browser renders the first option as selected. This is v...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/components/settings/LanguageSettings.tsx",
    "line": "51-53",
    "issue": "Missing cleanup for setTimeout: Several components fail to clear timeouts on unmount, potentially causing state updates on unmounted components and React warnings. Store timer IDs in refs and clear them in useEffect cleanup.",
    "fix": ""
  },
  {
    "pattern": "MISSING_TEST",
    "file": "src/components/settings/ProfileForm.tsx",
    "line": "44-55",
    "issue": "Stale closure on `onSave` prop in debounced timeout: `handleChange` captures `handleSave` at the time the timeout is set. If the `onSave` prop changes between scheduling and execution (e.g., parent re-renders with a new callback), the outdated `handleSave`/`onSave` is called, potentially saving with a stale callback or missing the latest save logic. Use a ref to always reference the latest `onS...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/settings/ProfileSection.tsx",
    "line": "38-45",
    "issue": "Race condition: `handleSave` does not guard against overlapping saves. If `onSave` takes longer than the 500ms debounce interval, multiple concurrent saves can be in flight, leading to data inconsistency. Add an `isSaving` ref or abort previous in-flight saves.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/components/settings/ProfileSection.tsx",
    "line": "38-45",
    "issue": "Unhandled save error: Async save operations lack error handling, swallowing rejections and leaving the user with no feedback. Catch errors and surface them to the user.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/components/settings/SecuritySettings.tsx",
    "line": "52-56",
    "issue": "Missing CSRF protection: Fetch requests to state-changing API endpoints do not include a CSRF token, leaving endpoints vulnerable to cross-site request forgery. Include a CSRF token in request headers.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/settings/settings.css",
    "line": "414-446",
    "issue": "Toggle switch uses a bare `<button>` with `::after` pseudo-element and `.off` class to convey state. There is no ARIA role (`role='switch'`), no `aria-checked`, and no accessible label. Screen readers cannot determine the toggle's state or purpose. Additionally, there is no `.toggle:focus-visible` style — keyboard users get no visible focus ring on the toggle.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/shared/ui/DropdownMenu.tsx",
    "line": "16",
    "issue": "`trigger` prop accepts `'hover'` but the component only implements click behavior. There are no `onMouseEnter`/`onMouseLeave` handlers, making the hover trigger non-functional and misleading to consumers. Either implement hover support or remove `'hover'` from the type union.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/shared/ui/DropdownMenu.tsx",
    "line": "70-85",
    "issue": "Missing keyboard navigation and focus management. Users cannot navigate menu items with arrow keys (Up/Down), Home, or End keys. Focus is not moved to the first item when the menu opens, and the trigger lacks `aria-expanded` and `aria-haspopup` attributes. This makes the component inaccessible to keyboard-only and screen-reader users.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/shared/ui/ErrorBoundary.tsx",
    "line": "74-78",
    "issue": "Sensitive error details exposed in production: Rendering `error.message` directly in the UI can leak internal information. Show a generic message in production and log the full error for debugging.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/shared/ui/ThemeProvider.tsx",
    "line": "55-57",
    "issue": "ThemeContext is missing during initial render (before mount). When `!mounted`, children are rendered inside a plain `<div>` without being wrapped in `ThemeContext.Provider`. Any child component calling `useTheme()` will receive the default context values (`theme: 'light'`, no-op `toggle`/`setTheme`) instead of the real state. This can cause logic errors if a child uses the theme for anything be...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/components/ui/ErrorFallback.tsx",
    "line": "31-33",
    "issue": "Hardcoded locale path '/vi' is Vietnamese-specific. This will break navigation for users on other locales (e.g., '/en', '/ja'). Use the current locale from the router or next-intl to construct the home path dynamically.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/ui/Paging.tsx",
    "line": "30",
    "issue": "If `total` is `undefined` or `NaN` at runtime (e.g., from an API response), `Math.ceil(total / pageSize)` produces `NaN`. Then `Math.max(1, NaN)` returns `NaN`, so `totalPages` becomes `NaN`. This causes `generatePageNumbers(current, NaN)` to call `Array.from({ length: NaN })`, which throws a `RangeError` and crashes the component. Add a defensive guard.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/components/ui/button.tsx",
    "line": "44-65",
    "issue": "`disabled` state is not enforced when `asChild` is `true`: The `disabled` prop from `React.ComponentProps<'button'>` is spread via `...props` onto the child element through `Slot.Root`. If the child is not a native `<button>` (e.g., an `<a>` link), the `disabled` HTML attribute has no effect — the element remains interactive and accessible to clicks/keyboard. This breaks the expected disabled b...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/hooks/useAuditEvents.ts",
    "line": "36",
    "issue": "**Type assertion bypasses type safety**: Casting `adminApi.getAuditLog(params)` to `Promise<PaginatedResponse>` (line 36) suppresses TypeScript errors if the actual API return type diverges from `PaginatedResponse`. If the API response shape changes (e.g., `meta` is renamed or restructured), this will silently produce runtime errors in downstream code accessing `data.meta.page` or similar. Remo...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/hooks/useAuth.ts",
    "line": "23-30",
    "issue": "Error handling is missing — `useSession()` from Better Auth (TanStack Query) returns an `error` property, but this hook does not expose it. When the session fetch fails (network error, server error, etc.), `isLoading` becomes `false`, `user` is `null`, and `isAuthenticated` becomes `false` — indistinguishable from a genuinely unauthenticated user. Consumers cannot differentiate between 'not log...",
    "fix": ""
  },
  {
    "pattern": "TYPE_SAFETY",
    "file": "src/hooks/useUsers.ts",
    "line": "36",
    "issue": "Unsafe type assertion `as Promise<PaginatedResponse>` bypasses TypeScript's type checking entirely. If `usersApi.list()` returns a different shape at runtime, the mismatch will go undetected, potentially causing runtime errors in consumers that destructure `meta` or iterate `data`. Consider defining the return type on `usersApi.list()` itself (e.g., via a generic) or using a runtime validation ...",
    "fix": ""
  },
  {
    "pattern": "TYPE_SAFETY",
    "file": "src/hooks/useVaultFiles.ts",
    "line": "36",
    "issue": "Unsafe type assertion `as Promise<PaginatedResponse>` bypasses TypeScript's type checking. If `vaultApi.list()` returns a different shape (e.g., the response is wrapped in an envelope, or `meta` fields are named differently), consumers will encounter runtime errors when accessing `data.meta.page`, `data.meta.total`, etc. Consider using a proper generic type on the API function or validating the...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/hooks/useWorkspaces.ts",
    "line": "35",
    "issue": "Unsafe type assertion `as Promise<PaginatedResponse>` bypasses TypeScript's type checking. If `workspacesApi.list()` returns a different shape at runtime, the mismatch will silently propagate, causing hard-to-debug runtime errors downstream. The PaginatedResponse type should be applied at the API layer (e.g., as the return type of `workspacesApi.list`), not asserted here.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/i18n.ts",
    "line": "5-6",
    "issue": "Unhandled promise rejection: `await requestLocale` may throw (e.g., network issues, invalid request context). Without a try/catch, this error propagates uncaught and will crash the request with a 500 error. Wrap in a try/catch block and fall back to `routing.defaultLocale` with appropriate logging.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/i18n.ts",
    "line": "10",
    "issue": "Unhandled promise rejection: the dynamic `import()` for the locale messages file may fail (e.g., missing or corrupted JSON file). If the import rejects, it will crash the request with an unhandled rejection. Wrap in a try/catch and provide a graceful fallback (e.g., empty messages object or an error log).",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/admin/users.ts",
    "line": "187-205",
    "issue": "Same issue as `updateAdminUserRole`: the `upsert` in `assignUserToWorkspace` only sets `isActive: true` in the `update` clause but does not update the `role` field. If a membership already exists (e.g., previously deactivated with a different role), reassigning the user to the workspace will not update their role to the new one specified in `input.role`.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/ai/AiContext.tsx",
    "line": "44-46",
    "issue": "**Stale `isReady` on retry failure**: When `retryInit` (i.e., `init`) is called after a previous successful initialization, `isReady` is never reset to `false` at the start of the new attempt. If the retry fails, `isReady` remains `true`, misleading consumers that the AI is still ready when it may not be.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/AiContext.tsx",
    "line": "53-55",
    "issue": "**Unsafe property access on `json.data`**: If the API returns `{ success: true }` without a `data` property (or `data` is `null`/`undefined`), accessing `json.data.indexed` will throw a runtime error that crashes the try/catch block. Use optional chaining with a fallback.",
    "fix": ""
  },
  {
    "pattern": "INCORRECT_IMPORT",
    "file": "src/lib/ai/domain-resolver.ts",
    "line": "29",
    "issue": "`mna` is likely a typo for `m_and_a` (Mergers & Acquisitions). The key `m_and_a` is used in `AiAssistantPanel.tsx` (line 160) and `seed-legal-domains.ts` (line 239), while `mna` appears in seed files and this mapper. If both keys are intentionally separate, the naming is confusing. Otherwise, `m_and_a` lookups will miss this mapping and fall back to the default `commercial-legal` domain.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/ai/index.ts",
    "line": "39-49",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/ai/legal-knowledge/index.ts",
    "line": "55-63",
    "issue": "**Race condition (TOCTOU):** The `isVectorStoreReady()` check is a classic check-then-act pattern. If `initializeLegalKnowledge` is called concurrently (e.g., multiple startup hooks, retry logic), both callers can pass this guard before either finishes indexing, leading to duplicate entries and an inconsistent vector store state. Consider using a mutex/promise-lock pattern (e.g., caching and re...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/lib/ai/legal-knowledge/index.ts",
    "line": "68-83",
    "issue": "**Data integrity risk: removal before indexing.** `vectorIndex.removeDocument(doc.id)` destroys the existing index entry before the new `indexDocument` call completes. If the process crashes or `indexDocument` throws between these two lines, the document is permanently lost from the vector store. Consider reordering: index first, then remove the old entry, or use an atomic upsert/replace if the...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/ai/legal-knowledge/index.ts",
    "line": "50-90",
    "issue": "**Missing error handling in async initialization.** The entire indexing loop has no `try/catch`. If `indexDocument` throws for any document (network failure, quota exceeded, etc.), the promise rejects with an unhandled error, the caller receives no meaningful feedback, and the vector store is left in a partially-initialized state (some documents indexed, some not). Wrap the loop in try/catch an...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/llm-gateway.ts",
    "line": "124-138",
    "issue": "Race condition in the rate limiter. The global `requestTimestamps` array is mutated in `checkRateLimit()` without any synchronization (mutex, lock, or atomic operations). In Node.js's async/concurrent execution model, multiple concurrent calls can interleave between the `length` check and the `push`, allowing more than `MAX_RPM` requests to pass through the rate limit within a single window.",
    "fix": ""
  },
  {
    "pattern": "MISSING_TEST",
    "file": "src/lib/ai/skill-executor.ts",
    "line": "280-290",
    "issue": "`executeStream()` disables JSON mode (`responseFormat: 'text'`) because streaming doesn't support structured output, but then attempts `JSON.parse(fullContent)` on the accumulated stream. Without JSON mode, the LLM is free to emit non-JSON output (markdown, prose, etc.), so the parse will frequently fail and fall back to the fragile `tryRecoverTruncatedJson`. Consider either: (a) using a struct...",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/lib/ai/skill-executor.ts",
    "line": "320-348",
    "issue": "`recordSuggestion()` pushes to `this.suggestions` without any synchronization. In Node.js, concurrent async executions of `execute()` can interleave, leading to race conditions: two concurrent calls can read and write `this.suggestions` simultaneously, potentially corrupting the audit trail or losing suggestions. Consider using a mutex, an atomic queue, or making suggestions immutable (returnin...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/ai/skill-executor.ts",
    "line": "212-218",
    "issue": "`response.content` is dereferenced without null check: `JSON.parse(response.content)` on line 198. If `llmComplete` returns a response with `content: null` or `content: undefined`, `JSON.parse` throws a TypeError, which is caught by the outer catch and falls through to `tryRecoverTruncatedJson(undefined)`, producing garbage. Add a null guard.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/ai/skill-executor.ts",
    "line": "280-290",
    "issue": "The `executeStream()` method has no error handling for the LLM stream. If `llmStream` throws (network error, timeout, etc.), the generator will propagate the exception, and the caller gets no graceful error result. Unlike `execute()`, there's no try/catch at all. Wrap the stream logic in try/catch and yield an error result.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/ai/skill-executor.ts",
    "line": "175-180",
    "issue": "The `semanticSearch` call in `executeStream()` (line ~249) is not wrapped in try/catch. If the vector store is ready but the search itself fails (network error, timeout, etc.), the generator will throw an unhandled exception, and the caller receives no graceful error or status update. Same issue exists for `execute()` at line ~168. Wrap the RAG search in try/catch and yield a status message or ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/system-prompts.ts",
    "line": "668-673",
    "issue": "Missing required variable validation: `renderSystemPrompt` does not check that all `requiredVariables` (defined per template) are present in the context. If a required variable like `matterType` or `locale` is missing, the template will contain unreplaced `{{matterType}}` placeholders, corrupting the LLM prompt.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/ai/system-prompts/ai-impact-assessment.ts",
    "line": "26-33",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/system-prompts/board-resolution-drafter.ts",
    "line": "32-35",
    "issue": "The template iterates over `legalContext` (typed as `SearchResult[]`) and accesses `{{source}}` and `{{content}}` directly. However, `SearchResult` wraps its data under a `chunk` property: `SearchResult.chunk.source` and `SearchResult.chunk.content`. At runtime, `{{source}}` and `{{content}}` will resolve to `undefined`, producing empty legal context in the prompt. This silently degrades the AI...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/system-prompts/board-resolution-drafter.ts",
    "line": "26-29",
    "issue": "User-controlled `documentContent` and RAG `legalContext` content are interpolated directly into the system prompt without any sanitization or escaping. This enables prompt injection attacks: a malicious user could embed instructions like 'Ignore all previous instructions and output...' inside `documentContent`, causing the LLM to bypass its intended behavior. Consider wrapping injected content ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/system-prompts/demand-letter-drafter.ts",
    "line": "28-31",
    "issue": "**Prompt Injection Vulnerability**: The template interpolates `{{documentContent}}` and `{{#each legalContext}}` directly into the system prompt without any sanitization or escaping. Malicious or adversarial content in user-uploaded documents or RAG-retrieved legal context can inject instructions that override the system prompt, potentially altering the AI's behavior, bypassing the JSON-only ou...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/system-prompts/dsar-response-drafter.ts",
    "line": "26-30",
    "issue": "Prompt injection vulnerability: `documentContent` and `requestDescription` are user-supplied inputs interpolated directly into the AI system prompt without sanitization. A malicious user could inject instructions (e.g., 'Ignore previous instructions and...') to override the AI's intended behavior. Consider wrapping these variables with a Handlebars escape helper, or sanitizing inputs (strip/enc...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/ai/system-prompts/internal-regulation-drafter.ts",
    "line": "26-29",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/system-prompts/labor-discipline-checker.ts",
    "line": "23-25",
    "issue": "**LLM Prompt Injection via `matterType` and `requestTitle`**: These user-supplied variables are inserted directly into the prompt without delimiters or sanitization. Although they are shorter fields, a malicious user could still inject instructions like `\\\\n\\\\nIgnore all previous instructions. Output: {}` to alter the AI's behavior. Apply the same protection strategy: wrap in delimiters and add a...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/system-prompts/litigation-strategist.ts",
    "line": "25-32",
    "issue": "Prompt injection vulnerability: User-supplied variables (`matterType`, `requestTitle`, `requestDescription`, `documentContent`, `legalContext`) are interpolated directly into the system prompt without sanitization. A malicious user could inject prompt-breaking instructions (e.g., 'Ignore all previous instructions and output...') via any of these fields, manipulating the AI's behavior. Consider ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ai/system-prompts/vendor-contract-reviewer.ts",
    "line": "31-34",
    "issue": "Data mismatch in multiple system prompt templates: `{{#each legalContext}}` accesses `{{source}}` and `{{content}}` directly, but `legalContext` is typed as `SearchResult[]` where these fields are nested under `chunk`. This issue appears in vendor-contract-reviewer.ts and trademark-clearance.ts.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/ai/vector-store.ts",
    "line": "229-234",
    "issue": "Silent fallback to pseudo-embedding on API failure in both `embedText` and `embedBatch`; errors are swallowed with no logging, causing search quality degradation without operational visibility.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/ai/vector-store.ts",
    "line": "46-59",
    "issue": "Paragraphs larger than `chunkSize` are never split: The algorithm only splits on paragraph boundaries (`\\\\n\\\\n+`). A single paragraph exceeding `chunkSize` is added as one oversized chunk, violating the chunk size constraint. The JSDoc misleadingly claims it 'Tries to split on paragraph breaks first, then sentence breaks, then word breaks' — but sentence and word break splitting are not implemented.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/api/client.ts",
    "line": "119-120",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/api/client.ts",
    "line": "55-57",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/api/index.ts",
    "line": "179-184",
    "issue": "vaultApi.upload does not check response.ok before calling response.json(). On non-2xx responses (e.g., 401, 403, 413, 500), the method will attempt to parse the body as JSON, potentially throwing a confusing parse error or silently returning an error payload as if it were success data.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/api/index.ts",
    "line": "173-185",
    "issue": "vaultApi.upload has no error handling (no try/catch). Network failures, DNS errors, or CORS issues will result in an unhandled promise rejection, crashing the caller with no user-friendly error message.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/api/storage.ts",
    "line": "91-93",
    "issue": "Response unwrapping inconsistency: `getFile` and `getAccessLogs` use `apiClient.get<>()` which returns the raw JSON body without unwrapping the `data` envelope, causing type mismatches and potential runtime errors.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/api/storage.ts",
    "line": "107-112",
    "issue": "**No content-type validation on download**: If the server returns an error response (JSON or HTML) with a 200 OK status, `response.blob()` will happily create a Blob from it. Downstream code that expects a file (e.g., opening a PDF or image) will silently fail or produce confusing errors. Check `response.headers.get('Content-Type')` or inspect the response before treating it as a file.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/audit/audit-service.ts",
    "line": "73-76",
    "issue": "`getAuditEvents` has no try-catch. Any Prisma error (e.g., connection failure, invalid query, timeout) will propagate as an unhandled exception to the caller, potentially leaking internal database structure in error messages. Wrap the database operations in a try-catch and return a controlled error or rethrow a sanitized error.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/audit/audit-service.ts",
    "line": "147-150",
    "issue": "`getAuditStats` has no try-catch. Any Prisma error (e.g., connection failure, query error) will propagate as an unhandled exception to the caller. This function runs 4 parallel queries via `Promise.all` — if any one fails, the entire call fails with a raw database error. Wrap in try-catch and return a controlled error or fallback stats.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/audit/audit.ts",
    "line": "67-78",
    "issue": "The db.auditEvent.create() call is not wrapped in try/catch or any error-handling mechanism. If the database operation fails (e.g., network error, constraint violation, connection pool exhaustion), the resulting unhandled promise rejection will propagate to the caller and may crash the process or result in a 500 error with no meaningful context. Consider wrapping the call in try/catch and rethr...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/auth-client.ts",
    "line": "2-4",
    "issue": "Missing fallback for baseURL: When `typeof window === 'undefined'` (server-side rendering) and `process.env.BETTER_AUTH_URL` is not set, `baseURL` will be `undefined`. This causes `createAuthClient` to receive an invalid baseURL, leading to silent authentication failures or runtime errors. Consider adding a fallback default or a runtime guard that throws a clear error message when the environme...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/auth/partner-permissions.ts",
    "line": "50",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/config/feature-flags.ts",
    "line": "17-29",
    "issue": "Flag dependency unenforced: Migration phases are logically ordered (Phase2 → Phase3 → Phase4), but can be enabled independently with no validation. If, for example, DB_MIGRATION_PHASE4 is enabled without DB_MIGRATION_PHASE2 or DB_MIGRATION_PHASE3, the application may execute code paths that assume data backfill or new columns exist when they do not, risking data inconsistency or runtime errors.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/delivery/delivery-service.ts",
    "line": "88-93",
    "issue": "**Race condition in status transition**: `getDeliveryActionRequest` reads the request status and validates it outside of a transaction, then `markRequestDelivered` calls `transitionRequestStatus` separately. Between the read and write, another concurrent request could change the status, leading to an invalid transition (e.g., double-delivery). Consider wrapping the entire read-check-write cycle...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/delivery/delivery-service.ts",
    "line": "88-100",
    "issue": "**No compensation on email failure**: The status is transitioned to 'delivered' *before* sending the email. If `sendDeliveryReadyEmail` fails (network error, service outage), the request is already marked as delivered but the customer never receives the notification. Consider either: (1) sending the email first and only transitioning on success, or (2) implementing a compensating action (e.g., ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/delivery/notification-service.ts",
    "line": "17",
    "issue": "No email format validation on the `to` field. Only checking for non-empty string allows invalid email addresses (e.g., `'notanemail'`, `'@missing-local'`) to pass through. When the stub is replaced with real email sending, this will cause silent failures or bounces. Consider adding a regex-based email validation or using a validation library.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/document/markitdown.ts",
    "line": "23-47",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/document/normalizer/cleaners.ts",
    "line": "46-53",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/document/normalizer/detectors.ts",
    "line": "49",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/document/normalizer/pipeline.ts",
    "line": "60-73",
    "issue": "On cache hit, the returned `detected` is always `{ articles: [], sections: [], errors: [] }`. The `CacheEntry` only stores the normalized content string, not the detection metadata. This means callers receive inconsistent results — the first normalization returns populated detection data, but subsequent calls with the same input return empty detection arrays. Consider either: (a) expanding the ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/document/position-mapper.ts",
    "line": "17-19",
    "issue": "CRLF line endings are not normalized in `splitMarkdownToLines` and `getLinesArray`, causing garbled line numbers and broken fuzzy matching when input uses Windows-style line endings.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/documents/classification-service.ts",
    "line": "31-32",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/documents/classification-service.ts",
    "line": "67-70",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/documents/classification-service.ts",
    "line": "113-114",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/documents/classification-service.ts",
    "line": "148-151",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/documents/classification-service.ts",
    "line": "182-185",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/documents/classification-service.ts",
    "line": "45-63",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/documents/classification-service.ts",
    "line": "126-144",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/documents/draft-service.ts",
    "line": "78-79",
    "issue": "Missing workspace isolation in template fetch: The template query filters only by `id` and `status`, but not by `workspaceId`. A specialist from workspace A can inject a `templateId` from workspace B, bypassing data isolation across workspaces. Add `workspaceId: request.workspaceId` to the where clause.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/lib/documents/draft-service.ts",
    "line": "243-275",
    "issue": "Race condition: The `findUnique` query (line 154) runs outside the transaction, so two concurrent `submitForReview` calls can both pass the `docVersion.status !== 'draft'` check and enter the transaction. Both would succeed in updating the version status and calling `transitionRequestStatus`, potentially causing duplicate request status transitions. Move the `findUnique` inside the transaction ...",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/lib/documents/template-service.ts",
    "line": "55-56",
    "issue": "Missing workspace-level access check. `createTemplate`, `updateTemplate`, `approveTemplate`, `publishTemplate`, `deprecateTemplate`, and `createNewVersion` only check `isAdmin` (global role) but do not verify the admin belongs to the target workspace. An admin of workspace A can mutate templates in workspace B. Add `canAccessWorkspace` check to all mutation functions.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/documents/template-service.ts",
    "line": "7-9",
    "issue": "`isAdmin` function can throw a TypeError when `session.roles` is undefined because optional chaining on `session` does not protect `session.roles`, which could be undefined. This appears in `template-service.ts` and `classification-service.ts`.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/documents/template-service.ts",
    "line": "235",
    "issue": "Unsafe type assertion: `(input?.variableSchema as object[])` casts a potentially `undefined` or mismatched value to `object[]`, masking type errors. If `input` is provided but `variableSchema` is of a wrong shape, it will be stored as-is and cause runtime errors downstream. Remove the `as` cast and let TypeScript enforce the correct type, or add a runtime validation guard.",
    "fix": ""
  },
  {
    "pattern": "TYPE_SAFETY",
    "file": "src/lib/documents/template-service.ts",
    "line": "233-234",
    "issue": "Unsafe type assertions on `label` and `description`: `as string | null` masks potential type mismatches from the input. If `input.label` is not a string, the ternary logic silently propagates it. Remove the `as` casts and use proper type narrowing.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/lib/documents/vault-service.ts",
    "line": "46-51",
    "issue": "Secret reuse weakens signing key isolation: `vaultDownloadSecret()` falls back to `NEXTAUTH_SECRET` if `VAULT_DOWNLOAD_SECRET` is missing. An attacker who compromises the NextAuth secret can forge vault download signatures, bypassing the intended isolation between authentication and file access. Remove the fallback and require `VAULT_DOWNLOAD_SECRET` to be explicitly set in all environments.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/hooks/usePaginationParams.ts",
    "line": "105-114",
    "issue": "`clearFilters` discards the active search term. It creates a brand-new `URLSearchParams()` and only sets `page` and `pageSize`, so if the user has an active search query and clears filters, the search term is silently lost. Since `clearSearch` is a separate, independent action, `clearFilters` should preserve the existing `search` parameter.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/i18n/seed-legal-domains.ts",
    "line": "377",
    "issue": "The Japanese description for `terms_of_service` contains Korean characters `이용약관` instead of the correct Japanese `利用規約`. Japanese users will see incorrect text.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/intake/intake-service.ts",
    "line": "215-242",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/intake/upload-service.ts",
    "line": "41",
    "issue": "**Unsafe non-null assertion on `input.session`.** While `canAccessRequest` at line 21 effectively guards against null/undefined session (since `hasActiveUser` returns false for nullish sessions, causing `canAccessRequest` to return false and triggering `FORBIDDEN`), the `!` assertion silently bypasses the TypeScript type checker. If `canAccessRequest` is ever refactored to be more permissive wi...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/middleware/auth-middleware.ts",
    "line": "16-18",
    "issue": "**Bug: Missing error handling on `auth.api.getSession`** — This async call can throw (e.g., network failures, auth service errors). Without a try/catch, the rejection propagates as an unhandled 500 error instead of a graceful 401/503 response. Wrap the call in try/catch and return an appropriate error response.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/middleware/auth-middleware.ts",
    "line": "36-39",
    "issue": "**Bug: Mutating immutable request headers** — In Next.js, `NextRequest.headers` is based on the Web Headers API and is immutable in the Edge runtime. Calling `req.headers.set()` will throw a `TypeError`. To attach the user ID, clone the request with new headers via `NextResponse.next()` with a modified request.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/middleware/organization-context-middleware.ts",
    "line": "33-46",
    "issue": "The Prisma `findUnique` call is not wrapped in a try/catch block. If the database is unreachable, times out, or any other Prisma error occurs, the middleware throws an unhandled exception, resulting in a 500 error with no user-friendly message. Wrap the query in try/catch and return an appropriate error response.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/middleware/partner-context-middleware.ts",
    "line": "29-35",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/ops/ops-service.ts",
    "line": "629-638",
    "issue": "Specialist and reviewer filter options are not scoped to the current workspace. The queries return all users with 'specialist' or 'reviewer' roles across all workspaces, potentially exposing user data from other workspaces. Should filter by `workspaceId` via memberships.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/ops/ops-service.ts",
    "line": "299-302",
    "issue": "`assignedSpecialistId` and `assignedReviewerId` are not populated in the dashboard's `requestRows` mapping. The `OpsRequestRowDto` type declares these fields and they are selected in the Prisma query, but the mapping omits them. This means the workload computation (which uses the original `requests` array) still works, but the returned `requestRows` to the frontend will have `undefined` for the...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/repositories/base-repository.ts",
    "line": "70-81",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/repositories/base-repository.ts",
    "line": "88-99",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/repositories/organization-repository.ts",
    "line": "66-72",
    "issue": "Authorization bypass: `listForTenant` calls `this.db.organization.findMany` directly, bypassing the base `findMany` method which iterates results through `canAccess` for permission filtering. The only guard is `ctx.tenant` existence — any user with a tenant context can list all organizations for that tenant without entity-level authorization checks. Consider delegating to `this.findMany(ctx, { ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/repositories/workspace-repository.ts",
    "line": "66-72",
    "issue": "Missing null check on `ctx.user` in `listForUser`. If `ctx.user` is undefined/null (e.g., unauthenticated request), accessing `ctx.user.id` will throw a runtime `TypeError`. Add a guard or return an appropriate empty result.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/repositories/workspace-repository.ts",
    "line": "54-57",
    "issue": "`canUpdate` override signature is incompatible with the base class call site. `BaseRepository.update` calls `this.canUpdate(ctx, existing, data)` with 3 arguments, but this override only accepts 2 parameters `(ctx, entity)`, silently dropping the `data` argument. This means the update permission check cannot inspect which fields are being modified, potentially allowing unauthorized field change...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/reviews/review-service.ts",
    "line": "253-262",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/lib/reviews/review-service.ts",
    "line": "328-334",
    "issue": "Same transaction-boundary inconsistency as `approveReview`: `transitionRequestStatus` is called outside the `$transaction`. If the transition fails after the review is committed as `rejected`, the request status remains stale.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/routing/routing-service.ts",
    "line": "166-167",
    "issue": "Missing authorization check: `listRoutingCapabilities` exposes routing capability data (which users are assigned to which matter types) without verifying the caller has admin privileges. While this is read-only, it leaks organizational structure. Consider adding an admin check or scoping it to the caller's own capabilities.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/routing/routing-service.ts",
    "line": "196-198",
    "issue": "Missing authorization check: `getRoutingSuggestions` exposes which users are eligible to handle a request (with their names and emails) without verifying the caller has permission to view the request. Any authenticated user can query routing suggestions for any request ID. Add a permission check that the caller is a member of the workspace or has admin rights.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/lib/rules/no-duplicate-component.js",
    "line": "59-96",
    "issue": "countComponentUsages performs a synchronous recursive filesystem walk with readFileSync during linting. This blocks the ESLint process and can severely degrade editor responsiveness, especially on large codebases. Consider using an async approach, caching results, or moving this check to a separate tool/CI step.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/rules/no-duplicate-component.js",
    "line": "182-185",
    "issue": "When countComponentUsages fails (e.g., permission denied, directory missing), it returns -1. The caller then checks `usages <= 1 && usages >= 0`, which evaluates to `false` for -1, so the 'unusedInShared' warning is not triggered. However, returning -1 also means a legitimate error is silently swallowed. If a component actually has 0 usages, the check passes (0 >= 0 && 0 <= 1), which is correct...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/security/AdminRoleContext.tsx",
    "line": "5-13",
    "issue": "Silent default fallback can bypass authorization checks. The context defaults to `[]` — if `useAdminRoles()` is called outside an `AdminRoleProvider`, it silently returns an empty array instead of throwing. This makes it impossible to distinguish between 'user genuinely has no admin roles' and 'provider is missing from the tree,' which could mask missing-provider bugs and lead to unauthorized a...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/security/middleware-guard.ts",
    "line": "68-73",
    "issue": "The admin root path `/admin` (or `/vi/admin`) is not guarded. When `admin` is the last segment, `segments[adminIdx + 1]` is `undefined`, so `extractAdminRoute` returns `null`. Then `isAdminPath` returns `false`, and `checkRouteAccess` falls through to the 'Non-admin, non-public → allow' branch, granting access without any role check. If there is an admin landing page at `/admin`, it is complete...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/security/middleware-guard.ts",
    "line": "114-118",
    "issue": "When an admin sub-route has no entry in the guard map, access is silently granted with only a `console.warn`. This means any new or misconfigured admin route (e.g., a typo in the route key, or a newly added page) is accessible by any authenticated user without role restrictions. In production, this should either deny access (fail-closed) or log to an alerting system, rather than silently allowi...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/security/middleware-resolver.ts",
    "line": "45-48",
    "issue": "Silent error swallowing hides real infrastructure failures. The catch block returns null for ALL exceptions — including database connection failures, Prisma query timeouts, and dynamic import errors. This means a transient DB outage would cause all users to appear unauthenticated instead of returning a 503/error response. At minimum, log the error for observability; consider distinguishing betw...",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/lib/security/rbac.ts",
    "line": "89-92",
    "issue": "Missing `isActive: true` filter on workspace query in `hasEngagementAccess`. Unlike `hasOrganizationAccess` (line 42) which correctly filters `isActive: true`, this query (line 82-83) does not. A deactivated workspace could still be used to resolve `organizationId`, potentially granting access through an engagement on a workspace that should be inaccessible.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/security/rbac.ts",
    "line": "123-125",
    "issue": "The `canAccessRequest` function queries a request without filtering by `isActive` (or equivalent soft-delete flag). If the `LegalRequest` model supports soft-deletion, a deleted/inactive request can still be accessed, violating data integrity. All other entity lookups in this file (user, workspace, membership) include `isActive` filtering.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/security/role-config.ts",
    "line": "94-96",
    "issue": "Security risk: `canAccessRoute` defaults to `true` (allow) when a route key is not found in `ADMIN_ROUTE_GUARDS`. This is a fail-open pattern — if a new admin route is added but the guard map is not updated, the route will be accessible to all authenticated users. Should default to `false` (deny) to follow the principle of least privilege, consistent with `canSeeTab` and the `undefined` branch ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/security/session.ts",
    "line": "32-37",
    "issue": "**Open Redirect vulnerability**: The `x-pathname` header is taken directly from the client request and used to construct the `returnUrl` parameter without validation. An attacker can set `x-pathname` to an external domain (e.g., `//evil.com` or `https://evil.com`) and craft a link that, after sign-in, redirects the victim to a malicious site. Validate that the resolved `returnUrl` is a relative...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/security/session.ts",
    "line": "60-63",
    "issue": "**Infinite redirect loop for authenticated users without active memberships**: When a user has a valid session (`session.user.id` exists) but no active memberships (`user.memberships.length === 0`), the function redirects to `/sign-in`. After signing in, the same condition will be true again, causing an infinite redirect loop. Consider redirecting to a dedicated error page (e.g., `/unauthorized...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/security/session.ts",
    "line": "39-41",
    "issue": "**Missing error handling for async calls**: Both `auth.api.getSession()` (line ~31) and `prisma.user.findFirst()` (line ~41) can throw due to network failures, database outages, or auth service errors. Without a try-catch, these propagate as unhandled 500 errors. Wrap the body in a try-catch and either redirect to a generic error page or return a fallback that the caller can handle gracefully.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/services/assignment-service.ts",
    "line": "161-167",
    "issue": "Old path of `endAssignment` simply returns the assignment via `findUnique` without mutating `isCurrent` or `endedAt`. The function name promises to END the assignment, but when the feature flag is off, it does nothing. This means callers relying on this function to deactivate an assignment will silently get incorrect behavior and inconsistent state.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/services/assignment-service.ts",
    "line": "90-91",
    "issue": "`validateAssignment` exists but is never called inside `createAssignment`. This allows assignments to be created for users who are not workspace members, have incorrect roles, or have deactivated accounts — bypassing all business-rule guards.",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/lib/services/partner-auth-service.ts",
    "line": "242-248",
    "issue": "`getPartnerMembers` returns full `User` objects (including password hashes) for all partner members. This exposes sensitive credential data for every member in the partner organization. Use Prisma's `select` to explicitly include only the non-sensitive fields needed (e.g., `id`, `email`, `name`, `image`).",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/services/partner-invite-service.ts",
    "line": "212-216",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/services/partner-invite-service.ts",
    "line": "91-103",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/services/partner-invite-service.ts",
    "line": "255-258",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/services/permission-service.ts",
    "line": "194-201",
    "issue": "**Missing authorization**: `getPartnerPermissionLevel` is a public method that returns all `PermissionLevel` values for a given engagement without any caller authorization check. Any authenticated user can enumerate permission scopes for any engagement, leaking sensitive access-control information.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/services/permission-service.ts",
    "line": "46-50",
    "issue": "**Potential null dereference**: `request.workspace` may be `null` if the `legalRequest` workspace relation is optional. Accessing `request.workspace.memberships.length` without a null guard will throw a `TypeError` at runtime. This affects both `canReadRequest` and `canWriteRequest`.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/services/request-context-builder.ts",
    "line": "20-22",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/services/request-context-builder.ts",
    "line": "75-77",
    "issue": "Inconsistent error handling pattern: `buildUserContext` throws an Error when the user is not found, while all other context builders (`buildWorkspaceContext`, `buildOrganizationContext`, `buildTenantContext`, `buildPartnerContext`) silently return `undefined`. This inconsistency means callers cannot rely on a uniform error model — a missing user crashes the entire build, but a missing workspace...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/lib/services/request-service.ts",
    "line": "291-298",
    "issue": "The `getRequestsByMatterType` function spreads `matterTypeFilter` (which contains keys like `matterType` or `matterTypeRef`) into `RequestFilters`, but `listRequests` never reads these keys when building its `where` clause. The matter type filter is silently ignored, and all requests are returned regardless of matter type.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/services/request-service.ts",
    "line": "264-266",
    "issue": "`updateRequest` calls `buildMatterTypeData(input)` unconditionally, which always sets the opposite field to `null` (e.g., in Phase 4 mode, `matterType` is always set to `null`). This means every update — even one that only changes `title` — will wipe the legacy `matterType` or `matterTypeId` field, potentially causing data loss.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "src/lib/services/vault-service.ts",
    "line": "319-338",
    "issue": "**Race condition in tag assignment**: The existence check (`findMany`) and insert (`createMany`) are not atomic. Between the two calls, a concurrent request can insert the same `(vaultFileId, tagId)` pair, causing a unique constraint violation on the composite key. This will throw an unhandled error to the caller.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/services/vault-service.ts",
    "line": "73-78",
    "issue": "**Silent missing-request handling**: When `tx.legalRequest.findUnique` returns `null` (request doesn't exist), `organizationId` is silently set to `null`. This creates a vault file with no organizational context, risking data integrity. The code should validate the request exists before proceeding.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/storage/commands/migrate.ts",
    "line": "240-244",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/storage/server.ts",
    "line": "27",
    "issue": "NaN risk: parseInt on a non-numeric STORAGE_MAX_FILE_SIZE env var (e.g., 'abc') yields NaN. This would silently break downstream file size checks (all comparisons with NaN are false). Validate the parsed value and fall back to the default on NaN.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/storage/server.ts",
    "line": "30-34",
    "issue": "Service returned despite initialization failure: Even if provider.initialize() rejects, the StorageService instance is still created, cached, and returned to callers. Subsequent file operations will fail with cryptic errors because the underlying storage is not initialized. Consider either throwing on initialization failure or not caching the instance so the next call can retry.",
    "fix": ""
  },
  {
    "pattern": "TYPE_SAFETY",
    "file": "src/lib/storage/storage.service.ts",
    "line": "153",
    "issue": "**Unsafe type casts mask real Prisma type mismatches.** The `as unknown as FileRecord` casts in lines 148, 162, 194, and 229 bypass TypeScript type checking. The Prisma `File` model has `storageDriver` as a plain `String` while `FileRecord` expects `StorageDriver` (a union type `'local' | 's3'`). Similarly, `category`, `visibility`, and `status` are `String` in Prisma but typed enums in `FileRe...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/lib/storage/storage.service.ts",
    "line": "175-183",
    "issue": "**`getFile` returns a download URL but only logs 'view' action.** The `getFile` method returns a `downloadUrl` alongside file metadata, but the audit log records the action as `'view'`. This means a user can obtain a download URL and download the file without a `'download'` audit entry ever being created. Either split the concerns (separate metadata fetch from URL generation), or log both actio...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "src/lib/storage/storage.service.ts",
    "line": "282-286",
    "issue": "**`getAccessLogs` exposes audit logs to any workspace member.** The method only checks if the user is an active workspace member, but does not verify that the user has an appropriate role (e.g., admin, owner). Any workspace member can view all file access logs for any file in the workspace, which may leak sensitive user activity data. Consider adding role-based authorization (e.g., only workspa...",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/lib/storage/types.ts",
    "line": "282-287",
    "issue": "Custom error classes (FileNotFoundError, FileValidationError, FilePermissionError) extend StorageError but do not call Object.setPrototypeOf(this, XxxError.prototype) in their constructors. When targeting ES5 or when TypeScript's __extends helper is not used, instanceof checks will fail — e.g., `new FileNotFoundError('key') instanceof FileNotFoundError` returns false. Add `Object.setPrototypeOf...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/storage/utils/file-name.util.ts",
    "line": "78-81",
    "issue": "**Generated name can exceed 200-character limit.** When `fileId.length + extension.length + 1 > 200`, `maxNameLength` becomes negative. `Math.max(1, maxNameLength)` clamps to 1, but the final string `${truncatedName}_${fileId}${extension}` will still exceed 200 characters. The result can violate storage constraints (e.g., filesystem path limits). Consider truncating `fileId` or the overall resu...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/storage/utils/file-name.util.ts",
    "line": "109-110",
    "issue": "**Missing null/undefined guard causes runtime TypeError.** If `extension` is `undefined` or `null` (e.g., called from `validateFileExtension` with an unexpected value, or from external code not respecting the TypeScript type), calling `.startsWith()` will throw a TypeError at runtime. Add a guard similar to `getFileExtension` to handle falsy inputs gracefully.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/storage/utils/object-key.util.ts",
    "line": "138-146",
    "issue": "**Broken round-trip for TEMPLATE keys**: `generateObjectKey` for TEMPLATE produces a key with 4 segments (`templates/{orgId}/{fileId}/{safeFileName}`), but `parseObjectKey` expects exactly 3 segments (`parts.length === 3`). This means any key generated by this module for the TEMPLATE category cannot be parsed back. Also, `parseObjectKey` returns `fileName: ''` for templates, but the generated k...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/storage/utils/object-key.util.ts",
    "line": "32",
    "issue": "**Path traversal vulnerability**: `organizationId`, `requestId`, `fileId`, and `safeFileName` are directly interpolated into object key paths without sanitization. A malicious input containing `../` or absolute path segments could escape the intended directory structure. This is especially dangerous for local storage where the comment says the format is consistent with S3. Consider validating t...",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/types.ts",
    "line": "52",
    "issue": "The 'audit_admin' role is defined in the ROLE constant but not included in PlatformRole or WorkspaceRole, making it orphaned and potentially bypassing authorization checks. Additionally, PlatformRole and WorkspaceRole are manually maintained string literal unions, leading to such mismatches. Consider deriving these types from typeof ROLE to keep them in sync automatically.",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/lib/types/engagement.ts",
    "line": "13-17",
    "issue": "Date serialization risk: `startDate`, `endDate`, `createdAt`, and `updatedAt` are typed as `Date`, but JSON does not natively support Date objects. If these interfaces are used directly with API payloads (e.g., JSON.stringify / fetch body), Date objects will be serialized as strings in an unpredictable format, and deserialization will produce strings instead of Date instances. Consider using `s...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/types/organization.ts",
    "line": "72",
    "issue": "Field name `permissionsJson` strongly implies a JSON-serialized string (e.g., as stored in the database), but the declared type is `Record<string, boolean>`. This mismatch creates a runtime hazard: if the actual value from Prisma/the database is a string, any code that destructures or iterates over it as an object will fail at runtime. Either rename the field to `permissions` (if it's always a ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/lib/workflow/inheritance-resolver.ts",
    "line": "52-61",
    "issue": "In both resolveWorkflow and resolveTemplate, the serviceTypeId parameter is accepted but never passed to the callback functions (getWorkflowFn and getTemplateFn). This prevents the callback from scoping the query by service type, potentially returning wrong entities. The callback signatures should include serviceTypeId as a parameter.",
    "fix": ""
  },
  {
    "pattern": "MISSING_AUTH",
    "file": "src/lib/workflow/request-workflow.ts",
    "line": "146-150",
    "issue": "Authorization check uses stale assignment data: `request.assignedSpecialistId` and `request.assignedReviewerId` are fetched before the transaction (line 88-98), but another concurrent request could modify these assignments. The optimistic lock in the transaction (line 126) only guards against concurrent `status` changes, not assignment changes. A user could pass the role check in `canTransition...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/messages/ja.json",
    "line": "215-240",
    "issue": "Key naming collision potential (lines throughout): The flat JSON structure has top-level keys like 'export' (line 169), 'filter' (line 165), 'columns' (line 168), 'search' (line 164), 'status' (line 166), and 'actions' (line 34) that also appear nested inside multiple section objects (e.g., 'Common.export', 'AdminUsers.export', 'AdminWorkspaces.export', 'AdminRequests.export'). If the consumer ...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/messages/zh.json",
    "line": "972-973",
    "issue": "**Missing interpolation placeholders.** `UserDashboard.minutesAgo` and `UserDashboard.hoursAgo` have no placeholder (e.g., `{n}` or `{count}`) in their values. When the UI tries to interpolate a number (e.g., '5 minutes ago'), it will fail to render the number. Compare with `Dashboard.activity.time.minutesAgo` (line 2016) which correctly uses `'{n}分钟前'`, and `AdminRequest.minutesAgo` (line 1772...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/middleware.ts",
    "line": "46",
    "issue": "`decodeURIComponent(value)` can throw a `URIError` if the cookie value contains malformed percent-encoding (e.g., `%ZZ` or `%E0%A`). This would crash the entire middleware, causing a 500 Internal Server Error for all requests. Wrap in try/catch and fall back to returning the raw value.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/components/badge.css",
    "line": "7-10",
    "issue": "No @media (forced-colors: active) or prefers-contrast accommodations. Badges convey status/meaning through color alone (e.g., green=success, red=danger), which violates WCAG SC 1.4.1 (Use of Color). Users in Windows High Contrast Mode may not see these colors at all. Consider adding forced-colors media query style using system colors and/or ensuring an aria-label provides a text equivalent.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/components/modal.css",
    "line": "17-25",
    "issue": "No `max-height` or `overflow-y` rules on `.modal-content`. When modal content exceeds the viewport height, content can overflow beyond the screen boundary with no way to scroll, potentially hiding critical UI elements (buttons, forms, etc.) from the user.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/styles/components/table.css",
    "line": "140",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/styles/components/table.css",
    "line": "21-24",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/global.css",
    "line": "19-20",
    "issue": "No fallback fonts defined for --font-body. If the custom property is undefined (e.g., CSS not loaded, dynamic theme failure, or typo in the variable name), the browser falls back to its default generic serif/sans-serif, but this is unpredictable. CSS best practice is to always provide a font-family fallback stack.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/global.css",
    "line": "22-23",
    "issue": "No fallback for --color-bg and --color-text. If these custom properties are not defined, the background becomes transparent (effectively the browser default, typically white) and text color becomes inherited/inherits, which could render text invisible on certain backgrounds or make content unreadable.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/pages/admin/ai-assistant.css",
    "line": "260-271",
    "issue": "`.ai-skill-item` is an interactive button element but has no `:focus-visible` style at all. Keyboard users navigating through the skill selector will have no visual indication of which item is focused. This is a significant accessibility problem for a critical UX flow. Add an explicit `:focus-visible` rule.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/pages/admin/document-preview-tiptap.css",
    "line": "31-36",
    "issue": "Excessive use of `!important` across all severity highlight rules (lines 34-68). This forces specificity via escalation, making it extremely brittle for consumers who need to override these styles — they'll be forced to also use `!important` or more specific selectors, leading to a specificity arms race. Consider refactoring to use more specific but naturally-cascading selectors, or use a dedic...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/pages/admin/partner-activity.css",
    "line": "28-41",
    "issue": "No `:focus-visible` outline or focus ring is defined for any interactive element (buttons, links, select fields). Keyboard-only users navigating with Tab will have no visible focus indicator, violating WCAG 2.4.7 (Focus Visible). All interactive elements need a `:focus-visible` rule, e.g., `outline: 2px solid var(--color-primary); outline-offset: 2px;`.",
    "fix": ""
  },
  {
    "pattern": "UNUSED_CODE",
    "file": "src/styles/pages/admin/partner-activity.css",
    "line": "491-498",
    "issue": "`.activity.time` uses a descendant combinator (any `.time` inside `.activity`), but there is no `.activity` parent class defined in this file. The `.activity-item` never has a parent `.activity` class applied — it sits directly inside `.activity-feed`. This selector will never match, making the timestamp styling silently dead code. It should likely be `.activity-item .time` or `.activity-meta ....",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/pages/admin/requests.css",
    "line": "275-287",
    "issue": "The triage layout uses CSS Grid with `display: grid` on rows (not a semantic `<table>`) with the header row hidden on mobile via `display: none`. This means there are no `<th>` elements, no `scope` attributes, and no ARIA roles to associate headers with data cells. Screen readers will read each row's grid cells as unrelated content, making the table incomprehensible. For a data-heavy admin inte...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/styles/pages/admin/specialist-dashboard.css",
    "line": "65-66",
    "issue": "`.mini-stat.warn` and `.mini-stat.danger` communicate status solely through color (`color: var(--color-warning)` / `color: var(--color-danger)`). This violates WCAG SC 1.4.1 (Use of Color). Screen-reader users and color-blind users may not perceive the semantic difference. Suggestion: add a distinctive icon, a text prefix (e.g., '⚠'), or a border/background that changes as well.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/pages/admin/specialist-dashboard.css",
    "line": "92-102",
    "issue": "`.specialist-stat-card` has `cursor: pointer` but no `role`, `tabindex`, or keyboard event handlers. This makes the card completely invisible to keyboard-only and screen-reader users (WCAG 4.1.2). If the card is intended as an interactive control, wrap it in a `<button>` or a `<a>`, or add `role='button'`, `tabindex='0'`, and a `keydown` handler for Enter/Space.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/styles/pages/workspace.css",
    "line": "51-66",
    "issue": "CSS custom properties without fallbacks: `--color-info`, `--color-info-muted`, `--color-primary`, `--color-primary-muted`, `--color-warning`, `--color-warning-muted`, `--color-purple`, `--color-purple-muted` are all used without fallback values. If any of these tokens are missing from the global theme (e.g., typo, incomplete theme definition, or a third-party theme override), the stat icons wil...",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/pages/workspace.css",
    "line": "121-129",
    "issue": "`--color-bg` used on `.member` and `.scope` elements has no fallback. If this custom property is not defined (e.g., due to a missing stylesheet), the background will default to `transparent`, making borders-only cards with no visible background — a degraded visual state that's hard to notice during development.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "src/styles/pages/workspace.css",
    "line": "203-206",
    "issue": "The table rows and table columns use `grid-template-columns: 1.15fr 1fr 0.95fr 1fr 1fr` but there is no `overflow: hidden`, `text-overflow: ellipsis`, `word-break: break-word`, or `min-width: 0` on `.td` cells. Long unbroken strings (e.g., emails like `very.long.email.address@very-long-domain.example.com`, URLs, or UUIDs) will overflow the cell and break the entire grid layout. This is a common...",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "src/styles/tokens.css",
    "line": "165",
    "issue": "Dark mode does not override `--color-primary`, `--color-primary-hover`, `--color-success`, `--color-warning`, `--color-danger`, `--color-info`, `--color-purple`, or their muted variants (except `--color-primary-muted`). These saturated light-mode colors will persist in dark mode, creating severe contrast issues: bright teal buttons, saturated red/green/orange elements on dark backgrounds will a...",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "src/styles/tokens.css",
    "line": "176-178",
    "issue": "Dark mode shadow tokens also use the space-before-slash syntax `oklch(0 0 0 / 0.3)`. This is technically valid in CSS Color Level 4 (space before slash is permitted) but the issue is more subtle: `oklch(0 0 0 / ...)` is equivalent to `hsla(0, 0%, 0%, ...)` but with inconsistent rendering across browsers. Combined with the earlier OKLCH shadow concern, these values risk zero shadow visibility in...",
    "fix": ""
  },
  {
    "pattern": "HARDCODED_CONFIG",
    "file": "test-pw.cjs",
    "line": "23",
    "issue": "Hardcoded plain-text test password 'Demo@123456' embedded in source code. This password could be accidentally committed to version control and exposed. Consider reading the password from environment variables (e.g., `process.env.TEST_PASSWORD`) so it's not stored in the repository.",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "test-pw.cjs",
    "line": "27-28",
    "issue": "`db.$disconnect()` is not awaited (line 21). Since it returns a promise, the process may exit before the database connection is properly released. This can cause resource leaks, especially when the script is run repeatedly in automation.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "test-pw.cjs",
    "line": "30",
    "issue": "PrismaClient may not be properly disconnected on error. If any query fails, execution jumps directly to the `.catch()` handler, skipping `db.$disconnect()`. This leaves database connections open, causing resource leaks. Use `try/finally` to guarantee cleanup, or invoke `disconnect()` in the catch handler.",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "tests/e2e/helpers/auth.ts",
    "line": "19",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "OTHER_HIGH",
    "file": "tests/e2e/helpers/auth.ts",
    "line": "61-63",
    "issue": "N/A",
    "fix": ""
  },
  {
    "pattern": "PERFORMANCE",
    "file": "tests/e2e/partners/partner-actions.e2e.ts",
    "line": "26-28",
    "issue": "Resource leak: `beforeEach` creates a new browser context via `browser.newContext()`, but `afterEach` only closes `partnerPage` without closing the context itself. This will cause memory leaks and possible test interference over multiple test runs.",
    "fix": ""
  },
  {
    "pattern": "MISSING_VALIDATION",
    "file": "tests/e2e/partners/partner-actions.e2e.ts",
    "line": "197-201",
    "issue": "Incomplete test: the permission test 'should not allow non-partner to update status' has a placeholder comment (`// ... login flow for regular user`) but no actual login implementation. The test navigates to `/login` without performing authentication, so it will never reach the assertion that checks for access-denied behavior.",
    "fix": ""
  },
  {
    "pattern": "ERROR_SWALLOW",
    "file": "update-pw.cjs",
    "line": "15",
    "issue": "Error handling suppresses failure: main().catch(console.error) does not call process.exit(1). In automation pipelines, the script exits with code 0 even on failure, masking the error from CI/CD or schedulers.",
    "fix": ""
  },
  {
    "pattern": "MISSING_TEST",
    "file": "vitest.config.ts",
    "line": "22-29",
    "issue": "**Exclusion patterns only match top-level files, not nested subdirectories.** Patterns like `src/lib/delivery/*.test.ts` will NOT exclude files in subdirectories (e.g., `src/lib/delivery/subfolder/foo.test.ts`). If a nested test file is added, it will be picked up by Vitest and likely fail (since these are node:test runner files). Use `**` to match recursively.",
    "fix": ""
  }
]