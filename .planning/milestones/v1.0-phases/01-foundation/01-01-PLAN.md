---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - tailwind.config.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - prisma/schema.prisma
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
  - FND-05
must_haves:
  truths:
    - "Ứng dụng Next.js TypeScript khởi động được bằng npm run dev."
    - "Prisma schema có user, workspace, membership, request, assignment, document, review, vault file, workflow transition, audit event foundation."
    - "Database dev nhận schema qua npx prisma db push sau khi schema đổi."
  artifacts:
    - path: "package.json"
      provides: "Next.js, TypeScript, Prisma, Tailwind scripts and dependencies"
    - path: "prisma/schema.prisma"
      provides: "Foundation data model for roles, workspaces, RBAC targets, workflow, audit"
      contains: "model AuditEvent"
    - path: "src/app/layout.tsx"
      provides: "Root app shell base"
  key_links:
    - from: "package.json"
      to: "prisma/schema.prisma"
      via: "prisma script uses schema"
      pattern: "prisma"
    - from: "prisma/schema.prisma"
      to: "database"
      via: "npx prisma db push"
      pattern: "datasource db"
---

<objective>
Tạo nền Next.js + TypeScript + Prisma + Tailwind và schema dữ liệu foundation cho tenant, user, role, request shell, workflow, audit.

Purpose: Phase 1 cần nền bảo mật và workflow trước khi intake/document/review/vault phase sau nối vào.
Output: App skeleton, Prisma schema, schema pushed vào dev database.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
Greenfield per D-01: no existing src/app code. Create contracts explicitly in Prisma schema.

Required enum values per D-03 and D-10:
```prisma
enum Role { customer specialist reviewer coordinator_admin super_admin }
enum RequestStatus { draft_intake intake_submitted triage assigned in_progress pending_review revision_required approved delivered closed cancelled }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js TypeScript project skeleton</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, tailwind.config.ts, src/app/globals.css, src/app/layout.tsx</files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md
    .planning/phases/01-foundation/01-UI-SPEC.md
    package.json
    tsconfig.json
    src/app/layout.tsx
  </read_first>
  <action>Create greenfield modular monolith skeleton per D-01 and D-16. package.json scripts: "dev": "next dev", "build": "next build", "lint": "next lint", "typecheck": "tsc --noEmit", "prisma:generate": "prisma generate", "db:push": "prisma db push". Dependencies: next, react, react-dom, @prisma/client, lucide-react. Dev dependencies: typescript, @types/node, @types/react, @types/react-dom, prisma, tailwindcss, postcss, autoprefixer, eslint, eslint-config-next. layout.tsx uses Vietnamese metadata title "Nền tảng pháp lý SME" and system sans stack from UI spec. globals.css defines CSS variables for #F8FAFC, #FFFFFF, #0F766E, #DC2626, #0F172A, #475569, #E2E8F0, #F1F5F9; no shadcn init per UI-SPEC.</action>
  <verify>
    <automated>npm install && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    grep -q '"dev": "next dev"' package.json
    grep -q '"@prisma/client"' package.json
    grep -q 'Nền tảng pháp lý SME' src/app/layout.tsx
    grep -q '#0F766E' src/app/globals.css
  </acceptance_criteria>
  <done>Next.js skeleton installs and typechecks; UI tokens match manual Tailwind contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create Prisma foundation schema</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-foundation/01-CONTEXT.md
    prisma/schema.prisma
  </read_first>
  <action>Create Prisma schema with PostgreSQL datasource env("DATABASE_URL") and client generator. Add enums Role, RequestStatus, AssignmentKind, AuditTargetType. Add models: User(id,email,name,isActive,createdAt,updatedAt,memberships,auditEvents), Workspace(id,name,slug,isActive,createdAt,updatedAt,memberships,requests,auditEvents), WorkspaceMembership(id,userId,workspaceId,role,isActive,createdAt,updatedAt,unique userId+workspaceId+role), LegalRequest(id,workspaceId,title,status,createdById,assignedSpecialistId,assignedReviewerId,createdAt,updatedAt,workspace,createdBy,workflowTransitions,auditEvents), RequestAssignment(id,requestId,userId,kind,reason,createdAt,createdById), Document(id,workspaceId,requestId,title,createdAt,updatedAt), Review(id,workspaceId,requestId,documentId,reviewerId,createdAt,updatedAt), VaultFile(id,workspaceId,requestId,storageKey,filename,createdAt,updatedAt), WorkflowTransition(id,requestId,actorId,fromStatus,toStatus,reason,createdAt), AuditEvent(id,actorId,workspaceId,action,targetType,targetId,requestId,correlationId,metadataSummary,createdAt). Use Json? no; D-15 says no sensitive legal content in audit metadata, so metadataSummary String? optional only. Add indexes on workspaceId, requestId, role, targetType+targetId, correlationId. Implement FND-01..FND-05 foundations without deferred chat/document generation/vault storage behavior.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'enum Role' prisma/schema.prisma
    grep -q 'customer' prisma/schema.prisma
    grep -q 'model Workspace' prisma/schema.prisma
    grep -q 'model LegalRequest' prisma/schema.prisma
    grep -q 'model WorkflowTransition' prisma/schema.prisma
    grep -q 'model AuditEvent' prisma/schema.prisma
    grep -q 'metadataSummary' prisma/schema.prisma
  </acceptance_criteria>
  <done>Schema validates and contains explicit tenant, RBAC target, workflow, and append-only audit structures.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push Prisma schema to dev database</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    prisma/schema.prisma
    package.json
    .env
  </read_first>
  <action>Run required schema push after schema file changes and before final verification. Command: npx prisma db push. If Prisma prompts destructive migration, use npx prisma db push --accept-data-loss only when DATABASE_URL points to greenfield local/dev database with no production data; record this safety note in plan summary. Do not use --accept-data-loss for shared/staging/production database.</action>
  <verify>
    <automated>npx prisma db push && npx prisma generate</automated>
  </verify>
  <acceptance_criteria>
    npx prisma db push exits 0
    npx prisma generate exits 0
  </acceptance_criteria>
  <done>Dev database matches Prisma foundation schema and Prisma client generated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→database | Application writes sensitive legal tenant/user/request/audit foundation data. |
| developer→dev database | Schema push can alter database objects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | prisma/schema.prisma | mitigate | Use backend-owned RequestStatus enum and WorkflowTransition model; no raw frontend status column mutation planned outside service in Plan 03. |
| T-01-01-02 | Information Disclosure | AuditEvent.metadataSummary | mitigate | Store metadataSummary String only; no Json legal content blob; D-15 identifiers/summaries only. |
| T-01-01-03 | Elevation of Privilege | WorkspaceMembership.role | mitigate | Role enum limited to D-03 five roles; server-side RBAC service in Plan 02 consumes enum. |
| T-01-01-04 | Tampering | npx prisma db push | mitigate | Blocking task allows --accept-data-loss only for greenfield local/dev DATABASE_URL; production/shared DB blocked. |
</threat_model>

<verification>
Run: npm run typecheck; npx prisma validate; npx prisma db push; npx prisma generate.
</verification>

<success_criteria>
Foundation app skeleton exists, Prisma schema validates, database schema push succeeds, all Phase 1 core entities exist.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
