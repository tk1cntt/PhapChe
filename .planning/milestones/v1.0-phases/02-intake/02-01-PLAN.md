---
phase: 02-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - src/lib/intake/catalog.ts
  - src/lib/intake/intake-service.ts
  - src/lib/intake/intake.test.ts
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-06]
must_haves:
  truths:
    - "Customer can select a seeded legal service type."
    - "System asks structured questions for selected matter type."
    - "System stores structured answers with schema version and labels."
    - "Unsupported intake moves through backend-safe triage path without customer direct triage mutation."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "MatterType and IntakeSubmission persistence"
      contains: "model MatterType"
    - path: "src/lib/intake/catalog.ts"
      provides: "MVP service catalog and question schemas"
      exports: ["getMatterType", "getMatterQuestions"]
    - path: "src/lib/intake/intake-service.ts"
      provides: "Create draft, save answers, submit, unsupported triage service"
      exports: ["createDraftIntake", "saveIntakeAnswers", "submitIntake"]
  key_links:
    - from: "src/lib/intake/intake-service.ts"
      to: "src/lib/workflow/request-workflow.ts"
      via: "transitionRequestStatus"
      pattern: "transitionRequestStatus"
    - from: "src/lib/intake/intake-service.ts"
      to: "prisma.IntakeSubmission"
      via: "schema-versioned structured answer persistence"
      pattern: "intakeSubmission"
---

<objective>
Create structured intake domain foundation for Phase 2.

Purpose: support customer chat/form intake with seeded matter types, schema-versioned answers, backend validation, and unsupported triage through workflow/audit service per D-01 through D-10 and resolved research decisions.
Output: Prisma models, seed data, intake catalog, service functions, tests.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-intake/02-CONTEXT.md
@.planning/phases/02-intake/02-RESEARCH.md
@prisma/schema.prisma
@src/lib/workflow/request-workflow.ts
@src/lib/security/rbac.ts
@src/lib/audit/audit.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persisted matter type and intake submission models</name>
  <files>prisma/schema.prisma, prisma/seed.ts</files>
  <read_first>
    - .planning/phases/02-intake/02-CONTEXT.md
    - .planning/phases/02-intake/02-RESEARCH.md
    - .planning/phases/02-intake/02-PATTERNS.md
    - .planning/phases/02-intake/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: seeded matter types include agency contract, labor contract, trademark registration, and unsupported/other.
    - Test 2: IntakeSubmission stores requestId, matterTypeKey, schemaVersion, answers Json, answer label metadata, and submittedAt.
  </behavior>
  <action>Add persisted `MatterType` and schema-versioned `IntakeSubmission` per resolved research question. Seed MVP catalog with stable English keys and Vietnamese labels per D-02, D-05. Do not store raw chat only. Do not add Phase 3 capability matrix.</action>
  <acceptance_criteria>
    - Modified files named in <files> exist.
    - Commands listed in <verify> exit 0.
    - Implemented code contains exact functions, routes, models, and copy named in <action>.
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Prisma validates; seed contains required MVP matter types and unsupported option.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build intake catalog and validation service</name>
  <files>src/lib/intake/catalog.ts, src/lib/intake/intake-service.ts, src/lib/intake/intake.test.ts</files>
  <read_first>
    - .planning/phases/02-intake/02-CONTEXT.md
    - .planning/phases/02-intake/02-RESEARCH.md
    - .planning/phases/02-intake/02-PATTERNS.md
    - .planning/phases/02-intake/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: required question validation blocks submit and leaves request in draft_intake per D-06.
    - Test 2: saved answers include labels/metadata, not machine keys only, per D-07.
    - Test 3: unsupported request submits through intake_submitted then backend-safe triage transition, never customer direct triage mutation, per D-08 and D-09.
  </behavior>
  <action>Create catalog helpers and service functions `createDraftIntake`, `saveIntakeAnswers`, `submitIntake`. Use `transitionRequestStatus` for draft -> intake_submitted and coordinator/system-safe backend path for unsupported intake_submitted -> triage per resolved research. Reuse RBAC/workspace checks and audit writer. Audit metadata must include ids, matter type key, counts only; no legal answer contents per D-18 through D-20.</action>
  <acceptance_criteria>
    - Modified files named in <files> exist.
    - Commands listed in <verify> exit 0.
    - Implemented code contains exact functions, routes, models, and copy named in <action>.
  </acceptance_criteria>
  <verify>
    <automated>npx tsx src/lib/intake/intake.test.ts</automated>
  </verify>
  <done>Tests prove structured persistence, required validation, audit-safe metadata, and unsupported triage path.</done>
</task>


<task type="auto" blocking="true">
  <name>Task 3: Push Prisma schema to database</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma
    - .planning/phases/02-intake/02-CONTEXT.md
    - .planning/phases/02-intake/02-RESEARCH.md
  </read_first>
  <action>Run `npx prisma db push` after schema changes and before phase verification. If Prisma reports interactive destructive-change prompt, stop and use `npx prisma db push --accept-data-loss` only after confirming destructive change is expected for local/test database.</action>
  <acceptance_criteria>
    - Terminal command `npx prisma db push` exits 0, or documented fallback `npx prisma db push --accept-data-loss` exits 0 for local/test database.
    - Terminal command `npx prisma validate` exits 0 after schema push.
  </acceptance_criteria>
  <verify>
    <automated>npx prisma db push</automated>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Live database schema matches Prisma schema before TypeScript/build verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer browser -> server action | untrusted matter keys and answers cross into backend |
| intake service -> workflow service | status mutation must remain workflow-owned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | intake-service.ts | mitigate | Validate matter key and required question keys server-side before submit. |
| T-02-01-02 | Repudiation | unsupported triage | mitigate | Use workflow/audit service for submitted -> triage transition; no direct DB status update. |
| T-02-01-03 | Information Disclosure | audit metadata | mitigate | Store ids/counts/matter type key only, no legal answer text. |
</threat_model>

<verification>
Run `npx prisma validate` and `npx tsx src/lib/intake/intake.test.ts`.
</verification>

<success_criteria>
INT-01, INT-02, INT-03, INT-06 domain foundation complete with persisted matter types, schema-versioned answers, and safe unsupported triage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake/02-01-SUMMARY.md`
</output>
