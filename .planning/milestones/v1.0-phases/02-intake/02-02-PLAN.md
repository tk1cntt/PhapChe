---
phase: 02-intake
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/intake/actions.ts
  - src/app/intake/page.tsx
  - src/app/intake/components.tsx
  - src/app/requests/[requestId]/page.tsx
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, INT-06]
must_haves:
  truths:
    - "Customer can complete service selection, guided questions, upload, review, and submit from /intake."
    - "Upload step calls backend attachIntakeFile and displays filename and size only."
    - "Review step shows matter type, answers, uploaded filenames/sizes, and privacy note before submit."
    - "Submitted request redirects to read-only Vietnamese status view."
  artifacts:
    - path: "src/app/intake/actions.ts"
      provides: "Server actions for create/save/upload/submit"
      exports: ["createIntakeDraftAction", "saveIntakeAnswersAction", "attachIntakeFileAction", "submitIntakeAction"]
    - path: "src/app/intake/page.tsx"
      provides: "Customer intake route"
    - path: "src/app/intake/components.tsx"
      provides: "Guided intake UI components"
    - path: "src/app/requests/[requestId]/page.tsx"
      provides: "Read-only customer status route"
  key_links:
    - from: "src/app/intake/actions.ts"
      to: "src/lib/intake/intake-service.ts"
      via: "server action calls create/save/submit"
      pattern: "submitIntake"
    - from: "src/app/intake/actions.ts"
      to: "src/lib/intake/upload-service.ts"
      via: "attachIntakeFileAction calls attachIntakeFile"
      pattern: "attachIntakeFile"
    - from: "src/app/intake/page.tsx"
      to: "src/app/requests/[requestId]/page.tsx"
      via: "redirect after submit"
      pattern: "redirect\\(`/requests/"
---

<objective>
Wire customer intake UI end-to-end, including upload and status route.

Purpose: deliver full customer path required by D-11 and D-16 without placeholders: service selection, questions, file upload, review, submit, and read-only status.
Output: `/intake` route, server actions, components, `/requests/[requestId]` status route.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-intake/02-CONTEXT.md
@.planning/phases/02-intake/02-RESEARCH.md
@.planning/phases/02-intake/02-UI-SPEC.md
@src/app/admin/components/ui.tsx
@src/lib/intake/intake-service.ts
@src/lib/intake/upload-service.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create server actions for intake mutations and upload wiring</name>
  <files>src/app/intake/actions.ts</files>
  <read_first>
    - .planning/phases/02-intake/02-CONTEXT.md
    - .planning/phases/02-intake/02-RESEARCH.md
    - .planning/phases/02-intake/02-PATTERNS.md
    - .planning/phases/02-intake/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `attachIntakeFileAction` accepts FormData with requestId/file, calls `attachIntakeFile`, returns filename and size.
    - Test 2: upload action never returns public URL and never accepts workspaceId from client.
    - Test 3: submit action redirects to `/requests/[requestId]` only after backend submit succeeds.
  </behavior>
  <action>Create server actions for create draft, save answers, upload, and submit. For upload, call `attachIntakeFile` from Plan 03/service output per D-11/D-13/D-16; pass requestId and uploaded File only, derive authorization/server context inside service, and return/display filename and size only. Do not leave placeholder upload UI. Do not expose public URLs. Submit action redirects to `/requests/[requestId]`; status route is in this same plan to avoid same-wave dependency warning.</action>
  <acceptance_criteria>
    - Modified files named in <files> exist.
    - Commands listed in <verify> exit 0.
    - Implemented code contains exact functions, routes, models, and copy named in <action>.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Actions compile; upload action is concrete end-to-end call to `attachIntakeFile`; no public URL returned.</done>
</task>

<task type="auto">
  <name>Task 2: Build /intake flow with progress, upload, and review display</name>
  <files>src/app/intake/page.tsx, src/app/intake/components.tsx</files>
  <read_first>
    - .planning/phases/02-intake/02-CONTEXT.md
    - .planning/phases/02-intake/02-RESEARCH.md
    - .planning/phases/02-intake/02-PATTERNS.md
    - .planning/phases/02-intake/02-UI-SPEC.md
  </read_first>
  <action>Build customer-facing `/intake` using Vietnamese copy and visual contract from UI spec per D-15/D-16. Flow must show 4 steps: `Dịch vụ`, `Câu hỏi`, `Tài liệu`, `Kiểm tra`. Upload step must submit to `attachIntakeFileAction`, show uploaded filename and size only, include privacy note `Tệp được lưu riêng tư theo hồ sơ và không tạo đường dẫn công khai.`, and no link/public URL. Review step must show matter type, answer labels/values, uploaded filenames/sizes, and submit CTA `Gửi yêu cầu`. Unsupported option remains submit-capable and shows human triage guidance, not rejection.</action>
  <acceptance_criteria>
    - Modified files named in <files> exist.
    - Commands listed in <verify> exit 0.
    - Implemented code contains exact functions, routes, models, and copy named in <action>.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`/intake` displays full flow; upload and review are concrete, not placeholders; UI copy is Vietnamese and no raw enum labels appear.</done>
</task>

<task type="auto">
  <name>Task 3: Move read-only customer status route into Plan 02</name>
  <files>src/app/requests/[requestId]/page.tsx</files>
  <read_first>
    - .planning/phases/02-intake/02-CONTEXT.md
    - .planning/phases/02-intake/02-RESEARCH.md
    - .planning/phases/02-intake/02-PATTERNS.md
    - .planning/phases/02-intake/02-UI-SPEC.md
  </read_first>
  <action>Create `/requests/[requestId]` route in this plan per checker warning. Load request server-side with RBAC via existing request access checks. Render Vietnamese status label and next-step guidance per D-10/D-17. Show uploaded file metadata as filename and size only if status page includes attachments; no public URLs and no status mutation controls. Triage status must show `Cần chuyên viên phân loại` and guidance from UI spec.</action>
  <acceptance_criteria>
    - Modified files named in <files> exist.
    - Commands listed in <verify> exit 0.
    - Implemented code contains exact functions, routes, models, and copy named in <action>.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Submit redirect target exists in same plan; status view is read-only, authorized, Vietnamese, and URL-free for files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer browser -> server action | untrusted FormData crosses into server |
| route param -> status page | untrusted requestId controls data read |
| file upload -> storage metadata | untrusted filename/size enters private metadata path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | attachIntakeFileAction | mitigate | Do not trust client workspaceId; service derives workspace from authorized request. |
| T-02-02-02 | Information Disclosure | Upload/review/status UI | mitigate | Display filename and size only; no public URLs or file content. |
| T-02-02-03 | Elevation of Privilege | /requests/[requestId] | mitigate | Guard status read with `canAccessRequest`; no mutation controls. |
</threat_model>

<verification>
Run `npm run typecheck`. Manual smoke: `/intake` select service, answer questions, upload file, see filename/size in upload and review, submit, land on `/requests/[requestId]` read-only status.
</verification>

<success_criteria>
INT-01 through INT-06 visible customer path works, including full upload wiring and status route in same plan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake/02-02-SUMMARY.md`
</output>
