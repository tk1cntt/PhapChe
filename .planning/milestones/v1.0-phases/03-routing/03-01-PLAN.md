---
phase: 03-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/routing/routing-service.ts
  - src/lib/routing/routing-service.test.ts
autonomous: true
requirements: [RTE-01, RTE-02, RTE-03]
must_haves:
  truths:
    - "Admin can define active matter types for routing."
    - "Admin can define specialist/reviewer capabilities by workspace, user, matter type, and kind."
    - "System returns separate eligible specialist and reviewer suggestions with Vietnamese reason text."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "RoutingCapability model and relations"
      contains: "model RoutingCapability"
    - path: "src/lib/routing/routing-service.ts"
      provides: "Matter type, capability, and suggestion service functions"
      exports: ["upsertMatterType", "upsertRoutingCapability", "getRoutingSuggestions"]
    - path: "src/lib/routing/routing-service.test.ts"
      provides: "Routing domain tests"
  key_links:
    - from: "src/lib/routing/routing-service.ts"
      to: "prisma.routingCapability"
      via: "capability CRUD and suggestion query"
      pattern: "routingCapability"
    - from: "src/lib/routing/routing-service.ts"
      to: "IntakeSubmission.matterTypeKey"
      via: "request suggestion lookup"
      pattern: "intakeSubmission"
---

<objective>
Build routing foundation: capability persistence plus service API for matter administration and eligibility suggestions.

Purpose: RTE-01..RTE-03 need backend-owned routing data before UI or assignment can safely depend on it.
Output: Prisma model, routing service, tests.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing/03-CONTEXT.md
@.planning/phases/03-routing/03-RESEARCH.md
@.planning/phases/03-routing/03-PATTERNS.md
@.planning/phases/03-routing/03-VALIDATION.md
@prisma/schema.prisma
@src/lib/security/rbac.ts
@src/lib/intake/catalog.ts

<interfaces>
Existing contracts:
```ts
// prisma/schema.prisma
enum AssignmentKind { specialist reviewer }
enum Role { customer specialist reviewer coordinator_admin super_admin }
model MatterType { key String @unique; workspaceId String?; label String; description String?; schemaVersion String; questionSchema Json; isActive Boolean }
model IntakeSubmission { requestId String @unique; matterTypeKey String; answers Json; answerLabels Json }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RoutingCapability schema and service tests</name>
  <read_first>prisma/schema.prisma, src/lib/security/rbac.ts, src/lib/intake/catalog.ts, .planning/phases/03-routing/03-CONTEXT.md, .planning/phases/03-routing/03-RESEARCH.md</read_first>
  <files>prisma/schema.prisma, src/lib/routing/routing-service.test.ts</files>
  <behavior>
    - Creating active capability for active specialist/reviewer membership makes user eligible per D-02/D-03.
    - Inactive capability, inactive user, or inactive workspace membership makes user ineligible per D-03.
    - Suggestions return separate specialist and reviewer arrays with reason "Phù hợp vai trò và năng lực với loại vụ việc này." per D-04/D-10.
  </behavior>
  <action>Following `03-PATTERNS.md` analog for `prisma/schema.prisma` (`RequestAssignment`, `MatterType`, `WorkspaceMembership`), add Prisma model `RoutingCapability` with fields `id`, `workspaceId`, `userId`, `matterTypeKey`, `kind AssignmentKind`, `isActive`, `createdAt`, `updatedAt`; relations to Workspace, User, MatterType; `@@unique([workspaceId, userId, matterTypeKey, kind])`; indexes `[workspaceId, matterTypeKey, kind, isActive]` and `[userId]`. Add relation fields `routingCapabilities` on User, Workspace, MatterType. Following `03-PATTERNS.md` analogs for `src/lib/routing/routing-service.test.ts` (`src/lib/intake/intake.test.ts`, `src/lib/workflow/request-workflow.test.ts`), create failing tests for `upsertMatterType`, `upsertRoutingCapability`, and `getRoutingSuggestions`. Use shared `MatterType` taxonomy per D-01. Do not add auto-routing, workload, SLA, reviewer queue, document generation, or AI matching.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains exact string `model RoutingCapability`.
    - `prisma/schema.prisma` contains exact string `@@unique([workspaceId, userId, matterTypeKey, kind])`.
    - `src/lib/routing/routing-service.test.ts` contains exact string `Phù hợp vai trò và năng lực với loại vụ việc này.`.
    - Tests fail before service implementation because `src/lib/routing/routing-service.ts` exports do not exist yet.
  </acceptance_criteria>
  <done>Capability schema contract and failing tests encode RTE-01..RTE-03 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement routing service foundation</name>
  <read_first>prisma/schema.prisma, src/lib/routing/routing-service.test.ts, src/lib/security/rbac.ts, src/lib/audit/audit.ts, .planning/phases/03-routing/03-CONTEXT.md</read_first>
  <files>src/lib/routing/routing-service.ts, src/lib/routing/routing-service.test.ts</files>
  <action>Following `03-PATTERNS.md` analog for `src/lib/routing/routing-service.ts` (`src/lib/intake/intake-service.ts`), create `src/lib/routing/routing-service.ts`. Export `upsertMatterType`, `upsertRoutingCapability`, `listRoutingMatterTypes`, `listRoutingCapabilities`, `getRoutingSuggestions`. Validate non-empty keys/ids. `upsertMatterType` writes `MatterType` label/description/schemaVersion/questionSchema/isActive using existing key per D-01. `upsertRoutingCapability` only accepts `kind` `specialist` or `reviewer`; validates active user and active workspace membership role matching kind per D-02/D-03. `getRoutingSuggestions({ requestId, workspaceId })` loads request intake matter type, filters active capabilities + active users + active memberships, returns `{ specialists, reviewers }` arrays sorted by `user.name asc`, then `user.id asc`, with `reason: "Phù hợp vai trò và năng lực với loại vụ việc này."`. No auto-select or assignment in this plan per D-05.</action>
  <verify>
    <automated>npx tsx src/lib/routing/routing-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/routing/routing-service.ts` exports exact names `upsertMatterType`, `upsertRoutingCapability`, `getRoutingSuggestions`.
    - Suggestion result object has keys `specialists` and `reviewers`.
    - Inactive users or inactive memberships do not appear in suggestions.
    - No code writes `assignedSpecialistId` or `assignedReviewerId` in this plan.
  </acceptance_criteria>
  <done>Routing foundation service passes tests and covers RTE-01..RTE-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Admin/server action -> routing service | Untrusted form values become routing configuration. |
| Routing service -> database | Capability rows affect assignment eligibility. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Tampering | `upsertRoutingCapability` | mitigate | Validate active workspace membership and role equals `kind`; reject mismatched specialist/reviewer capability. |
| T-03-02 | Information Disclosure | `getRoutingSuggestions` | mitigate | Scope query by `workspaceId` and request intake matter type; never return users from other workspaces. |
| T-03-03 | Denial of Service | capability unique tuple | mitigate | Use unique tuple to prevent duplicate capability spam for same user/matter/kind. |
</threat_model>

<verification>
Run `npx tsx src/lib/routing/routing-service.test.ts` and `npx tsc --noEmit`. Follow `.planning/phases/03-routing/03-VALIDATION.md`: typecheck after task, full test after wave.
</verification>

<success_criteria>
RTE-01, RTE-02, RTE-03 backend foundation exists and tests prove active membership/user filtering plus separated suggestions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing/03-01-SUMMARY.md`.
</output>
