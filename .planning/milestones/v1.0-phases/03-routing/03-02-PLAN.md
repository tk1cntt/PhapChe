---
phase: 03-routing
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/routing/routing-service.ts
  - src/lib/routing/routing-service.test.ts
autonomous: true
requirements: [RTE-04]
must_haves:
  truths:
    - "Coordinator/admin can assign or reassign specialist and reviewer with a required reason."
    - "Assignment updates LegalRequest assignee fields and appends RequestAssignment history rows."
    - "Assignment status changes use backend workflow transitions only."
    - "Audit events record routing metadata without sensitive intake content."
  artifacts:
    - path: "src/lib/routing/routing-service.ts"
      provides: "Assignment transaction service"
      exports: ["assignRequest"]
    - path: "src/lib/routing/routing-service.test.ts"
      provides: "Assignment, workflow, audit tests"
  key_links:
    - from: "assignRequest"
      to: "LegalRequest.assignedSpecialistId/assignedReviewerId"
      via: "transaction update"
      pattern: "assignedSpecialistId|assignedReviewerId"
    - from: "assignRequest"
      to: "RequestAssignment"
      via: "append-only create"
      pattern: "requestAssignment.create"
    - from: "assignRequest"
      to: "WorkflowTransition"
      via: "backend transition path"
      pattern: "triage.*assigned"
---

<objective>
Add safe assignment/reassignment transaction to routing service.

Purpose: RTE-04 requires coordinator-owned manual assignment with reason, history, audit, and workflow integrity.
Output: Tested assignment service.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing/03-CONTEXT.md
@.planning/phases/03-routing/03-RESEARCH.md
@.planning/phases/03-routing/03-PATTERNS.md
@.planning/phases/03-routing/03-VALIDATION.md
@src/lib/routing/routing-service.ts
@src/lib/workflow/request-workflow.ts
@src/lib/audit/audit.ts
@src/lib/security/rbac.ts

<interfaces>
Existing workflow:
```ts
export const REQUEST_TRANSITIONS = {
  intake_submitted: ['triage', 'cancelled'],
  triage: ['assigned', 'cancelled'],
} as const;
export async function transitionRequestStatus(input: { requestId: string; actorId: string; toStatus: RequestStatus; reason?: string | null; correlationId: string }): Promise<{ id: string; status: RequestStatus }>;
```
Existing audit:
```ts
export async function recordAuditEvent(input: { actorId?: string | null; workspaceId: string; action: string; targetType: 'ASSIGNMENT'; targetId: string; requestId?: string | null; correlationId: string; metadataSummary?: string | null }, db?: AuditDb);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assignment behavior tests</name>
  <read_first>src/lib/routing/routing-service.ts, src/lib/routing/routing-service.test.ts, src/lib/workflow/request-workflow.ts, src/lib/audit/audit.ts, .planning/phases/03-routing/03-CONTEXT.md</read_first>
  <files>src/lib/routing/routing-service.test.ts</files>
  <behavior>
    - Missing or whitespace reason rejects with `ASSIGNMENT_REASON_REQUIRED` per D-06/D-09.
    - Coordinator/admin assignment updates `LegalRequest.assignedSpecialistId` or `assignedReviewerId` per D-06.
    - Reassignment creates a second `RequestAssignment` row, never overwrites old row per D-16.
    - `intake_submitted` assignment reaches `assigned` through backend `intake_submitted -> triage -> assigned` path, not direct frontend status mutation per D-07 and resolved research question.
    - Audit metadata includes kind, assignee id, request id, matter type, and reason presence/short reason only; no intake answer text per D-15.
    - If workflow path is not allowed, no assignment fields, `RequestAssignment` history rows, or `AuditEvent` rows are created.
    - If assignment/history/audit write fails after workflow eligibility check, request status remains original and no partial assignment state exists.
  </behavior>
  <action>Following `03-PATTERNS.md` analogs for `src/lib/routing/routing-service.test.ts` (`src/lib/intake/intake.test.ts`, `src/lib/workflow/request-workflow.test.ts`), extend routing service tests for exported `assignRequest`. Tests must create coordinator/admin actor with active membership, specialist/reviewer users with matching capabilities, and requests in `intake_submitted` and `triage`. Assert assignment reason required, assignee fields updated, `RequestAssignment` count increases per reassignment, `WorkflowTransition` rows exist for allowed backend transitions, and `AuditEvent.metadataSummary` length <= 500 without answer labels/answer values. Add rollback-safety coverage by forcing assignment/history/audit write failure after workflow eligibility validation and asserting original request status, no assignee field change, no `RequestAssignment`, and no `AuditEvent`.</action>
  <verify>
    <automated>npx tsx src/lib/routing/routing-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains exact string `ASSIGNMENT_REASON_REQUIRED`.
    - Test file asserts at least two `RequestAssignment` rows after reassignment.
    - Test file asserts `metadataSummary` does not contain sensitive answer fixture text.
    - Test file asserts failed assignment/history/audit write after workflow eligibility check leaves original request status and no partial assignment state.
    - Tests fail before `assignRequest` implementation.
  </acceptance_criteria>
  <done>Failing tests lock RTE-04 security/workflow/audit/atomicity behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic assignment transaction</name>
  <read_first>src/lib/routing/routing-service.ts, src/lib/routing/routing-service.test.ts, src/lib/workflow/request-workflow.ts, src/lib/security/rbac.ts, src/lib/audit/audit.ts, .planning/phases/03-routing/03-CONTEXT.md</read_first>
  <files>src/lib/routing/routing-service.ts, src/lib/routing/routing-service.test.ts</files>
  <action>Export `assignRequest({ requestId, workspaceId, actorId, kind, assigneeId, reason, correlationId })`. Validate non-empty reason before writes. Authorize actor as active `coordinator_admin` or `super_admin` in workspace per D-14. Validate assignee active user, active workspace membership for `kind`, and active matching `RoutingCapability` for request matter type per D-02/D-03. Preserve backend state machine semantics per D-07: allowed assignment path is `intake_submitted -> triage -> assigned` or `triage -> assigned`; validate this workflow path before writes using workflow transition rules/status guards, but do not write status during this preflight. Then run one Prisma transaction that atomically updates request status using same current-status conflict guard pattern from `transitionRequestStatus`, updates `LegalRequest.assignedSpecialistId` for `specialist` or `assignedReviewerId` for `reviewer`, appends `RequestAssignment` row with full reason, and writes audit. Do not call `transitionRequestStatus` before assignment transaction if that would commit status separately. If implementation chooses helper, helper must accept transaction client and perform status write inside assignment transaction. For `intake_submitted`, transaction must apply guarded status progression through `triage` then `assigned` inside transaction; for `triage`, transaction must apply guarded `triage -> assigned`; for `assigned`, skip status write and still atomically update assignment/history/audit. If any status/assignment/history/audit write fails after workflow eligibility check, entire transaction must roll back so request status remains original and no partial assignment state exists. Write audit action `request.assigned`, targetType `ASSIGNMENT`, metadata summary like `kind=specialist; assignee=<id>; request=<id>; matter=<matterTypeKey>; reasonProvided=true; reason=<shortReason>` truncated to safe length. Do not write intake answers/labels into audit metadata.</action>
  <verify>
    <automated>npx tsx src/lib/routing/routing-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `assignRequest` exists and is exported.
    - Reason whitespace throws `ASSIGNMENT_REASON_REQUIRED`.
    - `RequestAssignment` rows are created with `kind`, `userId`, `createdById`, and `reason`.
    - `LegalRequest.status` becomes `assigned` from `intake_submitted` through `intake_submitted -> triage -> assigned`, or from `triage` through `triage -> assigned`, with all status writes inside the same assignment transaction.
    - `transitionRequestStatus` is not called before the assignment transaction in a way that can commit status separately from assignee/history/audit.
    - `metadataSummary` contains `reasonProvided=true` and does not contain intake answer text.
    - If assignment/history/audit write fails after workflow eligibility check, request status remains original and no partial assignment state exists.
  </acceptance_criteria>
  <done>Coordinator assignment service satisfies RTE-04 and locked decisions D-06, D-07, D-14, D-15, D-16 with atomic workflow/assignment/history/audit writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Coordinator form -> assignment service | Assignee id, kind, and reason are untrusted. |
| Assignment service -> workflow/audit/database | Critical state mutation and audit trail. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-04 | Elevation of Privilege | `assignRequest` actor auth | mitigate | Require active coordinator_admin or super_admin membership in request workspace before mutation. |
| T-03-05 | Tampering | assignment status update | mitigate | Validate allowed backend workflow path before writes, then perform guarded status writes inside same Prisma transaction as assignee/history/audit. |
| T-03-06 | Repudiation | reassignment history | mitigate | Always append `RequestAssignment`; never update prior history rows. |
| T-03-07 | Information Disclosure | audit metadata | mitigate | Build metadata from ids/kind/matter/reason presence/short reason only; test no intake answer text. |
| T-03-08 | Tampering | wrong-role assignee | mitigate | Validate active assignee membership role matches assignment kind and matching capability exists. |
</threat_model>

<verification>
Run `npx tsx src/lib/routing/routing-service.test.ts` and `npx tsc --noEmit`. Follow `.planning/phases/03-routing/03-VALIDATION.md`: typecheck after task, full test after wave.
</verification>

<success_criteria>
RTE-04 works through service: required reason, assignee update, history append, workflow transition, safe audit, and rollback-safe atomicity.
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing/03-02-SUMMARY.md`.
</output>
