---
phase: 03-routing
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/app/admin/routing/page.tsx
  - src/app/admin/routing/actions.ts
autonomous: true
requirements: [RTE-01, RTE-02, RTE-03, RTE-04]
must_haves:
  truths:
    - "Coordinator/admin sees routing queue for submitted/triage/assigned requests."
    - "Coordinator/admin can manage matter types and capabilities with Vietnamese labels."
    - "Coordinator/admin sees specialist and reviewer suggestions separately."
    - "Coordinator/admin must enter reason before assignment/reassignment."
  artifacts:
    - path: "src/app/admin/routing/page.tsx"
      provides: "Coordinator/admin routing UI"
    - path: "src/app/admin/routing/actions.ts"
      provides: "Server actions for routing forms"
  key_links:
    - from: "src/app/admin/routing/actions.ts"
      to: "src/lib/routing/routing-service.ts"
      via: "server actions call service functions"
      pattern: "assignRequest|upsertMatterType|upsertRoutingCapability"
    - from: "src/app/admin/routing/page.tsx"
      to: "src/app/admin/components/ui.tsx"
      via: "reused admin components"
      pattern: "Card|Table|Button|Badge|PageHeader"
---

<objective>
Build coordinator/admin routing screen and server actions using existing admin visual language.

Purpose: RTE-01..RTE-04 need internal UI for routing operations while backend remains source of truth.
Output: Admin routing page and actions.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing/03-CONTEXT.md
@.planning/phases/03-routing/03-UI-SPEC.md
@.planning/phases/03-routing/03-PATTERNS.md
@.planning/phases/03-routing/03-VALIDATION.md
@src/app/admin/components/ui.tsx
@src/app/admin/requests/page.tsx
@src/lib/routing/routing-service.ts
@src/lib/security/session.ts

<interfaces>
UI components:
```tsx
export function Button({ children, variant = 'primary', type = 'button', disabled = false }: ButtonProps)
export function Badge({ children, tone = 'neutral' })
export function Card({ children, className = '' })
export function Table({ headers, children })
export function PageHeader({ title, description, action })
```
Routing service expected:
```ts
upsertMatterType(...)
upsertRoutingCapability(...)
getRoutingSuggestions(...)
assignRequest(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create routing server actions</name>
  <read_first>src/lib/routing/routing-service.ts, src/lib/security/session.ts, src/app/admin/requests/page.tsx, .planning/phases/03-routing/03-CONTEXT.md, .planning/phases/03-routing/03-UI-SPEC.md</read_first>
  <files>src/app/admin/routing/actions.ts</files>
  <action>Following `03-PATTERNS.md` analog for `src/app/admin/routing/actions.ts` (`src/app/intake/actions.ts`), create server actions `saveMatterTypeAction`, `saveCapabilityAction`, and `assignRequestAction`. Each action reads current session server-side, validates required form values, calls routing service, and returns `{ ok: true, message: "Đã lưu phân công và ghi nhận lịch sử điều phối." }` or `{ ok: false, message: "Không thể lưu phân công. Kiểm tra chuyên viên, reviewer và lý do phân công rồi thử lại." }`. `assignRequestAction` must return missing reason copy `Nhập lý do phân công trước khi lưu.` and missing assignee copy `Chọn người xử lý trước khi lưu phân công.` before calling service. Use `revalidatePath('/admin/routing')` after success. Do not mutate request status in action; service owns workflow per D-07.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/routing/actions.ts` starts with exact string `'use server';`.
    - File contains exact strings `saveMatterTypeAction`, `saveCapabilityAction`, `assignRequestAction`.
    - File contains exact Vietnamese validation strings from UI spec lines 128-130.
    - File does not contain raw assignment field writes like `assignedSpecialistId:` or `assignedReviewerId:`.
  </acceptance_criteria>
  <done>Server actions safely bridge UI forms to backend routing service.</done>
</task>

<task type="auto">
  <name>Task 2: Create coordinator/admin routing page</name>
  <read_first>src/app/admin/components/ui.tsx, src/app/admin/requests/page.tsx, src/lib/routing/routing-service.ts, src/lib/security/session.ts, .planning/phases/03-routing/03-CONTEXT.md, .planning/phases/03-routing/03-UI-SPEC.md</read_first>
  <files>src/app/admin/routing/page.tsx</files>
  <action>Following `03-PATTERNS.md` analog for `src/app/admin/routing/page.tsx` (`src/app/admin/requests/page.tsx`), create server component page at `/admin/routing`. Use `AdminShell`, `PageHeader`, `Card`, `Table`, `Button`, `Badge` only. Page title must be `Điều phối yêu cầu pháp lý`; description must be `Xem yêu cầu đã gửi hoặc cần triage, kiểm tra gợi ý phù hợp và phân công người xử lý.`. Sections: `Hàng chờ điều phối`, `Loại vụ việc`, `Năng lực xử lý`. Routing queue columns exactly: `Yêu cầu`, `Khách hàng`, `Loại vụ việc`, `Trạng thái`, `Chuyên viên hiện tại`, `Reviewer hiện tại`, `Gợi ý chuyên viên`, `Gợi ý reviewer`, `Hành động`. Show suggestions separately with reason `Phù hợp vai trò và năng lực với loại vụ việc này.`. Assignment form must require explicit select, not auto-select first suggestion per D-05. Include reason label `Lý do phân công`, placeholder `Ví dụ: Phù hợp loại vụ việc và đang phụ trách nhóm hợp đồng lao động`, CTA `Phân công chuyên viên`, reassignment CTA `Cập nhật phân công`. Empty state uses exact UI spec copy. Do not add reviewer queue/QC UI, document drafting, vault versioning, final delivery, SLA scoring, or AI auto-routing.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Page contains exact strings `Điều phối yêu cầu pháp lý`, `Hàng chờ điều phối`, `Loại vụ việc`, `Năng lực xử lý`.
    - Table headers match UI spec required columns exactly.
    - Page imports from `src/app/admin/components/ui.tsx` or relative admin UI path; no new UI library import.
    - Page contains no strings `QC`, `draft`, `SLA`, `AI`, `Legal Vault`.
    - Primary assignment UI does not preselect first eligible candidate as final action.
  </acceptance_criteria>
  <done>Coordinator/admin screen implements D-08..D-10 and RTE-01..RTE-04 UI scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser form -> server action | User-controlled fields for matter, capability, and assignment. |
| Server action -> routing service | Trusted boundary after session and validation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-09 | Spoofing | server actions | mitigate | Read session with `requireAppSession`; do not trust form actor ids. |
| T-03-10 | Tampering | assignment form | mitigate | Validate required reason and assignee server-side before service call. |
| T-03-11 | Elevation of Privilege | admin routing page | mitigate | Service enforces coordinator/admin authorization; page does not act as security boundary. |
| T-03-12 | Information Disclosure | suggestions display | mitigate | Render only service-scoped suggestions; no cross-workspace user query in page. |
</threat_model>

<verification>
Run `npx tsc --noEmit`. Follow `.planning/phases/03-routing/03-VALIDATION.md`: typecheck after task, full test after wave. Then manually visit `/admin/routing` in app if dev server available.
</verification>

<success_criteria>
Coordinator/admin can use Vietnamese routing UI to manage matter types, capabilities, view suggestions, and submit assignments with required reason through server actions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing/03-03-SUMMARY.md`.
</output>
