---
phase: "05"
plan: "02"
wave: 1
type: execute
subsystem: review
autonomous: true
files_modified:
  - src/app/reviewer/requests/page.tsx
  - src/app/admin/components/ui.tsx
requirements_addressed:
  - REV-01
  - REV-02
---

<objective>
Create reviewer queue page listing document versions awaiting review, showing request title, matter type, submission time, and specialist name.
</objective>

<tasks>
<task>
<read_first>
- src/app/admin/components/ui.tsx
- src/app/specialist/requests/page.tsx (if exists, for pattern reference)
- src/lib/security/rbac.ts
</read_first>
<action>
Create the reviewer directory structure and queue page:

1. Create directory: `src/app/reviewer/`
2. Create file: `src/app/reviewer/requests/page.tsx`

```typescript
import { PageHeader } from "@/app/admin/components/ui";
import { Badge } from "@/app/admin/components/ui";
import { Button } from "@/app/admin/components/ui";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/app/admin/components/ui";
import { getServerSession } from "next-auth";
import { redirect } from "next/navigation";
import Link from "next/link";

// Get current reviewer session and their assigned reviews
async function getReviewerQueue(reviewerId: string) {
  // TODO: Query DocumentVersions in submitted_for_review status
  // where LegalRequest.assignedReviewerId === reviewerId
  // Include: request title, matter type, submission time, specialist name
  return []; // Placeholder
}

export default async function ReviewerQueuePage() {
  const session = await getServerSession();
  
  if (!session?.user) {
    redirect("/login");
  }

  const reviews = await getReviewerQueue(session.user.id);

  return (
    <div className="min-h-screen bg-[#F8FAFC]">
      <div className="mx-auto max-w-6xl px-6 py-8">
        <PageHeader
          title="Yêu cầu kiểm tra"
          description="Các tài liệu chờ bạn kiểm tra"
        />
        
        {reviews.length === 0 ? (
          <div className="rounded-xl border border-[#E2E8F0] bg-white p-12 text-center">
            <h3 className="mb-2 text-lg font-semibold text-[#0F172A]">
              Không có tài liệu chờ kiểm tra
            </h3>
            <p className="text-[#475569]">
              Tất cả tài liệu đã được xử lý. Quay lại khi có yêu cầu mới.
            </p>
          </div>
        ) : (
          <div className="rounded-xl border border-[#E2E8F0] bg-white">
            <Table>
              <TableHeader>
                <TableRow className="bg-[#F1F5F9]">
                  <TableHead className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#475569]">STT</TableHead>
                  <TableHead className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#475569]">Tiêu đề</TableHead>
                  <TableHead className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#475569]">Loại</TableHead>
                  <TableHead className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#475569]">Chuyên viên</TableHead>
                  <TableHead className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#475569]">Ngày nộp</TableHead>
                  <TableHead className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#475569]">Trạng thái</TableHead>
                  <TableHead className="text-[12px] font-semibold uppercase tracking-[0.08em] text-[#475569]">Thao tác</TableHead>
                </TableRow>
              </TableHeader>
              <TableBody>
                {reviews.map((review, index) => (
                  <TableRow key={review.id} className="border-t border-[#E2E8F0]">
                    <TableCell className="text-[14px] text-[#475569]">{index + 1}</TableCell>
                    <TableCell className="font-medium text-[#0F172A]">{review.requestTitle}</TableCell>
                    <TableCell className="text-[14px] text-[#475569]">{review.matterType}</TableCell>
                    <TableCell className="text-[14px] text-[#475569]">{review.specialistName}</TableCell>
                    <TableCell className="text-[14px] text-[#475569]">{review.submittedAt}</TableCell>
                    <TableCell>
                      <Badge variant="info">Đang chờ</Badge>
                    </TableCell>
                    <TableCell>
                      <Link href={`/reviewer/requests/${review.requestId}/review/${review.documentVersionId}`}>
                        <Button variant="primary" size="sm">
                          Xem chi tiết
                        </Button>
                      </Link>
                    </TableCell>
                  </TableRow>
                ))}
              </TableBody>
            </Table>
          </div>
        )}
      </div>
    </div>
  );
}
```
</action>
<acceptance_criteria>
- `grep -r "reviewer/requests" src/app/` returns the new page file
- `grep "Yêu cầu kiểm tra" src/app/reviewer/requests/page.tsx` returns page title
- `grep "Xem chi tiết" src/app/reviewer/requests/page.tsx` returns button text
- `grep "Không có tài liệu chờ kiểm tra" src/app/reviewer/requests/page.tsx` returns empty state
- File imports from `@/app/admin/components/ui`
</acceptance_criteria>
</task>
</tasks>

<verification>
1. Navigate to `/reviewer/requests` and verify page loads
2. Verify empty state displays when no reviews pending
3. Verify table displays with correct headers when reviews exist
4. Verify "Xem chi tiết" button navigates to correct URL
</verification>

<must_haves>
1. Reviewer queue page created at `src/app/reviewer/requests/page.tsx`
2. Page shows empty state when no reviews pending
3. Page shows table with: STT, Tiêu đề, Loại, Chuyên viên, Ngày nộp, Trạng thái, Thao tác
4. "Xem chi tiết" button links to `/reviewer/requests/[requestId]/review/[documentVersionId]`
5. Page uses existing admin UI components (PageHeader, Badge, Button, Table)
</must_haves>