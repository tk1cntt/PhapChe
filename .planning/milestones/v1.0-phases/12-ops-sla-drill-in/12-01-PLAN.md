---
phase: 12
name: ops-sla-drill-in
plan: 01
wave: 1
description: Verify OPS-04 SLA context is rendered on drill-in page
requirements: []
autonomous: true
---

# Plan 12-01: Verify SLA Drill-in Page

## Objective

Confirm that commit `f29979e` already closes OPS-04 by verifying the drill-in page (`/admin/ops/[requestId]/page.tsx`) renders all required SLA fields from `getOpsRequestTimeline`.

This is a **verification-only plan** — no new code is needed.

## Tasks

### Task 1: Verify SLA Context Rendering

**read_first:**
- `src/app/admin/ops/[requestId]/page.tsx`
- `src/lib/ops/ops-service.ts` (lines 325-418)

**action:**
1. Read `src/app/admin/ops/[requestId]/page.tsx` and confirm:
   - The page calls `getOpsRequestTimeline(session, requestId)` (line 45)
   - The SLA section renders within `{timeline ? (...)}` conditional (line 70)
   - `timeline.sla.currentStatusSince` is displayed as "Từ trạng thái hiện tại"
   - `timeline.sla.currentStatusAgeDays` is displayed as "Tuổi trạng thái"
   - `timeline.sla.pendingReviewSince` is displayed as "Chờ review từ"
   - `timeline.sla.deliveredAt` is displayed as "Đã giao lúc"
   - `timeline.sla.closedAt` is displayed as "Đã đóng lúc"
2. Read `src/lib/ops/ops-service.ts` and confirm:
   - `getOpsRequestTimeline` (line 325) computes all SLA fields from `WorkflowTransition.createdAt`
   - SLA fields are derived at lines 365-368
   - Return type `OpsTimelineDto.sla` includes all required fields (lines 63-69)
3. Confirm all labels use Vietnamese text (no raw English labels)

**acceptance_criteria:**
- `src/app/admin/ops/[requestId]/page.tsx` contains `timeline.sla.currentStatusSince` and `timeline.sla.currentStatusAgeDays`
- `src/app/admin/ops/[requestId]/page.tsx` contains `timeline.sla.pendingReviewSince`
- `src/app/admin/ops/[requestId]/page.tsx` contains `timeline.sla.deliveredAt`
- `src/app/admin/ops/[requestId]/page.tsx` contains `timeline.sla.closedAt`
- All five SLA labels are in Vietnamese (Từ trạng thái hiện tại, Tuổi trạng thái, Chờ review từ, Đã giao lúc, Đã đóng lúc)
- No raw legal content or PII printed in SLA section (safe display)
- `getOpsRequestTimeline` in `ops-service.ts` returns all 5 SLA fields in `OpsTimelineDto.sla`

### Task 2: Verify OPS-Service SLA Computation

**read_first:**
- `src/lib/ops/ops-service.test.ts`

**action:**
1. Read `src/lib/ops/ops-service.test.ts` and check for existing tests covering `getOpsRequestTimeline` SLA fields
2. If tests exist and pass: mark as verified
3. If tests don't exist: check if the phase goal requires tests (it doesn't — ROADMAP success criteria only require rendering, not testing)

**acceptance_criteria:**
- (No additional test requirement — phase goal is rendering, not test coverage)
- If tests exist: `npm test -- --grep "ops-service"` passes (informational only)

## must_haves

- [ ] `src/app/admin/ops/[requestId]/page.tsx` renders `currentStatusSince`, `pendingReviewSince`, `deliveredAt`, `closedAt` with Vietnamese labels
- [ ] SLA data comes from `getOpsRequestTimeline` (not mock data)
- [ ] SLA display is safe — no raw legal content or PII

## files_modified

(None — this is a verification-only plan; no files will be modified.)

## Verification

1. Read both files listed in Task 1 `read_first`
2. Confirm all required fields present with Vietnamese labels
3. Confirm safe display constraint is satisfied
4. Report findings

## Dependencies

None.
