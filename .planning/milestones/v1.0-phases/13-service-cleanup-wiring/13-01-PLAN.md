---
phase: 13-service-cleanup-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/specialist/requests/[requestId]/page.tsx
autonomous: true
requirements: []
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "Specialist page fetches document versions via `listDocumentVersions` service function instead of inline Prisma query"
    - "Template enrichment logic no longer duplicates what the service already provides"
    - "DocumentVersionsList component receives the same data shape as before"
  artifacts:
    - path: "src/app/specialist/requests/[requestId]/page.tsx"
      provides: "Specialist workbench with wired listDocumentVersions"
  key_links:
    - from: "src/app/specialist/requests/[requestId]/page.tsx"
      to: "src/lib/documents/draft-service.ts"
      via: "import { listDocumentVersions } and call"
      pattern: "listDocumentVersions"
---

<objective>
Wire `listDocumentVersions` from `draft-service.ts` into the specialist workbench, replacing the inline Prisma `documents.documentVersions` query and the ad-hoc template enrichment block.

Purpose: Eliminate the orphaned service export by making the specialist workbench the consumer of `listDocumentVersions`. `listDocumentVersions` already handles RBAC and template label enrichment, so the specialist page no longer duplicates that logic with a separate `prisma.documentTemplate.findMany` call.

Output: Modified `src/app/specialist/requests/[requestId]/page.tsx` — one import added, inline document-versions Prisma select removed, template enrichment block replaced with a simple data mapping.

Gap closure: Closes the `listDocumentVersions` orphaned export finding from v1.0-MILESTONE-AUDIT.md (MEDIUM).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-service-cleanup-wiring/13-CONTEXT.md

@src/app/specialist/requests/[requestId]/page.tsx
@src/lib/documents/draft-service.ts (lines 186-238: listDocumentVersions)
@src/app/specialist/requests/[requestId]/components/document-versions.tsx

Key decisions from CONTEXT.md:
- D-01: Replace inline Prisma query with `listDocumentVersions({ session, requestId })`
- D-02: `DocumentVersionsList` component interface unchanged — only data source changes
- D-03: Remove unused `templateMap` enrichment logic
</context>

<tasks>

<task type="auto">
<name>Task 1: Replace inline Prisma document-versions query with listDocumentVersions service call</name>
<files>src/app/specialist/requests/[requestId]/page.tsx</files>

<read_first>
- src/app/specialist/requests/[requestId]/page.tsx (full file)
- src/lib/documents/draft-service.ts lines 186-238 (listDocumentVersions return shape)
- src/app/specialist/requests/[requestId]/components/document-versions.tsx lines 1-22 (DocumentVersion component type)
</read_first>

<action>
Make the following changes to `src/app/specialist/requests/[requestId]/page.tsx`:

**1. Add import** (in the existing import block after line 9 or near other draft-service imports):

```ts
import { listDocumentVersions } from '@/lib/documents/draft-service';
```

**2. Remove the `documents` select** from the `prisma.legalRequest.findUnique` call at lines 60-76. Delete these lines entirely:

```ts
      documents: {
        select: {
          id: true,
          documentVersions: {
            select: {
              id: true,
              templateId: true,
              templateVersion: true,
              status: true,
              generatedContent: true,
              createdAt: true,
            },
            orderBy: { createdAt: 'desc' },
          },
        },
      },
```

Keep the trailing comma on the preceding line (line 59 `vaultFiles: { ... }` ) so the select object remains valid.

**3. Add the service call** after the request-not-found check (after line 78 `if (!request) notFound();`), before the status-label assignment (line 80):

```ts
const versionsFromService = await listDocumentVersions({ session, requestId }).catch(() => []);
```

The `.catch(() => [])` matches the existing error-handling pattern used for `getTemplatesForGeneration` and `listVaultFiles` on this page.

**4. Replace the inline enrichment block** (current lines 97-123) — the entire block from `const documentVersions =` through `const enrichedVersions =` — with a single mapping call:

```ts
const enrichedVersions = versionsFromService.map((v) => ({
  id: v.id,
  templateId: v.templateId,
  templateVersion: v.templateVersion,
  status: v.status,
  generatedContent: v.generatedContent,
  createdAt: v.createdAt,
  template: { label: v.templateLabel, version: v.templateVersion },
}));
```

This removes:
- The `request.documents.flatMap(...)` (no longer needed — `listDocumentVersions` does the flattening)
- The `templateIds` computation (handled inside `listDocumentVersions`)
- The `templateMap` construction using both `templates` and a separate `prisma.documentTemplate.findMany` call (D-03)
- The per-version enrichment loop (replaced by the map above)

**5. Verify that no imports become unused.** The only new import is `listDocumentVersions`. Existing imports remain:
- `prisma` — still used for `prisma.legalRequest.findUnique` at line 50
- `getTemplatesForGeneration` — still used for the GenerateDraftForm
- All other imports unchanged

Do NOT touch any other file. Do NOT modify the DocumentVersionsList component interface.
</action>

<acceptance_criteria>
1. The file no longer contains `documents.documentVersions` Prisma select inside the `findUnique` call
2. The file no longer contains the `templateMap` enrichment pattern (no `prisma.documentTemplate.findMany` called from the page)
3. The file imports and calls `listDocumentVersions` from `@/lib/documents/draft-service`
4. `npm run typecheck` passes with zero new errors
5. The `DocumentVersionsList` prop shape `{ id, templateId, templateVersion, status, generatedContent, createdAt, template: { label, version } }` is preserved exactly
</acceptance_criteria>

<verify>
  <automated>cd /mnt/d/PhapChe && npx tsc --noEmit 2>&1 | grep -v "admin/templates/" | head -30</automated>
</verify>

<done>
- Specialist page calls `listDocumentVersions({ session, requestId })` instead of inline Prisma
- All template enrichment is handled inside the service function, not the page
- `npm run typecheck` passes (excluding pre-existing `/admin/templates/` errors)
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> specialist page | Authenticated specialist browser session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | E (Info Disclosure) | specialist page data | accept | `listDocumentVersions` already enforces `canAccessRequest` RBAC at the service layer — same access control as the inline query it replaced |
</threat_model>

<verification>
1. `grep -n "listDocumentVersions" src/app/specialist/requests/\[requestId\]/page.tsx` shows import + call site
2. `grep -n "templateMap\|templateIds\|prisma.documentTemplate" src/app/specialist/requests/\[requestId\]/page.tsx` returns empty (removed enrichment block)
3. `grep -c "documents.*documentVersions" src/app/specialist/requests/\[requestId\]/page.tsx` returns 0
</verification>

<success_criteria>
1. The specialist page at `/specialist/requests/:requestId` renders document versions identically to before
2. Data source changed from inline Prisma to `listDocumentVersions` service function
3. No duplicate template enrichment queries
4. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-service-cleanup-wiring/13-01-SUMMARY.md`
</output>
