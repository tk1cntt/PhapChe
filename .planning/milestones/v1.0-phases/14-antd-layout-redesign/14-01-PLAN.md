---
phase: 14-antd-layout-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/providers/antd-provider.tsx
  - src/lib/navigation/breadcrumb-labels.ts
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements: []
user_setup:
  - service: npm registry
    why: "Install antd, @ant-design/icons, @ant-design/cssinjs"
    env_vars: []

must_haves:
  truths:
    - "Ant Design dependencies are installed and the project builds"
    - "Root layout wraps all pages with ConfigProvider, AntdRegistry, and App"
    - "ConfigProvider has correct theme tokens (teal accent, #F8FAFC background, 14px font)"
    - "Breadcrumb labels are configured for all route segments in centralized config"
    - "globals.css is simplified — CSS custom properties migrated to antd theme tokens"
  artifacts:
    - path: "package.json"
      provides: "Ant Design deps"
      contains: "antd"
    - path: "src/app/providers/antd-provider.tsx"
      provides: "Theme config + AntdRegistry + ConfigProvider + App wrapper"
      min_lines: 60
    - path: "src/lib/navigation/breadcrumb-labels.ts"
      provides: "Route segment to Vietnamese label mapping"
      min_lines: 30
    - path: "src/app/layout.tsx"
      provides: "Root layout wrapping children with antd provider"
      contains: "AntdRegistry"
    - path: "src/app/globals.css"
      provides: "Simplified CSS (no CSS custom properties, minimal reset)"
      min_lines: 20
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/providers/antd-provider.tsx"
      via: "import"
      pattern: "from.*antd-provider"
    - from: "src/app/providers/antd-provider.tsx"
      to: "@ant-design/cssinjs"
      via: "import"
      pattern: "AntdRegistry"
    - from: "src/app/providers/antd-provider.tsx"
      to: "antd"
      via: "import"
      pattern: "ConfigProvider|App"
---

<objective>
Install Ant Design UI library and set up the foundational provider infrastructure for the phase.

Purpose: Establish the antd provider tree (AntdRegistry + ConfigProvider + App) so downstream plans can use antd components in route group layouts and pages. Also create the breadcrumb label config and clean up globals.css.

Output:
- package.json with antd deps
- src/app/providers/antd-provider.tsx
- src/lib/navigation/breadcrumb-labels.ts
- src/app/layout.tsx (modified)
- src/app/globals.css (simplified)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-antd-layout-redesign/14-CONTEXT.md
@.planning/phases/14-antd-layout-redesign/14-UI-SPEC.md

<interfaces>
Key configs the executor needs — no codebase exploration needed for these.

From 14-UI-SPEC.md Theme Token Configuration:
```typescript
const theme: ThemeConfig = {
  token: {
    colorPrimary: '#0F766E',
    colorError: '#DC2626',
    colorBgLayout: '#F8FAFC',
    colorBgContainer: '#FFFFFF',
    colorBgElevated: '#FFFFFF',
    colorText: '#0F172A',
    colorTextSecondary: '#475569',
    colorBorder: '#E2E8F0',
    colorFillTertiary: '#F1F5F9',
    colorWhite: '#FFFFFF',
    fontSize: 14,
    borderRadius: 16,
    borderRadiusLG: 16,
    borderRadiusSM: 8,
    wireframe: false,
  },
  components: {
    Menu: {
      colorItemBgSelected: '#F0FDFA',
      colorItemTextSelected: '#0F766E',
      colorItemBgHover: '#F1F5F9',
      colorItemTextHover: '#0F172A',
      colorItemBg: 'transparent',
      colorItemText: '#475569',
      borderRadius: 12,
      itemMarginInline: 8,
      itemMarginBlock: 4,
    },
    Button: {
      borderRadius: 12,
      controlHeight: 40,
      primaryColor: '#FFFFFF',
      defaultBorderColor: '#CBD5E1',
      defaultColor: '#0F172A',
      defaultBg: '#FFFFFF',
      colorPrimaryHover: '#115E59',
      colorPrimaryActive: '#134E4A',
    },
    Card: { padding: 16, paddingLG: 24, borderRadius: 16, colorBorderSecondary: '#E2E8F0' },
    Table: {
      borderRadius: 16, colorBgContainer: '#FFFFFF', headerBg: '#F8FAFC',
      headerColor: '#64748B', headerSortActiveBg: '#F1F5F9',
      rowHoverBg: '#F8FAFC', borderColor: '#E2E8F0',
    },
    Tag: { borderRadius: 9999 },
    Modal: { borderRadius: 16, borderRadiusLG: 16 },
  },
};
```

From 14-UI-SPEC.md Breadcrumb Route Mapping:
```typescript
const breadcrumbLabels: Record<string, string> = {
  admin: 'Quan tri',
  users: 'Nguoi dung',
  workspaces: 'Workspace',
  requests: 'Ho so yeu cau',
  ops: 'Van hanh',
  audit: 'Audit',
  vault: 'Phan loai vault',
  specialist: 'Chuyen vien',
  reviewer: 'Nguoi duyet',
  customer: 'Khach hang',
  delivery: 'Ban giao',
  templates: 'Mau van ban',
  review: 'Duyet',
};
```

Existing root layout at src/app/layout.tsx (15 lines):
```typescript
import type { Metadata } from "next";
import "./globals.css";
export const metadata: Metadata = {
  title: "Nen tang phap ly SME",
  description: "Nen tang van hanh dich vu phap ly cho SME.",
};
export default function RootLayout({ children }) {
  return (<html lang="vi"><body>{children}</body></html>);
}
```

Existing globals.css at src/app/globals.css:
- CSS custom properties for --background, --surface, --accent, --destructive, --text-primary, --text-secondary, --border, --muted
- body with radial gradient background
- selection, a, button/input/select/textarea, table styles
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Ant Design dependencies</name>
  <files>
    package.json
  </files>
  <read_first>
    package.json (check current deps)
  </read_first>
  <action>
    Run `npm install antd@6.4.3 @ant-design/icons @ant-design/cssinjs` to add Ant Design v6.4.3, @ant-design/icons, and @ant-design/cssinjs (required for SSR compatibility with Next.js App Router).

    No other packages need to be added or removed. Tailwind v4, lucide-react, and all existing deps remain. lucide-react is kept in package.json for now (may still be used somewhere that is not in scope).

    After install, run `npm run typecheck` to confirm the new deps do not break compilation. Pre-existing TS errors outside of this plan's scope do not need fixing.
  </action>
  <verify>
    <automated>node -e "require('antd'); require('@ant-design/icons'); require('@ant-design/cssinjs'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"antd": "^6.4.3"`, `"@ant-design/icons"`, `"@ant-design/cssinjs"` in dependencies
    - `npm run typecheck` exits with code 0 (or only pre-existing errors outside scope)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create theme provider, breadcrumb config, update root layout, simplify globals.css</name>
  <files>
    src/app/providers/antd-provider.tsx (NEW)
    src/lib/navigation/breadcrumb-labels.ts (NEW)
    src/app/layout.tsx (modified)
    src/app/globals.css (modified)
  </files>
  <read_first>
    src/app/layout.tsx
    src/app/globals.css
    14-UI-SPEC.md (for exact theme token values and breadcrumb mapping)
    14-CONTEXT.md (D-05 ConfigProvider, D-06 Tailwind retained, D-07 CSS vars migrated)
  </read_first>
  <action>
    Create the following files:

    **A) src/lib/navigation/breadcrumb-labels.ts**
    Export a `breadcrumbLabels: Record<string, string>` constant and a helper `getBreadcrumbItems(pathname: string)` function per the UI-SPEC.md Breadcrumb Route Mapping table:
    - Map each route segment to its Vietnamese label: admin='Quan tri', users='Nguoi dung', workspaces='Workspace', requests='Ho so yeu cau', ops='Van hanh', audit='Audit', vault='Phan loai vault', specialist='Chuyen vien', reviewer='Nguoi duyet', customer='Khach hang', delivery='Ban giao', templates='Mau van ban', review='Duyet'
    - `getBreadcrumbItems` splits the pathname by `/`, filters empty segments and route group segments (those starting with `(`), maps each segment via breadcrumbLabels with fallback to the segment itself, builds cumulative href for each, and sets the last item's href to undefined (current page, not clickable). Returns `{ title: string; href?: string }[]`

    **B) src/app/providers/antd-provider.tsx** (new directory)
    Create a client component (`'use client'`) that wraps children with:
    1. `<AntdRegistry>` from `@ant-design/cssinjs`
    2. `<ConfigProvider theme={theme}>` with the exact ThemeConfig from UI-SPEC.md (copy-paste the `theme` object from the spec — all token values are pre-approved)
    3. `<App>` from `antd` (provides message/modal/notification hooks)

    The component signature: `export default function AntdProvider({ children }: { children: React.ReactNode })`

    **C) src/app/layout.tsx**
    Import `AntdProvider` from `@/app/providers/antd-provider`. Wrap `{children}` in `<AntdProvider>`. Keep the existing metadata export and `html lang="vi"`. Do NOT add any new imports other than AntdProvider.

    **D) src/app/globals.css**
    Simplify by removing all CSS custom properties (--background, --surface, --accent, etc.) and the `body` background gradient — these are now handled by antd ConfigProvider theme tokens. Keep ONLY:
    - `@tailwind base; @tailwind components; @tailwind utilities;`
    - `* { box-sizing: border-box; }`
    - `body { margin: 0; min-height: 100vh; font-family: ui-sans-serif, ...; line-height: 1.5; -webkit-font-smoothing: antialiased; color: #0F172A; }`
    - `::selection { background: rgba(15, 118, 110, 0.18); }`
    - `a { color: inherit; text-decoration: none; }`
    - `button, input, select, textarea { font: inherit; }`
    - `table { font-variant-numeric: tabular-nums; }`

    Do NOT add any other styles. The antd theme token system replaces all CSS variable usage.
  </action>
  <verify>
    <automated>npm run typecheck && grep -q "AntdProvider" src/app/layout.tsx && grep -q "AntdRegistry" src/app/providers/antd-provider.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/providers/antd-provider.tsx` exists with AntdRegistry + ConfigProvider + App wrapping children
    - `src/lib/navigation/breadcrumb-labels.ts` exports `breadcrumbLabels` record and `getBreadcrumbItems` function
    - `src/app/layout.tsx` wraps children in `<AntdProvider>`
    - `src/app/globals.css` no longer contains `:root { --background: ... }` or `radial-gradient`
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client browser → Next.js SSR | Provider tree renders on server and client |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | T | antd-provider client component | accept | Client component for ConfigProvider is expected; no sensitive logic in provider |
| T-14-01-02 | D | ConfigProvider tokens | accept | Theme tokens are public brand values, no secrets |
</threat_model>

<verification>
- [ ] `npm run dev` starts without antd-related errors
- [ ] Page renders with antd provider tree (check React DevTools for ConfigProvider)
- [ ] Breadcrumb getBreadcrumbItems returns correct labels for a test pathname
</verification>

<success_criteria>
- All 5 files modified/created with correct content
- `npm run typecheck` passes
- No runtime errors from antd provider tree
</success_criteria>

<output>
After completion, create `.planning/phases/14-antd-layout-redesign/14-01-SUMMARY.md`
</output>
