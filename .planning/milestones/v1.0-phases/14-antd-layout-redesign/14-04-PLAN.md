---
phase: 14-antd-layout-redesign
plan: 04
type: execute
wave: 3
depends_on:
  - 14-02-PLAN.md
files_modified:
  - src/app/admin/ops/page.tsx
  - src/app/admin/ops/[requestId]/page.tsx
  - src/app/admin/requests/page.tsx
  - src/app/admin/routing/page.tsx
  - src/app/admin/templates/page.tsx
  - src/app/admin/vault/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 6 remaining admin pages no longer import from AdminShell or ui.tsx"
    - "All pages use antd components: Tag, Card, Table, Typography, Button, Flex"
    - "Each page's visual behavior matches the original (same data, same layout)"
  artifacts:
    - path: "src/app/admin/ops/page.tsx"
      provides: "Ops dashboard with antd components"
      contains: "from 'antd'"
    - path: "src/app/admin/ops/[requestId]/page.tsx"
      provides: "Ops timeline with antd components"
      contains: "from 'antd'"
    - path: "src/app/admin/requests/page.tsx"
      provides: "Requests page with antd components"
      contains: "from 'antd'"
    - path: "src/app/admin/routing/page.tsx"
      provides: "Routing page with antd components"
      contains: "from 'antd'"
    - path: "src/app/admin/templates/page.tsx"
      provides: "Templates page with antd components"
      contains: "from 'antd'"
    - path: "src/app/admin/vault/page.tsx"
      provides: "Vault classification page with antd components"
      contains: "from 'antd'"
  key_links:
    - from: "all six pages"
      to: "antd"
      via: "import"
      pattern: "from 'antd'"
---

<objective>
Migrate the remaining 6 admin pages from custom UI components to Ant Design.

Purpose: Complete the admin route group migration by replacing AdminShell wrapper and all ui.tsx imports with antd components across ops, requests, routing, templates, and vault pages.

Output:
- 6 admin page files modified with antd component imports
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-antd-layout-redesign/14-CONTEXT.md
@.planning/phases/14-antd-layout-redesign/14-UI-SPEC.md
@.planning/phases/14-antd-layout-redesign/14-01-SUMMARY.md
@.planning/phases/14-antd-layout-redesign/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ops page + ops timeline page</name>
  <files>
    src/app/admin/ops/page.tsx
    src/app/admin/ops/[requestId]/page.tsx
  </files>
  <read_first>
    src/app/admin/ops/page.tsx
    src/app/admin/ops/[requestId]/page.tsx
  </read_first>
  <action>
    Migrate both ops pages following the established pattern from Plan 03:

    **src/app/admin/ops/page.tsx:**
    - Remove: `import { AdminShell } from '../components/admin-shell';`
    - Remove: `import { Badge, Button, Card, PageHeader, Table } from '../components/ui';`
    - Add: `import { Tag, Button, Card, Table, Typography, Flex, Row, Col } from 'antd';`
    - Remove `<AdminShell>` wrapper
    - Replace `<PageHeader>` with `<Flex vertical><Typography.Title level={3}>Van hanh</Typography.Title><Typography.Paragraph>...</Typography.Paragraph></Flex>`
    - Replace `<Badge>` with `<Tag>` (tone mapping: neutral=default, info=blue, warning=orange, accent=cyan, destructive=red, outline=default)
    - Replace `<Card>` with antd Card
    - Replace custom `<Table>` with antd `<Table dataSource={...} columns={...} pagination={false} />`
      - IMPORTANT: The ops page has TWO tables: one for requests, one for workload. Both need to be migrated.
      - The stat cards (grid of summary numbers) stay as custom divs — they don't need to be antd Card unless they already are. If they were styled divs, keep them. Only replace the `<Card>` wrapper components.
    - Replace `<Button type="submit">` with `<Button type="primary" htmlType="submit">Ap dung bo loc</Button>`
    - The "Xoa bo loc" link stays as a styled link — no need to make it an antd Button unless the original was Button (it was a link styled as Button in the original). Replace the inline styled link with `<Link href="/admin/ops"><Button>Xoa bo loc</Button></Link>`
    - Keep all form elements (input, select, textarea) as native HTML — they are not from ui.tsx. Only replace imports from `../components/ui`.
    - The `EmptyState` component on the page should be changed from a custom div to use antd Card + Typography, or kept as-is since it wasn't imported from ui.tsx. Use `const { Text, Title } = Typography;` and replace EmptyState with:
      ```tsx
      <Card>
        <Title level={4}>{title}</Title>
        <Text type="secondary">{body}</Text>
      </Card>
      ```

    **src/app/admin/ops/[requestId]/page.tsx:**
    - Remove: `import { AdminShell } from '../../components/admin-shell';`
    - Remove: `import { Badge, Card, PageHeader, Table } from '../../components/ui';`
    - Add: `import { Tag, Card, Table, Typography, Flex, Space } from 'antd';`
    - Remove `<AdminShell>` wrapper. **IMPORTANT:** Since this page is inside `/admin/ops/[requestId]`, the import of AdminShell uses `../../components/admin-shell` — a relative path. Remove this.
    - Replace `<PageHeader>` with Typography.Title + Paragraph
    - Replace `<Badge>` with `<Tag>` (tone mapping: info -> blue)
    - Replace `<Card>` with antd Card
    - Replace custom `<Table>` with antd `<Table dataSource={...} columns={[...]} pagination={false} />`
    - The "Quay lai van hanh" link should become `<Link href="/admin/ops"><Button>Quay lai van hanh</Button></Link>` or keep as a link if it wasn't a Button originally (it was a styled anchor, not a Button component). Keep the Link style without making it a Button.
    - Keep the SLA grid cards as styled divs — they weren't from ui.tsx.

    **Verify:** `npm run typecheck`
  </action>
  <verify>
    <automated>grep -q "from 'antd'" src/app/admin/ops/page.tsx && grep -q "from 'antd'" src/app/admin/ops/\[requestId\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both ops pages no longer import from admin-shell or ui.tsx
    - Both ops pages import from antd
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate requests, routing, templates, vault pages</name>
  <files>
    src/app/admin/requests/page.tsx
    src/app/admin/routing/page.tsx
    src/app/admin/templates/page.tsx
    src/app/admin/vault/page.tsx
  </files>
  <read_first>
    src/app/admin/requests/page.tsx
    src/app/admin/routing/page.tsx
    src/app/admin/templates/page.tsx
    src/app/admin/vault/page.tsx
  </read_first>
  <action>
    Migrate all four pages following the established pattern:

    **src/app/admin/requests/page.tsx:**
    - Remove: `import { AdminShell } from '../components/admin-shell';`
    - Remove: `import { Badge, Button, Card, PageHeader, Table } from '../components/ui';`
    - Add: `import { Tag, Button, Card, Table, Typography, Flex, Space } from 'antd';`
    - Remove `<AdminShell>` wrapper
    - Replace `<PageHeader>` with Typography.Title + Paragraph in Flex
    - Replace `<Badge>` with `<Tag>`
    - Replace `<Button>` with antd Button (variant=secondary -> `<Button>` with no type prop; variant=destructive -> `<Button danger>`)
    - Replace `<Card>` with antd Card
    - Replace custom `<Table>` with antd `<Table>`
    - Keep form elements as native HTML

    **src/app/admin/routing/page.tsx:**
    - Remove: `import { AdminShell } from '../components/admin-shell';`
    - Remove: `import { Badge, Button, Card, PageHeader, Table } from '../components/ui';`
    - Add: `import { Tag, Button, Card, Table, Typography, Flex } from 'antd';`
    - Remove `<AdminShell>` wrapper
    - Replace `<PageHeader>` with Typography.Title + Paragraph in Flex
    - Replace `<Badge>` with `<Tag>` (tone mapping per spec: accent=cyan, info=blue, warning=orange, neutral=default, destructive=red, outline=default)
    - Replace `<Button>` with antd `<Button>` (all plain secondary = no type; primary = type="primary"; destructive = danger)
    - Replace `<Card>` with antd Card
    - Replace both instances of custom `<Table>` with antd `<Table>` (the routing page has a main table for requests and another for matter types + capabilities)
    - Keep form elements (select, input, textarea) as native HTML — they were never from ui.tsx
    - The `SuggestionList` component uses custom className styling for its list items — keep those as-is since they weren't from ui.tsx

    **src/app/admin/templates/page.tsx:**
    - Remove: `import { AdminShell } from '../components/admin-shell';`
    - Remove: `import { Badge, Button, Card, PageHeader, Table } from '../components/ui';`
    - Add: `import { Tag, Button, Card, Table, Typography, Flex } from 'antd';`
    - Remove `<AdminShell>` wrapper
    - Replace `<PageHeader>` with Typography.Title + Paragraph + action Button in Flex
    - Replace `<Badge>` with `<Tag>` (STATUS_TONES mapping: neutral=default, info=blue, accent=cyan, destructive=red)
    - Replace `<Card>` with antd Card
    - Replace `<Table>` with antd `<Table>`
    - Keep the `MATTER_TYPE_LABELS` and `STATUS_LABELS` / `STATUS_TONES` records as-is

    **src/app/admin/vault/page.tsx:**
    - Remove: `import { AdminShell } from '../components/admin-shell';`
    - Remove: `import { Card, PageHeader, Table, Badge } from '../components/ui';`
    - Add: `import { Tag, Card, Table, Typography, Flex, Row, Col } from 'antd';`
    - Remove `<AdminShell>` wrapper
    - Replace `<PageHeader>` with Typography.Title + Paragraph in Flex
    - Replace `<Badge>` with `<Tag>`
    - Replace `<Card>` with antd Card
    - Replace `<Table>` with antd `<Table>`
    - Keep folder/tag form components as-is (they are imported from ./components/)
    - The inner `ul/li` lists for folders and tags are custom markup — keep as-is (they were not from ui.tsx)
    - The `MoveFileForm` component in the table stays as-is (it's a custom component)

    **Common pattern for all pages:**
    - PageHeader replacement: `<Flex vertical gap={4} style={{ marginBottom: 16 }}><Typography.Title level={3} style={{ margin: 0, fontSize: 30, fontWeight: 600 }}>{title}</Typography.Title><Typography.Paragraph style={{ color: '#475569', margin: 0, fontSize: 16 }}>{description}</Typography.Paragraph></Flex>`
    - If PageHeader had an `action` prop, add the action Button inside the Flex: `<Flex vertical gap={4} style={{ marginBottom: 16 }}><Flex justify="space-between" align="flex-start">{title+description}{actionButton}</Flex></Flex>`

    **Verify:** `npm run typecheck`
  </action>
  <verify>
    <automated>grep -q "from 'antd'" src/app/admin/requests/page.tsx && grep -q "from 'antd'" src/app/admin/routing/page.tsx && grep -q "from 'antd'" src/app/admin/templates/page.tsx && grep -q "from 'antd'" src/app/admin/vault/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 admin pages import from antd and do not import from admin-shell or ui.tsx
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → page | Server-fetched data renders in antd components |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | I | Table columns with user data | accept | All data is server-rendered; antd React rendering prevents XSS |
</threat_model>

<verification>
- [ ] All 6 pages no longer import AdminShell or from ui.tsx
- [ ] All 6 pages import from antd
- [ ] PageHeader (title + description) is replaced with Typography.Title + Paragraph
- [ ] Tables use dataSource + columns API
- [ ] Badge replaced with Tag using correct tone-to-color mapping
- [ ] Typecheck passes
</verification>

<success_criteria>
- All 9 admin pages (3 in Plan 03 + 6 in Plan 04) migrated to antd
- No admin page imports from AdminShell or ui.tsx
- Only the `ui.tsx` and `admin-shell.tsx` files remain as orphans for cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/14-antd-layout-redesign/14-04-SUMMARY.md`
</output>
