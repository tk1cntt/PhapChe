---
phase: 14-antd-layout-redesign
plan: 06
type: execute
wave: 2
depends_on:
  - 14-01-PLAN.md
files_modified:
  - src/app/customer/layout.tsx
  - src/app/customer/requests/[requestId]/page.tsx
  - src/app/requests/[requestId]/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Customer route group has minimal layout (no sidebar, only header with breadcrumb)"
    - "Customer request detail page uses antd Tag, Button, Card, Typography"
    - "General request status page also uses antd Tag, Card, Typography"
  artifacts:
    - path: "src/app/customer/layout.tsx"
      provides: "Customer layout with antd Layout + Header + Breadcrumb (no sidebar)"
      min_lines: 30
    - path: "src/app/customer/requests/[requestId]/page.tsx"
      provides: "Customer delivery page with antd components"
      contains: "from 'antd'"
    - path: "src/app/requests/[requestId]/page.tsx"
      provides: "Request status page with antd components"
      contains: "from 'antd'"
  key_links:
    - from: "customer layout"
      to: "src/lib/navigation/breadcrumb-labels.ts"
      via: "import getBreadcrumbItems"
      pattern: "breadcrumb-labels"
---

<objective>
Create customer route group layout and migrate customer-facing pages to antd.

Purpose: The customer role gets a minimal layout (no sidebar, minimal chrome) as specified in D-03. Both customer-facing pages (customer request detail and general request status) use antd components.

Output:
- src/app/customer/layout.tsx (NEW)
- 2 page files modified with antd imports
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-antd-layout-redesign/14-CONTEXT.md
@.planning/phases/14-antd-layout-redesign/14-UI-SPEC.md
@.planning/phases/14-antd-layout-redesign/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create customer route group layout (no sidebar, minimal chrome)</name>
  <files>src/app/customer/layout.tsx</files>
  <read_first>
    14-UI-SPEC.md (layout architecture: customer = Layout only, no sidebar, header with breadcrumb)
    src/lib/navigation/breadcrumb-labels.ts
  </read_first>
  <action>
    Create `src/app/customer/layout.tsx` — a client component with minimal chrome — no sidebar, only a header with breadcrumb:

    ```
    'use client';
    import { usePathname } from 'next/navigation';
    import { Layout, Breadcrumb, Typography } from 'antd';
    import { getBreadcrumbItems } from '@/lib/navigation/breadcrumb-labels';

    const { Content, Header } = Layout;

    export default function CustomerLayout({ children }: { children: React.ReactNode }) {
      const pathname = usePathname();
      const breadcrumbItems = getBreadcrumbItems(pathname);

      return (
        <Layout style={{ minHeight: '100vh' }}>
          <Header
            style={{
              background: '#FFFFFF',
              borderBottom: '1px solid #E2E8F0',
              padding: '0 24px',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
              height: 48,
            }}
          >
            <Breadcrumb items={breadcrumbItems} />
            <Typography.Text style={{ color: '#0F766E', fontWeight: 600, fontSize: 13 }}>
              GitNexus Legal
            </Typography.Text>
          </Header>
          <Content
            style={{
              padding: 24,
              background: '#F8FAFC',
              minHeight: 'calc(100vh - 48px)',
            }}
          >
            {children}
          </Content>
        </Layout>
      );
    }
    ```

    Key design decisions:
    - No Sider (per D-03: customer layout = minimal, no sidebar)
    - Header has breadcrumb on left, brand on right
    - Same content area styling as other layouts
  </action>
  <verify>
    <automated>grep -q "getBreadcrumbItems" src/app/customer/layout.tsx && grep -q "Layout.*Header\|Header.*Content" src/app/customer/layout.tsx && ! grep -q "Sider" src/app/customer/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Customer layout created with antd Layout + Header (no Sider)
    - Breadcrumb uses centralized config
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate customer delivery page and request status page to antd</name>
  <files>
    src/app/customer/requests/[requestId]/page.tsx
    src/app/requests/[requestId]/page.tsx
  </files>
  <read_first>
    src/app/customer/requests/[requestId]/page.tsx
    src/app/requests/[requestId]/page.tsx
  </read_first>
  <action>
    Migrate both customer-facing pages:

    **src/app/customer/requests/[requestId]/page.tsx:**
    - Remove: `import { Badge, Button, Card, PageHeader } from '@/app/admin/components/ui';`
    - Add: `import { Tag, Button, Card, Typography, Flex } from 'antd';`
    - Remove the `<main>` wrapper (route group layout provides the shell)
    - Replace `<PageHeader>` with Typography.Title + Paragraph in Flex
    - Replace `<Badge tone={status.tone}>` with `<Tag color={toneToColor(status.tone)}>`
    - Replace `<Button>` with antd `<Button type="primary">Tai xuong</Button>`
    - Replace `<Card>` with antd Card
    - The inner styled divs (`rounded-xl border border-[#E2E8F0] p-4`) keep their Tailwind classes — they were not from ui.tsx

    **src/app/requests/[requestId]/page.tsx:**
    This page is NOT inside a route group — it's at `/requests/[requestId]`. It does NOT have a route group layout yet, so:
    - Remove: `import { Badge, Card, PageHeader } from '@/app/admin/components/ui';`
    - Add: `import { Tag, Card, Typography, Flex } from 'antd';`
    - Replace `<Badge>` with `<Tag>`
    - Replace `<PageHeader>` with Typography.Title + Paragraph in Flex
    - Replace `<Card>` with antd Card
    - Keep the `<main>` wrapper (no route group layout exists at this level)
    - Keep all inner styled divs as-is

    **Tone-to-color mapping for both pages:**
    - neutral='default', info='blue', warning='orange', accent='cyan', destructive='red', outline='default'

    **Verify:** `npm run typecheck`
  </action>
  <verify>
    <automated>grep -q "from 'antd'" src/app/customer/requests/\[requestId\]/page.tsx && grep -q "from 'antd'" src/app/requests/\[requestId\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both pages import from antd instead of admin/components/ui
    - Customer page uses antd Tag, Button, Card, Typography
    - Request status page uses antd Tag, Card, Typography
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → customer page | Customer views own request delivery status |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | I | customer page tags | accept | Status labels are hardcoded, no user input in Tag component |
</threat_model>

<verification>
- [ ] Customer layout renders without sidebar
- [ ] Customer delivery page shows download buttons and status tags with antd
- [ ] Request status page renders with antd cards and tags
</verification>

<success_criteria>
- Customer layout created
- Both customer-facing pages migrated to antd
- Typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-antd-layout-redesign/14-06-SUMMARY.md`
</output>
