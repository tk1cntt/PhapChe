---
phase: 14-antd-layout-redesign
plan: 08
type: execute
wave: 4
depends_on:
  - 14-04-PLAN.md
  - 14-05-PLAN.md
  - 14-06-PLAN.md
  - 14-07-PLAN.md
files_modified:
  - src/app/admin/templates/[templateId]/page.tsx
  - src/app/admin/templates/new/page.tsx
  - src/app/admin/vault/components/folder-form.tsx
  - src/app/admin/vault/components/move-file-form.tsx
  - src/app/admin/vault/components/tag-form.tsx
  - src/app/intake/components.tsx
  - src/app/intake/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Template detail, new template, vault sub-components no longer import from admin/components/ui"
    - "Intake components no longer import from admin/components/ui"
    - "No remaining files in the project import from admin/components/ui.tsx"
  artifacts:
    - path: "src/app/admin/templates/[templateId]/page.tsx"
      provides: "Template detail page with antd components"
      contains: "from 'antd'"
    - path: "src/app/admin/templates/new/page.tsx"
      provides: "New template page with antd components"
      contains: "from 'antd'"
    - path: "src/app/intake/components.tsx"
      provides: "Intake UI components with antd imports"
      contains: "from 'antd'"
  key_links:
    - from: "All migrated files"
      to: "antd"
      via: "import"
      pattern: "from 'antd'"
---

<objective>
Migrate remaining files that still import from `admin/components/ui.tsx` — template pages, vault sub-components, and intake components.

Purpose: Complete the migration of all ui.tsx consumers so the orphaned files can be safely removed in Plan 09.

Output:
- 5 admin sub-pages/sub-components migrated
- Intake components migrated
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-antd-layout-redesign/14-CONTEXT.md
@.planning/phases/14-antd-layout-redesign/14-UI-SPEC.md
@.planning/phases/14-antd-layout-redesign/14-01-SUMMARY.md
@.planning/phases/14-antd-layout-redesign/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate template detail page and new template page</name>
  <files>
    src/app/admin/templates/[templateId]/page.tsx
    src/app/admin/templates/new/page.tsx
  </files>
  <read_first>
    src/app/admin/templates/[templateId]/page.tsx
    src/app/admin/templates/new/page.tsx
  </read_first>
  <action>
    Migrate both template pages:

    **src/app/admin/templates/[templateId]/page.tsx:**
    - Remove: `import { AdminShell } from '../../components/admin-shell';`
    - Remove: `import { Badge, Button, Card } from '../../components/ui';`
    - Add: `import { Tag, Button, Card, Typography, Flex, Space } from 'antd';`
    - Remove `<AdminShell>` wrapper (route group layout provides the shell)
    - Replace `<Badge>` with `<Tag>` (STATUS_TONES mapping: neutral=default, info=blue, accent=cyan, destructive=red)
    - Replace `<Button>` with antd Button:
      - `<Button type="submit" variant="primary">` -> `<Button type="primary" htmlType="submit">`
      - `<Button variant="secondary">` -> `<Button>`
      - `<Button type="submit" variant="destructive">` -> `<Button danger htmlType="submit">`
    - Replace `<Card>` with antd Card
    - Keep all form elements, anchor tags, and layout markup as-is
    - The page has 3 modes (edit, new_version, view) — they all need the same import changes

    **src/app/admin/templates/new/page.tsx:**
    - Remove: `import { AdminShell } from '../../components/admin-shell';`
    - Remove: `import { Button, Card } from '../../components/ui';`
    - Add: `import { Button, Card, Typography, Flex } from 'antd';`
    - Remove `<AdminShell>` wrapper
    - Replace `<Button type="submit" variant="primary">` with `<Button type="primary" htmlType="submit">`
    - Replace `<Card>` with antd Card
    - Keep the back link and form elements as-is

    **Verify:** `npm run typecheck`
  </action>
  <verify>
    <automated>grep -q "from 'antd'" src/app/admin/templates/\[templateId\]/page.tsx && grep -q "from 'antd'" src/app/admin/templates/new/page.tsx && ! grep -q "from.*components/ui" src/app/admin/templates/\[templateId\]/page.tsx && ! grep -q "from.*components/ui" src/app/admin/templates/new/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both template pages import from antd, not from admin-shell or ui.tsx
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate vault sub-components (folder-form, move-file-form, tag-form)</name>
  <files>
    src/app/admin/vault/components/folder-form.tsx
    src/app/admin/vault/components/move-file-form.tsx
    src/app/admin/vault/components/tag-form.tsx
  </files>
  <read_first>
    src/app/admin/vault/components/folder-form.tsx
    src/app/admin/vault/components/move-file-form.tsx
    src/app/admin/vault/components/tag-form.tsx
  </read_first>
  <action>
    Migrate all three vault sub-components:

    **For each file:**
    - Remove: `import { Badge, Button } from '../../components/ui';` (or similar relative path)
    - Add: `import { Tag, Button } from 'antd';`
    - Replace `<Badge tone="destructive">` with `<Tag color="red">`
    - Replace `<Badge tone="accent">` with `<Tag color="cyan">`
    - Replace `<Button type="submit">` with `<Button type="primary" htmlType="submit">`
    - Replace `<Button type="submit" disabled={pending}>` with `<Button type="primary" htmlType="submit" loading={pending}>`

    **move-file-form.tsx specifics:**
    - The `feedback` state uses `tone: 'accent' | 'destructive'`. Keep the state type but change the render:
      replace `<Badge tone={feedback.tone}>` with `<Tag color={feedback.tone === 'accent' ? 'cyan' : 'red'}>`
    - The file also has `<button>` elements (styled HTML buttons) — these are NOT from ui.tsx, keep them as-is

    **folder-form.tsx and tag-form.tsx:**
    - These use `Badge` for success/error feedback messages
    - Replace `<Badge tone="destructive">` with `<Tag color="red">`
    - Replace `<Badge tone="accent">` with `<Tag color="cyan">`

    **Verify:** `npm run typecheck`
  </action>
  <verify>
    <automated>for f in folder-form.tsx move-file-form.tsx tag-form.tsx; do grep -q "from 'antd'" src/app/admin/vault/components/$f || echo "FAIL: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 3 vault sub-components import Tag/Button from antd
    - Badge replaced with Tag, Button replaced with antd Button
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Migrate intake components to antd</name>
  <files>
    src/app/intake/components.tsx
    src/app/intake/page.tsx
  </files>
  <read_first>
    src/app/intake/components.tsx
    src/app/intake/page.tsx
  </read_first>
  <action>
    Migrate both intake files:

    **src/app/intake/components.tsx:**
    - Remove: `import { Badge, Button, Card, PageHeader } from '@/app/admin/components/ui';`
    - Add: `import { Tag, Button, Card, Typography, Flex } from 'antd';`
    - Replace `<PageHeader title="..." description="..." />` with `<Flex vertical gap={4} style={{ marginBottom: 16 }}><Typography.Title level={3} style={{ margin: 0, fontSize: 30, fontWeight: 600 }}>{title}</Typography.Title><Typography.Paragraph style={{ color: '#475569', margin: 0, fontSize: 16 }}>{description}</Typography.Paragraph></Flex>`
    - Replace `<Badge tone="warning">` with `<Tag color="orange">`
    - Replace `<Button type="submit">` with `<Button type="primary" htmlType="submit">`
    - Replace `<Card>` with antd Card
    - Replace `<Badge>` (in QuestionStep) with `<Tag>`
    - The `IntakeShell` component returns a `<main>` element with Tailwind classes — keep as-is (not from ui.tsx)

    **src/app/intake/page.tsx:**
    - This page does NOT import from ui.tsx directly — it imports from `./components`. So no changes needed here. Verify on re-read.

    **Verify:** `npm run typecheck`
  </action>
  <verify>
    <automated>grep -q "from 'antd'" src/app/intake/components.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Intake components import from antd instead of admin/components/ui
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → forms | Template and vault forms accept user input |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08-01 | I | form feedback badges | accept | Component replacement only; no logic change |
</threat_model>

<verification>
- [ ] All 7 files no longer import from admin/components/ui
- [ ] All 7 files import from antd
- [ ] Template pages no longer wrap in AdminShell
- [ ] Vault sub-components replace Badge->Tag, Button->antd Button
- [ ] Intake components replace PageHeader->Typography, Badge->Tag
- [ ] Typecheck passes
</verification>

<success_criteria>
- All remaining ui.tsx consumers migrated to antd
- No file in the project imports from `admin/components/ui.tsx` (except the file itself)
- Typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-antd-layout-redesign/14-08-SUMMARY.md`
</output>
