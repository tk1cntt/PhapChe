---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/prisma.ts
  - src/lib/security/session.ts
  - src/lib/security/rbac.ts
  - src/lib/audit/audit.ts
  - src/lib/audit/audit.test.ts
autonomous: true
requirements:
  - FND-02
  - FND-03
  - FND-04
must_haves:
  truths:
    - "Server-side RBAC decides access for request, document, review, and vault file targets."
    - "Workspace isolation denies customer access outside own workspace."
    - "Audit event creation never stores raw sensitive legal content."
  artifacts:
    - path: "src/lib/security/rbac.ts"
      provides: "Permission checks for FND-02 and FND-04"
      exports: ["canAccessWorkspace", "canAccessRequest", "canAccessDocument", "canAccessReview", "canAccessVaultFile"]
    - path: "src/lib/audit/audit.ts"
      provides: "Append-only audit event writer for FND-03"
      exports: ["recordAuditEvent"]
    - path: "src/lib/prisma.ts"
      provides: "Shared Prisma client"
  key_links:
    - from: "src/lib/security/rbac.ts"
      to: "prisma.workspaceMembership"
      via: "membership lookup"
      pattern: "workspaceMembership"
    - from: "src/lib/audit/audit.ts"
      to: "prisma.auditEvent.create"
      via: "append-only create"
      pattern: "auditEvent.create"
---

<objective>
Tạo lớp bảo mật server-side: session contract, RBAC theo workspace/assignment/admin, audit append-only.

Purpose: FND-02/FND-03/FND-04 phải là backend enforcement, frontend chỉ UX theo D-06/D-07.
Output: Prisma client, session helpers, RBAC helpers, audit writer with tests.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@prisma/schema.prisma

<interfaces>
Use Prisma enums generated from Plan 01:
```ts
Role: 'customer' | 'specialist' | 'reviewer' | 'coordinator_admin' | 'super_admin'
RequestStatus: 'draft_intake' | 'intake_submitted' | 'triage' | 'assigned' | 'in_progress' | 'pending_review' | 'revision_required' | 'approved' | 'delivered' | 'closed' | 'cancelled'
```

Session contract to create:
```ts
export type AppSession = { userId: string; activeWorkspaceId?: string | null; roles: Array<'customer' | 'specialist' | 'reviewer' | 'coordinator_admin' | 'super_admin'> };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create server session and RBAC helpers</name>
  <files>src/lib/prisma.ts, src/lib/security/session.ts, src/lib/security/rbac.ts</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-foundation/01-CONTEXT.md
    prisma/schema.prisma
    src/lib/prisma.ts
    src/lib/security/session.ts
    src/lib/security/rbac.ts
  </read_first>
  <behavior>
    - super_admin can access any workspace/request/document/review/vault file.
    - coordinator_admin can access any resource in workspace where membership is active.
    - customer can access only active membership workspace resources.
    - specialist can access request only when assignedSpecialistId equals session.userId or coordinator_admin/super_admin applies.
    - reviewer can access review/request only when assignedReviewerId or review.reviewerId equals session.userId or admin applies.
  </behavior>
  <action>Create prisma singleton in src/lib/prisma.ts using globalThis cache for dev. Create session.ts with AppRole union matching D-03 and AppSession type. Create rbac.ts with exported async functions: canAccessWorkspace(session, workspaceId), canAccessRequest(session, requestId), canAccessDocument(session, documentId), canAccessReview(session, reviewId), canAccessVaultFile(session, vaultFileId). Each function must query database server-side; do not trust client role claims alone where workspace membership/assignment must be checked. Use least privilege defaults per D-08: return false on missing session, inactive user, inactive membership, absent assignment, absent target. Do not include frontend-only checks as security controls per D-07.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'export type AppSession' src/lib/security/session.ts
    grep -q 'canAccessRequest' src/lib/security/rbac.ts
    grep -q 'assignedSpecialistId' src/lib/security/rbac.ts
    grep -q 'assignedReviewerId' src/lib/security/rbac.ts
    grep -q 'workspaceMembership' src/lib/security/rbac.ts
    grep -q 'return false' src/lib/security/rbac.ts
  </acceptance_criteria>
  <done>Server-side RBAC helpers enforce workspace and assignment boundaries for all FND-02 resource categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create append-only audit writer</name>
  <files>src/lib/audit/audit.ts, src/lib/audit/audit.test.ts</files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md
    prisma/schema.prisma
    src/lib/audit/audit.ts
    src/lib/audit/audit.test.ts
  </read_first>
  <behavior>
    - recordAuditEvent creates AuditEvent with actorId, workspaceId, action, targetType, targetId, requestId, correlationId, metadataSummary.
    - recordAuditEvent rejects metadataSummary longer than 500 chars.
    - recordAuditEvent does not expose update/delete helpers.
  </behavior>
  <action>Create recordAuditEvent(input, db = prisma) in src/lib/audit/audit.ts so workflow services can pass a Prisma transaction client. Input fields: actorId?: string | null, workspaceId?: string | null, action: string, targetType: 'USER' | 'WORKSPACE' | 'REQUEST' | 'ASSIGNMENT' | 'DOCUMENT' | 'REVIEW' | 'VAULT_FILE', targetId: string, requestId?: string | null, correlationId: string, metadataSummary?: string | null. Validate action non-empty, targetId non-empty, correlationId non-empty, metadataSummary length <= 500. Write only db.auditEvent.create; do not create update/delete audit functions to preserve D-12 append-only. Tests can mock db.auditEvent.create; assert raw legal content object is not accepted because API only accepts metadataSummary string per D-15.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'recordAuditEvent' src/lib/audit/audit.ts
    grep -q 'auditEvent.create' src/lib/audit/audit.ts
    grep -q 'db = prisma' src/lib/audit/audit.ts
    ! grep -q 'auditEvent.update' src/lib/audit/audit.ts
    ! grep -q 'auditEvent.delete' src/lib/audit/audit.ts
    grep -q 'metadataSummary' src/lib/audit/audit.ts
    grep -q '500' src/lib/audit/audit.ts
  </acceptance_criteria>
  <done>Audit writer is append-only by exported API and constrains metadata to non-sensitive summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server helpers | Client/session input reaches server authorization decisions. |
| server→audit log | Critical action metadata enters permanent audit store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | AppSession | mitigate | RBAC verifies active membership/assignment in DB; does not trust roles array alone for tenant resources. |
| T-01-02-02 | Elevation of Privilege | rbac.ts | mitigate | Least privilege returns false on missing target/session/membership/assignment; super_admin only global allow. |
| T-01-02-03 | Information Disclosure | rbac.ts | mitigate | canAccessDocument/canAccessReview/canAccessVaultFile require workspace/request/assignment checks before allow. |
| T-01-02-04 | Tampering | audit.ts | mitigate | Expose create-only recordAuditEvent; no update/delete helper; DB row model has createdAt default. |
| T-01-02-05 | Information Disclosure | audit.ts | mitigate | metadataSummary string max 500 chars; no Json/raw legal content input. |
</threat_model>

<verification>
Run npm run typecheck. Grep acceptance criteria. If test runner added by executor, run audit test too.
</verification>

<success_criteria>
RBAC and audit helpers exist, typecheck, and encode D-06/D-08/D-12/D-15 server-side boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
