---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/workflow/request-workflow.ts
  - src/lib/workflow/request-workflow.test.ts
autonomous: true
requirements:
  - FND-05
  - FND-03
must_haves:
  truths:
    - "Request status changes happen only through backend transition function."
    - "Allowed transitions match D-10 initial request state set."
    - "Each transition records actor, timestamp, from-state, to-state, optional reason, and audit event."
  artifacts:
    - path: "src/lib/workflow/request-workflow.ts"
      provides: "Backend request workflow state machine"
      exports: ["REQUEST_TRANSITIONS", "getAllowedTransitions", "transitionRequestStatus"]
    - path: "src/lib/workflow/request-workflow.test.ts"
      provides: "Transition rule tests"
  key_links:
    - from: "src/lib/workflow/request-workflow.ts"
      to: "prisma.workflowTransition.create"
      via: "transition persistence"
      pattern: "workflowTransition.create"
    - from: "src/lib/workflow/request-workflow.ts"
      to: "recordAuditEvent"
      via: "audit status changes"
      pattern: "recordAuditEvent"
---

<objective>
Tạo backend state machine cho request status và ghi workflow/audit khi chuyển trạng thái.

Purpose: FND-05 và D-09 cấm hard-code status mutation ở frontend; mọi status change cần backend rule và audit.
Output: Transition map, allowed-transition helper, transition executor.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@prisma/schema.prisma

<interfaces>
Request statuses required by D-10:
```ts
type RequestStatus = 'draft_intake' | 'intake_submitted' | 'triage' | 'assigned' | 'in_progress' | 'pending_review' | 'revision_required' | 'approved' | 'delivered' | 'closed' | 'cancelled';
```

Transition API to create:
```ts
export function getAllowedTransitions(status: RequestStatus): RequestStatus[];
export async function transitionRequestStatus(input: { requestId: string; actorId: string; toStatus: RequestStatus; reason?: string | null; correlationId: string }): Promise<{ id: string; status: RequestStatus }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define request transition map and tests</name>
  <files>src/lib/workflow/request-workflow.ts, src/lib/workflow/request-workflow.test.ts</files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md
    prisma/schema.prisma
    src/lib/workflow/request-workflow.ts
    src/lib/workflow/request-workflow.test.ts
  </read_first>
  <behavior>
    - draft_intake can transition to intake_submitted or cancelled.
    - intake_submitted can transition to triage or cancelled.
    - triage can transition to assigned or cancelled.
    - assigned can transition to in_progress or cancelled.
    - in_progress can transition to pending_review or cancelled.
    - pending_review can transition to revision_required or approved.
    - revision_required can transition to in_progress or cancelled.
    - approved can transition to delivered.
    - delivered can transition to closed.
    - closed and cancelled have no outgoing transitions.
  </behavior>
  <action>Create REQUEST_TRANSITIONS constant exactly matching behavior above and D-10 statuses. Create getAllowedTransitions(status) returning readonly array copy. Create tests or compile-time assertions for every status and for invalid direct transition draft_intake -> approved. Do not add workflow builder or configurable transitions; D-02 and deferred ideas require fixed backend state machine.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'REQUEST_TRANSITIONS' src/lib/workflow/request-workflow.ts
    grep -q 'draft_intake' src/lib/workflow/request-workflow.ts
    grep -q 'intake_submitted' src/lib/workflow/request-workflow.ts
    grep -q 'cancelled' src/lib/workflow/request-workflow.ts
    grep -q 'getAllowedTransitions' src/lib/workflow/request-workflow.ts
    ! grep -qi 'workflow builder' src/lib/workflow/request-workflow.ts
  </acceptance_criteria>
  <done>All D-10 statuses have explicit backend transition rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transition executor with workflow row and audit</name>
  <files>src/lib/workflow/request-workflow.ts</files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md
    prisma/schema.prisma
    src/lib/audit/audit.ts
    src/lib/workflow/request-workflow.ts
  </read_first>
  <behavior>
    - transitionRequestStatus loads current request status from DB.
    - It rejects toStatus not in getAllowedTransitions(currentStatus).
    - It writes LegalRequest.status update and WorkflowTransition row in one transaction.
    - It calls recordAuditEvent with action "request.status_changed" and metadataSummary "fromStatus -> toStatus".
  </behavior>
  <action>Implement transitionRequestStatus(input) using prisma.$transaction. Load request by id with workspaceId and current status. If request missing, throw Error("REQUEST_NOT_FOUND"). If toStatus not allowed, throw Error("INVALID_REQUEST_TRANSITION"). In transaction update legalRequest.status, create workflowTransition with actorId/fromStatus/toStatus/reason, then call recordAuditEvent after transaction using workspaceId, action "request.status_changed", targetType "REQUEST", targetId requestId, requestId, correlationId, metadataSummary `${fromStatus} -> ${toStatus}`. Do not expose any generic setStatus function.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'transitionRequestStatus' src/lib/workflow/request-workflow.ts
    grep -q 'INVALID_REQUEST_TRANSITION' src/lib/workflow/request-workflow.ts
    grep -q 'workflowTransition.create' src/lib/workflow/request-workflow.ts
    grep -q 'legalRequest.update' src/lib/workflow/request-workflow.ts
    grep -q 'request.status_changed' src/lib/workflow/request-workflow.ts
    ! grep -q 'setStatus' src/lib/workflow/request-workflow.ts
  </acceptance_criteria>
  <done>Status transitions are backend-owned, persisted, and audited with actor/from/to/reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→workflow service | User action attempts to mutate request status. |
| workflow service→database | State transition writes persistent request status and transition history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | transitionRequestStatus | mitigate | Validate toStatus against REQUEST_TRANSITIONS before DB update; reject invalid direct jumps. |
| T-01-03-02 | Repudiation | WorkflowTransition | mitigate | Persist actorId, fromStatus, toStatus, reason, createdAt for every status change per D-11. |
| T-01-03-03 | Repudiation | AuditEvent | mitigate | Call recordAuditEvent with correlationId and metadataSummary for each transition per D-14. |
| T-01-03-04 | Elevation of Privilege | request-workflow.ts | transfer | Authorization must be checked by caller using Plan 02 RBAC before invoking transition; workflow still enforces state integrity. |
</threat_model>

<verification>
Run npm run typecheck. Grep acceptance criteria for no generic setStatus and required audit/workflow writes.
</verification>

<success_criteria>
Request status changes have one backend entry point, fixed transition map, workflow history, and audit event.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
