---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/app/page.tsx
  - src/app/admin/users/page.tsx
  - src/app/admin/workspaces/page.tsx
  - src/app/admin/requests/page.tsx
  - src/app/admin/audit/page.tsx
  - src/app/admin/components/admin-shell.tsx
  - src/app/admin/components/ui.tsx
autonomous: false
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
  - FND-05
must_haves:
  truths:
    - "Admin có màn Users hiển thị đủ 5 role và user management foundation."
    - "Admin có màn Workspaces thể hiện SME isolation và membership visibility."
    - "Admin có màn Requests hiển thị status từ backend-owned workflow, không raw status dropdown."
    - "Admin có màn Audit hiển thị audit event fields không chứa legal content thô."
  artifacts:
    - path: "src/app/admin/users/page.tsx"
      provides: "Admin user management UI foundation"
    - path: "src/app/admin/workspaces/page.tsx"
      provides: "Workspace and membership visibility UI"
    - path: "src/app/admin/requests/page.tsx"
      provides: "Request shell and workflow transition UI contract"
    - path: "src/app/admin/audit/page.tsx"
      provides: "Audit timeline UI foundation"
  key_links:
    - from: "src/app/admin/requests/page.tsx"
      to: "src/lib/workflow/request-workflow.ts"
      via: "allowed transition display contract"
      pattern: "getAllowedTransitions"
    - from: "src/app/admin/audit/page.tsx"
      to: "AuditEvent fields"
      via: "table columns"
      pattern: "correlationId"
---

<objective>
Tạo admin UI foundation bằng Tailwind thủ công: Users, Workspaces, Requests, Audit.

Purpose: FND-01 visible for admin, and FND-02..FND-05 visible as safe UX while security remains server-side.
Output: Accessible Vietnamese admin pages, no shadcn, no deferred features.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@src/lib/security/rbac.ts
@src/lib/workflow/request-workflow.ts

<interfaces>
UI must use Vietnamese copy from UI-SPEC. Role labels:
- customer: Khách hàng
- specialist: Chuyên viên
- reviewer: Reviewer
- coordinator_admin: Điều phối viên
- super_admin: Super admin

Status mutation rule: display allowed transition buttons from backend result only; never render arbitrary status dropdown.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin shell and UI primitives</name>
  <files>src/app/page.tsx, src/app/admin/components/admin-shell.tsx, src/app/admin/components/ui.tsx</files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md
    src/app/globals.css
    src/app/page.tsx
    src/app/admin/components/admin-shell.tsx
    src/app/admin/components/ui.tsx
  </read_first>
  <action>Create root page redirect/link to /admin/users. Create AdminShell with header, left nav desktop, top nav small screens, max-width 1280px, 24px desktop padding, 16px mobile padding. Nav items: Người dùng, Workspace, Hồ sơ yêu cầu, Audit. Add note in component comments or helper text: nav hiding is UX only; server enforces permissions per D-07. Create minimal Button, Badge, Card, Table primitives with Tailwind classes matching UI-SPEC colors/spacing/typography. Do not initialize shadcn. Do not add third-party registry blocks.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'Người dùng' src/app/admin/components/admin-shell.tsx
    grep -q 'Workspace' src/app/admin/components/admin-shell.tsx
    grep -q 'Hồ sơ yêu cầu' src/app/admin/components/admin-shell.tsx
    grep -q 'Audit' src/app/admin/components/admin-shell.tsx
    grep -q 'server' src/app/admin/components/admin-shell.tsx
    grep -q 'max-w-\[1280px\]' src/app/admin/components/admin-shell.tsx
  </acceptance_criteria>
  <done>Admin shell exists with UI spec layout and manual primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Create Users and Workspaces pages</name>
  <files>src/app/admin/users/page.tsx, src/app/admin/workspaces/page.tsx</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-foundation/01-CONTEXT.md
    .planning/phases/01-foundation/01-UI-SPEC.md
    src/app/admin/components/ui.tsx
    src/app/admin/users/page.tsx
    src/app/admin/workspaces/page.tsx
  </read_first>
  <action>Create Users page with Vietnamese heading "Quản lý người dùng", primary CTA "Tạo người dùng", role badges for exactly D-03 roles: customer, specialist, reviewer, coordinator_admin, super_admin with UI-SPEC labels. Include table columns: Tên, Email, Vai trò, Workspace, Trạng thái. Include visible copy that role/workspace changes are recorded in audit. Create Workspaces page with heading "Workspace khách hàng", CTA "Tạo workspace", table columns: Tên workspace, Slug, Thành viên, Trạng thái. Include permission denied copy exactly: "Bạn không có quyền xem nội dung này. Nếu cần truy cập, hãy liên hệ quản trị viên." Use mock/static safe sample rows only for UI scaffold; no legal content. Do not create intake chat/form.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'Quản lý người dùng' src/app/admin/users/page.tsx
    grep -q 'Tạo người dùng' src/app/admin/users/page.tsx
    grep -q 'coordinator_admin' src/app/admin/users/page.tsx
    grep -q 'Workspace khách hàng' src/app/admin/workspaces/page.tsx
    grep -q 'Tạo workspace' src/app/admin/workspaces/page.tsx
    grep -q 'Bạn không có quyền xem nội dung này' src/app/admin/workspaces/page.tsx
  </acceptance_criteria>
  <done>Admin can see user/workspace management foundation for FND-01 and FND-04 UX.</done>
</task>

<task type="auto">
  <name>Task 3: Create Requests and Audit pages with workflow-safe controls</name>
  <files>src/app/admin/requests/page.tsx, src/app/admin/audit/page.tsx</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-foundation/01-CONTEXT.md
    .planning/phases/01-foundation/01-UI-SPEC.md
    src/lib/workflow/request-workflow.ts
    src/app/admin/components/ui.tsx
    src/app/admin/requests/page.tsx
    src/app/admin/audit/page.tsx
  </read_first>
  <action>Create Requests page with heading "Hồ sơ yêu cầu", CTA "Tạo hồ sơ yêu cầu", status badges for D-10 statuses and labels from UI-SPEC. Render allowed transition buttons derived from getAllowedTransitions(sampleStatus), not a select/dropdown. Include reason textarea label "Lý do chuyển trạng thái" for cancellation/revision-like transition examples. Include copy "Trạng thái này chỉ có thể thay đổi qua quy trình hợp lệ trên máy chủ." Create Audit page with heading "Audit", table columns: Thời gian, Actor, Workspace, Hành động, Đối tượng, Mã tương quan, Tóm tắt metadata. Do not show raw legal content. Do not build ops dashboard/SLA/workload analytics.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'Hồ sơ yêu cầu' src/app/admin/requests/page.tsx
    grep -q 'getAllowedTransitions' src/app/admin/requests/page.tsx
    ! grep -q '<select' src/app/admin/requests/page.tsx
    grep -q 'Trạng thái này chỉ có thể thay đổi qua quy trình hợp lệ trên máy chủ' src/app/admin/requests/page.tsx
    grep -q 'Mã tương quan' src/app/admin/audit/page.tsx
    grep -q 'Tóm tắt metadata' src/app/admin/audit/page.tsx
  </acceptance_criteria>
  <done>Requests UI respects backend workflow ownership; Audit UI shows required fields without sensitive content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify admin UI contract</name>
  <files>src/app/admin/users/page.tsx, src/app/admin/workspaces/page.tsx, src/app/admin/requests/page.tsx, src/app/admin/audit/page.tsx</files>
  <what-built>Admin shell and Users/Workspaces/Requests/Audit pages using manual Tailwind primitives.</what-built>
  <how-to-verify>
    1. Run npm run dev.
    2. Visit http://localhost:3000/admin/users, /admin/workspaces, /admin/requests, /admin/audit.
    3. Confirm Vietnamese copy appears.
    4. Confirm Requests page shows transition buttons, not raw status dropdown.
    5. Confirm no chat intake, reviewer checklist, vault browser, delivery, or ops dashboard appears.
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu đúng, hoặc mô tả lỗi UI cần sửa.</resume-signal>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>Pause only after automated build/typecheck passes; ask human to verify visual and interaction contract.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    Human approval received with text "approved".
  </acceptance_criteria>
  <done>Human confirms Phase 1 admin UI matches UI-SPEC and phase boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→admin UI | User sees UI controls that could imply permissions. |
| admin UI→workflow service | Status transition control invokes backend-owned transition path in later wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Elevation of Privilege | admin navigation | mitigate | UI copy states nav hiding is UX only; server-side RBAC remains enforcement per D-07. |
| T-01-04-02 | Tampering | requests page | mitigate | Render allowed transition buttons from getAllowedTransitions; no arbitrary status dropdown. |
| T-01-04-03 | Information Disclosure | audit page | mitigate | Audit table shows metadataSummary only, no raw legal content, matching D-15. |
| T-01-04-04 | Information Disclosure | permission denied state | mitigate | Use generic Vietnamese denied copy that does not reveal whether hidden resource exists. |
</threat_model>

<verification>
Run npm run typecheck. Human verifies UI pages at localhost URLs.
</verification>

<success_criteria>
Phase 1 admin UI exposes user/workspace/request/audit foundation, follows Vietnamese/UI contract, and does not implement deferred features.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
