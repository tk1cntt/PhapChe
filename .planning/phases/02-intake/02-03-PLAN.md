---
phase: 02-intake
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/lib/intake/upload-service.ts
  - src/lib/intake/upload-service.test.ts
autonomous: true
requirements: [INT-04]
must_haves:
  truths:
    - "Customer upload creates private VaultFile metadata tied to request/workspace."
    - "Upload service enforces request authorization server-side."
    - "Upload audit stores metadata summary only, never document contents."
  artifacts:
    - path: "src/lib/intake/upload-service.ts"
      provides: "attachIntakeFile upload service"
      exports: ["attachIntakeFile"]
    - path: "src/lib/intake/upload-service.test.ts"
      provides: "Upload authorization/private semantics tests"
  key_links:
    - from: "src/lib/intake/upload-service.ts"
      to: "src/lib/security/rbac.ts"
      via: "canAccessRequest"
      pattern: "canAccessRequest"
    - from: "src/lib/intake/upload-service.ts"
      to: "prisma.vaultFile"
      via: "private storageKey metadata write"
      pattern: "vaultFile.create"
    - from: "src/app/intake/actions.ts"
      to: "src/lib/intake/upload-service.ts"
      via: "Plan 02 attachIntakeFileAction imports attachIntakeFile"
      pattern: "attachIntakeFile"
---

<objective>
Create private intake upload service consumed by `/intake` server action.

Purpose: satisfy INT-04 and D-11 through D-13 with private storageKey semantics, authorization, and audit metadata only. Plan 02 wires customer UI/action to this service.
Output: `attachIntakeFile` service and tests.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-intake/02-CONTEXT.md
@.planning/phases/02-intake/02-RESEARCH.md
@prisma/schema.prisma
@src/lib/security/rbac.ts
@src/lib/audit/audit.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement private upload attachment service</name>
  <files>src/lib/intake/upload-service.ts, src/lib/intake/upload-service.test.ts</files>
  <read_first>
    - .planning/phases/02-intake/02-CONTEXT.md
    - .planning/phases/02-intake/02-RESEARCH.md
    - .planning/phases/02-intake/02-PATTERNS.md
    - .planning/phases/02-intake/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: authorized customer can attach file to own request and receives filename, size, vaultFileId only.
    - Test 2: service rejects attaching file to request outside accessible workspace.
    - Test 3: service creates VaultFile with private storageKey and no public URL.
    - Test 4: audit metadata includes filename/size/hash or summary only, no document contents.
  </behavior>
  <action>Create `attachIntakeFile({ session, requestId, file })` or equivalent existing session shape. Server loads request, calls existing RBAC request access check, derives workspaceId from request, writes test-safe local/mock private storageKey metadata and VaultFile row, records audit event. Return only vaultFileId, filename, size, contentType if needed, and storage-private flag; never return `storageKey` or public URL. Keep physical byte storage local/mock-compatible per resolved research; no S3/R2 hardening.</action>
  <acceptance_criteria>
    - Modified files named in <files> exist.
    - Commands listed in <verify> exit 0.
    - Implemented code contains exact functions, routes, models, and copy named in <action>.
  </acceptance_criteria>
  <verify>
    <automated>npx tsx src/lib/intake/upload-service.test.ts</automated>
  </verify>
  <done>Upload service proves private, authorized, audited file attachment to request; no public URL exists in return value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server action -> upload service | requestId and File metadata enter trusted service boundary |
| upload service -> storage/VaultFile | untrusted filename and bytes become private metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | attachIntakeFile | mitigate | Load request by id and derive workspaceId; reject unauthorized request. |
| T-02-03-02 | Information Disclosure | VaultFile return payload | mitigate | Return filename/size/id only; never storageKey/public URL. |
| T-02-03-03 | Repudiation | upload audit | mitigate | Record audit event with metadata summary only. |
</threat_model>

<verification>
Run `npx tsx src/lib/intake/upload-service.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
INT-04 backend upload path exists and Plan 02 can call it end-to-end from `/intake`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake/02-03-SUMMARY.md`
</output>
