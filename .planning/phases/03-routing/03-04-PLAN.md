---
phase: 03-routing
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/app/specialist/requests/page.tsx
  - src/app/specialist/requests/[requestId]/page.tsx
autonomous: true
requirements: [RTE-05]
must_haves:
  truths:
    - "Specialist sees only requests assigned to them."
    - "Specialist can open assigned request detail."
    - "Detail shows intake summary and supporting file metadata only."
  artifacts:
    - path: "src/app/specialist/requests/page.tsx"
      provides: "Assigned specialist queue"
    - path: "src/app/specialist/requests/[requestId]/page.tsx"
      provides: "Assigned request detail"
  key_links:
    - from: "src/app/specialist/requests/page.tsx"
      to: "LegalRequest.assignedSpecialistId"
      via: "server-side Prisma where clause"
      pattern: "assignedSpecialistId.*session.userId"
    - from: "src/app/specialist/requests/[requestId]/page.tsx"
      to: "canAccessRequest"
      via: "detail authorization"
      pattern: "canAccessRequest"
---

<objective>
Build specialist assigned queue and detail pages.

Purpose: RTE-05 needs server-authorized specialist work entry point after coordinator assignment.
Output: Specialist queue and request detail UI.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing/03-CONTEXT.md
@.planning/phases/03-routing/03-UI-SPEC.md
@.planning/phases/03-routing/03-PATTERNS.md
@.planning/phases/03-routing/03-VALIDATION.md
@src/app/admin/components/ui.tsx
@src/app/requests/[requestId]/page.tsx
@src/lib/security/rbac.ts
@src/lib/security/session.ts
@prisma/schema.prisma

<interfaces>
Existing access:
```ts
export async function canAccessRequest(session: AppSession | null | undefined, requestId: string): Promise<boolean>;
export async function requireAppSession(): Promise<AppSession>;
```
Existing request detail pattern:
```tsx
const session = await requireAppSession();
if (!(await canAccessRequest(session, requestId))) notFound();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create specialist assigned queue</name>
  <read_first>src/app/admin/components/ui.tsx, src/app/requests/[requestId]/page.tsx, src/lib/security/session.ts, prisma/schema.prisma, .planning/phases/03-routing/03-CONTEXT.md, .planning/phases/03-routing/03-UI-SPEC.md</read_first>
  <files>src/app/specialist/requests/page.tsx</files>
  <action>Following `03-PATTERNS.md` analog for `src/app/specialist/requests/page.tsx` (`src/app/admin/requests/page.tsx`), create server component page `/specialist/requests`. Require session server-side. Query `LegalRequest` with `where: { workspaceId: session.activeWorkspaceId, assignedSpecialistId: session.userId }` and include customer/user, intake matter type, status, createdAt. Use `PageHeader`, `Card`, `Table`, `Badge`, `Button` existing components. Title exact `Yêu cầu được giao`; description exact `Danh sách yêu cầu đã được điều phối cho bạn.`. Table columns exactly `Yêu cầu`, `Khách hàng`, `Loại vụ việc`, `Trạng thái`, `Ngày gửi`, `Hành động`. Action links to `/specialist/requests/${request.id}`. Do not show unassigned requests, reviewer assignments, document drafting, reviewer QC, final delivery, or vault download/preview controls.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File contains exact string `assignedSpecialistId: session.userId`.
    - File contains exact title `Yêu cầu được giao` and required table headers.
    - File does not query all requests then filter in browser.
    - File contains no document drafting or reviewer QC copy.
  </acceptance_criteria>
  <done>Specialist queue is server-side scoped to assigned requests only per D-11.</done>
</task>

<task type="auto">
  <name>Task 2: Create specialist request detail</name>
  <read_first>src/app/requests/[requestId]/page.tsx, src/app/admin/components/ui.tsx, src/lib/security/rbac.ts, src/lib/security/session.ts, prisma/schema.prisma, .planning/phases/03-routing/03-CONTEXT.md, .planning/phases/03-routing/03-UI-SPEC.md</read_first>
  <files>src/app/specialist/requests/[requestId]/page.tsx</files>
  <action>Following `03-PATTERNS.md` analog for `src/app/specialist/requests/[requestId]/page.tsx` (`src/app/requests/[requestId]/page.tsx`), create server component detail page `/specialist/requests/[requestId]`. Use `requireAppSession`, then `canAccessRequest(session, requestId)` and `notFound()` on false. Query request title/status/customer, intake submission `matterTypeKey`, `answerLabels`, `answers`, and vault file metadata `id`, `filename`, `storageKey`, `createdAt` if model supports it; if file size/type fields do not exist, show filename and createdAt only. Page title exact `Chi tiết yêu cầu`; include headings `Tóm tắt thông tin tiếp nhận` and `Tệp khách hàng đã gửi`. Render intake summary as Vietnamese label/value rows using `answerLabels` when present; do not expose raw JSON dump if labels exist. Show file metadata only; no download/preview links unless an existing authorized pattern already exists in read files. Do not add document drafting features per D-12 and deferred Phase 4.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File contains exact string `canAccessRequest(session, requestId)`.
    - File contains exact strings `Chi tiết yêu cầu`, `Tóm tắt thông tin tiếp nhận`, `Tệp khách hàng đã gửi`.
    - File does not contain strings `Tạo bản nháp`, `Review`, `Duyệt`, `Tải xuống`.
    - Detail page shows file metadata without new download or preview behavior.
  </acceptance_criteria>
  <done>Specialist detail shows authorized intake and file metadata only per D-12/RTE-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Specialist browser -> server page | Request ID in URL can be guessed. |
| Server page -> database | Reads sensitive intake and file metadata. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-13 | Information Disclosure | specialist queue | mitigate | Query includes `assignedSpecialistId: session.userId` and workspace scope server-side. |
| T-03-14 | Information Disclosure | specialist detail | mitigate | Call `canAccessRequest(session, requestId)` before reading/rendering details. |
| T-03-15 | Elevation of Privilege | route params | mitigate | Use `notFound()` on failed authorization; do not trust URL id. |
| T-03-16 | Information Disclosure | file metadata | accept | Metadata needed for work start; no download/preview links added in Phase 3. |
</threat_model>

<verification>
Run `npx tsc --noEmit`. Follow `.planning/phases/03-routing/03-VALIDATION.md`: typecheck after task, full test after wave. Manually verify specialist session sees only assigned queue if session fixture available.
</verification>

<success_criteria>
RTE-05 complete: specialist can see assigned queue and details with intake summary/supporting metadata, protected server-side.
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing/03-04-SUMMARY.md`.
</output>
