---
phase: 03-routing
plan: 05
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - data/seed.ts
  - package.json
autonomous: true
requirements: [RTE-01, RTE-02, RTE-03, RTE-04, RTE-05]
must_haves:
  truths:
    - "Database schema is pushed after Prisma schema changes."
    - "Seed data demonstrates at least one eligible specialist and reviewer for a seeded matter type."
    - "Phase routing passes typecheck and routing tests."
  artifacts:
    - path: "data/seed.ts"
      provides: "Seed routing capabilities"
    - path: "package.json"
      provides: "Optional seed script if missing"
  key_links:
    - from: "data/seed.ts"
      to: "RoutingCapability"
      via: "seeded specialist/reviewer capabilities"
      pattern: "routingCapability"
    - from: "prisma/schema.prisma"
      to: "database"
      via: "npx prisma db push"
      pattern: "db push"
---

<objective>
Finalize Phase 3 with schema push, seed capability data, and verification.

Purpose: Prisma schema changes must reach database; routing needs demo data proving suggestions can appear.
Output: DB pushed, seed updated, checks passing.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing/03-CONTEXT.md
@.planning/phases/03-routing/03-RESEARCH.md
@.planning/phases/03-routing/03-VALIDATION.md
@package.json
@data/seed.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed routing capabilities</name>
  <read_first>data/seed.ts, package.json, prisma/schema.prisma, src/lib/intake/catalog.ts, .planning/phases/03-routing/03-CONTEXT.md</read_first>
  <files>data/seed.ts, package.json</files>
  <action>Update existing seed flow to create or upsert at least one active `RoutingCapability` with `kind: 'specialist'` and one active `RoutingCapability` with `kind: 'reviewer'` for same seeded matter type such as `labor_contract` or `agency_contract`. Ensure seeded users have active workspace memberships with roles `specialist` and `reviewer`. Keep seed minimal and deterministic. If `package.json` lacks seed script and existing project uses `data/seed.ts`, add script `"seed": "tsx data/seed.ts"`. Do not add broad fake workload/SLA/AI/demo dashboards.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `data/seed.ts` contains exact string `routingCapability`.
    - Seed includes one `kind: 'specialist'` and one `kind: 'reviewer'` capability for same matter key.
    - Seed ensures active workspace memberships for specialist and reviewer users.
    - `package.json` script change only if needed for existing seed execution.
  </acceptance_criteria>
  <done>Seed data can produce visible specialist/reviewer suggestions per Claude discretion and RTE-03.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push Prisma schema and run verification</name>
  <read_first>prisma/schema.prisma, package.json, src/lib/routing/routing-service.test.ts, .planning/phases/03-routing/03-01-SUMMARY.md, .planning/phases/03-routing/03-02-SUMMARY.md, .planning/phases/03-routing/03-03-SUMMARY.md, .planning/phases/03-routing/03-04-SUMMARY.md</read_first>
  <files>prisma/schema.prisma, package.json, data/seed.ts</files>
  <action>[BLOCKING] After all schema file modifications from prior plans are complete, run `npx prisma db push`. If Prisma requires destructive-change confirmation in non-TTY, run `npx prisma db push --accept-data-loss` only after confirming generated prompt concerns expected Phase 3 schema delta. Then run `npx prisma generate`, `npx tsx src/lib/routing/routing-service.test.ts`, and `npx tsc --noEmit`. If DB connection is unavailable, stop and report blocker; do not mark phase verified. This task is mandatory before final verification.</action>
  <verify>
    <automated>npx prisma db push && npx prisma generate && npx tsx src/lib/routing/routing-service.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Command `npx prisma db push` completes successfully or explicit DB blocker is reported.
    - `npx prisma generate` completes successfully.
    - `npx tsx src/lib/routing/routing-service.test.ts` passes.
    - `npx tsc --noEmit` passes.
    - Phase not marked complete if schema push fails.
  </acceptance_criteria>
  <done>Live schema synced and Phase 3 verification commands pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Seed script -> database | Seed data can create users/capabilities affecting routing suggestions. |
| Prisma schema push -> database | Schema changes alter persistence and constraints. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-17 | Tampering | seed capabilities | mitigate | Use deterministic upserts and active membership role matching capability kind. |
| T-03-18 | Denial of Service | schema push | mitigate | Run required Prisma push after schema changes and before verification; stop on failure. |
| T-03-19 | Elevation of Privilege | seeded users/memberships | mitigate | Seed only intended specialist/reviewer roles; no extra super_admin privileges for routing demo. |
</threat_model>

<verification>
Required full check: `npx prisma db push && npx prisma generate && npx tsx src/lib/routing/routing-service.test.ts && npx tsc --noEmit`. Follow `.planning/phases/03-routing/03-VALIDATION.md` before `/gsd-verify-work`: full suite green.
</verification>

<success_criteria>
Prisma schema is live, seed data demonstrates routing suggestions, and Phase 3 tests/typecheck pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing/03-05-SUMMARY.md`.
</output>
