---
phase: 06-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/delivery/delivery-service.ts
  - src/lib/delivery/delivery-service.test.ts
autonomous: true
requirements:
  - DLV-01
  - DLV-03
must_haves:
  truths:
    - "Customer can load own request delivery data only through server-side session/RBAC."
    - "Customer sees only final document versions and related vault-file metadata."
    - "Customer never receives drafts, reviewer comments, generated content, input snapshots, internal notes, or storage keys."
  artifacts:
    - path: "src/lib/delivery/delivery-service.ts"
      provides: "Customer-safe final-document DTO service"
      exports: ["getCustomerDeliveryRequest"]
    - path: "src/lib/delivery/delivery-service.test.ts"
      provides: "Regression tests for final-only visibility and secrecy"
  key_links:
    - from: "src/lib/delivery/delivery-service.ts"
      to: "src/lib/security/rbac.ts"
      via: "canAccessRequest plus creator/workspace checks"
      pattern: "canAccessRequest"
    - from: "src/lib/delivery/delivery-service.ts"
      to: "DocumentVersion.status"
      via: "Prisma where status final"
      pattern: "status: 'final'"
---

<objective>
Build delivery service boundary that returns customer-safe final document data.

Purpose: satisfy DLV-01/DLV-03 and locked decisions D-02, D-04, D-05, D-06 before any UI/API consumes data.
Output: `getCustomerDeliveryRequest()` service plus tests proving final-only data and no sensitive fields.

Schema note: no `prisma/schema.prisma` modification in this plan; no schema push task needed.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-delivery/06-CONTEXT.md
@.planning/phases/06-delivery/06-RESEARCH.md
@.planning/phases/06-delivery/06-PATTERNS.md
@prisma/schema.prisma
@src/lib/security/rbac.ts
@src/lib/documents/vault-service.ts
@src/lib/documents/vault-service.test.ts

<interfaces>
Existing patterns executor must use:
```ts
export async function canAccessRequest(session: AppSession | null | undefined, requestId: string): Promise<boolean>;
export type AppSession = { userId: string; activeWorkspaceId: string; roles: Role[] };
```

Create this new contract:
```ts
export type CustomerDeliveryDocument = {
  documentId: string;
  documentTitle: string;
  documentVersionId: string;
  templateVersion: number;
  createdAt: Date;
  vaultFileId: string;
  filename: string | null;
  size: number | null;
  contentType: string | null;
};

export type CustomerDeliveryRequest = {
  id: string;
  title: string;
  status: RequestStatus;
  createdAt: Date;
  matterTypeKey: string | null;
  documents: CustomerDeliveryDocument[];
};

export async function getCustomerDeliveryRequest(session: AppSession, requestId: string): Promise<CustomerDeliveryRequest>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing service tests for customer final-document visibility</name>
  <files>src/lib/delivery/delivery-service.test.ts</files>
  <read_first>
    <file>.planning/phases/06-delivery/06-CONTEXT.md</file>
    <file>.planning/phases/06-delivery/06-PATTERNS.md</file>
    <file>src/lib/documents/vault-service.test.ts</file>
    <file>prisma/schema.prisma</file>
  </read_first>
  <behavior>
    - Own customer request with one `DocumentVersion.status = 'final'` and one draft returns exactly one final document.
    - Different customer in same/other workspace gets `FORBIDDEN` or not-found behavior; no data returned.
    - JSON string of result does not contain `storageKey`, `generatedContent`, `inputSnapshot`, `review`, `checklist`, `comment`.
  </behavior>
  <action>Create `src/lib/delivery/delivery-service.test.ts` using `node:test`, `assert`, Prisma seed/cleanup pattern from vault-service tests. Seed customer, workspace membership, request, document, final version, draft version, vault file linked to final version, and vault file linked to draft version. Tests must import `getCustomerDeliveryRequest` before implementation and initially fail because service file/function does not exist.</action>
  <verify>
    <automated>npm test -- src/lib/delivery/delivery-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains exact strings: `getCustomerDeliveryRequest`, `status: 'final'`, `storageKey`, `generatedContent`, `inputSnapshot`.
    - Running command fails before Task 2 for missing export or failing assertions.
  </acceptance_criteria>
  <done>Failing tests encode D-02/D-04/D-05/D-06 and DLV-01/DLV-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement customer-safe delivery DTO service</name>
  <files>src/lib/delivery/delivery-service.ts, src/lib/delivery/delivery-service.test.ts</files>
  <read_first>
    <file>src/lib/security/rbac.ts</file>
    <file>src/lib/documents/vault-service.ts</file>
    <file>src/lib/delivery/delivery-service.test.ts</file>
    <file>prisma/schema.prisma</file>
  </read_first>
  <action>Create `src/lib/delivery/delivery-service.ts`. Export `getCustomerDeliveryRequest(session, requestId)`. First call `canAccessRequest(session, requestId)`; if false throw `FORBIDDEN`. Query `LegalRequest` with `id`, `workspaceId: session.activeWorkspaceId`, `createdById: session.userId`. Select only request id/title/status/createdAt/matterTypeKey and documents with `DocumentVersion.status = 'final'`. For each final version, include only vault files where `documentVersionId` matches final version id; return fields in contract only. Do not select `storageKey`, `generatedContent`, `inputSnapshot`, reviews, checklist answers, reviewer comments, or internal notes. If request missing after RBAC/creator/workspace filters, throw `REQUEST_NOT_FOUND`.</action>
  <verify>
    <automated>npm test -- src/lib/delivery/delivery-service.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/delivery/delivery-service.ts` exports `getCustomerDeliveryRequest`.
    - Service query includes `createdById: session.userId` and `workspaceId: session.activeWorkspaceId`.
    - Service query includes `status: 'final'`.
    - Service file does not contain selected fields `storageKey: true`, `generatedContent: true`, `inputSnapshot: true`.
    - Tests pass.
  </acceptance_criteria>
  <done>Customer-safe backend DTO exists and tests prove final-only visibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Customer browser -> server service | Untrusted requestId/session context reaches final-document query. |
| Server service -> database | Sensitive document/review/vault data selected from Prisma. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | `getCustomerDeliveryRequest` | mitigate | Select whitelist fields only; never select `storageKey`, `generatedContent`, `inputSnapshot`, reviews, checklist comments. |
| T-06-01-02 | Elevation of Privilege | `getCustomerDeliveryRequest` | mitigate | Require `canAccessRequest`, `workspaceId: session.activeWorkspaceId`, and `createdById: session.userId`. |
| T-06-01-03 | Tampering | Test seed cleanup | mitigate | Use safe DATABASE_URL guard and cleanup pattern from `vault-service.test.ts`. |
</threat_model>

<verification>
Run `npm test -- src/lib/delivery/delivery-service.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
DLV-01 and DLV-03 covered. D-02/D-04/D-05/D-06 implemented server-side. No deferred ideas included.
</success_criteria>

<output>
After completion, create `.planning/phases/06-delivery/06-01-SUMMARY.md`.
</output>
