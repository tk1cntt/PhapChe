---
phase: 06-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/documents/vault-service.ts
  - src/app/api/vault/[vaultFileId]/download/route.ts
  - src/lib/delivery/delivery-service.test.ts
autonomous: true
requirements:
  - DLV-02
  - DLV-03
must_haves:
  truths:
    - "Customer can download final documents through 15-minute signed links."
    - "Download route rejects customer access to non-final or unrelated vault files."
    - "Download/access audit contains safe identifiers and expiry, never legal content or raw storage key."
  artifacts:
    - path: "src/app/api/vault/[vaultFileId]/download/route.ts"
      provides: "Server-side download route"
      exports: ["GET"]
    - path: "src/lib/documents/vault-service.ts"
      provides: "Hardened signed URL abstraction"
      exports: ["requestVaultFileAccess"]
  key_links:
    - from: "src/app/api/vault/[vaultFileId]/download/route.ts"
      to: "src/lib/documents/vault-service.ts"
      via: "requestVaultFileAccess"
      pattern: "requestVaultFileAccess"
    - from: "src/lib/documents/vault-service.ts"
      to: "src/lib/audit/audit.ts"
      via: "recordAuditEvent"
      pattern: "vault.access_requested"
---

<objective>
Harden final-document download path.

Purpose: satisfy DLV-02/DLV-03 and decisions D-07 through D-10 with server-side validation, 15-minute TTL, safe audit, and no raw `storageKey` exposure.
Output: signed download route plus hardened vault access service tests.

Schema note: no `prisma/schema.prisma` modification in this plan; no schema push task needed.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-delivery/06-CONTEXT.md
@.planning/phases/06-delivery/06-RESEARCH.md
@.planning/phases/06-delivery/06-PATTERNS.md
@.planning/phases/06-delivery/06-01-SUMMARY.md
@src/lib/documents/vault-service.ts
@src/lib/security/rbac.ts
@src/lib/audit/audit.ts
@prisma/schema.prisma

<interfaces>
Use contract from Plan 01:
```ts
export async function getCustomerDeliveryRequest(session: AppSession, requestId: string): Promise<CustomerDeliveryRequest>;
```

Route signature must use Next v15+ pattern:
```ts
export async function GET(
  request: Request,
  { params }: { params: Promise<{ vaultFileId: string }> },
) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add download access regression tests</name>
  <files>src/lib/delivery/delivery-service.test.ts</files>
  <read_first>
    <file>src/lib/delivery/delivery-service.test.ts</file>
    <file>src/lib/documents/vault-service.ts</file>
    <file>.planning/phases/06-delivery/06-CONTEXT.md</file>
  </read_first>
  <behavior>
    - `requestVaultFileAccess(customerSession, finalVaultFileId)` returns access URL with 15-minute expiry.
    - `requestVaultFileAccess(customerSession, draftVaultFileId)` throws `FORBIDDEN`.
    - Returned JSON never includes seeded raw `storageKey` value.
    - Audit metadata contains `vaultFileId=`, `requestId=`, `expiresAt=` and does not contain `storageKey` or generated legal content.
  </behavior>
  <action>Extend existing Phase 06 service test with download cases. Use same seed data from Plan 01. Test expiry delta is between 14 and 16 minutes from current time. Test customer cannot access draft-linked vault file. Test assigned specialist/coordinator can still request access only if existing RBAC allows and file is final; if broad internal access behavior is unclear, keep test focused on customer role per D-09.</action>
  <verify>
    <automated>npm test -- src/lib/delivery/delivery-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains exact strings: `requestVaultFileAccess`, `expiresAt`, `draftVaultFileId`, `vaultFileId=`, `requestId=`, `expiresAt=`.
    - Tests fail before service hardening because draft customer access is not blocked or audit lacks expiry.
  </acceptance_criteria>
  <done>Failing tests define signed download and secrecy behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden vault signed URL service and add download route</name>
  <files>src/lib/documents/vault-service.ts, src/app/api/vault/[vaultFileId]/download/route.ts</files>
  <read_first>
    <file>src/lib/documents/vault-service.ts</file>
    <file>src/lib/security/rbac.ts</file>
    <file>src/lib/audit/audit.ts</file>
    <file>src/lib/delivery/delivery-service.test.ts</file>
    <file>.planning/phases/06-delivery/06-RESEARCH.md</file>
  </read_first>
  <action>Modify `requestVaultFileAccess(session, vaultFileId, correlationId?)` per D-07/D-08/D-09/D-10. After `canAccessVaultFile`, query vault file with `id`, `requestId`, `workspaceId`, `filename`, `documentVersionId`, and linked `documentVersion.status`. For sessions containing `customer`, require `documentVersionId` exists, linked status is `'final'`, vault file request belongs to same active workspace, and request creator equals `session.userId`; otherwise throw `FORBIDDEN`. Keep TTL exactly `15 * 60 * 1000`. Return signed access metadata that never exposes raw `storageKey`: `{ accessUrl, expiresAt, filename, contentType }`. If production signer/storage provider is configured, `accessUrl` must be provider signed URL with 15-minute expiry. If no production signer exists, `accessUrl` must be same server-mediated route `/api/vault/${vaultFileId}/download?expires=${expiresAt.getTime()}` as local/dev fallback; fallback must still stream or return actual file bytes from server-side storage lookup and must not expose raw `storageKey`. Audit action `vault.access_requested` with metadata summary `vaultFileId=${vaultFileId}; requestId=${requestId}; expiresAt=${expiresAt.toISOString()}` only. Create route `src/app/api/vault/[vaultFileId]/download/route.ts` with `GET` Promise params, `requireAppSession()`, call `requestVaultFileAccess`, and then perform real download behavior: redirect to external provider signed URL when `accessUrl` points away from this same route; otherwise validate `expires`, reject expired links with 410 Vietnamese copy, and return server-mediated file response/stream from the configured local/dev storage boundary with `Content-Disposition: attachment; filename="..."`, `Content-Type`, and `Cache-Control: no-store`. JSON-only confirmation is forbidden. Route must never return raw `storageKey`, legal content in logs, or cacheable response. Errors `FORBIDDEN` return 404, unauthenticated returns 401, all with `no-store`.</action>
  <verify>
    <automated>npm test -- src/lib/delivery/delivery-service.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/documents/vault-service.ts` no longer contains `token=stub`.
    - Audit metadata includes `expiresAt=` and does not include selected `storageKey`.
    - Route exports `GET` with `{ params }: { params: Promise<{ vaultFileId: string }> }`.
    - Route responses set `Cache-Control: no-store`.
    - Route never returns JSON-only success/confirmation for valid download; valid download returns either `Response.redirect(...)` to provider signed URL or file response/stream with `Content-Disposition: attachment`.
    - Local/dev fallback is real server-mediated download behind signer/storage boundary and does not expose raw `storageKey`.
    - Tests and typecheck pass.
  </acceptance_criteria>
  <done>Final document download path validates access server-side, signs for 15 minutes, returns real file download or signed storage redirect, and audits safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/api/vault/[vaultFileId]/download` | Untrusted `vaultFileId` and query params. |
| Download route -> vault service | Session and file identifier checked before signed URL/response. |
| Vault service -> audit log | Sensitive legal file context reduced to safe identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Elevation of Privilege | Download route | mitigate | Require session, `canAccessVaultFile`, customer ownership/workspace, and final document linkage. |
| T-06-02-02 | Information Disclosure | `requestVaultFileAccess` | mitigate | Do not return or log raw `storageKey`; return server-mediated URL/provider signed URL and safe metadata only. |
| T-06-02-03 | Information Disclosure | Download response cache | mitigate | Set `Cache-Control: no-store`; TTL 15 minutes; expired response 410. |
| T-06-02-04 | Repudiation | Vault access audit | mitigate | Record actorId, vaultFileId, requestId, expiry timestamp via `recordAuditEvent`. |
</threat_model>

<verification>
Run `npm test -- src/lib/delivery/delivery-service.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
DLV-02/DLV-03 covered. D-07/D-08/D-09/D-10 implemented. No e-sign, download analytics, or notification preferences added.
</success_criteria>

<output>
After completion, create `.planning/phases/06-delivery/06-02-SUMMARY.md`.
</output>
