---
phase: 06-delivery
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/specialist/requests/[requestId]/actions.ts
  - src/app/specialist/requests/[requestId]/page.tsx
autonomous: true
requirements:
  - DLV-01
  - DLV-02
  - DLV-03
  - DLV-04
  - DLV-05
gap_closure: true
must_haves:
  truths:
    - "Internal deliver action returns visible success or failure state instead of swallowing errors."
    - "Internal close action returns visible validation, success, or failure state instead of swallowing errors."
    - "Specialist request page renders returned action feedback in Vietnamese near delivery/close controls."
    - "Delivery/close status changes still go through backend service and workflow state machine only."
  artifacts:
    - path: "src/app/specialist/requests/[requestId]/actions.ts"
      provides: "Server actions returning action result state"
      exports: ["markDeliveredAction", "closeDeliveredAction", "SpecialistRequestActionResult"]
    - path: "src/app/specialist/requests/[requestId]/page.tsx"
      provides: "Visible form feedback rendering for delivery/close actions"
  key_links:
    - from: "src/app/specialist/requests/[requestId]/page.tsx"
      to: "src/app/specialist/requests/[requestId]/actions.ts"
      via: "form action return/result state"
      pattern: "markDeliveredAction|closeDeliveredAction"
    - from: "src/app/specialist/requests/[requestId]/actions.ts"
      to: "src/lib/delivery/delivery-service.ts"
      via: "markRequestDelivered/closeDeliveredRequest service calls"
      pattern: "markRequestDelivered|closeDeliveredRequest"
---

<objective>
Close Phase 06 internal action-feedback gap.

Purpose: make deliver/close failures visible per verification gap while preserving backend workflow authority from D-15/D-18/D-19.
Output: server actions return structured result and specialist page displays feedback.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-delivery/06-CONTEXT.md
@.planning/phases/06-delivery/06-PATTERNS.md
@.planning/phases/06-delivery/06-VERIFICATION.md
@.planning/phases/06-delivery/06-UI-SPEC.md
@src/app/specialist/requests/[requestId]/actions.ts
@src/app/specialist/requests/[requestId]/page.tsx
@src/app/admin/routing/actions.ts
@src/app/admin/components/ui.tsx

<interfaces>
Current result type:
```ts
export type SpecialistRequestActionResult = {
  ok: boolean;
  message: string;
};
```
Required action signatures:
```ts
export async function markDeliveredAction(formData: FormData): Promise<SpecialistRequestActionResult>;
export async function closeDeliveredAction(formData: FormData): Promise<SpecialistRequestActionResult>;
```
Existing UI primitive:
```tsx
export function Card({ children, className = '' }: { children: ReactNode; className?: string })
export function Button({ children, variant = 'primary', type = 'button', disabled = false }: ButtonProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Return structured delivery and close action results</name>
  <files>src/app/specialist/requests/[requestId]/actions.ts</files>
  <read_first>
    <file>.planning/phases/06-delivery/06-VERIFICATION.md</file>
    <file>.planning/phases/06-delivery/06-PATTERNS.md</file>
    <file>src/app/specialist/requests/[requestId]/actions.ts</file>
    <file>src/app/admin/routing/actions.ts</file>
    <file>src/lib/delivery/delivery-service.ts</file>
  </read_first>
  <behavior>
    - `markDeliveredAction()` returns `{ ok: true, message: 'Đã đánh dấu hồ sơ là đã giao.' }` after successful service call and revalidation.
    - `markDeliveredAction()` returns `{ ok: false, message: 'Không thể giao hồ sơ. Vui lòng kiểm tra tài liệu cuối cùng và quyền xử lý.' }` on service/session error.
    - `closeDeliveredAction()` returns `{ ok: false, message: 'Nhập lý do đóng hồ sơ trước khi lưu.' }` when reason is blank.
    - `closeDeliveredAction()` returns `{ ok: true, message: 'Đã đóng hồ sơ.' }` after successful service call and revalidation.
    - `closeDeliveredAction()` returns `{ ok: false, message: 'Không thể đóng hồ sơ. Vui lòng kiểm tra trạng thái và quyền xử lý.' }` on service/session error.
  </behavior>
  <action>Modify `src/app/specialist/requests/[requestId]/actions.ts` surgically. Keep existing `SpecialistRequestActionResult`. Change `markDeliveredAction(formData: FormData): Promise<void>` to `Promise<SpecialistRequestActionResult>`. Return `deliverySuccessMessage` after `revalidatePath`, return `deliveryErrorMessage` in catch. Change `closeDeliveredAction(formData: FormData): Promise<void>` to `Promise<SpecialistRequestActionResult>`. Return `closeReasonMessage` when reason blank, `closeSuccessMessage` after `revalidatePath`, and `closeErrorMessage` in catch. Remove all `void deliverySuccessMessage`, `void deliveryErrorMessage`, `void closeReasonMessage`, `void closeSuccessMessage`, `void closeErrorMessage`. Do not add direct Prisma updates. Do not swallow errors without returning result; either return failure result or rethrow, but prefer return result because UI must display it.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Actions file contains `Promise<SpecialistRequestActionResult>` for both `markDeliveredAction` and `closeDeliveredAction`.
    - Actions file contains `return { ok: true, message: deliverySuccessMessage }` and `return { ok: false, message: deliveryErrorMessage }`.
    - Actions file contains `return { ok: false, message: closeReasonMessage }`, `return { ok: true, message: closeSuccessMessage }`, and `return { ok: false, message: closeErrorMessage }`.
    - Actions file contains no `void delivery` and no `void close` message discard patterns.
    - Actions file does not contain `prisma.legalRequest.update`.
    - Typecheck passes.
  </acceptance_criteria>
  <done>Server actions produce caller-visible result for every success, validation failure, and caught failure path.</done>
</task>

<task type="auto">
  <name>Task 2: Display delivery and close action feedback in specialist page</name>
  <files>src/app/specialist/requests/[requestId]/page.tsx</files>
  <read_first>
    <file>.planning/phases/06-delivery/06-UI-SPEC.md</file>
    <file>.planning/phases/06-delivery/06-PATTERNS.md</file>
    <file>src/app/specialist/requests/[requestId]/page.tsx</file>
    <file>src/app/specialist/requests/[requestId]/actions.ts</file>
    <file>src/app/admin/components/ui.tsx</file>
  </read_first>
  <action>Update specialist request detail page so action result messages are visible. Because current page is a server component using direct form actions, use minimal Next-safe approach: route actions should redirect back with short non-sensitive status query params, or page should use a small colocated client form component with `useActionState`. Choose smallest change that typechecks. Preferred if minimal: create/inline client component only for the `Giao và đóng hồ sơ` Card that calls `markDeliveredAction`/`closeDeliveredAction` with `useActionState`, renders message text after submit, and keeps same forms/copy. If using redirect query params instead, actions must encode only known status keys, not raw error strings. Feedback text must render inside `Giao và đóng hồ sơ` Card under relevant form as Vietnamese copy from action result. Success tone can use green/teal text; failure tone use destructive red. Keep D-15/D-19 backend authority: UI only displays message; actions still call delivery service. Do not create customer download confirmation or analytics.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Specialist UI renders returned action message or mapped status message near `Giao và đóng hồ sơ` controls.
    - Feedback text includes exact failure copy `Không thể giao hồ sơ. Vui lòng kiểm tra tài liệu cuối cùng và quyền xử lý.` and `Không thể đóng hồ sơ. Vui lòng kiểm tra trạng thái và quyền xử lý.` somewhere in page/component source or action-source mapping.
    - UI still contains `Giao cho khách hàng`, `Đóng hồ sơ`, and `Lý do đóng hồ sơ`.
    - Specialist page/component does not contain `prisma.legalRequest.update`.
    - Typecheck passes.
  </acceptance_criteria>
  <done>Specialist/coordinator sees clear success or failure feedback after delivery/close submissions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Specialist browser -> server action | Untrusted form data attempts status mutation. |
| Server action -> delivery service | Authenticated action requests backend workflow mutation. |
| Server action -> UI feedback | Error details are converted into customer-safe/internal-safe Vietnamese messages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | Deliver/close forms | mitigate | Actions still call `markRequestDelivered`/`closeDeliveredRequest`; no direct Prisma status mutation. |
| T-06-06-02 | Repudiation | Close action | mitigate | Blank reason blocked before service; nonblank reason passed to workflow transition audit. |
| T-06-06-03 | Information Disclosure | Error feedback | mitigate | Catch blocks return generic Vietnamese messages only; do not render raw exception, storageKey, legal content, or stack trace. |
| T-06-06-04 | Denial of Service | Invalid repeated submissions | accept | Existing backend state machine rejects invalid status; UI feedback shows failure without extra retry machinery. |
</threat_model>

<verification>
Run:
1. `npm run typecheck`

Static checks:
- `src/app/specialist/requests/[requestId]/actions.ts` must not contain `void delivery` or `void close` message discard patterns.
- Specialist page/component must render feedback text near `Giao và đóng hồ sơ`.
</verification>

<success_criteria>
Gap closed: internal deliver/close actions no longer silently discard failures; user sees validation/success/failure feedback while backend state machine remains sole authority.
</success_criteria>

<output>
After completion, create `.planning/phases/06-delivery/06-06-SUMMARY.md`.
</output>
