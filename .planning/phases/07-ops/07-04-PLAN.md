---
phase: 07-ops
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - src/lib/ops/ops-service.test.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - OPS-05
must_haves:
  truths:
    - All Phase 7 requirements OPS-01 through OPS-05 are covered by executable code paths.
    - High-severity ops security regressions are blocked by automated checks.
    - Phase 7 remains MVP-only with no schema edits, analytics dependencies, or deferred features.
  artifacts:
    - path: src/lib/ops/ops-service.test.ts
      provides: final source-level regression checks for Phase 7 requirements, UI links, and security controls
  key_links:
    - from: src/lib/ops/ops-service.test.ts
      to: src/lib/ops/ops-service.ts
      via: security and data-source invariants
      pattern: readFileSync.*ops-service.ts
    - from: src/lib/ops/ops-service.test.ts
      to: src/app/admin/ops/page.tsx and src/app/admin/ops/[requestId]/page.tsx
      via: route/link/copy invariants
      pattern: readFileSync.*ops/page.tsx
---

<objective>
Add final Phase 7 regression checks that lock OPS-01 through OPS-05 coverage, high-severity security controls, and out-of-scope boundaries after backend and UI plans are implemented.

Purpose: Ensure the phase is not merely compiling, but also protected against regressions in RBAC, server-side filtering, safe audit metadata, and MVP scope.
Output: Extended `src/lib/ops/ops-service.test.ts`; no production code changes unless a check reveals a Phase 7 implementation gap.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-ops/07-CONTEXT.md
@.planning/phases/07-ops/07-RESEARCH.md
@.planning/phases/07-ops/07-PATTERNS.md
@.planning/phases/07-ops/07-UI-SPEC.md
@src/lib/ops/ops-service.test.ts
@src/lib/ops/ops-service.ts
@src/app/admin/ops/page.tsx
@src/app/admin/ops/[requestId]/page.tsx
@src/app/admin/components/admin-shell.tsx
@package.json
@.planning/phases/07-ops/07-01-SUMMARY.md
@.planning/phases/07-ops/07-02-SUMMARY.md
@.planning/phases/07-ops/07-03-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend ops regression tests for requirement coverage and security controls</name>
  <files>src/lib/ops/ops-service.test.ts</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` OPS-01 through OPS-05.
    - `.planning/phases/07-ops/07-CONTEXT.md` decisions D-01 through D-15 and deferred ideas.
    - `.planning/phases/07-ops/07-UI-SPEC.md` required copy and exclusions.
    - `src/lib/ops/ops-service.test.ts` from Plan 07-01.
    - `src/lib/ops/ops-service.ts` from Plan 07-01.
    - `src/app/admin/ops/page.tsx` from Plan 07-02.
    - `src/app/admin/ops/[requestId]/page.tsx` from Plan 07-03.
  </read_first>
  <behavior>
    - Test fails if any OPS-01..OPS-05 code path or UI link disappears.
    - Test fails if high-severity RBAC, server-side filtering, safe timeline metadata, or persisted metric source controls disappear.
    - Test fails if deferred Phase 7 features appear in ops sources.
  </behavior>
  <action>Extend `src/lib/ops/ops-service.test.ts` to read the ops service and both ops page sources. Add explicit assertion messages mentioning OPS-01, OPS-02, OPS-03, OPS-04, and OPS-05. Assert OPS-01 tokens for status/specialist/reviewer/aging counts; OPS-02 tokens for server-side filters workspace/customer, matter type, status, assignee, reviewer, date from/to; OPS-03 tokens for workload per specialist/reviewer; OPS-04 tokens for SLA/current-status timestamps from `WorkflowTransition.createdAt`; OPS-05 tokens for timeline from `AuditEvent` + `WorkflowTransition`, `correlationId`, `reason`, and `metadataSummary`. Add page assertions that `/admin/ops` uses `getOpsDashboard` and timeline links, and `/admin/ops/[requestId]` uses `getOpsRequestTimeline`. Add negative assertions for deferred/out-of-scope features: charting, CSV/PDF export, saved views, fuzzy search, employee scoring/capacity scoring, advanced SLA policy engine/escalation, raw legal content, raw storage keys, and schema push requirement.</action>
  <acceptance_criteria>
    - Test source includes readable assertion messages mentioning `OPS-01`, `OPS-02`, `OPS-03`, `OPS-04`, and `OPS-05`.
    - Test fails if either ops page stops using the service layer.
    - Test fails if sensitive field names or deferred feature keywords appear in Phase 7 ops sources.
    - Test does not require a live database.
  </acceptance_criteria>
  <verify><automated>cd /mnt/d/PhapChe/.claude/worktrees/agent-af38988ba6a4b99e7 && npx tsx src/lib/ops/ops-service.test.ts && grep -n "OPS-01\|OPS-02\|OPS-03\|OPS-04\|OPS-05" src/lib/ops/ops-service.test.ts</automated></verify>
  <done>Ops regression tests prove all Phase 7 requirements and security controls are represented by automated checks.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 7 CLI verification suite</name>
  <files>src/lib/ops/ops-service.test.ts, src/lib/ops/ops-service.ts, src/app/admin/ops/page.tsx, src/app/admin/ops/[requestId]/page.tsx, src/app/admin/components/admin-shell.tsx</files>
  <read_first>
    - `package.json` scripts.
    - `.planning/phases/07-ops/07-UI-SPEC.md` required labels.
    - `src/lib/ops/ops-service.test.ts` after Task 1.
    - `src/lib/ops/ops-service.ts`.
    - `src/app/admin/ops/page.tsx`.
    - `src/app/admin/ops/[requestId]/page.tsx`.
    - `src/app/admin/components/admin-shell.tsx`.
  </read_first>
  <action>Run final checks and fix only Phase 7 files if a check fails: `npx tsx src/lib/ops/ops-service.test.ts`, `npm run typecheck`, grep for required UI labels and route links, and grep absence of sensitive fields/deferred features. Do not edit `prisma/schema.prisma`, migrations, `package.json`, dependency lock files, or unrelated files. Do not run `npx prisma db push` because Phase 7 plans do not modify schema.</action>
  <acceptance_criteria>
    - Ops service regression test passes.
    - Typecheck passes.
    - UI labels `Vận hành`, `Tổng quan vận hành`, `Bộ lọc hồ sơ`, `Danh sách hồ sơ`, `Workload chuyên viên và reviewer`, and `Timeline audit` are present.
    - Sensitive fields and deferred feature keywords are absent from `src/lib/ops` and `src/app/admin/ops`.
    - No schema push or migration is performed.
  </acceptance_criteria>
  <verify><automated>cd /mnt/d/PhapChe/.claude/worktrees/agent-af38988ba6a4b99e7 && npx tsx src/lib/ops/ops-service.test.ts && npm run typecheck && grep -R "Vận hành\|Tổng quan vận hành\|Bộ lọc hồ sơ\|Danh sách hồ sơ\|Workload chuyên viên và reviewer\|Timeline audit" src/app/admin/ops src/app/admin/components/admin-shell.tsx && ! grep -R "generatedContent\|generalComment\|storageKey\|rawAnswer\|fileContent\|rawContent\|chart\|CSV\|PDF\|saved view\|fuzzy\|scoring\|capacity\|escalation" src/lib/ops src/app/admin/ops</automated></verify>
  <done>Final Phase 7 automated checks pass without schema changes, new dependencies, or deferred features.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Verification tests -> production source | Tests encode required security invariants and must fail on unsafe source changes. |
| Final CLI checks -> release confidence | Executor relies on automated checks to decide Phase 7 readiness. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-04-01 | Information Disclosure | final regression test | mitigate | Add assertions that fail if ops service/page imports or renders sensitive field names: `generatedContent`, `generalComment`, `storageKey`, raw answer/content fields, or object dumps. |
| T-07-04-02 | Tampering | query param/filter regression | mitigate | Add assertions for parser/allowlist/date/status handling and shared filter builder tokens so query param tampering cannot silently degrade. |
| T-07-04-03 | Integrity | metrics consistency regression | mitigate | Assert dashboard/page uses `getOpsDashboard` and service uses persisted Prisma count/groupBy/workflow/audit sources; no fixture metrics. |
| T-07-04-04 | Repudiation | timeline/audit traceability | mitigate | Assert timeline page links to `getOpsRequestTimeline` and displays correlation ID/reason/metadataSummary safe fields. |
</threat_model>

<verification>
Run:
- `cd /mnt/d/PhapChe/.claude/worktrees/agent-af38988ba6a4b99e7 && npx tsx src/lib/ops/ops-service.test.ts`
- `cd /mnt/d/PhapChe/.claude/worktrees/agent-af38988ba6a4b99e7 && npm run typecheck`
- `cd /mnt/d/PhapChe/.claude/worktrees/agent-af38988ba6a4b99e7 && grep -R "OPS-01\|OPS-02\|OPS-03\|OPS-04\|OPS-05" src/lib/ops/ops-service.test.ts`
- `cd /mnt/d/PhapChe/.claude/worktrees/agent-af38988ba6a4b99e7 && ! grep -R "generatedContent\|generalComment\|storageKey\|rawAnswer\|fileContent\|chart\|CSV\|PDF\|saved view\|fuzzy\|scoring" src/lib/ops src/app/admin/ops`
</verification>

<success_criteria>
Phase 7 is ready when the ops regression test and TypeScript check pass, all required Vietnamese UI labels/routes are present, and automated negative checks confirm no sensitive timeline fields or deferred analytics features were introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ops/07-04-SUMMARY.md`.
</output>
