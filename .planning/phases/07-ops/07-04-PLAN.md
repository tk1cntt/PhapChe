---
phase: 07-ops
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified:
  - src/lib/ops/ops-service.test.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
  - OPS-05
must_haves:
  truths:
    - All Phase 7 requirements OPS-01 through OPS-05 are covered by executable code paths.
    - High-severity ops security regressions are blocked by automated checks.
    - Phase 7 remains MVP-only: no schema edits, no analytics dependencies, no deferred features.
  artifacts:
    - path: src/lib/ops/ops-service.test.ts
      provides: final source-level regression checks for Phase 7 requirements and security controls
  key_links:
    - from: src/lib/ops/ops-service.test.ts
      to: src/lib/ops/ops-service.ts
      via: security and data-source invariants
    - from: src/lib/ops/ops-service.test.ts
      to: src/app/admin/ops/page.tsx and src/app/admin/ops/[requestId]/page.tsx
      via: UI route/link/copy invariants
---

<objective>
Bổ sung final verification plan cho Phase 7 để khóa coverage OPS-01..OPS-05, threat controls, và out-of-scope boundaries sau khi backend và UI đã có.

Purpose: Đảm bảo phase không chỉ compile mà còn có đường dẫn kiểm chứng grep/test/CLI cho metric, filter, workload, SLA, timeline và security.
Output: Mở rộng `src/lib/ops/ops-service.test.ts` hoặc thêm assertions trong cùng file test hiện có; không sửa code production trừ khi test phát hiện thiếu sót trong kế hoạch trước.
</objective>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Verification tests -> production source | Tests encode required security invariants and must fail on unsafe source changes. |
| Final CLI checks -> release confidence | Human/Claude relies on automated checks to decide Phase 7 readiness. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-04-01 | Information Disclosure | final regression test | High | mitigate/block | Add assertions that fail if ops service/page imports or renders sensitive field names: `generatedContent`, `generalComment`, `storageKey`, raw answer/content fields, object dumps. |
| T-07-04-02 | Tampering | query param/filter regression | High | mitigate/block | Add assertions for parser/allowlist/date/status handling and shared filter builder tokens so query param tampering cannot silently degrade. |
| T-07-04-03 | Integrity | metrics consistency regression | High | mitigate/block | Assert dashboard/page uses `getOpsDashboard` and service uses persisted Prisma count/groupBy/workflow/audit sources; no fixture metrics. |
| T-07-04-04 | Repudiation | timeline/audit traceability | Medium | mitigate | Assert timeline page links to `getOpsRequestTimeline` and displays correlation ID/reason/metadataSummary safe fields. |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend ops regression tests for requirement coverage and security controls</name>
  <files>
    - src/lib/ops/ops-service.test.ts
  </files>
  <read_first>
    - `/mnt/d/PhapChe/.planning/REQUIREMENTS.md` OPS-01 through OPS-05.
    - `/mnt/d/PhapChe/.planning/phases/07-ops/07-CONTEXT.md` decisions and deferred ideas.
    - `/mnt/d/PhapChe/src/lib/ops/ops-service.test.ts` from Plan 07-01.
    - `/mnt/d/PhapChe/src/app/admin/ops/page.tsx` from Plan 07-02.
    - `/mnt/d/PhapChe/src/app/admin/ops/[requestId]/page.tsx` from Plan 07-03.
  </read_first>
  <action>Extend `src/lib/ops/ops-service.test.ts` to read the ops service and both ops page sources. Add explicit assertions mapping OPS-01..OPS-05: status/specialist/reviewer/aging counts, server-side filters for workspace/matter/status/assignee/reviewer/date, workload per specialist/reviewer, SLA timestamps from workflow transitions, and request timeline from audit/workflow. Add negative assertions for deferred/out-of-scope features: charting, CSV/PDF export, saved views, fuzzy search, employee scoring, advanced SLA policy engine, schema migration requirement.</action>
  <acceptance_criteria>
    - Test file includes readable assertion messages mentioning OPS-01, OPS-02, OPS-03, OPS-04, OPS-05.
    - Test fails if either ops page stops using the service layer.
    - Test fails if sensitive field names or deferred features appear in Phase 7 ops sources.
  </acceptance_criteria>
  <done>
    - `npx tsx src/lib/ops/ops-service.test.ts` exits 0.
    - `grep -n "OPS-01\|OPS-02\|OPS-03\|OPS-04\|OPS-05" src/lib/ops/ops-service.test.ts` returns matches.
  </done>
  <verify><automated>cd /mnt/d/PhapChe && npx tsx src/lib/ops/ops-service.test.ts && grep -n "OPS-01\|OPS-02\|OPS-03\|OPS-04\|OPS-05" src/lib/ops/ops-service.test.ts</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 7 CLI verification suite</name>
  <files>
    - src/lib/ops/ops-service.test.ts
    - src/lib/ops/ops-service.ts
    - src/app/admin/ops/page.tsx
    - src/app/admin/ops/[requestId]/page.tsx
    - src/app/admin/components/admin-shell.tsx
  </files>
  <read_first>
    - `/mnt/d/PhapChe/package.json` scripts.
    - `/mnt/d/PhapChe/.planning/phases/07-ops/07-UI-SPEC.md` required copy labels.
  </read_first>
  <action>Run final checks and fix only Phase 7 files if a check fails: `npx tsx src/lib/ops/ops-service.test.ts`, `npm run typecheck`, grep for UI labels and route links, grep absence of sensitive fields/deferred features. Do not edit schema, package dependencies, or unrelated files.</action>
  <acceptance_criteria>
    - Ops service test passes.
    - Typecheck passes.
    - UI labels `Vận hành`, `Tổng quan vận hành`, `Bộ lọc hồ sơ`, `Danh sách hồ sơ`, `Workload chuyên viên và reviewer`, `Timeline audit` are present.
    - Sensitive fields and deferred feature keywords are absent from ops sources.
  </acceptance_criteria>
  <done>
    - `npx tsx src/lib/ops/ops-service.test.ts` exits 0.
    - `npm run typecheck` exits 0.
    - Required Vietnamese UI label grep returns matches.
    - Sensitive/deferred keyword grep returns no matches in ops sources.
  </done>
  <verify><automated>cd /mnt/d/PhapChe && npx tsx src/lib/ops/ops-service.test.ts && npm run typecheck && grep -R "Vận hành\|Tổng quan vận hành\|Bộ lọc hồ sơ\|Danh sách hồ sơ\|Workload chuyên viên và reviewer\|Timeline audit" src/app/admin/ops src/app/admin/components/admin-shell.tsx && ! grep -R "generatedContent\|generalComment\|storageKey\|rawAnswer\|fileContent\|chart\|CSV\|PDF\|saved view\|fuzzy\|scoring" src/lib/ops src/app/admin/ops</automated></verify>
</task>
</tasks>

<verification>
- `cd /mnt/d/PhapChe && npx tsx src/lib/ops/ops-service.test.ts`
- `cd /mnt/d/PhapChe && npm run typecheck`
- `cd /mnt/d/PhapChe && grep -R "OPS-01\|OPS-02\|OPS-03\|OPS-04\|OPS-05" src/lib/ops/ops-service.test.ts`
- `cd /mnt/d/PhapChe && ! grep -R "generatedContent\|generalComment\|storageKey\|rawAnswer\|fileContent\|chart\|CSV\|PDF\|saved view\|fuzzy\|scoring" src/lib/ops src/app/admin/ops`
</verification>

<must_haves>
- Final checks prove OPS-01..OPS-05 are covered.
- High severity unauthorized data disclosure, sensitive legal content leakage, query param tampering, and metrics inconsistency are represented by failing tests or grep checks.
- No schema edit, no `npx prisma db push`, no new dependencies, and no deferred Phase 7 ideas.
</must_haves>

<success_criteria>
Phase 7 is ready when the ops regression test and TypeScript check pass, all required Vietnamese UI labels/routes are present, and automated negative checks confirm no sensitive timeline fields or deferred analytics features were introduced.
</success_criteria>
