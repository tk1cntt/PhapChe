---
phase: 14-antd-layout-redesign
plan: 05
type: execute
wave: 2
depends_on:
  - 14-01-PLAN.md
files_modified:
  - src/app/specialist/layout.tsx
  - src/app/specialist/requests/page.tsx
  - src/app/specialist/requests/[requestId]/page.tsx
  - src/app/specialist/requests/[requestId]/components/document-versions.tsx
  - src/app/specialist/requests/[requestId]/components/delivery-actions.tsx
  - src/app/specialist/requests/[requestId]/components/generate-draft-form.tsx
  - src/app/specialist/requests/[requestId]/components/vault-files.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Specialist route group has an antd Layout with compact Sider for case queue focus"
    - "Specialist list page and detail page use antd components (Tag, Card, Table, Typography)"
    - "All 4 specialist sub-components (document-versions, delivery-actions, generate-draft-form, vault-files) use antd Tag/Button instead of custom ui.tsx imports"
  artifacts:
    - path: "src/app/specialist/layout.tsx"
      provides: "Specialist layout with antd Layout+Sider+Menu+Breadcrumb"
      min_lines: 40
    - path: "src/app/specialist/requests/page.tsx"
      provides: "Specialist queue page with antd components"
      contains: "from 'antd'"
    - path: "src/app/specialist/requests/[requestId]/page.tsx"
      provides: "Specialist detail page with antd components"
      contains: "from 'antd'"
  key_links:
    - from: "specialist layout"
      to: "src/lib/navigation/breadcrumb-labels.ts"
      via: "import getBreadcrumbItems"
      pattern: "breadcrumb-labels"
---

<objective>
Create specialist route group layout and migrate all specialist pages and sub-components to antd.

Purpose: The specialist role gets its own route group layout (compact sidebar focused on case queue). All specialist-facing pages and their sub-components use antd components instead of the old admin ui.tsx imports.

Output:
- src/app/specialist/layout.tsx (NEW)
- 6 files modified with antd imports
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-antd-layout-redesign/14-CONTEXT.md
@.planning/phases/14-antd-layout-redesign/14-UI-SPEC.md
@.planning/phases/14-antd-layout-redesign/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create specialist route group layout with antd Layout + Sider + Menu + Breadcrumb</name>
  <files>src/app/specialist/layout.tsx</files>
  <read_first>
    src/app/specialist/requests/page.tsx (existing, to understand current wrapper)
    14-UI-SPEC.md (layout architecture section — specialist: compact sidebar, minimal header)
    src/lib/navigation/breadcrumb-labels.ts
  </read_first>
  <action>
    Create `src/app/specialist/layout.tsx` — a client component:

    ```
    'use client';
    import Link from 'next/link';
    import { usePathname } from 'next/navigation';
    import { Layout, Menu, Breadcrumb, Typography } from 'antd';
    import { FileTextOutlined, OrderedListOutlined } from '@ant-design/icons';
    import { getBreadcrumbItems } from '@/lib/navigation/breadcrumb-labels';

    const { Sider, Content, Header } = Layout;

    const navItems = [
      { key: '/specialist/requests', icon: <OrderedListOutlined />, label: <Link href="/specialist/requests">Hang cho</Link> },
    ];

    export default function SpecialistLayout({ children }: { children: React.ReactNode }) {
      const pathname = usePathname();
      const breadcrumbItems = getBreadcrumbItems(pathname);
      // Match any /specialist/requests prefix for selected key
      const selectedKey = pathname.startsWith('/specialist/requests') ? '/specialist/requests' : pathname;

      return (
        <Layout style={{ minHeight: '100vh' }}>
          <Sider
            breakpoint="lg"
            collapsedWidth={64}
            theme="light"
            width={200}
            style={{ borderRight: '1px solid #E2E8F0' }}
          >
            <div style={{ padding: '16px', textAlign: 'center' }}>
              <Typography.Text strong style={{ color: '#0F766E', fontSize: 14 }}>
                GitNexus Legal
              </Typography.Text>
              <Typography.Paragraph style={{ fontSize: 12, color: '#475569', margin: 0 }}>
                Chuyen vien
              </Typography.Paragraph>
            </div>
            <Menu
              mode="inline"
              selectedKeys={[selectedKey]}
              items={navItems}
              style={{ borderRight: 'none' }}
            />
          </Sider>
          <Layout>
            <Header
              style={{
                background: '#FFFFFF',
                borderBottom: '1px solid #E2E8F0',
                padding: '0 24px',
                display: 'flex',
                alignItems: 'center',
                height: 48,
              }}
            >
              <Breadcrumb items={breadcrumbItems} />
            </Header>
            <Content
              style={{
                padding: 24,
                background: '#F8FAFC',
                minHeight: 'calc(100vh - 48px)',
              }}
            >
              {children}
            </Content>
          </Layout>
        </Layout>
      );
    }
    ```

    Key differences from admin layout:
    - Sider width=200 (narrower than admin's default)
    - Only 1 nav item (queue), with prefix matching so detail pages also show it as selected
    - Subtitle "Chuyen vien" under brand
    - Same breadcrumb and content styling
  </action>
  <verify>
    <automated>grep -q "Layout.*Sider\|Sider" src/app/specialist/layout.tsx && grep -q "getBreadcrumbItems" src/app/specialist/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Specialist layout created with antd Layout + Sider + Menu + Breadcrumb
    - Nav items focus on case queue navigation
    - Breadcrumb uses centralized config
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate specialist pages and sub-components to antd</name>
  <files>
    src/app/specialist/requests/page.tsx
    src/app/specialist/requests/[requestId]/page.tsx
    src/app/specialist/requests/[requestId]/components/document-versions.tsx
    src/app/specialist/requests/[requestId]/components/delivery-actions.tsx
    src/app/specialist/requests/[requestId]/components/generate-draft-form.tsx
    src/app/specialist/requests/[requestId]/components/vault-files.tsx
  </files>
  <read_first>
    src/app/specialist/requests/page.tsx
    src/app/specialist/requests/[requestId]/page.tsx
    src/app/specialist/requests/[requestId]/components/document-versions.tsx
    src/app/specialist/requests/[requestId]/components/delivery-actions.tsx
    src/app/specialist/requests/[requestId]/components/generate-draft-form.tsx
    src/app/specialist/requests/[requestId]/components/vault-files.tsx
    14-UI-SPEC.md (for tone-to-color mapping and component replacements)
  </read_first>
  <action>
    Migrate all 6 files:

    **src/app/specialist/requests/page.tsx:**
    - Remove: `import { Badge, Button, Card, PageHeader, Table } from '@/app/admin/components/ui';`
    - Add: `import { Tag, Button, Card, Table, Typography, Flex } from 'antd';`
    - Remove the `<main>` wrapper (the route group layout provides the shell). The component now returns its content directly.
    - Replace `<PageHeader>` with Typography.Title + Paragraph in Flex
    - Replace `<Badge>` with `<Tag>` (tone mapping per spec)
    - Replace `<Button variant="secondary">` with `<Button>`
    - Replace `<Card>` with antd Card
    - Replace custom `<Table headers={[...]}>` with antd `<Table dataSource={...} columns={[...]} pagination={false} />`
    - Keep Link from next/link

    **src/app/specialist/requests/[requestId]/page.tsx:**
    - Remove: `import { Badge, Card, PageHeader } from '@/app/admin/components/ui';`
    - Add: `import { Tag, Card, Typography, Flex, Space, Descriptions } from 'antd';`
    - Remove the `<main>` wrapper
    - Replace `<PageHeader>` with Typography.Title + Paragraph in Flex
    - Replace `<Badge>` with `<Tag>` (status tone mapping)
    - Replace `<Card>` with antd Card
    - The info card with customer info and matter type can use antd Descriptions, or keep as styled divs. Since the original used styled divs (not ui.tsx components), keep them as styled divs — only replace `<Card>` wrappers. However, the inner styled divs with `rounded-xl border border-[#E2E8F0]` should be kept since they are custom Tailwind, not from ui.tsx.
    - **IMPORTANT:** The sub-components (document-versions, delivery-actions, etc.) still import from ui.tsx — they will be fixed in their own files below. This page's typecast of `import('@/lib/documents/template-service').TemplateVariable` should be kept as-is.

    **src/app/specialist/requests/[requestId]/components/document-versions.tsx:**
    - Remove: `import { Badge } from '@/app/admin/components/ui';`
    - Add: `import { Tag } from 'antd';`
    - Replace each `<Badge tone={statusInfo.tone}>` with `<Tag color={mapToneToColor(statusInfo.tone)}>`
    - tone-to-color mapping: neutral=default, info=blue, warning=orange, accent=cyan
    - Keep all other styling and logic intact — only replace the Badge import

    **src/app/specialist/requests/[requestId]/components/delivery-actions.tsx:**
    - Remove: `import { Badge, Button } from '@/app/admin/components/ui';`
    - Add: `import { Tag, Button } from 'antd';`
    - Replace `<Badge tone={result.ok ? 'accent' : 'destructive'}>` with `<Tag color={result.ok ? 'cyan' : 'red'}>`
    - Replace `<Button>` with antd `<Button>` (type="primary" for primary, no extra props for the plain button)
    - Replace `<Button type="submit">` with `<Button type="primary" htmlType="submit">`

    **src/app/specialist/requests/[requestId]/components/generate-draft-form.tsx:**
    - Remove: `import { Button } from '@/app/admin/components/ui';`
    - Add: `import { Button } from 'antd';`
    - Replace `<Button type="submit" disabled={isSubmitting}>` with `<Button type="primary" htmlType="submit" loading={isSubmitting}>`
    - Keep all form logic, state management, and styling intact

    **src/app/specialist/requests/[requestId]/components/vault-files.tsx:**
    - Remove: `import { Badge } from '@/app/admin/components/ui';`
    - Add: `import { Tag } from 'antd';`
    - Replace `<Badge tone={fileKindTone(file.fileKind)}>` with `<Tag color={mapToneToColor(fileKindTone(file.fileKind))}>`
    - tone mapping: info=blue, warning=orange, neutral=default

    **NOTE on styled buttons and elements:** The document-versions.tsx has several `<button>` elements (not `<Button>` from ui.tsx) with inline classes like `inline-flex min-h-10 items-center...`. These are plain HTML button elements with Tailwind classes — they are NOT from ui.tsx. Keep them as-is. Only replace `<Badge>` (which was imported from ui.tsx).

    **Verify:** `npm run typecheck`
  </action>
  <verify>
    <automated>grep -q "from 'antd'" src/app/specialist/requests/page.tsx && grep -q "from 'antd'" src/app/specialist/requests/\[requestId\]/page.tsx && grep -q "from 'antd'" src/app/specialist/requests/\[requestId\]/components/document-versions.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 6 specialist files import from antd instead of admin/components/ui
    - Specialist list and detail pages use antd Tag, Card, Table, Typography
    - Sub-components use antd Tag and Button
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → specialist pages | Specialist views own request queue and details |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | I | antd Tag with status labels | accept | Status labels are hardcoded, no user input in Tag |
</threat_model>

<verification>
- [ ] Specialist layout renders with compact Sider
- [ ] Specialist queue page shows table with antd Table
- [ ] Specialist detail page renders cards and tags
- [ ] Sub-components render correctly (document versions, delivery actions, etc.)
</verification>

<success_criteria>
- Specialist layout created
- All 6 specialist files migrated to antd
- Typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-antd-layout-redesign/14-05-SUMMARY.md`
</output>
