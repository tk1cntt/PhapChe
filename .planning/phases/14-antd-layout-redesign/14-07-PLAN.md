---
phase: 14-antd-layout-redesign
plan: 07
type: execute
wave: 2
depends_on:
  - 14-01-PLAN.md
files_modified:
  - src/app/reviewer/layout.tsx
  - src/app/reviewer/requests/page.tsx
  - src/app/reviewer/requests/[requestId]/review/[documentVersionId]/page.tsx
  - src/app/reviewer/requests/[requestId]/review/[documentVersionId]/components/start-review-button.tsx
  - src/app/reviewer/requests/[requestId]/review/[documentVersionId]/components/review-form.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Reviewer route group has antd Layout with minimal sidebar for queue nav"
    - "Reviewer queue page uses antd Tag, Button, Card, Table, Typography"
    - "Reviewer detail page uses antd Button, Card, Typography"
    - "Both reviewer sub-components (start-review-button, review-form) use antd Button, Card, Tag"
  artifacts:
    - path: "src/app/reviewer/layout.tsx"
      provides: "Reviewer layout with antd Layout+Sider+Menu+Breadcrumb"
      min_lines: 40
    - path: "src/app/reviewer/requests/page.tsx"
      provides: "Reviewer queue page with antd components"
      contains: "from 'antd'"
    - path: "src/app/reviewer/requests/[requestId]/review/[documentVersionId]/page.tsx"
      provides: "Reviewer detail page with antd components"
      contains: "from 'antd'"
  key_links:
    - from: "reviewer layout"
      to: "src/lib/navigation/breadcrumb-labels.ts"
      via: "import getBreadcrumbItems"
      pattern: "breadcrumb-labels"
---

<objective>
Create reviewer route group layout and migrate all reviewer pages and sub-components to antd.

Purpose: The reviewer role gets a minimal layout with sidebar focused on review queue navigation. All reviewer pages and their sub-components use antd components instead of the old admin ui.tsx imports.

Output:
- src/app/reviewer/layout.tsx (NEW)
- 4 files modified with antd imports
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-antd-layout-redesign/14-CONTEXT.md
@.planning/phases/14-antd-layout-redesign/14-UI-SPEC.md
@.planning/phases/14-antd-layout-redesign/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reviewer route group layout with antd Layout + Sider + Menu + Breadcrumb</name>
  <files>src/app/reviewer/layout.tsx</files>
  <read_first>
    src/app/reviewer/requests/page.tsx
    14-UI-SPEC.md (layout architecture: reviewer minimal sidebar, minimal chrome)
    src/lib/navigation/breadcrumb-labels.ts
  </read_first>
  <action>
    Create `src/app/reviewer/layout.tsx` — a client component with minimal sidebar focused on queue:

    ```
    'use client';
    import Link from 'next/link';
    import { usePathname } from 'next/navigation';
    import { Layout, Menu, Breadcrumb, Typography } from 'antd';
    import { OrderedListOutlined } from '@ant-design/icons';
    import { getBreadcrumbItems } from '@/lib/navigation/breadcrumb-labels';

    const { Sider, Content, Header } = Layout;

    const navItems = [
      { key: '/reviewer/requests', icon: <OrderedListOutlined />, label: <Link href="/reviewer/requests">Hang cho duyet</Link> },
    ];

    export default function ReviewerLayout({ children }: { children: React.ReactNode }) {
      const pathname = usePathname();
      const breadcrumbItems = getBreadcrumbItems(pathname);
      const selectedKey = pathname.startsWith('/reviewer/requests') ? '/reviewer/requests' : pathname;

      return (
        <Layout style={{ minHeight: '100vh' }}>
          <Sider
            breakpoint="lg"
            collapsedWidth={64}
            theme="light"
            width={200}
            style={{ borderRight: '1px solid #E2E8F0' }}
          >
            <div style={{ padding: '16px', textAlign: 'center' }}>
              <Typography.Text strong style={{ color: '#0F766E', fontSize: 14 }}>
                GitNexus Legal
              </Typography.Text>
              <Typography.Paragraph style={{ fontSize: 12, color: '#475569', margin: 0 }}>
                Nguoi duyet
              </Typography.Paragraph>
            </div>
            <Menu
              mode="inline"
              selectedKeys={[selectedKey]}
              items={navItems}
              style={{ borderRight: 'none' }}
            />
          </Sider>
          <Layout>
            <Header
              style={{
                background: '#FFFFFF',
                borderBottom: '1px solid #E2E8F0',
                padding: '0 24px',
                display: 'flex',
                alignItems: 'center',
                height: 48,
              }}
            >
              <Breadcrumb items={breadcrumbItems} />
            </Header>
            <Content
              style={{
                padding: 24,
                background: '#F8FAFC',
                minHeight: 'calc(100vh - 48px)',
              }}
            >
              {children}
            </Content>
          </Layout>
        </Layout>
      );
    }
    ```

    Key design decisions:
    - Compact sidebar similar to specialist (width=200)
    - 1 nav item: review queue, with prefix matching for detail pages
    - Subtitle "Nguoi duyet" under brand
    - Same breadcrumb and content area pattern as other layouts
  </action>
  <verify>
    <automated>grep -q "Layout.*Sider\|Sider" src/app/reviewer/layout.tsx && grep -q "getBreadcrumbItems" src/app/reviewer/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Reviewer layout created with antd Layout + Sider + Menu + Breadcrumb
    - Queue nav item with prefix matching
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Migrate reviewer pages and sub-components to antd</name>
  <files>
    src/app/reviewer/requests/page.tsx
    src/app/reviewer/requests/[requestId]/review/[documentVersionId]/page.tsx
    src/app/reviewer/requests/[requestId]/review/[documentVersionId]/components/start-review-button.tsx
    src/app/reviewer/requests/[requestId]/review/[documentVersionId]/components/review-form.tsx
  </files>
  <read_first>
    src/app/reviewer/requests/page.tsx
    src/app/reviewer/requests/[requestId]/review/[documentVersionId]/page.tsx
    src/app/reviewer/requests/[requestId]/review/[documentVersionId]/components/start-review-button.tsx
    src/app/reviewer/requests/[requestId]/review/[documentVersionId]/components/review-form.tsx
  </read_first>
  <action>
    Migrate all 4 files:

    **src/app/reviewer/requests/page.tsx:**
    - Remove: `import { Badge, Button, Card, PageHeader, Table } from '@/app/admin/components/ui';`
    - Add: `import { Tag, Button, Card, Table, Typography, Flex } from 'antd';`
    - Remove the `<main>` wrapper (route group layout now provides the shell)
    - Replace `<PageHeader>` with Typography.Title + Paragraph in Flex
    - Replace `<Badge>` with `<Tag>` (accent=cyan, destructive=red)
    - Replace `<Button variant="secondary">` with `<Button>`
    - Replace `<Card>` with antd Card
    - Replace custom `<Table>` with antd `<Table dataSource={pendingReviews} columns={[...]} pagination={false} />`
    - Keep the notice badge block — replace `<Badge tone="accent">` and `<Badge tone="destructive">` with `<Tag color="cyan">` and `<Tag color="red">`
    - Keep Link from next/link

    **src/app/reviewer/requests/[requestId]/review/[documentVersionId]/page.tsx:**
    - Remove: `import { Button, Card, PageHeader } from '@/app/admin/components/ui';`
    - Add: `import { Button, Card, Typography, Flex } from 'antd';`
    - Remove the `<main>` wrapper
    - Replace `<PageHeader>` with Typography.Title + Paragraph + action button in Flex
    - Replace `<Button variant="secondary">` with `<Button>`
    - Replace `<Card>` with antd Card
    - Keep the `<pre>` content display as-is (it's a pre tag with Tailwind classes, not from ui.tsx)
    - Keep the start-review-button and review-form imports as-is (they will import from antd themselves)
    - Keep the data fetching and RBAC logic entirely unchanged
    - **IMPORTANT:** The inner styled divs (grid, card content) keep their Tailwind classes

    **src/app/reviewer/requests/[requestId]/review/[documentVersionId]/components/start-review-button.tsx:**
    - Remove: `import { Button, Card } from '@/app/admin/components/ui';`
    - Add: `import { Button, Card } from 'antd';`
    - Replace `<Button type="submit" variant="primary">` with `<Button type="primary" htmlType="submit">Bat dau duyet</Button>`
    - Keep all logic and state management unchanged

    **src/app/reviewer/requests/[requestId]/review/[documentVersionId]/components/review-form.tsx:**
    - Remove: `import { Badge, Button, Card } from '@/app/admin/components/ui';`
    - Add: `import { Tag, Button, Card } from 'antd';`
    - Replace `<Badge tone={result.ok ? 'accent' : 'destructive'}>` with `<Tag color={result.ok ? 'cyan' : 'red'}>`
    - Replace `<Button type="submit" variant="primary">` with `<Button type="primary" htmlType="submit">Duyet</Button>`
    - Replace `<Button type="submit" variant="destructive">` with `<Button danger htmlType="submit">Yeu cau chinh sua</Button>`
    - Keep all checklist logic, state management, and form structure unchanged
    - Keep the inner `<div>` and `<input>` elements as-is (they are not from ui.tsx)

    **Tone-to-color mapping for all:**
    - neutral='default', info='blue', warning='orange', accent='cyan', destructive='red', outline='default'

    **Verify:** `npm run typecheck`
  </action>
  <verify>
    <automated>grep -q "from 'antd'" src/app/reviewer/requests/page.tsx && grep -q "from 'antd'" src/app/reviewer/requests/\[requestId\]/review/\[documentVersionId\]/page.tsx && grep -q "from 'antd'" src/app/reviewer/requests/\[requestId\]/review/\[documentVersionId\]/components/review-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 reviewer files import from antd instead of admin/components/ui
    - Queue page uses antd Table, Tag, Card
    - Detail page uses antd Card, Button, Typography
    - Sub-components use antd Button, Card, Tag
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → reviewer pages | Reviewer views review queue and document content |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-01 | I | document content in pre tag | accept | Content is server-rendered from DB, not user-submitted; displayed in pre tag with no XSS risk |
</threat_model>

<verification>
- [ ] Reviewer layout renders with compact Sider
- [ ] Queue page shows table of pending reviews with antd Table
- [ ] Detail page shows document content with review form
- [ ] Start review and review form work with antd buttons
</verification>

<success_criteria>
- Reviewer layout created
- All 4 reviewer files migrated to antd
- Typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-antd-layout-redesign/14-07-SUMMARY.md`
</output>
