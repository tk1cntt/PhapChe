---
phase: 14-antd-layout-redesign
plan: 09
type: execute
wave: 5
depends_on:
  - 14-08-PLAN.md
files_modified:
  - src/app/admin/components/ui.tsx
  - src/app/admin/components/admin-shell.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "ui.tsx is deleted — no file in the project imports from it anymore"
    - "admin-shell.tsx is deleted — no file in the project imports from it anymore"
  artifacts:
    - path: "src/app/admin/components/ui.tsx"
      provides: "Deleted (no remaining consumers)"
      contains: "DELETED"
    - path: "src/app/admin/components/admin-shell.tsx"
      provides: "Deleted (replaced by route group layout)"
      contains: "DELETED"
  key_links:
    - from: "Deleted files"
      to: "antd route group layouts"
      via: "replaced by"
      pattern: ""
---

<objective>
Remove orphaned custom UI files after all consumers have been migrated to antd.

Purpose: After Plans 03-08 complete, no file should import from `admin/components/ui.tsx` or `admin/components/admin-shell.tsx`. These files are now dead code. This plan deletes them.

Output:
- ui.tsx deleted
- admin-shell.tsx deleted
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-antd-layout-redesign/14-CONTEXT.md
@.planning/phases/14-antd-layout-redesign/14-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify zero consumers and delete orphaned files</name>
  <files>
    src/app/admin/components/ui.tsx
    src/app/admin/components/admin-shell.tsx
  </files>
  <read_first>
    (No files to read — this is a deletion task)
  </read_first>
  <action>
    **Step 1 — Verify zero consumers:**
    Run `grep -r "admin/components/ui\|admin/components/admin-shell" src/ --include="*.tsx" --include="*.ts"` to confirm that no file in `src/` still imports from either file. The only match should be the files themselves (self-referencing imports are not possible from `src/app/admin/components/`). If ANY consumer remains, STOP and identify which plan missed it — do NOT delete.

    **Step 2 — Delete files:**
    Only proceed if Step 1 confirms zero consumers.
    - Delete `src/app/admin/components/ui.tsx`
    - Delete `src/app/admin/components/admin-shell.tsx`

    **Step 3 — Verify deletion:**
    Run `npm run typecheck` to confirm no import errors from the deleted files.
  </action>
  <verify>
    <automated>test ! -f src/app/admin/components/ui.tsx && test ! -f src/app/admin/components/admin-shell.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/admin/components/ui.tsx` no longer exists
    - `src/app/admin/components/admin-shell.tsx` no longer exists
    - `npm run typecheck` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| N/A | File deletion only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09-01 | D | File deletion | mitigate | Verify zero consumers before deleting (Step 1) |
</threat_model>

<verification>
- [ ] `grep -r "admin/components/ui" src/` returns no results
- [ ] `grep -r "admin-shell" src/` returns no results
- [ ] `npm run typecheck` passes
</verification>

<success_criteria>
- Both orphaned files deleted
- Project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-antd-layout-redesign/14-09-SUMMARY.md`
</output>
