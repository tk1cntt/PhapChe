---
phase: 16
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/validate-phase-16-routes.cjs
  - prisma/seed.ts
  - src/auth.ts
  - src/components/auth/SignInForm.tsx
requirements: []
---

<objective>
Create the Phase 16 validation harness and role-aware fixture/session setup so every route from the original 14-route fail list has a concrete validation scenario before implementation fixes proceed.
</objective>

<must_haves>
- The validation harness covers exactly the 14 failed routes listed in `260606-pfi-SUMMARY.md`.
- The harness supports role-specific validation groups: admin, specialist, reviewer, customer.
- Dynamic route IDs are discovered from valid seeded/dev data instead of placeholders.
- Screenshots are saved only after validation PASS.
- Authorization is not bypassed or weakened.
</must_haves>

<threat_model>
- **Threat:** Weakening auth to make screenshots pass. **Mitigation:** validation must login/use role-appropriate users; no code path should skip `requireAppSession()` or role checks.
- **Threat:** Leaking legal data in screenshots/logs. **Mitigation:** validation output stores route/status/error previews only; do not print raw document content beyond page-visible safe text.
</threat_model>

<tasks>

<task id="16-01-01" type="execute">
<description>Create role-aware validation harness from the quick capture script.</description>
<read_first>
- `.planning/quick/260606-pfi-m-t-t-c-c-c-trang-hi-n-c-ki-m-tra-hi-n-t/validate-and-capture.cjs`
- `.planning/quick/260606-pfi-m-t-t-c-c-c-trang-hi-n-c-ki-m-tra-hi-n-t/validation-results.json`
- `.planning/quick/260606-pfi-m-t-t-c-c-c-trang-hi-n-c-ki-m-tra-hi-n-t/260606-pfi-SUMMARY.md`
- `.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/16-CONTEXT.md`
- `.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/16-VALIDATION.md`
</read_first>
<action>
Create `.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/validate-phase-16-routes.cjs` by adapting the quick script with these concrete changes:
- `FAILED_ROUTES` array contains these exact route templates: `/admin/ops`, `/admin/ops/[requestId]`, `/admin/routing`, `/admin/templates`, `/admin/templates/[templateId]`, `/admin/templates/new`, `/admin/users`, `/admin/vault`, `/customer/requests/[requestId]`, `/requests/[requestId]`, `/reviewer/requests`, `/reviewer/requests/[requestId]/review/[documentVersionId]`, `/specialist/requests`, `/specialist/requests/[requestId]`.
- Add CLI group filtering: `--group queues`, `--group admin`, `--group dynamic`, `--quick`.
- Load `.env.local` and `.env` like the quick script.
- Discover IDs from Prisma for `requestId`, `templateId`, `documentVersionId` using role-appropriate ownership/assignment where possible.
- Output JSON to `.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/phase-16-validation-results.json`.
- Save screenshots under `.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/screenshots/` only when validation PASS.
- Treat HTTP 500, blank body, Next runtime overlay, pageerror, severe console errors, and accidental visible 404 for valid seeded scenarios as FAIL.
</action>
<verify>
Run `node .planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/validate-phase-16-routes.cjs --quick` after the dev server is available.
</verify>
<acceptance_criteria>
- `validate-phase-16-routes.cjs` contains `FAILED_ROUTES`.
- `validate-phase-16-routes.cjs` contains all 14 exact route template strings listed above.
- `validate-phase-16-routes.cjs` contains `--group` handling.
- `validate-phase-16-routes.cjs` writes `phase-16-validation-results.json`.
- `validate-phase-16-routes.cjs` writes screenshots only inside a branch where route validation passed.
</acceptance_criteria>
</task>

<task id="16-01-02" type="execute">
<description>Ensure validation can authenticate/seed role-specific demo users without weakening app auth.</description>
<read_first>
- `prisma/seed.ts`
- `src/auth.ts`
- `src/components/auth/SignInForm.tsx`
- `src/lib/security/session.ts`
- `prisma/schema.prisma`
</read_first>
<action>
Inspect existing seed/auth/session code and make the minimum changes needed so the validation harness can authenticate role-specific users:
- specialist: `specialist.demo@example.test` / `Demo@123456`
- reviewer: `reviewer.demo@example.test` / `Demo@123456`
- admin/coordinator: add or reuse `admin.demo@example.test` / `Demo@123456` with role `coordinator_admin` if not already present.
- customer: add or reuse `customer.demo@example.test` / `Demo@123456` with role `customer` if needed for customer-owned request detail route.
If the users already exist through Phase 15 work, do not duplicate them; adjust the validation harness to use existing credentials. Preserve `requireAppSession()` and role checks.
</action>
<verify>
Run `npm run seed` if seed changes are made, then run the validation harness quick mode.
</verify>
<acceptance_criteria>
- If `prisma/seed.ts` is modified, it contains `admin.demo@example.test` or the validation harness documents an existing admin credential.
- If customer route validation needs customer ownership, either `prisma/seed.ts` contains `customer.demo@example.test` or the validation harness derives an existing customer user.
- No modified source file contains code that bypasses `requireAppSession()` for screenshot validation.
</acceptance_criteria>
</task>

</tasks>

<verification>
- Run quick validation harness after dev server is running.
- Confirm route list and role scenarios are present in generated JSON.
- Confirm screenshots are not produced for failing routes.
</verification>

<success_criteria>
- Phase 16 validation harness exists and covers all 14 original failures.
- Role-aware credentials/fixtures are available or documented by the harness.
- No app authorization is weakened.
</success_criteria>
