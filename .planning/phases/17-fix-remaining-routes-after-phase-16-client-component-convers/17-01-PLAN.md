---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/ops/[requestId]/OpsTimelineTable.tsx
  - src/app/admin/ops/[requestId]/page.tsx
  - src/app/admin/users/UsersPageClient.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "/admin/ops/[requestId] returns HTTP 200 with timeline table rendered (not blank)"
    - "/admin/users renders without antd deprecation warning in console"
  artifacts:
    - path: "src/app/admin/ops/[requestId]/OpsTimelineTable.tsx"
      provides: "OpsTimelineTable client component with proper default export"
      min_lines: 90
    - path: "src/app/admin/users/UsersPageClient.tsx"
      provides: "Space without deprecated direction prop"
  key_links:
    - from: "src/app/admin/ops/[requestId]/page.tsx"
      to: "src/app/admin/ops/[requestId]/OpsTimelineTable.tsx"
      via: "import default export"
      pattern: "import OpsTimelineTable from"
---

<objective>
Fix two remaining page errors: (1) HTTP 500 on /admin/ops/[requestId] caused by OpsTimelineTable being undefined at render time, and (2) antd Space `direction` deprecation warning on /admin/users.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
## Phase 16 Validation Results (relevant failures)

**/admin/ops/[requestId]:**
- HTTP 500, Blank body
- Console error: `Error: Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: undefined. You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.`
- Error location: `Check the render method of OpsRequestTimelinePage`

**/admin/users:**
- HTTP 200, but console warning: `Warning: [antd: Space] direction is deprecated. Please use orientation instead.`

## Current File Analysis

### OpsTimelineTable.tsx (existing, likely correct but verify)
```typescript
// Line 95: export default function OpsTimelineTable(...)
```
- Uses `'use client'` directive
- Exports OpsTimelineRow type
- Default export: `export default function OpsTimelineTable({ rows }: { rows: OpsTimelineRow[] })`

### page.tsx import (line 5-6)
```typescript
import OpsTimelineTable from './OpsTimelineTable';
import type { OpsTimelineRow } from './OpsTimelineTable';
```
- Correct relative import path (same directory)
- Imports default export AND named type — both should work

### UsersPageClient.tsx (line 59)
```typescript
<Space direction="vertical" size={16}>
```
- `direction` prop deprecated in antd 5.8+, should be `orientation`
</context>

<tasks>

<task id="17-01-01" type="execute">
  <name>Task 1: Verify and fix OpsTimelineTable default export</name>
  <files>src/app/admin/ops/[requestId]/OpsTimelineTable.tsx, src/app/admin/ops/[requestId]/page.tsx</files>
  <action>
    The 500 error "Element type is invalid: expected a string...but got: undefined" in `OpsRequestTimelinePage` render method means OpsTimelineTable resolves to undefined at runtime. Despite the source file having `export default function OpsTimelineTable`, Next.js may not have picked up the file correctly, or there is a build artifact issue.

    Do NOT assume the existing file is correct. Verify by reading the actual file contents on disk, then:

    1. Read the raw contents of `src/app/admin/ops/[requestId]/OpsTimelineTable.tsx` to confirm:
       - Line 1 contains `'use client'` (required for client components in Next.js App Router)
       - The export is `export default function OpsTimelineTable` (not named export)
       - The file does NOT have `export =` (TypeScript module syntax conflict)

    2. If the file is correct, the issue is a stale Next.js build artifact. Run `npx tsc --noEmit` to check for TypeScript errors in this specific file.

    3. If TSC passes, run the dev server and check if the 500 persists. If it persists, the component may need to be re-exported.

    4. If the file is missing or broken, recreate OpsTimelineTable.tsx following this exact pattern:
       - Line 1: `'use client'`
       - Export `OpsTimelineRow` type interface
       - Export default function `OpsTimelineTable` accepting `rows: OpsTimelineRow[]`
       - Use antd Table with columns: Time, Actor, Kind, Action/status change, Target identifier, Reason, Correlation ID, metadataSummary
       - NO other exports besides default

    5. Verify page.tsx import uses `import OpsTimelineTable from './OpsTimelineTable'` (relative path, default import)
  </action>
  <verify>
    <automated>npx tsc --noEmit src/app/admin/ops/\[requestId\]/OpsTimelineTable.tsx 2>&amp;1 | head -20</automated>
  </verify>
  <done>TypeScript compiles OpsTimelineTable without errors. The file has 'use client', a default export, and is importable from page.tsx.</done>
</task>

<task id="17-01-02" type="execute">
  <name>Task 2: Fix antd Space deprecated direction prop</name>
  <files>src/app/admin/users/UsersPageClient.tsx</files>
  <action>
    In `src/app/admin/users/UsersPageClient.tsx`, line 59 has:
    ```tsx
    <Space direction="vertical" size={16}>
    ```

    antd 5.8+ deprecated `direction` in favor of `orientation`. Replace:
    - `direction="vertical"` -> `orientation="vertical"`

    The `size` prop is still valid. Keep it.

    Change line 59 from:
    ```tsx
    <Space direction="vertical" size={16}>
    ```
    To:
    ```tsx
    <Space orientation="vertical" size={16}>
    ```

    No other changes needed.
  </action>
  <verify>
    <automated>grep -n 'direction=' src/app/admin/users/UsersPageClient.tsx 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <done>No `direction=` prop remains in UsersPageClient.tsx. `orientation` is used instead.</done>
</task>

</tasks>

<verification>
- `/admin/users` page renders without "Space direction is deprecated" warning
- `/admin/ops/[requestId]` returns HTTP 200 (not 500) with timeline table visible
</verification>

<success_criteria>
- OpsTimelineTable.tsx has `'use client'` and `export default` function
- TypeScript compilation passes for both files
- No deprecated antd Space props in UsersPageClient.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/17-fix-remaining-routes-after-phase-16-client-component-convers/17-01-SUMMARY.md`
</output>
