---
phase: 17
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - .planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/phase-17-validation-results.json
  - .planning/phases/17-fix-remaining-routes-after-phase-16-client-component-convers/17-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 8 previously failing routes are re-validated"
    - "Validation results show PASS for routes that previously failed"
    - "Screenshots captured for newly passing routes"
  artifacts:
    - path: ".planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/phase-17-validation-results.json"
      provides: "Complete validation results with pass/fail for all 14 routes"
    - path: ".planning/phases/17-fix-remaining-routes-after-phase-16-client-component-convers/17-VERIFICATION.md"
      provides: "Phase 17 verification report"
  key_links:
    - from: "validate-phase-16-routes.cjs"
      to: "src/app/admin/ops/[requestId]/page.tsx"
      via: "HTTP request to live server"
---

<objective>
Run final validation of all 14 routes against the live application to confirm Phase 17 fixes resolved the remaining failures. Capture pass/fail status and screenshots for passing routes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
## Phase 16 Validation Results (baseline)

| Route | Previous Status | Phase 16 Issue |
|-------|---------------|----------------|
| /admin/ops | FAIL | Sign-in failed |
| /admin/ops/[requestId] | FAIL | HTTP 500, OpsTimelineTable undefined |
| /admin/routing | PASS | - |
| /admin/templates | PASS | - |
| /admin/templates/[templateId] | FAIL | 404 (placeholder ID) |
| /admin/templates/new | PASS | - |
| /admin/users | FAIL | antd Space direction warning |
| /admin/vault | PASS | - |
| /customer/requests/[requestId] | FAIL | 404 (placeholder ID) |
| /requests/[requestId] | FAIL | 404 (placeholder ID) |
| /reviewer/requests | PASS | - |
| /reviewer/requests/[requestId]/review/[documentVersionId] | FAIL | 404 (placeholder ID) |
| /specialist/requests | PASS | - |
| /specialist/requests/[requestId] | FAIL | 404 (placeholder ID) |

## Expected After Phase 17 Fixes

| Route | Expected Status | Fix Applied |
|-------|----------------|-------------|
| /admin/ops | PASS | Already working (session issue resolved in Phase 16) |
| /admin/ops/[requestId] | PASS | OpsTimelineTable default export verified/fixed |
| /admin/templates/[templateId] | PASS | Real template ID from database |
| /admin/users | PASS | Space direction replaced with orientation |
| /customer/requests/[requestId] | PASS | Real request ID from database |
| /requests/[requestId] | PASS | Real request ID from database |
| /reviewer/requests/[requestId]/review/[documentVersionId] | PASS | Real IDs from database |
| /specialist/requests/[requestId] | PASS | Real request ID from database |

## Validation Harness Location

`node .planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/validate-phase-16-routes.cjs`

Output: `.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/phase-17-validation-results.json`
</context>

<tasks>

<task id="17-03-01" type="execute">
  <name>Task 1: Run full validation harness against all 14 routes</name>
  <files>.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/validate-phase-16-routes.cjs</files>
  <action>
    Ensure the dev server is running. If not, start it with `npm run dev` in a background shell.

    Run the validation harness without group filtering to validate ALL 14 routes:

    ```bash
    node .planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/validate-phase-16-routes.cjs
    ```

    This will:
    1. Authenticate as each role (admin, specialist, reviewer, customer)
    2. Validate all 14 routes with real IDs from Plan 17-02
    3. Output results to `phase-17-validation-results.json`
    4. Capture screenshots only for PASS routes

    Wait for completion. Check the output JSON for:
    - `passCount` and `failCount` in the summary
    - Check that `fixtures.samples` contains real IDs (not placeholders)
    - Check individual route results for PASS/fail status
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const r = JSON.parse(fs.readFileSync('.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/phase-17-validation-results.json', 'utf8')); console.log('Pass:', r.results.filter(x=>x.validation==='PASS').length, '/ Fail:', r.results.filter(x=>x.validation==='FAIL').length); console.log('Samples:', JSON.stringify(r.fixtures.samples));"</automated>
  </verify>
  <done>Validation run completes with results for all 14 routes. passCount >= failCount (target: 12+ PASS, 0-2 acceptable FAIL for edge cases).</done>
</task>

<task id="17-03-02" type="execute">
  <name>Task 2: Analyze and document results</name>
  <files>.planning/phases/17-fix-remaining-routes-after-phase-16-client-component-convers/17-VERIFICATION.md</files>
  <action>
    Read the phase-17-validation-results.json file and create a verification report at:
    `.planning/phases/17-fix-remaining-routes-after-phase-16-client-component-convers/17-VERIFICATION.md`

    The report should include:

    1. **Summary table** - All 14 routes with previous status, current status, and any remaining issues
    2. **Fixtures section** - Confirm real IDs were resolved (show first 8 chars of IDs, not full IDs)
    3. **Pass list** - Routes that now pass
    4. **Fail list** - Routes that still fail, with specific error messages
    5. **Screenshots** - List of screenshots captured

    For each previously-failing route, state whether it now passes or still fails.

    If any routes still fail, categorize:
    - Expected (no data in database for that scenario)
    - Unexpected (should work but doesn't — needs further investigation)

    Format as markdown.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-fix-remaining-routes-after-phase-16-client-component-convers/17-VERIFICATION.md && echo "VERIFICATION exists"</automated>
  </verify>
  <done>17-VERIFICATION.md exists with pass/fail analysis for all 14 routes.</done>
</task>

</tasks>

<verification>
- Validation harness completes without errors
- All 8 previously-failing routes are re-validated
- phase-17-validation-results.json contains complete results for all 14 routes
- 17-VERIFICATION.md documents the outcomes
</verification>

<success_criteria>
- `passCount` >= 12 (at least 12 of 14 routes pass)
- Previously-failing routes (ops/[requestId], users, dynamic routes) show PASS in results
- `fixtures.samples` contains real Prisma IDs (not placeholder strings)
- Screenshots exist for newly-passing routes in the screenshots directory
</success_criteria>

<output>
After completion, create `.planning/phases/17-fix-remaining-routes-after-phase-16-client-component-convers/17-03-SUMMARY.md`
</output>
