---
phase: "18"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "e2e/auth.spec.ts"
  - "e2e/intake.spec.ts"
autonomous: true
requirements: []
must_haves:
  truths:
    - "Auth tests pass: login form renders, validation works, redirect works"
    - "Intake tests pass: service selection, questions, file upload, review, submit"
  artifacts:
    - path: "e2e/auth.spec.ts"
      provides: "Playwright tests for sign-in screen"
    - path: "e2e/intake.spec.ts"
      provides: "Playwright tests for intake flow"
---

<objective>
Create Playwright E2E tests for Auth (sign-in) and Intake screens. Test all user flows and verify functionality works correctly.
</objective>

<context>
@.planning/phases/18-ui-test-cases/18-CONTEXT.md
@src/components/auth/SignInForm.tsx
@src/app/intake/components.tsx
@.planning/phases/16-fix-14-failed-routes-discovered-by-validated-screenshot-capt/validate-phase-16-routes.cjs
</context>

<tasks>

<task id="18-01-01" type="execute">
<name>Create auth E2E tests</name>
<files>src/components/auth/SignInForm.tsx</files>
<action>
Create `e2e/auth.spec.ts` with Playwright tests:

1. Setup: Install playwright if not exists (`npm install -D @playwright/test`, `npx playwright install chromium`)

2. Create `e2e/auth.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test.describe('Sign-In Screen', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/sign-in');
  });

  test('renders login form correctly', async ({ page }) => {
    await expect(page.locator('input[placeholder*="Email"]')).toBeVisible();
    await expect(page.locator('input[placeholder*="Mật khẩu"]')).toBeVisible();
    await expect(page.locator('button:has-text("Đăng nhập")')).toBeVisible();
  });

  test('shows validation errors for empty fields', async ({ page }) => {
    await page.click('button:has-text("Đăng nhập")');
    await expect(page.locator('text=Email là bắt buộc')).toBeVisible();
  });

  test('shows error for invalid credentials', async ({ page }) => {
    await page.fill('input[placeholder*="Email"]', 'wrong@test.com');
    await page.fill('input[placeholder*="Mật khẩu"]', 'wrongpass');
    await page.click('button:has-text("Đăng nhập")');
    await expect(page.locator('text=Email hoặc mật khẩu không đúng')).toBeVisible({ timeout: 5000 });
  });

  test('redirects to intake on successful login', async ({ page }) => {
    await page.fill('input[placeholder*="Email"]', 'admin.demo@example.test');
    await page.fill('input[placeholder*="Mật khẩu"]', 'Demo@123456');
    await page.click('button:has-text("Đăng nhập")');
    await page.waitForURL('**/intake**', { timeout: 10000 });
    await expect(page.url()).toContain('/intake');
  });
});
```

3. Create `playwright.config.ts`:
```typescript
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
});
```

4. Add to package.json scripts:
```json
"test:e2e": "playwright test",
"test:e2e:ui": "playwright test --ui"
```
</action>
<verify>
grep -c "test\(" e2e/auth.spec.ts && grep -c "expect(" e2e/auth.spec.ts</verify>
<done>Auth tests created and verified with Playwright</done>
</task>

<task id="18-01-02" type="execute">
<name>Create intake E2E tests</name>
<files>src/app/intake/components.tsx</files>
<action>
Create `e2e/intake.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Intake Flow', () => {
  let authenticatedPage: any;

  test.beforeAll(async ({ browser }) => {
    const context = await browser.newContext();
    const page = await context.newPage();
    await page.goto('/sign-in');
    await page.fill('input[placeholder*="Email"]', 'admin.demo@example.test');
    await page.fill('input[placeholder*="Mật khẩu"]', 'Demo@123456');
    await page.click('button:has-text("Đăng nhập")');
    await page.waitForURL('**/intake**', { timeout: 10000 });
    authenticatedPage = page;
  });

  test('renders intake page with header', async ({ page }) => {
    await page.goto('/intake');
    await expect(page.locator('text=Gửi yêu cầu pháp lý')).toBeVisible();
    await expect(page.locator('text=Dịch vụ')).toBeVisible();
  });

  test('shows service selection options', async ({ page }) => {
    await page.goto('/intake');
    const options = page.locator('text=Công việc').first();
    await expect(options).toBeVisible();
  });

  test('can select service option', async ({ page }) => {
    await page.goto('/intake');
    const option = page.locator('.ant-radio-wrapper').first();
    await option.click();
    await expect(option).toHaveClass(/ant-radio-wrapper-checked/);
  });

  test('proceeds to questions step', async ({ page }) => {
    await page.goto('/intake');
    await page.click('button:has-text("Tiếp tục")');
    await page.waitForTimeout(1000);
    // Should show questions step
    await expect(page.locator('text=Thông tin cần cung cấp')).toBeVisible({ timeout: 5000 });
  });

  test('shows upload step', async ({ page }) => {
    // After filling questions, should show upload step
    await page.goto('/intake');
    await page.click('button:has-text("Tiếp tục")');
    await page.waitForTimeout(2000);
    await expect(page.locator('text=Tài liệu hỗ trợ')).toBeVisible({ timeout: 5000 });
  });
});
```

Also create `e2e/helpers.ts` for authentication utility:
```typescript
export async function loginAs(page: Page, role: 'admin' | 'specialist' | 'reviewer' | 'customer') {
  const credentials = {
    admin: { email: 'admin.demo@example.test', password: 'Demo@123456' },
    specialist: { email: 'specialist.demo@example.test', password: 'Demo@123456' },
    reviewer: { email: 'reviewer.demo@example.test', password: 'Demo@123456' },
    customer: { email: 'customer.demo@example.test', password: 'Demo@123456' },
  };

  const cred = credentials[role];
  await page.goto('/sign-in');
  await page.fill('input[placeholder*="Email"]', cred.email);
  await page.fill('input[placeholder*="Mật khẩu"]', cred.password);
  await page.click('button:has-text("Đăng nhập")');
  await page.waitForURL('**/intake**', { timeout: 15000 });
}
```
</action>
<verify>
grep -c "test(" e2e/intake.spec.ts && test -d e2e</verify>
<done>Intake tests created with helper utilities</done>
</task>

<task id="18-01-03" type="execute">
<name>Run auth and intake tests</name>
<files>e2e/auth.spec.ts, e2e/intake.spec.ts</files>
<action>
1. Ensure dev server is running on localhost:3000
2. Run tests:
   ```bash
   npx playwright test e2e/auth.spec.ts e2e/intake.spec.ts --reporter=list
   ```
3. Fix any failures:
   - If page elements not found, update selectors
   - If tests fail, fix the application code
   - Re-run until all pass
4. Take screenshots of passing tests for documentation
</action>
<verify>
npx playwright test e2e/auth.spec.ts e2e/intake.spec.ts --reporter=list 2>&1 | tail -20</verify>
<done>All auth and intake tests pass</done>
</task>

</tasks>

<verification>
Run `npx playwright test e2e/auth.spec.ts e2e/intake.spec.ts` and verify all tests pass with 0 failures.
</verification>

<success_criteria>
- e2e/auth.spec.ts exists with 4+ test cases
- e2e/intake.spec.ts exists with 4+ test cases
- All auth tests pass
- All intake tests pass
- Screenshots captured for documentation
</success_criteria>

<output>
After completion, create `.planning/phases/18-ui-test-cases/18-01-SUMMARY.md`
</output>
