---
phase: "18"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - "e2e/admin.spec.ts"
autonomous: true
requirements: []
must_haves:
  truths:
    - "Admin screens tests pass: ops, routing, templates, users, vault"
  artifacts:
    - path: "e2e/admin.spec.ts"
      provides: "Playwright tests for all admin screens"
---

<objective>
Create Playwright E2E tests for all admin screens: ops dashboard, routing, templates, users, vault.
</objective>

<context>
@.planning/phases/18-ui-test-cases/18-CONTEXT.md
@e2e/helpers.ts
@src/app/admin/**/*.tsx
</context>

<tasks>

<task id="18-03-01" type="execute">
<name>Create admin ops tests</name>
<files>src/app/admin/ops/page.tsx</files>
<action>
Create `e2e/admin.spec.ts` with tests for all admin screens:

```typescript
import { test, expect } from '@playwright/test';
import { loginAs } from './helpers';

test.describe('Admin Screens', () => {
  test.beforeEach(async ({ page }) => {
    await loginAs(page, 'admin');
  });

  // Ops Dashboard
  test('admin ops renders correctly', async ({ page }) => {
    await page.goto('/admin/ops');
    await page.waitForTimeout(2000);
    // Should show ops-related content
    const content = await page.content();
    expect(content.length > 100).toBeTruthy();
  });

  // Routing
  test('admin routing renders correctly', async ({ page }) => {
    await page.goto('/admin/routing');
    await page.waitForTimeout(2000);
    const content = await page.content();
    expect(content.length > 100).toBeTruthy();
  });

  // Templates
  test('admin templates renders correctly', async ({ page }) => {
    await page.goto('/admin/templates');
    await page.waitForTimeout(2000);
    await expect(page.locator('text=Mẫu')).toBeVisible();
  });

  test('admin templates new form works', async ({ page }) => {
    await page.goto('/admin/templates/new');
    await page.waitForTimeout(2000);
    const content = await page.content();
    expect(content.length > 50).toBeTruthy();
  });

  // Users
  test('admin users renders correctly', async ({ page }) => {
    await page.goto('/admin/users');
    await page.waitForTimeout(2000);
    await expect(page.locator('text=Người dùng')).toBeVisible();
  });

  // Vault
  test('admin vault renders correctly', async ({ page }) => {
    await page.goto('/admin/vault');
    await page.waitForTimeout(2000);
    const content = await page.content();
    expect(content.length > 50).toBeTruthy();
  });

  // Navigation
  test('admin sidebar navigation works', async ({ page }) => {
    await page.goto('/admin');
    await page.waitForTimeout(1000);
    // Check for sidebar links
    const hasLinks = await page.locator('a[href*="/admin/"]').count() > 0;
    expect(hasLinks).toBeTruthy();
  });
});
```
</action>
<verify>
test -f e2e/admin.spec.ts && grep -c "test\(" e2e/admin.spec.ts</verify>
<done>Admin tests created</done>
</task>

<task id="18-03-02" type="execute">
<name>Run admin tests</name>
<files>e2e/admin.spec.ts</files>
<action>
1. Run admin tests:
   ```bash
   npx playwright test e2e/admin.spec.ts --reporter=list
   ```

2. Fix any failures:
   - Update selectors if UI changed
   - Fix application bugs
   - Re-run until all pass

3. Report results
</action>
<verify>
npx playwright test e2e/admin.spec.ts --reporter=list 2>&1 | tail -20</verify>
<done>All admin tests pass</done>
</task>

</tasks>

<verification>
Run all admin tests and verify they pass.
</verification>

<success_criteria>
- e2e/admin.spec.ts with 6+ tests for ops, routing, templates, users, vault
- All admin tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-ui-test-cases/18-03-SUMMARY.md`
</output>
