---
phase: 23-quick-wins
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ErrorFallback.tsx
  - src/components/ui/PageSkeleton.tsx
  - src/components/ui/CardSkeleton.tsx
  - src/app/[locale]/admin/error.tsx
  - src/app/[locale]/admin/requests/page.tsx
  - src/app/[locale]/admin/workspaces/page.tsx
  - src/app/[locale]/admin/templates/TemplatesPageClient.tsx
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
  - ERR-04
  - LOAD-01
  - LOAD-02
  - LOAD-03
  - LOAD-04
user_setup: []
must_haves:
  truths:
    - "Admin pages display skeleton screens while fetching data"
    - "Admin pages show error message with retry button on failure"
    - "Errors are logged to console for debugging"
    - "ErrorFallback and Skeleton components are shared, not duplicated"
  artifacts:
    - path: "src/components/ui/ErrorFallback.tsx"
      provides: "Shared error fallback with retry capability"
      exports: ["ErrorFallback"]
    - path: "src/components/ui/PageSkeleton.tsx"
      provides: "Skeleton for table-based pages"
      exports: ["PageSkeleton"]
    - path: "src/components/ui/CardSkeleton.tsx"
      provides: "Skeleton for card-based pages"
      exports: ["CardSkeleton"]
  key_links:
    - from: "src/app/[locale]/admin/error.tsx"
      to: "src/components/ui/ErrorFallback.tsx"
      via: "import and render"
    - from: "src/app/[locale]/admin/requests/page.tsx"
      to: "src/components/ui/PageSkeleton.tsx"
      via: "replace Spin with PageSkeleton"
---

<objective>
Create shared ErrorFallback and Skeleton components, then integrate them into admin pages.

Purpose: Improve perceived performance with skeleton loading and provide graceful error recovery.
Output: 3 reusable UI components + integration into 3 admin pages.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-quick-wins/23-CONTEXT.md
@.planning/REQUIREMENTS.md

Existing patterns (read before implementing):

From src/app/[locale]/admin/error.tsx:
- Error Boundary pattern using Next.js error.tsx
- Uses Ant Design Result component for error display
- Includes useEffect for console.error logging
- Has reset() function for retry
- Handles UNAUTHENTICATED special case

From src/app/reviewer/requests/loading.tsx:
- Shows Spin during loading
- Will be replaced with skeleton components
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app/[locale]/admin/error.tsx:
```typescript
// Error Boundary props
interface AdminErrorProps {
  error: Error & { digest?: string };
  reset: () => void;
}
```

From TemplatesPageClient.tsx (loading state pattern):
```typescript
// Current loading state pattern
const [loading, setLoading] = useState(true);
// Shows: <Flex justify="center" style={{ padding: 48 }}><Spin /></Flex>
// Will replace with: <PageSkeleton /> or <CardSkeleton />
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create shared ErrorFallback component</name>
  <files>src/components/ui/ErrorFallback.tsx</files>
  <action>
Create ErrorFallback component with:
- Props: { error: Error & { digest?: string }, onRetry?: () => void }
- Display error title: "Something went wrong"
- Display error message from error.message (or generic fallback)
- Display error.stack in console (via useEffect)
- Retry button: calls onRetry if provided, else window.location.reload()
- "Go to Home" button linking to /[locale] or /
- Use Ant Design Result component with status="error"
- Export named: ErrorFallback

Note: This replaces the inline error UI in admin/error.tsx with a reusable component per D-03, D-04.
  </action>
  <verify>
    <automated>Get-ChildItem -Path src/components/ui -Filter ErrorFallback.tsx | Select-Object -First 1</automated>
  </verify>
  <done>ErrorFallback.tsx exists with error display, retry button, console logging, and Go to Home option</done>
</task>

<task type="auto">
  <name>Task 2: Create PageSkeleton and CardSkeleton components</name>
  <files>src/components/ui/PageSkeleton.tsx, src/components/ui/CardSkeleton.tsx</files>
  <action>
Create PageSkeleton component (src/components/ui/PageSkeleton.tsx):
- Props: { rows?: number } (default: 5)
- Use Ant Design Skeleton component with active={true}
- Include Skeleton paragraph with rows={rows}
- Include Skeleton title above
- Wrap in Card component for visual match with actual pages
- Export named: PageSkeleton

Create CardSkeleton component (src/components/ui/CardSkeleton.tsx):
- Props: { count?: number } (default: 3)
- Render count number of Card components with Skeleton inside
- Each Card contains: Skeleton title + paragraph
- Use Ant Design Skeleton active mode
- Export named: CardSkeleton

Note: Per D-05, use built-in antd Skeleton (no additional dependency).
  </action>
  <verify>
    <automated>Get-ChildItem -Path src/components/ui -Filter *Skeleton.tsx | Select-Object -First 1</automated>
  </verify>
  <done>PageSkeleton.tsx and CardSkeleton.tsx exist, both using antd Skeleton, both exported</done>
</task>

<task type="auto">
  <name>Task 3: Integrate shared components into admin pages</name>
  <files>src/app/[locale]/admin/error.tsx, src/app/[locale]/admin/requests/page.tsx, src/app/[locale]/admin/workspaces/page.tsx, src/app/[locale]/admin/templates/TemplatesPageClient.tsx</files>
  <action>
Update admin/error.tsx:
- Import ErrorFallback from @/components/ui/ErrorFallback
- Replace inline Result with: <ErrorFallback error={error} onRetry={reset} />
- Keep the UNAUTHENTICATED handling before rendering ErrorFallback

Update requests/page.tsx (table-based):
- Currently has no loading state (static data)
- Add loading state with useState(false) for future data fetch simulation
- Replace content with: {loading ? <PageSkeleton rows={5} /> : (actual content)}
- Demonstrate skeleton integration pattern

Update workspaces/page.tsx (table-based):
- Same pattern as requests: add loading state + PageSkeleton
- Replace Spin-style loading with PageSkeleton

Update TemplatesPageClient.tsx (card-based):
- Currently uses Spin for loading (see lines 31-37)
- Replace Spin loading with CardSkeleton count={3}
- Import CardSkeleton from @/components/ui/CardSkeleton
  </action>
  <verify>
    <automated>Get-Content src/components/ui/ErrorFallback.tsx | Select-String -Pattern "export" | Select-Object -First 1</automated>
  </verify>
  <done>All 4 files updated: error.tsx uses ErrorFallback, requests/workspaces use PageSkeleton, TemplatesPageClient uses CardSkeleton</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) | ErrorBoundary catches React errors; Skeleton shows during async data fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Denial of Service | ErrorFallback | accept | Ant Design Result is lightweight; retry reloads page |
| T-23-02 | Information Disclosure | ErrorFallback | mitigate | Only display error.message (safe), log error.stack to console (dev-only) |
| T-23-03 | Elevation of Privilege | Skeleton loading | accept | Loading state has no security implications |
</threat_model>

<verification>
- ErrorFallback component renders with error message and retry button
- PageSkeleton renders with configurable row count
- CardSkeleton renders with configurable card count
- admin/error.tsx imports and uses ErrorFallback
- requests, workspaces use PageSkeleton during loading
- TemplatesPageClient uses CardSkeleton during loading
- console.error called with error.stack on error boundary trigger
</verification>

<success_criteria>
- ErrorFallback.tsx created and used in admin/error.tsx
- PageSkeleton.tsx created and used in 2 table-based pages
- CardSkeleton.tsx created and used in TemplatesPageClient
- All admin pages (ERR-01) now use ErrorFallback (via error.tsx)
- Error fallback displays message + retry button (ERR-02)
- Errors logged to console (ERR-03)
- Shared component used across pages (ERR-04)
- Admin pages show skeleton during loading (LOAD-01)
- PageSkeleton matches table structure (LOAD-02)
- CardSkeleton for card content (LOAD-03)
- Components reusable via imports (LOAD-04)
</success_criteria>

<output>
After completion, create `.planning/phases/23-quick-wins/23-01-SUMMARY.md`
</output>
