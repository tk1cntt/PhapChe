---
phase: 25-pagination-search
plan: "01"
type: execute
wave: 1
depends_on:
  - "24-01"
files_modified:
  - src/lib/hooks/usePaginationParams.ts
  - src/lib/hooks/useDebounce.ts
  - src/hooks/useRequests.ts
  - src/hooks/useUsers.ts
  - src/hooks/useAuditEvents.ts
  - src/app/api/requests/route.ts
  - src/app/api/users/route.ts
  - src/app/api/audit/events/route.ts
  - src/app/admin/requests/page.tsx
  - src/app/admin/users/page.tsx
  - src/app/admin/audit/page.tsx
  - src/app/admin/users/AdminUsersTable.tsx
  - src/app/admin/ops/AdminOpsTables.tsx
  - src/app/admin/layout.tsx
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - SCH-01
  - SCH-02
  - SCH-03
  - SCH-04
user_setup: []

must_haves:
  truths:
    - "Tables display pagination controls with page sizes: 10, 25, 50"
    - "Current page number persists in URL query params (?page=2&pageSize=25)"
    - "Pagination fetches only requested page from server (server-side)"
    - "Global search bar visible in admin header"
    - "Column filters available in table headers"
    - "Search input debounces by 300ms before triggering query"
    - "Search and filter state persists in URL params"
  artifacts:
    - path: "src/lib/hooks/usePaginationParams.ts"
      provides: "URL sync for page, pageSize, search, filters"
      exports: ["usePaginationParams"]
    - path: "src/lib/hooks/useDebounce.ts"
      provides: "Debounce hook for search input"
      exports: ["useDebounce"]
    - path: "src/app/api/requests/route.ts"
      provides: "Server-side pagination for requests"
      exports: ["GET"]
    - path: "src/app/admin/layout.tsx"
      provides: "Global search bar in header"
    - path: "src/app/admin/requests/page.tsx"
      provides: "Paginated requests table with column filters"
    - path: "src/app/admin/users/page.tsx"
      provides: "Paginated users table with column filters"
    - path: "src/app/admin/audit/page.tsx"
      provides: "Paginated audit table with column filters"
  key_links:
    - from: "src/lib/hooks/usePaginationParams.ts"
      to: "Next.js useSearchParams"
      via: "useSearchParams and useRouter for URL sync"
    - from: "src/app/admin/layout.tsx"
      to: "src/app/admin/requests/page.tsx"
      via: "global search triggers page navigation with search param"
    - from: "src/app/admin/requests/page.tsx"
      to: "src/app/api/requests/route.ts"
      via: "useRequests hook with pagination params"
---

<objective>
Implement server-side pagination and search/filter with URL persistence across admin tables.

Purpose: Allow users to navigate large datasets efficiently with bookmarkable URLs and real-time search.
Output: Pagination controls, global search bar, column filters, debounced search across requests/users/audit tables.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 25 goal and requirements)
@.planning/phases/24-tanstack-query/24-01-SUMMARY.md (TanStack Query foundation)
@src/hooks/useRequests.ts (existing query hook - will be enhanced)
@src/app/admin/layout.tsx (admin header - will add search bar)
@src/app/admin/users/AdminUsersTable.tsx (existing table pattern)
@src/app/admin/ops/AdminOpsTables.tsx (existing table pattern)
@src/app/api/users/route.ts (existing API - will add pagination)
@src/app/api/audit/events/route.ts (existing API - will add pagination)

# TanStack Query patterns from Phase 24:
- Query key convention: ['entity', workspaceId?, options]
- useQuery for data fetching
- QueryClient configured with staleTime: 30s, gcTime: 5min
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From existing code:
```typescript
// User row type
export type UserRow = {
  key: string;
  name: string;
  email: string;
  role: string;
  workspace: string;
  status: string;
};

// Ops request row type
export type OpsRequestRow = {
  id: string;
  title: string;
  status: RequestStatus;
  // ...
};

// TanStack Query hook pattern
export function useRequests(workspaceId?: string, options?: { page?: number; pageSize?: number; search?: string }) {
  return useQuery({
    queryKey: queryKeys.requests(workspaceId, options),
    queryFn: () => fetchRequests(workspaceId, options),
  });
}
```

From antd Table pagination:
```typescript
// Table pagination config
const paginationConfig = {
  current: page,
  pageSize: pageSize,
  total: total,
  showSizeChanger: true,
  pageSizeOptions: ['10', '25', '50'],
  showTotal: (total: number) => `Tổng ${total} bản ghi`,
  onChange: (newPage: number, newPageSize: number) => {
    // Update URL params
  },
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create usePaginationParams hook for URL sync</name>
  <files>src/lib/hooks/usePaginationParams.ts</files>
  <action>
Create usePaginationParams hook that syncs pagination and search state with URL query params:

1. Create hook: `usePaginationParams(defaultPageSize?: number)`
2. Use `useSearchParams` from next/navigation to read URL params
3. Use `useRouter` and `usePathname` for URL updates
4. Return and manage:
   - `page`: number (default: 1, from ?page param)
   - `pageSize`: number (default: 10, from ?pageSize param)
   - `search`: string (default: '', from ?search param)
   - `filters`: Record<string, string> (from ?filter_* params)
   - `setPage(page: number)`: updates ?page in URL
   - `setPageSize(size: number)`: updates ?pageSize in URL
   - `setSearch(term: string)`: updates ?search in URL
   - `setFilter(key: string, value: string)`: updates ?filter_[key] in URL
   - `clearFilters()`: removes all filter params

5. Use `nuqs` or manual URLSearchParams manipulation
6. Update URL imperatively with router.push to preserve scroll position

This satisfies PAGE-02: Page state synced to URL, SCH-04: Search state persisted in URL.
  </action>
  <verify>
<automated>Get-Content src/lib/hooks/usePaginationParams.ts | Select-String -Pattern "export.*usePaginationParams"</automated>
  </verify>
  <done>usePaginationParams hook exists with page, pageSize, search, filters state synced to/from URL params</done>
</task>

<task type="auto">
  <name>Task 2: Create useDebounce hook for search input</name>
  <files>src/lib/hooks/useDebounce.ts</files>
  <action>
Create useDebounce hook for debouncing search input:

1. Create hook: `useDebounce<T>(value: T, delay: number): T`
2. Use useState and useEffect pattern:
   - Track a debouncedValue state
   - useEffect sets up a setTimeout with the delay
   - Cleanup timeout on value change or unmount
3. Export both named exports:
   - `useDebounce<T>(value: T, delay: number): T`
   - `useDebouncedCallback<T extends (...args: unknown[]) => void>(fn: T, delay: number): T`

4. In usePaginationParams, use useDebounce internally:
   - Debounce search value changes by 300ms before updating URL

This satisfies SCH-03: Debounced search with 300ms delay.
  </action>
  <verify>
<automated>Get-Content src/lib/hooks/useDebounce.ts | Select-String -Pattern "export.*useDebounce"</automated>
  </verify>
  <done>useDebounce hook exists with configurable delay, default 300ms used for search</done>
</task>

<task type="auto">
  <name>Task 3: Add pagination support to API routes</name>
  <files>src/app/api/requests/route.ts, src/app/api/users/route.ts, src/app/api/audit/events/route.ts</files>
  <action>
Update all three API routes to support server-side pagination:

1. src/app/api/requests/route.ts:
   - Add pagination params: ?page=1&pageSize=10&search=&status=&workspaceId=
   - Use Prisma skip/take: skip = (page - 1) * pageSize, take = pageSize
   - Return: { data: Request[], total: number, page: number, pageSize: number }
   - Add search filter on title using contains (case-insensitive)
   - Add column filters via where clause

2. src/app/api/users/route.ts:
   - Add pagination params: ?page=1&pageSize=10&search=&role=
   - Use Prisma findMany with skip/take for pagination
   - Add search filter on name and email using contains
   - Add role filter
   - Return same structure as requests

3. src/app/api/audit/events/route.ts:
   - Add pagination params: ?page=1&pageSize=10&search=&actorId=&action=
   - Replace hardcoded take: 100 with skip/take pagination
   - Add search filter on action and metadataSummary
   - Return same structure

This satisfies PAGE-03: Server-side pagination for requests, users, audit tables.
  </action>
  <verify>
<automated>Get-Content src/app/api/requests/route.ts | Select-String -Pattern "skip.*take|pagination"</automated>
  </verify>
  <done>All three API routes support pagination with skip/take and return { data, total, page, pageSize }</done>
</task>

<task type="auto">
  <name>Task 4: Create enhanced query hooks with pagination</name>
  <files>src/hooks/useRequests.ts, src/hooks/useUsers.ts, src/hooks/useAuditEvents.ts</files>
  <action>
Update existing hooks and create new ones with pagination support:

1. Update src/hooks/useRequests.ts:
   - Enhance useRequests to accept pagination options
   - New signature: useRequests(options?: { page?: number; pageSize?: number; search?: string; filters?: Record<string, string> })
   - Query key includes all options: ['requests', workspaceId, options]
   - Fetch from /api/requests with query params

2. Create src/hooks/useUsers.ts:
   - Follow same pattern as useRequests
   - Export queryKeys.users() with options
   - Fetch from /api/users with pagination params

3. Create src/hooks/useAuditEvents.ts:
   - Follow same pattern
   - Export queryKeys.auditEvents() with options
   - Fetch from /api/audit/events with pagination params

This satisfies PAGE-01: Tables support pagination with page sizes, and establishes query hook pattern for all entities.
  </action>
  <verify>
<automated>Get-Content src/hooks/useRequests.ts | Select-String -Pattern "pageSize|search"</automated>
  </verify>
  <done>Three hooks exist: useRequests, useUsers, useAuditEvents - all with pagination options</done>
</task>

<task type="auto">
  <name>Task 5: Add global search bar to admin header</name>
  <files>src/app/admin/layout.tsx</files>
  <action>
Update admin layout header to include global search bar:

1. Import Input.Search from antd
2. Add search bar to Header section, between Breadcrumb and right side
3. Style search bar:
   - width: 300px
   - placeholder: "Tìm kiếm..."
   - onSearch: navigate to /admin/requests?search={value}

4. Add useDebounce to debounce search input:
   - Debounce 300ms before navigation
   - Use router.push to navigate with search param

5. Preserve existing Breadcrumb on the left side
6. Search bar on the right side of header

This satisfies SCH-01: Global search bar in admin header.
  </action>
  <verify>
<automated>Get-Content src/app/admin/layout.tsx | Select-String -Pattern "Input\.Search|Search"</automated>
  </verify>
  <done>Admin header displays Input.Search component, triggers navigation to requests page with search param</done>
</task>

<task type="auto">
  <name>Task 6: Create paginated admin table pages</name>
  <files>src/app/admin/requests/page.tsx, src/app/admin/users/page.tsx, src/app/admin/audit/page.tsx</files>
  <action>
Create or update three paginated admin pages:

1. src/app/admin/requests/page.tsx:
   - Create as client component with 'use client'
   - Use usePaginationParams for page, pageSize, search, filters
   - Use useRequests hook with pagination options
   - Render Table with pagination config:
     - current: page from URL
     - pageSize: pageSize from URL
     - pageSizeOptions: ['10', '25', '50']
     - showTotal: (total) => `Tổng ${total} bản ghi`
   - Add column filter dropdowns in table headers (Antd Table column filters)
   - Sync filter changes to URL via setFilter

2. src/app/admin/users/page.tsx:
   - Similar pattern using useUsers hook
   - Table with pagination + column filters for role

3. src/app/admin/audit/page.tsx:
   - Similar pattern using useAuditEvents hook
   - Table with pagination + column filters for action

For each page:
- Use Table from antd with pagination={false} (manual control)
- Add custom pagination footer with PageSize selector
- Show loading skeleton while fetching

This satisfies PAGE-01, PAGE-02, SCH-02 for all three tables.
  </action>
  <verify>
<automated>Get-Content src/app/admin/requests/page.tsx | Select-String -Pattern "pageSizeOptions"</automated>
  </verify>
  <done>Three admin pages exist with paginated tables, page sizes [10, 25, 50], and column filters synced to URL</done>
</task>

<task type="auto">
  <name>Task 7: Update existing table components with pagination</name>
  <files>src/app/admin/users/AdminUsersTable.tsx, src/app/admin/ops/AdminOpsTables.tsx</files>
  <action>
Update existing table components to support pagination:

1. AdminUsersTable.tsx:
   - Add pagination prop: { current: number; pageSize: number; total: number; onChange: (page, pageSize) => void }
   - Add filters prop: { role?: string }
   - Update Table to use pagination config
   - Add column filters for role via filters prop

2. AdminOpsTables.tsx:
   - Add pagination prop to requests table
   - Ops tables can remain unpaginated (smaller dataset)
   - Add column filters for status in requests table

This satisfies SCH-02: Column filters in table headers.
  </action>
  <verify>
<automated>Get-Content src/app/admin/users/AdminUsersTable.tsx | Select-String -Pattern "pagination|onChange"</automated>
  </verify>
  <done>Table components updated with pagination prop and column filters support</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) | User input (search, filters, page) crosses here via URL params |
| API server | Server receives pagination params and returns paginated data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Denial of Service | Pagination | mitigate | Limit max pageSize to 100, server enforces max |
| T-25-02 | Information Disclosure | Search | mitigate | Search only within user's workspace (server enforces via session) |
| T-25-03 | Tampering | URL params | accept | URL params are validated server-side before use in queries |
</threat_model>

<verification>
1. Verify pagination hooks and utilities exist
2. Verify API routes return paginated response with total count
3. Verify admin pages render with pagination controls
4. Verify URL params update on page/filter change
5. Verify search debounces 300ms before API call
6. Verify column filters appear in table headers
</verification>

<success_criteria>
1. Tables (requests, users, audit) display pagination controls with page sizes: 10, 25, 50
2. Current page number persists in URL query params (e.g., ?page=2&pageSize=25)
3. Pagination fetches only requested page from server (server-side)
4. Global search bar visible in admin header
5. Column filters available in table headers
6. Search input debounces by 300ms before triggering query
7. Search and filter state persists in URL params
</success_criteria>

<output>
After completion, create `.planning/phases/25-pagination-search/25-01-SUMMARY.md`
</output>
