# Phase 26 Plan: UI Brand Refresh

**Phase:** 26 - ui-refresh  
**Goal:** Update giao diện giống template `images/template.png` 100%  
**Depends on:** Phase 25

---

## Current State vs Template

| Element | Current (Phase 14) | Template |
|---------|-------------------|----------|
| Sidebar BG | #FFFFFF | **#E8E8E8 (gray)** |
| Logo Text | "GitNexus Legal" | **"PHÁP CHỂ"** (uppercase) |
| Logo Color | #0F766E (teal) | **#27AE60 (green)** |
| Nav Icons | @ant-design/icons | **gray icons, black text** |
| Nav Items | Users, Requests, Audit... | **Trang chủ, Biểu mẫu, Theo dõi, Tra cứu, Tài khoản** |
| Logout | Yes | **Đăng xuất (bottom)** |
| Header BG | #FFFFFF | **#FFFFFF** |
| Header Title | none | **"Quản lý hồ sơ"** |
| Header Right | Breadcrumb | **Bell icon + Avatar "NV"** |
| Content BG | #F8FAFC | **#F5F5F5** |
| Welcome | none | **"Xin chào, [Name]!"** |
| Stats Cards | none | **4 cards with colored left border** |
| Recent Table | none | **"Yêu cầu gần đây" table** |

---

## Implementation Tasks

### Task 1: Update Global Theme Tokens

**File:** `src/app/providers/theme-provider.tsx` (or `src/app/layout.tsx`)

```typescript
// SIDEBAR
colorBgSidebar: '#E8E8E8',  // NEW - gray sidebar bg

// BRAND
colorPrimary: '#27AE60',    // GREEN for logo/active nav
colorPrimaryButton: '#3498DB', // BLUE for primary buttons

// CONTENT
colorBgLayout: '#F5F5F5',   // CHANGED from #F8FAFC
```

### Task 2: Update Admin Layout Component

**File:** `src/app/(admin)/layout.tsx`

```tsx
<Layout style={{ minHeight: '100vh' }}>
  {/* SIDEBAR - Gray background */}
  <Sider 
    style={{ 
      background: '#E8E8E8',  // Gray sidebar
      borderRight: 'none'
    }}
    breakpoint="lg"
    collapsedWidth={64}
  >
    {/* Logo Section */}
    <div style={{ 
      padding: '20px 16px',
      borderBottom: '1px solid #D0D0D0',
      display: 'flex',
      alignItems: 'center',
      gap: '12px'
    }}>
      <EnvironmentOutlined style={{ fontSize: '24px', color: '#27AE60' }} />
      <span style={{ 
        fontSize: '18px', 
        fontWeight: 700, 
        color: '#27AE60',
        letterSpacing: '0.5px'
      }}>
        PHÁP CHỂ
      </span>
    </div>

    {/* Navigation - Gray icons, black text */}
    <Menu 
      mode="inline"
      style={{ 
        background: 'transparent',  // Transparent to show gray bg
        borderRight: 'none'
      }}
      items={navItems}
    />

    {/* Logout at bottom */}
    <div style={{ 
      position: 'absolute', 
      bottom: '60px', 
      left: 0, 
      right: 0,
      padding: '0 16px'
    }}>
      <Button 
        type="text" 
        icon={<LogoutOutlined />}
        style={{ 
          width: '100%', 
          justifyContent: 'flex-start',
          color: '#333'
        }}
      >
        Đăng xuất
      </Button>
    </div>
  </Sider>

  {/* MAIN CONTENT */}
  <Layout>
    {/* Header */}
    <Header style={{ 
      background: '#FFFFFF', 
      borderBottom: '1px solid #E0E0E0',
      padding: '0 24px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between'
    }}>
      <Title level={4} style={{ margin: 0 }}>
        Quản lý hồ sơ
      </Title>
      
      <Space>
        <Badge count={3}>
          <BellOutlined style={{ fontSize: '18px', cursor: 'pointer' }} />
        </Badge>
        <Avatar style={{ background: '#27AE60' }}>NV</Avatar>
      </Space>
    </Header>

    {/* Content */}
    <Content style={{ 
      padding: '24px',
      background: '#F5F5F5',  // Light gray content bg
      minHeight: 'calc(100vh - 64px)'
    }}>
      {children}
    </Content>
  </Layout>
</Layout>
```

### Task 3: Update Navigation Items

**File:** `src/lib/navigation/admin-nav.ts` (create/update)

```typescript
export const adminNavItems = [
  { key: 'home', icon: <HomeOutlined />, label: 'Trang chủ', path: '/vi/admin' },
  { key: 'templates', icon: <FileTextOutlined />, label: 'Biểu mẫu', path: '/vi/admin/templates' },
  { key: 'tracking', icon: <EyeOutlined />, label: 'Theo dõi', path: '/vi/admin/requests' },
  { key: 'search', icon: <SearchOutlined />, label: 'Tra cứu', path: '/vi/admin/vault' },
  { key: 'account', icon: <UserOutlined />, label: 'Tài khoản', path: '/vi/admin/users' },
];
```

### Task 4: Create Dashboard Stats Cards Component

**File:** `src/app/(admin)/admin/components/stats-cards.tsx`

```tsx
interface StatCardProps {
  title: string;
  count: number;
  borderColor: string;
  icon: React.ReactNode;
}

const StatCard = ({ title, count, borderColor, icon }: StatCardProps) => (
  <Card
    style={{ 
      borderLeft: `4px solid ${borderColor}`,
      borderRadius: '8px'
    }}
    styles={{ body: { padding: '20px' } }}
  >
    <Flex justify="space-between" align="center">
      <div>
        <Typography.Text type="secondary">{title}</Typography.Text>
        <Title level={2} style={{ margin: '8px 0 0' }}>{count}</Title>
      </div>
      <div style={{ fontSize: '32px', color: borderColor }}>
        {icon}
      </div>
    </Flex>
  </Card>
);

export const DashboardStats = () => (
  <Row gutter={[16, 16]}>
    <Col xs={24} sm={12} lg={6}>
      <StatCard title="Tổng hợp" count={150} borderColor="#8E8E8E" icon={<FileTextOutlined />} />
    </Col>
    <Col xs={24} sm={12} lg={6}>
      <StatCard title="Đang xử lý" count={25} borderColor="#F39C12" icon={<ClockCircleOutlined />} />
    </Col>
    <Col xs={24} sm={12} lg={6}>
      <StatCard title="Đã hoàn thành" count={120} borderColor="#27AE60" icon={<CheckCircleOutlined />} />
    </Col>
    <Col xs={24} sm={12} lg={6}>
      <StatCard title="Khẩn cấp" count={5} borderColor="#E74C3C" icon={<ExclamationCircleOutlined />} />
    </Col>
  </Row>
);
```

### Task 5: Update Admin Dashboard Page

**File:** `src/app/(admin)/admin/page.tsx`

```tsx
export default function AdminDashboard() {
  return (
    <div>
      {/* Welcome Section */}
      <Card styles={{ body: { padding: '24px' } }}>
        <Title level={3}>Xin chào, Quản trị viên!</Title>
        <Typography.Paragraph type="secondary">
          Chào mừng bạn đến với hệ thống quản lý pháp lý
        </Typography.Paragraph>
      </Card>

      {/* Stats Cards */}
      <div style={{ marginTop: '24px' }}>
        <DashboardStats />
      </div>

      {/* Recent Requests Table */}
      <Card 
        title="Yêu cầu gần đây" 
        style={{ marginTop: '24px' }}
        styles={{ body: { padding: 0 } }}
      >
        <Table 
          dataSource={recentRequests}
          columns={columns}
          pagination={false}
        />
      </Card>
    </div>
  );
}
```

### Task 6: Update CSS / Tailwind Config

**File:** `src/app/globals.css`

```css
:root {
  /* Sidebar */
  --sidebar-bg: #E8E8E8;
  --sidebar-border: #D0D0D0;
  
  /* Content */
  --content-bg: #F5F5F5;
  
  /* Brand */
  --brand-green: #27AE60;
  --button-blue: #3498DB;
  
  /* Stats Card Borders */
  --stat-gray: #8E8E8E;
  --stat-orange: #F39C12;
  --stat-green: #27AE60;
  --stat-red: #E74C3C;
}
```

---

## Files to Modify

| File | Changes |
|------|---------|
| `src/app/providers/theme-provider.tsx` | Update `colorBgLayout`, `colorPrimary`, add `colorPrimaryButton` |
| `src/app/(admin)/layout.tsx` | Complete rewrite with template layout |
| `src/lib/navigation/admin-nav.ts` | New 5-item nav + logout |
| `src/app/(admin)/admin/components/stats-cards.tsx` | NEW - 4 stat cards |
| `src/app/(admin)/admin/page.tsx` | Add welcome + stats + recent table |
| `src/app/globals.css` | Add CSS variables for template colors |

## Files to Create

| File | Purpose |
|------|---------|
| `src/app/(admin)/admin/components/stats-cards.tsx` | Stats dashboard cards |
| `src/lib/navigation/admin-nav.ts` | Navigation config |

---

## Verification

1. **Screenshot comparison** - Admin dashboard must match template exactly
2. **Color verification** - Use browser dev tools to check hex values
3. **E2E tests** - All existing tests must still pass

---

## Success Criteria

- [ ] Sidebar background: #E8E8E8 (exactly like template)
- [ ] Logo: "PHÁP CHỂ" uppercase in #27AE60
- [ ] Nav items: Trang chủ, Biểu mẫu, Theo dõi, Tra cứu, Tài khoản
- [ ] Logout button visible at bottom of sidebar
- [ ] Header: "Quản lý hồ sơ" title + bell + avatar
- [ ] Content BG: #F5F5F5
- [ ] Welcome message: "Xin chào, [Name]!"
- [ ] 4 Stats cards with colored left borders
- [ ] Recent requests table
