---
phase: 27-create-request
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/[workspaceSlug]/create/page.tsx
  - src/app/[locale]/customer/components/UserLayout.tsx
autonomous: true
requirements:
  - CUST-CREATE-01
  - CUST-CREATE-04
  - CUST-CREATE-05
must_haves:
  truths:
    - "User can access Create Request page via URL"
    - "Page renders with UserLayout wrapper (sidebar + topbar)"
    - "4-step wizard displays with step indicators"
    - "Sidebar summary panel shows workspace info"
    - "Checklist panel displays 4 preparation items"
  artifacts:
    - path: src/app/[locale]/[workspaceSlug]/create/page.tsx
      provides: Create request page route
      min_lines: 50
    - path: src/app/[locale]/customer/components/UserLayout.tsx
      provides: Sidebar with active nav item for create
      contains: Tạo hồ sơ
---

<objective>
Create the Create Request page route with UserLayout wrapper and 4-step wizard step 1 display.

Purpose: Establish the page foundation and wizard structure for the Create Request flow.
Output: Create page.tsx with wizard UI, UserLayout integration, sidebar navigation update.
</objective>

<context>
@.planning/phases/27-create-request/27-CONTEXT.md
@.planning/phases/27-create-request/27-UI-SPEC.md
@.planning/ROADMAP.md
@layout/user-create-request.html
@src/app/[locale]/customer/components/UserLayout.tsx
@src/app/[locale]/customer/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create page route with UserLayout</name>
  <files>src/app/[locale]/[workspaceSlug]/create/page.tsx</files>
  <read_first>
    - src/app/[locale]/customer/components/UserLayout.tsx
    - src/app/[locale]/customer/page.tsx
    - layout/user-create-request.html (lines 1-150 for structure)
  </read_first>
  <action>
Create the Create Request page at src/app/[locale]/[workspaceSlug]/create/page.tsx:

1. Import UserLayout component from @/[locale]/customer/components/UserLayout
2. Import useParams from next/navigation to get workspaceSlug
3. For demo, use hardcoded user data:
   - userName: "Mai Phương"
   - userRole: "Customer"
   - workspaceName: "Công ty An Phát"
4. Wrap content with UserLayout passing user/workspace props
5. Add page header with:
   - Title: "Gửi yêu cầu pháp lý"
   - Subtitle: "Trả lời vài câu hỏi để chuyên viên có đủ thông tin tiếp nhận hồ sơ và xử lý đúng SLA."
6. Add action buttons:
   - Ghost button: "Lưu nháp"
   - Primary button: "Xem hồ sơ nháp" (teal gradient)
7. Export default page component
  </action>
  <verify>
grep -l "Gửi yêu cầu pháp lý" src/app/\[locale\]/\[workspaceSlug\]/create/page.tsx && echo "Page created"
  </verify>
  <done>
Create Request page exists with UserLayout wrapper and page header with title/subtitle</done>
</task>

<task type="auto">
  <name>Task 2: Add create page navigation to UserLayout</name>
  <files>src/app/[locale]/customer/components/UserLayout.tsx</files>
  <read_first>
    - src/app/[locale]/customer/components/UserLayout.tsx
  </read_first>
  <action>
Update UserLayout.tsx navItems array to include Create Request:

1. Add new nav item:
   - href: `/${workspaceSlug}/create`
   - label: "Tạo hồ sơ"
   - icon: FileText
   - active: pathname.includes('/create')

Note: Import FileText from lucide-react if not already imported.
  </action>
  <verify>
grep -n "create" src/app/\[locale\]/customer/components/UserLayout.tsx | head -3
  </verify>
  <done>
UserLayout has navigation link to Create Request page</done>
</task>

<task type="auto">
  <name>Task 3: Create WizardSteps component</name>
  <files>src/components/create-request/WizardSteps.tsx</files>
  <read_first>
    - layout/user-create-request.html (lines 936-964 for wizard structure)
    - src/app/[locale]/customer/components/UserLayout.tsx (for patterns)
  </read_first>
  <action>
Create WizardSteps component at src/components/create-request/WizardSteps.tsx:

1. Create component with props: currentStep (1-4), steps array
2. Default steps: ["Dịch vụ", "Câu hỏi", "Tài liệu", "Kiểm tra"]
3. Render step indicators:
   - Step number in circle (28px diameter)
   - Active: background #10b981, text white
   - Inactive: background #eef2f7, text #64748b
   - Connecting lines: height 2px, background #dbe3ec
4. Use Tailwind classes for styling:
   - w-7 h-7 rounded-full for step circles
   - bg-emerald-500 for active, bg-gray-100 for inactive
5. Import and use Check icon (lucide-react) for completed steps
6. Export component with TypeScript interfaces
  </action>
  <verify>
ls src/components/create-request/WizardSteps.tsx && echo "Component created"
  </verify>
  <done>
WizardSteps component displays 4-step progress indicator with active/inactive styling</done>
</task>

</tasks>

<verification>
1. Navigate to /an-phat/create renders page
2. Wizard steps display with "Dịch vụ" active (green)
3. Other steps show gray with numbers
</verification>

<success_criteria>
- Page route created at /[locale]/[workspaceSlug]/create
- UserLayout displays with "Tạo hồ sơ" nav item active
- WizardSteps shows 4 steps with correct styling
</success_criteria>
