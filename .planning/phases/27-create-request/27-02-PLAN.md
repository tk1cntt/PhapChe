---
phase: 27-create-request
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/create-request/ServiceCard.tsx
  - src/components/create-request/ServiceTypeSelector.tsx
  - src/components/create-request/SummaryPanel.tsx
  - src/components/create-request/ChecklistPanel.tsx
autonomous: true
requirements:
  - CUST-CREATE-02
  - CUST-CREATE-03
  - CUST-CREATE-04
  - CUST-CREATE-05
must_haves:
  truths:
    - "User can see 5 service type options"
    - "User can select a service type"
    - "Sidebar shows summary of selected service"
    - "Sidebar shows checklist with 4 items"
  artifacts:
    - path: src/components/create-request/ServiceTypeSelector.tsx
      provide: Service selection with radio-style cards
      min_lines: 80
    - path: src/components/create-request/SummaryPanel.tsx
      provide: Sidebar summary panel
      min_lines: 50
---

<objective>
Create ServiceCard, ServiceTypeSelector, SummaryPanel, and ChecklistPanel components for wizard step 1.

Purpose: Enable user to select legal service type and display sidebar information.
Output: Service selection UI with 5 options and sidebar panels.
</objective>

<context>
@.planning/phases/27-create-request/27-CONTEXT.md
@.planning/phases/27-create-request/27-UI-SPEC.md
@layout/user-create-request.html (lines 470-600 for service cards, 1100-1200 for sidebar)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServiceCard component</name>
  <files>src/components/create-request/ServiceCard.tsx</files>
  <read_first>
    - layout/user-create-request.html (lines 477-545)
    - src/app/[locale]/customer/components/UserLayout.tsx (for patterns)
  </read_first>
  <action>
Create ServiceCard component at src/components/create-request/ServiceCard.tsx:

1. Create interface for service data:
   ```typescript
   interface ServiceOption {
     id: string;
     title: string;
     description: string;
     tags: Array<{label: string; variant: 'green' | 'blue' | 'orange' | 'purple' | 'red'}>;
     estimatedTime?: string;
   }
   ```

2. Component props: service (ServiceOption), selected (boolean), onSelect callback

3. Render radio-style card:
   - Radio circle (18px) with 1.5px border
   - Selected: border-color #087f78, background #ecfdf9, inner green circle
   - Hover: border-color rgba(8, 127, 120, 0.35), background #fbfffe

4. Tags display with correct colors per UI-SPEC:
   - green: bg-emerald-100 text-emerald-700
   - blue: bg-blue-100 text-blue-700
   - orange: bg-orange-100 text-orange-700
   - purple: bg-purple-100 text-purple-700
   - red: bg-red-100 text-red-700

5. Use Tailwind for styling, follow Inter font from template
  </action>
  <verify>
grep -l "ServiceCard" src/components/create-request/ServiceCard.tsx && echo "Created"
  </verify>
  <done>
ServiceCard displays service option with radio selection and colored tags</done>
</task>

<task type="auto">
  <name>Task 2: Create ServiceTypeSelector component</name>
  <files>src/components/create-request/ServiceTypeSelector.tsx</files>
  <read_first>
    - src/components/create-request/ServiceCard.tsx
    - layout/user-create-request.html (lines 988-1044 for service list)
  </read_first>
  <action>
Create ServiceTypeSelector component at src/components/create-request/ServiceTypeSelector.tsx:

1. Define 5 service options matching template (hardcoded for now):
   - Soạn hợp đồng đại lý (Khuyến nghị, green, 2-3 ngày)
   - Soạn hợp đồng lao động (Nhanh, blue)
   - Đăng ký nhãn hiệu (IP, purple)
   - Rà soát hợp đồng / NDA (Cần tài liệu, orange)
   - Dịch vụ khác / chưa rõ loại việc (Phân loại, red)

2. State: selectedId (default: first service)

3. Render:
   - Header: "Bạn cần hỗ trợ việc gì?" with FileText icon
   - Service list with ServiceCard components
   - First service pre-selected

4. Create card with border and styling per UI-SPEC:
   - border: 1px solid #dfe7f1
   - border-radius: 12px
   - padding: 17px
  </action>
  <verify>
grep -l "ServiceTypeSelector" src/components/create-request/ServiceTypeSelector.tsx && echo "Created"
  </verify>
  <done>
ServiceTypeSelector displays 5 service options with first one pre-selected</done>
</task>

<task type="auto">
  <name>Task 3: Create SummaryPanel component</name>
  <files>src/components/create-request/SummaryPanel.tsx</files>
  <read_first>
    - layout/user-create-request.html (lines 1103-1149 for summary panel)
  </read_first>
  <action>
Create SummaryPanel component at src/components/create-request/SummaryPanel.tsx:

1. Props: selectedService (string), workspaceName (string)

2. Display 4 summary items:
   - Dịch vụ đã chọn: [service name] with mini-icon (1)
   - Workspace: [name] · an-phat with mini-icon (W)
   - Dự kiến xử lý: 2-3 ngày làm việc sau khi đủ tài liệu with mini-icon (S)
   - Trạng thái: Hồ sơ nháp, chưa gửi cho chuyên viên with mini-icon (D)

3. Summary item styling:
   - padding: 14px
   - border: 1px solid #edf2f7
   - border-radius: 12px
   - background: #fbfdff

4. Mini-icon styling:
   - 34px x 34px
   - border-radius: 10px
   - background: linear-gradient(135deg, #d4f4ed, #eefbf8)
   - color: #087f78
   - font-weight: 800

5. Card shadow: box-shadow: 0 10px 25px rgba(15, 23, 42, 0.04)
  </action>
  <verify>
grep -l "SummaryPanel" src/components/create-request/SummaryPanel.tsx && echo "Created"
  </verify>
  <done>
SummaryPanel displays 4 summary items with mini-icons</done>
</task>

<task type="auto">
  <name>Task 4: Create ChecklistPanel component</name>
  <files>src/components/create-request/ChecklistPanel.tsx</files>
  <read_first>
    - layout/user-create-request.html (lines 1151-1197 for checklist)
  </read_first>
  <action>
Create ChecklistPanel component at src/components/create-request/ChecklistPanel.tsx:

1. Define 4 checklist items (all pre-checked):
   - Thông tin đối tác: Tên pháp lý, mã số thuế, địa chỉ, người đại diện.
   - Điều khoản thương mại: Chiết khấu, hoa hồng, doanh số, kỳ thanh toán.
   - Phạm vi đại lý: Khu vực, sản phẩm, quyền độc quyền hoặc không độc quyền.
   - Tài liệu liên quan: Báo giá, mẫu hợp đồng cũ, chính sách bán hàng nếu có.

2. Render each item:
   - Check icon: 22px circle, background #ccfbf1, color #0f766e
   - Title: font-weight 800
   - Description: text-sm text-gray-500

3. Use Check icon from lucide-react

4. Card styling matches SummaryPanel
  </action>
  <verify>
grep -l "ChecklistPanel" src/components/create-request/ChecklistPanel.tsx && echo "Created"
  </verify>
  <done>
ChecklistPanel displays 4 pre-checked preparation items</done>
</task>

</tasks>

<verification>
1. Service cards render with 5 options
2. First option selected by default (teal border, green background)
3. Tags display correct colors
4. Summary panel shows selected service
5. Checklist shows 4 items with green checkmarks
</verification>

<success_criteria>
- 5 service type cards render
- Selection updates summary panel
- Tags display correct colors per service type
- Sidebar panels display with correct styling
</success_criteria>
