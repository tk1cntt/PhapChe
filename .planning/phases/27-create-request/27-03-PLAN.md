---
phase: 27-create-request
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
  - 27-02
files_modified:
  - src/app/[locale]/[workspaceSlug]/create/page.tsx
  - src/components/create-request/RequestForm.tsx
  - prisma/schema.prisma
autonomous: true
requirements:
  - CUST-CREATE-06
must_haves:
  truths:
    - "User can see form fields for workspace, priority, contact info"
    - "User can fill in description"
    - "Page displays floating chat button"
    - "Action buttons work correctly"
  artifacts:
    - path: src/components/create-request/RequestForm.tsx
      provide: Form fields for request creation
      min_lines: 60
    - path: src/app/[locale]/[workspaceSlug]/create/page.tsx
      provide: Full page with form integration
      min_lines: 100
---

<objective>
Create form fields, integrate all components into page, add floating chat button.

Purpose: Complete wizard step 1 UI with form inputs and action buttons.
Output: Full Create Request page with service selection and form fields.
</objective>

<context>
@.planning/phases/27-create-request/27-CONTEXT.md
@.planning/phases/27-create-request/27-01-PLAN.md
@.planning/phases/27-create-request/27-02-PLAN.md
@layout/user-create-request.html (lines 1046-1100 for form, 750-768 for floating chat)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RequestForm component</name>
  <files>src/components/create-request/RequestForm.tsx</files>
  <read_first>
    - layout/user-create-request.html (lines 1046-1100)
    - src/app/[locale]/customer/components/UserLayout.tsx
  </read_first>
  <action>
Create RequestForm component at src/components/create-request/RequestForm.tsx:

1. Form fields matching template:
   - Workspace dropdown: options from context (Công ty An Phát, Công ty Minh Khang, Workspace nội bộ)
   - Priority dropdown: Thông thường, Cần xử lý sớm, Khẩn cấp
   - Contact name: pre-filled from user profile ("Mai Phương")
   - Contact email: pre-filled from user profile ("mai.phuong@anphat.vn")
   - Description textarea: placeholder text from template

2. Styling per UI-SPEC:
   - Select height: 44px, h-11
   - Input height: 44px, h-11
   - Textarea height: 94px, h-24
   - Border-radius: 8px
   - Padding: 0 13px
   - Focus: border-teal-600, shadow ring

3. Use Tailwind classes:
   - border border-gray-200 rounded-lg
   - focus:border-teal-600 focus:ring-2 focus:ring-teal-500/20
   - text-sm text-gray-700

4. Section styling:
   - Border-top: 1px solid #e5e7eb
   - Section title: font-bold text-lg

5. Add action buttons:
   - Ghost button "Quay lại dashboard"
   - Ghost button "Lưu nháp"
   - Primary button "Tiếp tục →" (teal gradient)
  </action>
  <verify>
grep -l "RequestForm" src/components/create-request/RequestForm.tsx && echo "Created"
  </verify>
  <done>
RequestForm displays all form fields with correct styling</done>
</task>

<task type="auto">
  <name>Task 2: Create FloatingChatButton component</name>
  <files>src/components/create-request/FloatingChatButton.tsx</files>
  <read_first>
    - layout/user-create-request.html (lines 750-768)
  </read_first>
  <action>
Create FloatingChatButton component at src/components/create-request/FloatingChatButton.tsx:

1. Fixed position: bottom-5 right-5
2. Styling:
   - Min-width: 118px
   - Height: 48px
   - Border-radius: 999px (pill shape)
   - Background: linear-gradient(180deg, #ef4444, #dc2626)
   - Border: 3px solid #facc15
   - Box-shadow: 0 12px 26px rgba(15, 23, 42, 0.22)

3. Content:
   - Circle icon (28px) with rgba(255,255,255,0.18) background
   - Text: "Hỗ trợ"

4. Use Tailwind for positioning and colors
  </action>
  <verify>
grep -l "FloatingChatButton" src/components/create-request/FloatingChatButton.tsx && echo "Created"
  </verify>
  <done>
FloatingChatButton displays with red gradient and yellow border</done>
</task>

<task type="auto">
  <name>Task 3: Integrate components into page</name>
  <files>src/app/[locale]/[workspaceSlug]/create/page.tsx</files>
  <read_first>
    - src/app/[locale]/[workspaceSlug]/create/page.tsx
    - src/components/create-request/ServiceTypeSelector.tsx
    - src/components/create-request/SummaryPanel.tsx
    - src/components/create-request/ChecklistPanel.tsx
    - src/components/create-request/RequestForm.tsx
    - src/components/create-request/FloatingChatButton.tsx
  </read_first>
  <action>
Update create/page.tsx to integrate all components:

1. Imports:
   - WizardSteps from @/components/create-request/WizardSteps
   - ServiceTypeSelector from @/components/create-request/ServiceTypeSelector
   - SummaryPanel from @/components/create-request/SummaryPanel
   - ChecklistPanel from @/components/create-request/ChecklistPanel
   - RequestForm from @/components/create-request/RequestForm
   - FloatingChatButton from @/components/create-request/FloatingChatButton

2. State management:
   - selectedService state (default: first service)
   - Pass setSelectedService to ServiceTypeSelector and SummaryPanel

3. Layout structure:
   - 2-column grid: 1fr 360px (use Tailwind grid grid-cols-[1fr_360px])
   - Gap: 24px

4. Main column content:
   - WizardSteps (currentStep={1})
   - Card with border and shadow:
     - Header with title and badge "Bước 1 / 4"
     - ServiceTypeSelector
     - RequestForm
   - Use shadow: shadow-lg for main card

5. Sidebar column:
   - SummaryPanel
   - ChecklistPanel

6. FloatingChatButton at bottom of page
  </action>
  <verify>
grep -c "import.*ServiceTypeSelector" src/app/\[locale\]/\[workspaceSlug\]/create/page.tsx
  </verify>
  <done>
Create Request page displays full wizard step 1 with all components</done>
</task>

</tasks>

<verification>
1. Page renders with 2-column layout
2. ServiceTypeSelector shows 5 options
3. SummaryPanel shows selected service
4. ChecklistPanel shows 4 items
5. Form fields display with correct styling
6. Floating chat button visible at bottom-right
</verification>

<success_criteria>
- All components render on page
- Service selection updates summary
- Form fields are functional
- Floating chat button styled correctly
</success_criteria>
