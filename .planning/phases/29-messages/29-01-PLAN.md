---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/[workspaceSlug]/messages/page.tsx
  - src/app/[locale]/[workspaceSlug]/messages/messages.css
  - src/app/[locale]/customer/components/UserLayout.tsx
autonomous: true
requirements:
  - CUST-MSG-01
  - CUST-MSG-02
  - CUST-MSG-03
  - CUST-MSG-04

must_haves:
  truths:
    - "User sees 3-column message layout with proper grid structure"
    - "User sees 4 stat cards with message statistics"
    - "User can navigate to Messages page via sidebar"
  artifacts:
    - path: "src/app/[locale]/[workspaceSlug]/messages/page.tsx"
      provides: "Messages page route with UserLayout wrapper"
      min_lines: 80
    - path: "src/app/[locale]/[workspaceSlug]/messages/messages.css"
      provides: "Message layout CSS with 3-column grid, panel styles"
      min_lines: 50
  key_links:
    - from: "src/app/[locale]/customer/components/UserLayout.tsx"
      to: "src/app/[locale]/[workspaceSlug]/messages/page.tsx"
      via: "UserLayout nav item includes /messages href"
---

<objective>
Create the Messages page route with UserLayout wrapper, 3-column grid layout, and 4 stat cards. This establishes the foundation for thread list, chat panel, and info panel components.
</objective>

<context>
@layout/user-messages.html
@src/app/[locale]/customer/page.tsx
@src/app/[locale]/customer/components/UserLayout.tsx
@src/app/[locale]/customer/components/StatCard.tsx
@src/app/[locale]/customer/components/FloatingChatButton.tsx
</context>

<interfaces>
From UserLayout.tsx:
```typescript
export interface UserLayoutProps {
  children: React.ReactNode;
  userName: string;
  userRole: string;
  workspaceName: string;
  workspaceSlug: string;
}
```

From StatCard.tsx:
```typescript
export interface StatCardProps {
  title: string;
  value: number;
  description: string;
  icon: 'file' | 'clock' | 'check' | 'folder' | 'alert';
  variant: 'blue' | 'green' | 'orange' | 'purple' | 'red';
}
```

From FloatingChatButton.tsx (Phase 26):
```typescript
interface FloatingChatButtonProps {
  notificationCount?: number;
  notificationText?: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create messages page route with UserLayout wrapper</name>
  <files>
    - src/app/[locale]/[workspaceSlug]/messages/page.tsx
    - src/app/[locale]/[workspaceSlug]/messages/messages.css
  </files>
  <read_first>
    - src/app/[locale]/customer/page.tsx
    - src/app/[locale]/customer/components/UserLayout.tsx
    - src/app/[locale]/[workspaceSlug]/cases/page.tsx
  </read_first>
  <action>
    Create directory structure `src/app/[locale]/[workspaceSlug]/messages/`

    Create `page.tsx` with:
    1. Async server component with Prisma queries for message stats
    2. Import and use UserLayout wrapper with session data
    3. Create 3-column message layout grid using CSS class `.message-layout`
    4. Add 4 StatCard components with stats from database counts
    5. Import FloatingChatButton with notification count
    6. Export async function matching route: `export default async function MessagesPage()`

    Create `messages.css` with:
    1. `.message-layout` - grid: 360px 1fr 320px, gap 20px
    2. `.thread-list`, `.chat-panel`, `.info-panel` - panel styles: background #fff, border 1px solid var(--border), border-radius 15px, box-shadow var(--soft-shadow), overflow hidden
    3. Stat cards grid: grid-template-columns repeat(4, 1fr), gap 18px
    4. Page header: h1 31px, subtitle 15px color #5f6e83
    5. Import messages.css in page.tsx

    Stats to compute from database:
    - conversations: total unique message threads count
    - unread: messages where recipientId = current user and isRead = false
    - replied: messages sent by current user in last 30 days
    - attachments: related documents/files count (placeholder 0 for now)

    All stats values from Prisma queries, not hardcoded.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|Error|Error:)"; echo "Build check passed"</automated>
  </verify>
  <done>
    Messages page renders at /[workspaceSlug]/messages with UserLayout, 3-column grid, and 4 stat cards showing values from database.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update UserLayout navigation to include messages route</name>
  <files>src/app/[locale]/customer/components/UserLayout.tsx</files>
  <read_first>src/app/[locale]/customer/components/UserLayout.tsx</read_first>
  <action>
    Update UserLayout navItems array to use [workspaceSlug] route pattern:

    Find existing messages nav item and update href:
    ```typescript
    { href: `/${workspaceSlug}/messages`, label: 'Tin nhan', icon: MessageSquare, active: pathname.includes('/messages') },
    ```

    The UserLayout already has MessageSquare imported. No other changes needed.
  </action>
  <verify>
    <automated>grep -n "messages" src/app/[locale]/customer/components/UserLayout.tsx</automated>
  </verify>
  <done>
    UserLayout sidebar navigation includes /messages link with workspaceSlug parameter.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create sample seed data for message threads</name>
  <files>
    - prisma/seed-messages.ts
    - prisma/seed.ts
  </files>
  <read_first>
    - prisma/schema.prisma
    - prisma/seed.ts
  </read_first>
  <action>
    Create seed script `prisma/seed-messages.ts` to add sample message data:

    Sample threads (per D-17):
    - Thread 1: HL - "REQ-2026-019 · Phu luc SLA" - last message 12 phut ago
    - Thread 2: QD - "REQ-2026-021 · Hop dong dich vu" - last message 45 phut ago
    - Thread 3: MT - "Thong bao workspace" - last message 2h ago
    - Thread 4: KA - "REQ-2026-012 · Nhan hieu" - last message 1d ago

    Each thread should have:
    - At least 2-3 message records in Message table
    - senderId = specialist user
    - recipientId = customer user
    - Sample content matching template (in Vietnamese)
    - isRead status varying per thread

    Add export to prisma/seed.ts:
    ```typescript
    await seedMessages();
    ```

    Run seed script after creating:
    ```bash
    npx prisma db seed
    ```
  </action>
  <verify>
    <automated>npx ts-node --compiler-options '{"module":"CommonJS"}' prisma/seed-messages.ts 2>&1 | head -20</automated>
  </verify>
  <done>
    Database has sample message threads and messages for 4 sample users (HL, QD, MT, KA) visible in UI.
  </done>
</task>

</tasks>

<verification>
- [ ] Page renders at correct route /[workspaceSlug]/messages
- [ ] UserLayout wrapper displays sidebar and topbar
- [ ] 3-column grid layout visible with proper widths (360px | 1fr | 320px)
- [ ] 4 stat cards display with icons and database values
- [ ] FloatingChatButton visible in bottom-right corner
- [ ] Sidebar navigation highlights Messages item when on messages page
</verification>

<success_criteria>
1. Messages page accessible at `/[workspaceSlug]/messages`
2. UserLayout displays with sidebar (262px) + topbar (76px)
3. 3-column message layout grid with gap 20px
4. Stat cards show: Cuộc hội thoại, Tin chưa đọc, Đã phản hồi, Tệp đính kèm
5. Stat values from Prisma database queries (not hardcoded)
6. Sample message data seeded in database
7. FloatingChatButton visible with notification count
</success_criteria>

<output>
After completion, create `.planning/phases/29-messages/29-01-SUMMARY.md`
</output>
