---
phase: 30-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/[workspaceSlug]/workspace/page.tsx
  - prisma/seed.sql
autonomous: true
requirements:
  - CUST-WS-01
  - CUST-WS-02
  - CUST-WS-03
  - CUST-WS-04
user_setup: []

must_haves:
  truths:
    - "User can access workspace page at /{workspaceSlug}/workspace"
    - "User sees 4 stat cards with workspace data from database"
    - "User sees workspace banner with company name from database"
    - "User sees member grid with members from database"
  artifacts:
    - path: "src/app/[locale]/[workspaceSlug]/workspace/page.tsx"
      provides: "Page route with UserLayout and database queries"
      exports: ["default export"]
    - path: "prisma/seed.sql"
      provides: "Seed data for workspace an-phat with members"
      contains: "an-phat, WorkspaceMembership, LegalRequest, VaultFile"
  key_links:
    - from: "src/app/[locale]/[workspaceSlug]/workspace/page.tsx"
      to: "prisma/schema.prisma"
      via: "Prisma queries"
      pattern: "prisma\\.workspace|prisma\\.workspaceMembership"
---

<objective>
Tao trang workspace voi route, layout, va database queries. Trang hien thi 4 stat cards, banner, member grid, permission panel, va resource table tu database.

Purpose: Foundation cho workspace page - route, layout wrapper, database queries, seed data
Output: Workspace page voi data tu database (khong co hardcoded values)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-workspace/30-CONTEXT.md
@layout/user-workspace.html
@prisma/schema.prisma
@src/app/[locale]/customer/components/UserLayout.tsx

## Template Structure (layout/user-workspace.html)

```html
<!-- Page structure -->
<div class="app">
  <aside class="sidebar">...</aside>
  <main class="main">
    <header class="topbar">...</header>
    <section class="content">
      <div class="page-header">
        <h1>Workspace</h1>
        <p class="subtitle">Xem thành viên...</p>
      </div>
      <div class="workspace-banner">...</div>
      <div class="stats">4 stat cards</div>
      <div class="member-grid">member panel + permission panel</div>
      <div class="table-card">resource table</div>
    </section>
  </main>
  <div class="floating-chat">...</div>
</div>
```

## Database Schema

Prisma models can query:
- `prisma.workspace.findFirst({ where: { slug: workspaceSlug } })`
- `prisma.workspaceMembership.findMany({ where: { workspaceId }, include: { user: true } })`
- `prisma.legalRequest.count({ where: { workspaceId } })`
- `prisma.vaultFile.count({ where: { workspaceId } })`
- `prisma.auditEvent.count({ where: { workspaceId } })`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace page route</name>
  <files>src/app/[locale]/[workspaceSlug]/workspace/page.tsx</files>
  <read_first>
    - src/app/[locale]/customer/components/UserLayout.tsx
    - prisma/schema.prisma (Workspace, WorkspaceMembership, LegalRequest, VaultFile models)
    - layout/user-workspace.html
  </read_first>
  <action>
Tao file `src/app/[locale]/[workspaceSlug]/workspace/page.tsx` voi:

1. **Imports:**
   - React, useEffect, useState
   - UserLayout tu `../../customer/components/UserLayout`
   - Prisma client tu `@/lib/prisma`
   - Lucide icons (Building2, Users, FileText, Lock)

2. **Types:**
   ```typescript
   interface WorkspaceData {
     id: string;
     name: string;
     slug: string;
     isActive: boolean;
   }

   interface MemberData {
     id: string;
     name: string;
     email: string;
     role: string;
     isActive: boolean;
   }

   interface StatsData {
     memberCount: number;
     activeMemberCount: number;
     invitedMemberCount: number;
     requestCount: number;
     processingRequestCount: number;
     vaultFileCount: number;
     auditEventCount: number;
   }
   ```

3. **Page Component:**
   - Nhan `params: { locale: string; workspaceSlug: string }`
   - Fetch workspace tu database: `prisma.workspace.findFirst({ where: { slug: workspaceSlug } })`
   - Fetch members: `prisma.workspaceMembership.findMany({ where: { workspaceId }, include: { user: true } })`
   - Count requests: `prisma.legalRequest.count({ where: { workspaceId } })`
   - Count processing requests: `prisma.legalRequest.count({ where: { workspaceId, status: 'in_progress' } })`
   - Count vault files: `prisma.vaultFile.count({ where: { workspaceId } })`
   - Fetch latest audit event timestamp: `prisma.auditEvent.findFirst({ where: { workspaceId }, orderBy: { createdAt: 'desc' } })`

4. **Stat Cards (4 cards):**
   - Card 1 (green/Building2): Workspace - Active - hien thi `workspace.isActive ? 'Active' : 'Inactive'`
   - Card 2 (blue/Users): Thanh vien - hien thi `memberCount` voi description `{activeMemberCount} active, {invitedMemberCount} invited`
   - Card 3 (orange/FileText): Ho so - hien thi `requestCount` voi description `{processingRequestCount} dang xu ly`
   - Card 4 (purple/Lock): Vault scope - hien thi `vaultFileCount > 0 ? 96 : 0` (percentage placeholder)

5. **Workspace Banner:**
   - Gradient background: `background: linear-gradient(135deg,#fff,#f0fdfa)`
   - Hien thi workspace.name: "Cong ty An Phat"
   - Description: "Workspace rieng cho ho so, tai lieu, thanh vien va phan quyen du lieu cua cong ty."
   - Button: "Moi thanh vien" (visual only)

6. **Member Grid (2-column):**
   - Left panel: "Thanh vien workspace" voi "Quan ly ->" link
   - Right panel: "Quyen & bao mat"
   - Member items: map qua members, hien thi avatar (initials), name, email, role badge
   - Permission scopes: 3 items (Tenant isolation, Vai tro hien tai, Audit)

7. **Resource Table:**
   - 5 columns: Tai nguyen, So luong, Trang thai, Cap nhat gan nhat, Thao tac
   - 3 rows: Ho so phap ly, Tai lieu vault, Loi moi thanh vien
   - Status badges: green cho Healthy/Encrypted, orange cho Pending

8. **Floating Chat Button:**
   - Reuse pattern tu Phase 26 FloatingChatButton
   - Position: fixed right:22px, bottom:20px
   - Red gradient background, "2 Tin moi" badge

9. **Export:**
   ```typescript
   export default async function WorkspacePage({ params }: { params: { locale: string; workspaceSlug: string } }) {
     // ... fetch data
     return <UserLayout userName="Mai Phuong" userRole="Customer" workspaceName={workspace.name} workspaceSlug={workspace.slug}>
       {/* page content */}
     </UserLayout>
   }
   ```

Luu y: StatCard component chi nhan `value: number` nhung stat Workspace can hien thi text "Active". De xu ly:
- Tao wrapper component hoac su dung conditional rendering
- Stat 1: Hien thi text "Active" thay vi so
  </action>
  <verify>
    <automated>grep -r "workspace/page.tsx" src/app --include="*.tsx" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - [ ] File ton tai tai `src/app/[locale]/[workspaceSlug]/workspace/page.tsx`
    - [ ] Import UserLayout tu customer/components
    - [ ] Fetch workspace, members, requests, vault files tu database
    - [ ] Co 4 stat cards hien thi du lieu tu database
    - [ ] Co workspace banner hien thi workspace.name
    - [ ] Co member grid hien thi members tu database
    - [ ] Co permission panel voi 3 scope items
    - [ ] Co resource table voi 5 columns
    - [ ] Co floating chat button
  </acceptance_criteria>
  <done>Workspace page route tao thanh cong voi database queries cho tat ca data</done>
</task>

<task type="auto">
  <name>Task 2: Update seed data for workspace</name>
  <files>prisma/seed.sql</files>
  <read_first>
    - prisma/schema.prisma
    - prisma/seed.sql (neu ton tai)
  </read_first>
  <action>
Cap nhat hoac tao `prisma/seed.sql` voi data cho workspace "an-phat":

1. **Workspace record:**
   ```sql
   INSERT OR IGNORE INTO Workspace (id, name, slug, isActive, createdAt, updatedAt)
   VALUES ('ws-anphat-001', 'Cong ty An Phat', 'an-phat', 1, datetime('now'), datetime('now'));
   ```

2. **Users (4 sample members):**
   ```sql
   INSERT OR IGNORE INTO User (id, email, name, isActive, emailVerified, createdAt, updatedAt)
   VALUES
     ('user-mp-001', 'mai.phuong@anphat.vn', 'Mai Phuong', 1, 1, datetime('now'), datetime('now')),
     ('user-la-001', 'linh.anh@anphat.vn', 'Linh Anh', 1, 1, datetime('now'), datetime('now')),
     ('user-vt-001', 'trang.van@anphat.vn', 'Van Trang', 1, 1, datetime('now'), datetime('now')),
     ('user-hn-001', 'nam.hoang@anphat.vn', 'Nam Hoang', 1, 1, datetime('now'), datetime('now'));
   ```

3. **WorkspaceMembership (4 members):**
   ```sql
   INSERT OR IGNORE INTO WorkspaceMembership (id, userId, workspaceId, role, isActive, createdAt, updatedAt)
   VALUES
     ('wm-001', 'user-mp-001', 'ws-anphat-001', 'owner', 1, datetime('now'), datetime('now')),
     ('wm-002', 'user-la-001', 'ws-anphat-001', 'finance', 1, datetime('now'), datetime('now')),
     ('wm-003', 'user-vt-001', 'ws-anphat-001', 'viewer', 1, datetime('now'), datetime('now')),
     ('wm-004', 'user-hn-001', 'ws-anphat-001', 'customer', 0, datetime('now'), datetime('now'));
   ```

4. **LegalRequests (12 requests):**
   Tao 12 requests voi various statuses:
   ```sql
   INSERT OR IGNORE INTO LegalRequest (id, workspaceId, title, status, createdById, createdAt, updatedAt)
   VALUES
     ('req-001', 'ws-anphat-001', 'Hop dong thue nha', 'in_progress', 'user-mp-001', datetime('now'), datetime('now')),
     ('req-002', 'ws-anphat-001', 'NDA cong ty ABC', 'in_progress', 'user-mp-001', datetime('now'), datetime('now')),
     ('req-003', 'ws-anphat-001', 'Phu luc hop dong', 'pending_review', 'user-la-001', datetime('now'), datetime('now'));
   -- Tao them 9 requests nua voi cac trang thai khac nhau
   ```

5. **VaultFiles (36 files):**
   ```sql
   INSERT OR IGNORE INTO VaultFile (id, requestId, workspaceId, actorId, filename, fileKind, createdAt)
   SELECT 'vf-' || substr(hex(randomblob(4)), 1, 8), 'req-001', 'ws-anphat-001', 'user-mp-001', 'Hop_dong_' || n || '.pdf', 'contract', datetime('now')
   FROM (SELECT 1 AS n UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5
         UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10
         UNION SELECT 11 UNION SELECT 12 UNION SELECT 13 UNION SELECT 14 UNION SELECT 15
         UNION SELECT 16 UNION SELECT 17 UNION SELECT 18 UNION SELECT 19 UNION SELECT 20
         UNION SELECT 21 UNION SELECT 22 UNION SELECT 23 UNION SELECT 24 UNION SELECT 25
         UNION SELECT 26 UNION SELECT 27 UNION SELECT 28 UNION SELECT 29 UNION SELECT 30
         UNION SELECT 31 UNION SELECT 32 UNION SELECT 33 UNION SELECT 34 UNION SELECT 35
         UNION SELECT 36);
   ```

6. **AuditEvents (for timestamps):**
   ```sql
   INSERT OR IGNORE INTO AuditEvent (id, actorId, workspaceId, action, targetType, targetId, createdAt)
   VALUES
     ('ae-001', 'user-mp-001', 'ws-anphat-001', 'view', 'request', 'req-001', '2026-06-10 21:15:00'),
     ('ae-002', 'user-la-001', 'ws-anphat-001', 'upload', 'vault_file', 'vf-00000001', '2026-06-10 20:48:00');
   ```

Chay seed script:
```bash
sqlite3 data/bot.db < prisma/seed.sql
```
  </action>
  <verify>
    <automated>sqlite3 data/bot.db "SELECT COUNT(*) FROM WorkspaceMembership WHERE workspaceId = 'ws-anphat-001';"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Workspace "an-phat" co trong database
    - [ ] 4 WorkspaceMembership records cho 4 thanh vien
    - [ ] 12 LegalRequest records trong workspace
    - [ ] 36 VaultFile records trong workspace
    - [ ] AuditEvent records voi timestamps phu hop
  </acceptance_criteria>
  <done>Seed data tao thanh cong cho workspace an-phat</done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. Navigate to `/{workspaceSlug}/workspace` - page loads without error
2. Verify 4 stat cards display with correct values from database
3. Verify workspace banner shows "Cong ty An Phat"
4. Verify member grid shows 4 members with role badges
5. Verify permission panel shows 3 scope items
6. Verify resource table shows 3 rows with correct data
7. Verify floating chat button visible
</verification>

<success_criteria>
- Workspace page route tao tai `src/app/[locale]/[workspaceSlug]/workspace/page.tsx`
- Tat ca data lay tu database (Workspace, WorkspaceMembership, LegalRequest, VaultFile, AuditEvent)
- Seed script chay thanh cong voi data mau
- Page hien thi dung cau truc nhu template HTML
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/30-workspace/30-01-SUMMARY.md`
</output>
