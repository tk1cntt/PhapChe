---
phase: 30-workspace
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01-PLAN.md
  - 30-02-PLAN.md
files_modified:
  - src/app/[locale]/customer/components/Workspace/ResourceTable.tsx
  - src/app/[locale]/customer/components/Workspace/index.ts
  - src/app/[locale]/[workspaceSlug]/workspace/page.tsx
autonomous: true
requirements:
  - CUST-WS-01
  - CUST-WS-04
user_setup: []

must_haves:
  truths:
    - "User sees resource table with 5 columns (Tai nguyen, So luong, Trang thai, Cap nhat gan nhat, Thao tac)"
    - "User sees 3 resource rows: Ho so phap ly (12 ho so, Healthy), Tai lieu vault (36 tep, Encrypted), Loi moi thanh vien (2 loi moi, Pending)"
    - "All workspace page components integrated into final page"
  artifacts:
    - path: "src/app/[locale]/customer/components/Workspace/ResourceTable.tsx"
      provides: "Resource table with 5 columns and 3 rows"
      exports: ["ResourceTable"]
    - path: "src/app/[locale]/customer/components/Workspace/index.ts"
      provides: "Re-exports for all workspace components"
      exports: ["WorkspaceBanner", "StatsGrid", "MemberGrid", "ResourceTable"]
    - path: "src/app/[locale]/[workspaceSlug]/workspace/page.tsx"
      provides: "Integrated workspace page with all components"
  key_links:
    - from: "page.tsx"
      to: "Workspace/index.ts"
      via: "import all components"
      pattern: "import.*Workspace.*from"
---

<objective>
Tao ResourceTable component, integrate tat ca components vao page, va tao index exports. Day la plan cuoi cung de hoan thanh workspace page.

Purpose: Integration cuoi cung - resource table + page composition
Output: Hoan chinh workspace page voi tat ca components
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@layout/user-workspace.html
@src/app/[locale]/customer/components/Badge.tsx
@src/app/[locale]/customer/components/Workspace/WorkspaceBanner.tsx
@src/app/[locale]/customer/components/Workspace/StatsGrid.tsx
@src/app/[locale]/customer/components/Workspace/MemberGrid.tsx

## Template CSS (layout/user-workspace.html)

```css
/* Resource Table */
.table-card { background: #fff; border: 1px solid var(--border); border-radius: 15px; box-shadow: var(--soft-shadow); overflow: hidden; }
.table-head, .table-row { display: grid; }
.table-head { background: linear-gradient(180deg,#f8fafc,#f5f7fb); border-bottom: 1px solid var(--border); grid-template-columns: 1.1fr .9fr .9fr 1fr .8fr; }
.table-row { min-height: 72px; border-bottom: 1px solid var(--border); background: #fff; }
.table-row:hover { background: #fbfdff; }
.th { min-height: 54px; display: flex; align-items: center; padding: 0 18px; color: #59687e; font-size: 14px; font-weight: 700; border-right: 1px solid var(--border); }
.th:last-child { border-right: none; }
.td { display: flex; align-items: center; padding: 0 18px; font-size: 14px; color: #0f172a; font-weight: 500; border-right: 1px solid var(--border); }
.td:last-child { border-right: none; }
.action-link { color: #087f78; font-weight: 800; text-decoration: none; white-space: nowrap; }

/* Stack */
.stack strong { display: block; font-size: 14px; margin-bottom: 4px; color: #0f172a; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.stack span { display: block; font-size: 12px; color: #64748b; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

/* Floating Chat */
.floating-chat { position: fixed; right: 22px; bottom: 20px; min-width: 118px; height: 48px; border-radius: 999px; background: linear-gradient(180deg,#ef4444,#dc2626); color: #fff; border: 3px solid #facc15; box-shadow: 0 12px 26px rgba(15,23,42,.22); display: flex; align-items: center; justify-content: center; gap: 8px; font-weight: 800; padding: 0 14px; }
.floating-chat span:first-child { width: 28px; height: 28px; border-radius: 50%; background: rgba(255,255,255,.18); display: flex; align-items: center; justify-content: center; }
```

## Template Data (layout/user-workspace.html)

```html
<div class="table-card">
  <div class="table-head">
    <div class="th">Tài nguyên</div>
    <div class="th">Số lượng</div>
    <div class="th">Trạng thái</div>
    <div class="th">Cập nhật gần nhất</div>
    <div class="th">Thao tác</div>
  </div>
  <div class="table-row">
    <div class="td"><div class="stack"><strong>Hồ sơ pháp lý</strong><span>Requests trong workspace</span></div></div>
    <div class="td">12 hồ sơ</div>
    <div class="td"><span class="badge green">Healthy</span></div>
    <div class="td">10/06/2026 21:15</div>
    <div class="td"><a class="action-link">Mở →</a></div>
  </div>
  <div class="table-row">
    <div class="td"><div class="stack"><strong>Tài liệu vault</strong><span>Hợp đồng, NDA, phụ lục</span></div></div>
    <div class="td">36 tệp</div>
    <div class="td"><span class="badge green">Encrypted</span></div>
    <div class="td">10/06/2026 20:48</div>
    <div class="td"><a class="action-link">Mở →</a></div>
  </div>
  <div class="table-row">
    <div class="td"><div class="stack"><strong>Lời mời thành viên</strong><span>Đang chờ xác nhận email</span></div></div>
    <div class="td">2 lời mời</div>
    <div class="td"><span class="badge orange">Pending</span></div>
    <div class="td">09/06/2026 16:00</div>
    <div class="td"><a class="action-link">Gửi lại →</a></div>
  </div>
</div>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResourceTable component</name>
  <files>src/app/[locale]/customer/components/Workspace/ResourceTable.tsx</files>
  <read_first>
    - layout/user-workspace.html
    - src/app/[locale]/customer/components/Badge.tsx
  </read_first>
  <action>
Tao file `src/app/[locale]/customer/components/Workspace/ResourceTable.tsx`:

```typescript
'use client';

import React from 'react';
import Link from 'next/link';
import { Badge } from '../Badge';

export interface ResourceData {
  requestCount: number;
  vaultFileCount: number;
  invitedCount: number;
  lastRequestUpdate: string | null;
  lastVaultUpdate: string | null;
  lastInviteUpdate: string | null;
}

export interface ResourceTableProps {
  resources: ResourceData;
}

function formatDate(dateStr: string | null): string {
  if (!dateStr) return '-';
  const date = new Date(dateStr);
  const day = String(date.getDate()).padStart(2, '0');
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const year = date.getFullYear();
  const hours = String(date.getHours()).padStart(2, '0');
  const minutes = String(date.getMinutes()).padStart(2, '0');
  return `${day}/${month}/${year} ${hours}:${minutes}`;
}

export function ResourceTable({ resources }: ResourceTableProps): JSX.Element {
  const rows = [
    {
      name: 'Hồ sơ pháp lý',
      description: 'Requests trong workspace',
      count: `${resources.requestCount} hồ sơ`,
      status: 'Healthy' as const,
      statusVariant: 'green' as const,
      date: resources.lastRequestUpdate,
      action: 'Mở →',
      href: `../cases`,
    },
    {
      name: 'Tài liệu vault',
      description: 'Hợp đồng, NDA, phụ lục',
      count: `${resources.vaultFileCount} tệp`,
      status: 'Encrypted' as const,
      statusVariant: 'green' as const,
      date: resources.lastVaultUpdate,
      action: 'Mở →',
      href: `../documents`,
    },
    {
      name: 'Lời mời thành viên',
      description: 'Đang chờ xác nhận email',
      count: `${resources.invitedCount} lời mời`,
      status: 'Pending' as const,
      statusVariant: 'orange' as const,
      date: resources.lastInviteUpdate,
      action: 'Gửi lại →',
      href: '#',
    },
  ];

  return (
    <div className="table-card">
      {/* Table Header */}
      <div className="table-head">
        <div className="th">Tài nguyên</div>
        <div className="th">Số lượng</div>
        <div className="th">Trạng thái</div>
        <div className="th">Cập nhật gần nhất</div>
        <div className="th">Thao tác</div>
      </div>

      {/* Table Rows */}
      {rows.map((row, index) => (
        <div key={index} className="table-row">
          <div className="td">
            <div className="stack">
              <strong>{row.name}</strong>
              <span>{row.description}</span>
            </div>
          </div>
          <div className="td">{row.count}</div>
          <div className="td">
            <Badge variant={row.statusVariant}>{row.status}</Badge>
          </div>
          <div className="td">{formatDate(row.date)}</div>
          <div className="td">
            {row.href.startsWith('#') ? (
              <span className="action-link">{row.action}</span>
            ) : (
              <Link href={row.href} className="action-link">
                {row.action}
              </Link>
            )}
          </div>
        </div>
      ))}
    </div>
  );
}
```

CSS Grid for header/rows:
- `.table-head, .table-row { display: grid; grid-template-columns: 1.1fr .9fr .9fr 1fr .8fr; }`
  </action>
  <verify>
    <automated>grep -r "ResourceTable" src/app --include="*.tsx" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Component co export ResourceTable
    - [ ] Props nhan ResourceData interface
    - [ ] 5 columns: Tai nguyen, So luong, Trang thai, Cap nhat gan nhat, Thao tac
    - [ ] 3 rows: Ho so phap ly, Tai lieu vault, Loi moi thanh vien
    - [ ] Status badges: green cho Healthy/Encrypted, orange cho Pending
    - [ ] Action links: "Mo ->", "Gui lai ->"
    - [ ] Date format: DD/MM/YYYY HH:mm
  </acceptance_criteria>
  <done>ResourceTable component tao thanh cong</done>
</task>

<task type="auto">
  <name>Task 2: Create index exports and integrate into page</name>
  <files>
    - src/app/[locale]/customer/components/Workspace/index.ts
    - src/app/[locale]/[workspaceSlug]/workspace/page.tsx
  </files>
  <read_first>
    - src/app/[locale]/customer/components/Workspace/WorkspaceBanner.tsx
    - src/app/[locale]/customer/components/Workspace/StatsGrid.tsx
    - src/app/[locale]/customer/components/Workspace/MemberGrid.tsx
    - src/app/[locale]/customer/components/Workspace/ResourceTable.tsx
  </read_first>
  <action>
Tao file `src/app/[locale]/customer/components/Workspace/index.ts`:

```typescript
// Re-export all workspace components
export { WorkspaceBanner } from './WorkspaceBanner';
export type { WorkspaceBannerProps } from './WorkspaceBanner';

export { StatsGrid } from './StatsGrid';
export type { StatsGridProps, StatsData } from './StatsGrid';

export { MemberGrid } from './MemberGrid';
export type { MemberGridProps, MemberData } from './MemberGrid';

export { ResourceTable } from './ResourceTable';
export type { ResourceTableProps, ResourceData } from './ResourceTable';
```

Cap nhat file `src/app/[locale]/[workspaceSlug]/workspace/page.tsx`:

1. **Imports them:**
   ```typescript
   import {
     WorkspaceBanner,
     StatsGrid,
     MemberGrid,
     ResourceTable,
   } from '../../customer/components/Workspace';
   ```

2. **Component body - them bien computed:**
   ```typescript
   // Tinh toan invited members count
   const invitedCount = members.filter(m => !m.isActive).length;
   
   // Resource data
   const resourceData: ResourceData = {
     requestCount: stats.requestCount,
     vaultFileCount: stats.vaultFileCount,
     invitedCount: invitedCount,
     lastRequestUpdate: latestAuditEvent?.createdAt.toISOString() || null,
     lastVaultUpdate: latestAuditEvent?.createdAt.toISOString() || null,
     lastInviteUpdate: '2026-06-09T16:00:00.000Z', // From seed
   };
   ```

3. **Render them cac component:**
   ```typescript
   return (
     <UserLayout ...>
       <div className="page-header">
         <div>
           <h1>Workspace</h1>
           <p className="subtitle">
             Xem thành viên, quyền truy cập, tài nguyên và phạm vi dữ liệu của workspace {workspace.name}.
           </p>
         </div>
       </div>
       
       <WorkspaceBanner
         workspaceName={workspace.name}
         workspaceSlug={workspace.slug}
       />
       
       <StatsGrid stats={stats} />
       
       <MemberGrid members={memberList} />
       
       <ResourceTable resources={resourceData} />
       
       <FloatingChatButton />
     </UserLayout>
   );
   ```

4. **Them FloatingChatButton:**
   Neu chua co, them component hoac inline:
   ```typescript
   function FloatingChatButton() {
     return (
       <div className="floating-chat">
         <span>N</span>
         <span>2 Tin mới</span>
       </div>
     );
   }
   ```

5. **Them CSS inline hoac import global styles:**
   Dam bao cac class names co san:
   - `.page-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 22px; }`
   - `h1 { font-size: 31px; letter-spacing: -0.8px; margin-bottom: 12px; color: #020617; }`
   - `.subtitle { font-size: 15px; color: #5f6e83; font-weight: 500; }`
  </action>
  <verify>
    <automated>grep -r "WorkspaceBanner\|StatsGrid\|MemberGrid\|ResourceTable" "src/app/\[locale\]/\[workspaceSlug\]/workspace/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] index.ts export tat ca 4 components
    - [ ] page.tsx import tu Workspace/index.ts
    - [ ] page.tsx render WorkspaceBanner, StatsGrid, MemberGrid, ResourceTable
    - [ ] page.tsx include FloatingChatButton
    - [ ] ResourceData computed dung tu database queries
  </acceptance_criteria>
  <done>Integration hoan thanh, tat ca components duoc su dung trong page</done>
</task>

</tasks>

<verification>
## Overall Phase Checks

1. Navigate to `/an-phat/workspace` - page loads without error
2. Verify 4 stat cards: Workspace (Active), Thanh vien (6), Ho so (12), Vault scope (96%)
3. Verify workspace banner shows "Cong ty An Phat"
4. Verify member grid shows 4 members with avatars (MP, LA, VT, HN)
5. Verify permission panel shows 3 scopes
6. Verify resource table shows 3 rows with correct data
7. Verify floating chat button visible with "2 Tin moi"
8. Check console for any errors

## UI Verification Checklist

- [ ] Stats: 4 cards in grid layout
- [ ] Banner: Gradient background, workspace name, invite button
- [ ] Member grid: 2 columns (members panel + permission panel)
- [ ] Members: Avatar circles with initials, role badges
- [ ] Permission: 3 scope items with titles and descriptions
- [ ] Resource table: 5 columns, 3 rows, status badges
- [ ] Floating chat: Fixed position, red gradient, "2 Tin moi"
</verification>

<success_criteria>
- ResourceTable component tao tai duong dan dung
- Workspace/index.ts export tat ca components
- Workspace page integrated voi tat ca components
- Tat ca data tu database, khong co hardcoded values trong components
- UI hien thi dung nhu template HTML
</success_criteria>

<output>
Sau khi hoan thanh, tao `.planning/phases/30-workspace/30-03-SUMMARY.md`
</output>
