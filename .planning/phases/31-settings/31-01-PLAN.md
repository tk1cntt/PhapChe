---
phase: "31"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/[workspaceSlug]/settings/page.tsx
  - src/app/[locale]/customer/components/Settings/SettingsMenu.tsx
  - src/app/[locale]/customer/components/Settings/SettingsStats.tsx
  - src/app/[locale]/customer/components/Settings/index.ts
autonomous: true
requirements:
  - CUST-SET-01
  - CUST-SET-02
user_setup: []

must_haves:
  truths:
    - "User sees 6 tabs in vertical menu on left"
    - "User sees 4 stat cards at top (Tai khoan, Bao mat, Thong bao, Workspace)"
    - "User can click tabs to switch active state"
  artifacts:
    - path: "src/app/[locale]/[workspaceSlug]/settings/page.tsx"
      provides: "Settings page route with UserLayout"
    - path: "src/app/[locale]/customer/components/Settings/SettingsMenu.tsx"
      provides: "6-tab vertical navigation menu"
    - path: "src/app/[locale]/customer/components/Settings/SettingsStats.tsx"
      provides: "4 stat cards matching template values"
  key_links:
    - from: "settings/page.tsx"
      to: "SettingsMenu.tsx"
      via: "activeTab state + onTabChange callback"
    - from: "settings/page.tsx"
      to: "SettingsStats.tsx"
      via: "direct component import"
---

<objective>
Settings page route and settings menu with 6 tabs, 4 stat cards, and tab switching logic.
</objective>

<context>
@layout/user-settings.html
@src/app/[locale]/customer/components/UserLayout.tsx
@src/app/[locale]/customer/components/StatCard.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From StatCard.tsx:
```typescript
export interface StatCardProps {
  title: string;
  value: number;
  description: string;
  icon: 'file' | 'clock' | 'check' | 'folder' | 'alert';
  variant: 'blue' | 'green' | 'orange' | 'purple' | 'red';
}
```

From UserLayout.tsx:
```typescript
export interface UserLayoutProps {
  children: React.ReactNode;
  userName: string;
  userRole: string;
  workspaceName: string;
  workspaceSlug: string;
}
```

Settings tabs enum (for tab switching):
```typescript
export type SettingsTab = 'profile' | 'security' | 'notifications' | 'workspace' | 'language' | 'audit';
```

Settings tab config:
```typescript
export interface SettingsTabConfig {
  id: SettingsTab;
  label: string;
  icon: string; // emoji
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsStats component with 4 stat cards</name>
  <files>src/app/[locale]/customer/components/Settings/SettingsStats.tsx</files>
  <action>
Create SettingsStats.tsx with 4 stat cards matching template layout/user-settings.html lines 75-80:

1. Import StatCard variant or create inline styling
2. Render 4 stat cards in .stats grid:
   - Tai khoan (Active) - green variant - "Email da xac thuc"
   - Bao mat (2FA) - blue variant - "Dang bat OTP email"
   - Thong bao (6) - orange variant - "Kenh dang bat"
   - Workspace (1) - purple variant - "Cong ty An Phat"

3. Use exact CSS classes from template:
   - .stats { grid-template-columns: repeat(4,1fr); gap: 18px; }
   - .stat-card { border-radius: 15px; height: 126px; }
   - .stat-icon.green { color: #0f766e; background: linear-gradient(135deg,#d4f4ed,#eefbf8); }
   - .stat-icon.blue { color: #2563eb; background: linear-gradient(135deg,#dfe8ff,#eef4ff); }
   - .stat-icon.orange { color: #f97316; background: linear-gradient(135deg,#ffe2bf,#fff1df); }
   - .stat-icon.purple { color: #7c3aed; background: linear-gradient(135deg,#ede9fe,#f5f3ff); }

Per D-01: StatCard variant green for Account, blue for Security, orange for Notifications, purple for Workspace.
  </action>
  <verify>
    <automated>grep -l "Tài khoản\|Tai khoan" src/app/\[locale\]/customer/components/Settings/SettingsStats.tsx</automated>
  </verify>
  <done>4 stat cards render with correct values: Tai khoan (Active), Bao mat (2FA), Thong bao (6), Workspace (1)</done>
</task>

<task type="auto">
  <name>Task 2: Create SettingsMenu component with 6 vertical tabs</name>
  <files>src/app/[locale]/customer/components/Settings/SettingsMenu.tsx</files>
  <action>
Create SettingsMenu.tsx with vertical tab navigation matching template layout/user-settings.html lines 82-89:

1. Create settings menu with 6 tabs:
   - "👤 Ho so ca nhan" (profile) - active by default
   - "🔐 Bao mat dang nhap" (security)
   - "🔔 Thong bao" (notifications)
   - "🏢 Workspace" (workspace)
   - "🌐 Ngon ngu & giao dien" (language)
   - "🧾 Audit ca nhan" (audit)

2. Use exact CSS classes from template:
   - .settings-menu { background: #fff; border: 1px solid var(--border); border-radius: 15px; padding: 14px; }
   - .settings-tab { height: 52px; border-radius: 10px; padding: 0 14px; font-size: 14px; font-weight: 800; color: #64748b; margin-bottom: 6px; }
   - .settings-tab.active { background: #ecfdf9; color: #087f78; }

3. Accept props: activeTab (SettingsTab), onTabChange (callback)

4. Each tab click triggers onTabChange with tab id

Per D-02: Tab-based navigation with vertical menu on left (360px width), active tab: background #ecfdf9, color #087f78.
  </action>
  <verify>
  <automated>grep -l "settings-tab active" src/app/\[locale\]/customer/components/Settings/SettingsMenu.tsx</automated>
  </verify>
  <done>6 tabs render in vertical list with correct labels and icons, active state styling applied</done>
</task>

<task type="auto">
  <name>Task 3: Create settings page route with UserLayout and tab state</name>
  <files>src/app/[locale]/[workspaceSlug]/settings/page.tsx</files>
  <action>
Create settings page at src/app/[locale]/[workspaceSlug]/settings/page.tsx:

1. Wrap with UserLayout (from ../../customer/components/UserLayout)
2. Use useState for activeTab state (default: 'profile')
3. Import SettingsStats and SettingsMenu from ../../customer/components/Settings
4. Render in order:
   - Page header with h1 "Cai dat" and subtitle
   - SettingsStats component
   - SettingsMenu with activeTab and onTabChange handlers
   - Placeholder div for form content (will be filled in Plan 31-02)

5. Use exact CSS classes from template:
   - .settings-grid { grid-template-columns: 360px 1fr; gap: 20px; }
   - .form-card { background: #fff; border: 1px solid var(--border); border-radius: 15px; }

6. Mock data for page props:
   - userName: "Mai Phuong"
   - userRole: "Customer"
   - workspaceName: "Cong ty An Phat"
   - workspaceSlug: "an-phat"

Per D-02: Settings menu width is 360px, form-card takes remaining space.
  </action>
  <verify>
  <automated>grep -l "settings-page\|SettingsPage" src/app/\[locale\]/\[workspaceSlug\]/settings/page.tsx</automated>
  </verify>
  <done>Settings page renders at /an-phat/settings with UserLayout, 4 stat cards, and 6-tab menu</done>
</task>

</tasks>

<verification>
- Visit /an-phat/settings and verify page loads
- Verify 4 stat cards with values: Active, 2FA, 6, 1
- Verify 6 tabs visible in left menu
- Verify clicking tab changes active state (background #ecfdf9)
</verification>

<success_criteria>
- Settings page route exists at correct path
- 4 stat cards render with template CSS classes
- 6 tabs render with correct labels, icons, and active state styling
- Tab switching works via React state
</success_criteria>

<output>
After completion, create `.planning/phases/31-settings/31-01-SUMMARY.md`
</output>
