---
phase: "31"
plan: "02"
type: execute
wave: 2
depends_on:
  - "31-01"
files_modified:
  - src/app/[locale]/customer/components/Settings/ProfileForm.tsx
  - src/app/[locale]/customer/components/Settings/ToggleRow.tsx
  - src/app/[locale]/customer/components/Settings/SettingsForm.tsx
autonomous: true
requirements:
  - CUST-SET-03
  - CUST-SET-04
user_setup: []

must_haves:
  truths:
    - "User sees 6-field profile form with 2-column grid"
    - "User sees 3 notification toggles with correct on/off states"
    - "User sees 2 security toggles with correct on/off states"
    - "User sees 'Doi mat khau' button in ghost style"
  artifacts:
    - path: "src/app/[locale]/customer/components/Settings/ToggleRow.tsx"
      provides: "Reusable toggle switch component"
    - path: "src/app/[locale]/customer/components/Settings/ProfileForm.tsx"
      provides: "6-field form with 2-column grid layout"
    - path: "src/app/[locale]/customer/components/Settings/SettingsForm.tsx"
      provides: "Combined form with all sections"
  key_links:
    - from: "SettingsForm.tsx"
      to: "ProfileForm.tsx"
      via: "direct import and composition"
    - from: "SettingsForm.tsx"
      to: "ToggleRow.tsx"
      via: "direct import for all toggles"
---

<objective>
Profile form with 6 fields, 3 notification toggles, and 2 security toggles matching template exactly.
</objective>

<context>
@layout/user-settings.html
@.planning/phases/31-settings/31-01-PLAN.md
</context>

<interfaces>
Settings tab type:
```typescript
export type SettingsTab = 'profile' | 'security' | 'notifications' | 'workspace' | 'language' | 'audit';
```

Profile form data shape:
```typescript
export interface ProfileFormData {
  name: string;
  email: string;
  phone: string;
  title: string;
  defaultWorkspace: string;
  timezone: string;
}
```

Notification toggle data:
```typescript
export interface NotificationToggle {
  id: string;
  label: string;
  description: string;
  enabled: boolean;
}
```

Security toggle data:
```typescript
export interface SecurityToggle {
  id: string;
  label: string;
  description: string;
  enabled: boolean;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create ToggleRow reusable component</name>
  <files>src/app/[locale]/customer/components/Settings/ToggleRow.tsx</files>
  <action>
Create ToggleRow.tsx - a reusable toggle switch component:

1. Props:
   - label: string
   - description: string
   - checked: boolean
   - onChange: (checked: boolean) => void

2. Render toggle row matching template layout/user-settings.html lines 102-104:
   - .toggle-row { display: flex; align-items: center; justify-content: space-between; border: 1px solid #edf2f7; background: #fbfdff; border-radius: 12px; padding: 15px; margin-bottom: 12px; }
   - .toggle { width: 46px; height: 26px; border-radius: 999px; background: #087f78; position: relative; }
   - .toggle:after { content: ""; position: absolute; right: 3px; top: 3px; width: 20px; height: 20px; background: #fff; border-radius: 50%; }
   - .toggle.off { background: #cbd5e1; }
   - .toggle.off:after { left: 3px; right: auto; }
   - .stack strong { font-size: 14px; color: #0f172a; }
   - .stack span { font-size: 12px; color: #64748b; }

3. Click handler toggles state and calls onChange

Per D-04 and D-05: Toggle style 46x26px, green (#087f78) when on, gray (#cbd5e1) when off.
  </action>
  <verify>
  <automated>grep -l "toggle off\|toggle-row" src/app/\[locale\]/customer/components/Settings/ToggleRow.tsx</automated>
  </verify>
  <done>ToggleRow renders with correct styling and click toggles state</done>
</task>

<task type="auto">
  <name>Task 2: Create ProfileForm component with 6 fields</name>
  <files>src/app/[locale]/customer/components/Settings/ProfileForm.tsx</files>
  <action>
Create ProfileForm.tsx with 6-field form matching template layout/user-settings.html lines 91-99:

1. Render "Thong tin ca nhan" panel title with "Luu thay doi" button

2. 6 fields in 2-column grid:
   - Ho va ten: input value="Mai Phuong"
   - Email: input value="mai.phuong@anphat.vn"
   - So dien thoai: input value="+84 912 345 678"
   - Chuc danh: input value="Head of Legal Operations"
   - Workspace mac dinh: select with "Cong ty An Phat" option
   - Mui gio: select with "Asia/Ho_Chi_Minh (ICT)" option

3. Use exact CSS from template:
   - .form-section { padding: 24px; border-bottom: 1px solid var(--border); }
   - .field-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
   - .field { display: grid; gap: 8px; }
   - .field label { font-size: 13px; font-weight: 800; color: #334155; }
   - .field input, .field select { height: 44px; border: 1px solid var(--border); border-radius: 8px; padding: 0 13px; }
   - .create-btn { height: 45px; padding: 0 18px; border: none; border-radius: 8px; background: linear-gradient(180deg,#0b8f86,#087970); color: #fff; font-weight: 700; box-shadow: 0 8px 18px rgba(8,127,120,.25); cursor: pointer; }

4. Form uses local state with initial values from props
5. Save button logs form values (integration in Plan 31-03)

Per D-03: Field style height 44px, border-radius 8px. Save button gradient teal with "Luu thay doi" text.
  </action>
  <verify>
  <automated>grep -l "field-grid\|Ho va ten" src/app/\[locale\]/customer/components/Settings/ProfileForm.tsx</automated>
  </verify>
  <done>Profile form renders with 6 fields in 2-column grid with correct values</done>
</task>

<task type="auto">
  <name>Task 3: Create SettingsForm component combining all sections</name>
  <files>src/app/[locale]/customer/components/Settings/SettingsForm.tsx</files>
  <action>
Create SettingsForm.tsx that combines ProfileForm, notification toggles, and security toggles:

1. Import ProfileForm and ToggleRow

2. Notification section (3 toggles) per D-04:
   - "Email khi chuyen vien phan hoi" - on (default)
     description: "Nhan email khi co tin nhan moi trong ho so"
   - "Nhac SLA truoc han" - on (default)
     description: "Thong bao truoc 6 gio khi ho so can phan hoi"
   - "Tom tat hang tuan" - off (default)
     description: "Gui bao cao tinh trang ho so vao thu Hai"

3. Security section (2 toggles + button) per D-05:
   - "Xac thuc 2 buoc" - on (default)
     description: "Ma OTP gui qua email khi dang nhap thiet bi moi"
   - "Thong bao dang nhap la" - on (default)
     description: "Canh bao khi phat hien IP hoac thiet bi moi"
   - "Doi mat khau" button - ghost style:
     description: "ghost-btn { height: 45px; padding: 0 16px; border: 1px solid var(--border); background: #fff; border-radius: 8px; color: #1e293b; font-weight: 800; cursor: pointer; }"

4. Section titles:
   - "Thong bao" for notification section
   - "Bao mat" for security section

5. All sections inside .form-card with .form-section wrappers

Per D-04: 3 notification toggles. Per D-05: 2 security toggles + ghost password button.
  </action>
  <verify>
  <automated>grep -l "Thong bao\|Bao mat\|Doi mat khau" src/app/\[locale\]/customer/components/Settings/SettingsForm.tsx</automated>
  </verify>
  <done>SettingsForm renders all 3 sections: profile form, 3 notification toggles, 2 security toggles + password button</done>
</task>

</tasks>

<verification>
- Verify 6 fields render with values from template
- Verify toggle states: 2 on, 1 off for notifications; 2 on for security
- Verify Save button has gradient teal styling
- Verify Password button has ghost border styling
</verification>

<success_criteria>
- ProfileForm renders 6 fields in 2-column grid with template values
- ToggleRow component reusable with correct styling
- SettingsForm renders all sections with correct toggle states
- All CSS classes match template exactly
</success_criteria>

<output>
After completion, create `.planning/phases/31-settings/31-02-SUMMARY.md`
</output>
