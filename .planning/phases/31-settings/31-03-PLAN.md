---
phase: "31"
plan: "03"
type: execute
wave: 3
depends_on:
  - "31-01"
  - "31-02"
files_modified:
  - src/app/[locale]/[workspaceSlug]/settings/page.tsx
  - src/app/[locale]/customer/components/Settings/index.ts
  - prisma/seed.ts
autonomous: true
requirements:
  - CUST-SET-01
  - CUST-SET-02
  - CUST-SET-03
  - CUST-SET-04
  - CUST-SET-05
user_setup: []

must_haves:
  truths:
    - "User sees complete settings page with all sections integrated"
    - "User sees floating chat button with 2 notifications"
    - "All data comes from database seed"
  artifacts:
    - path: "src/app/[locale]/[workspaceSlug]/settings/page.tsx"
      provides: "Final settings page with all components"
    - path: "src/app/[locale]/customer/components/Settings/index.ts"
      provides: "Component exports"
    - path: "prisma/seed.ts"
      provides: "User settings seed data"
  key_links:
    - from: "settings/page.tsx"
      to: "SettingsForm"
      via: "import and render"
    - from: "settings/page.tsx"
      to: "FloatingChatButton"
      via: "import and render"
---

<objective>
Integrate SettingsForm into page, add floating chat button, update seed data, and verify full settings page.
</objective>

<context>
@layout/user-settings.html
@.planning/phases/31-settings/31-01-PLAN.md
@.planning/phases/31-settings/31-02-PLAN.md
@src/app/[locale]/customer/components/FloatingChatButton.tsx
</context>

<interfaces>
FloatingChatButton component interface:
```typescript
export interface FloatingChatButtonProps {
  notificationCount: number;
  notificationText?: string;
}
```

Current settings page structure (from 31-01):
- Has UserLayout wrapper
- Has SettingsStats component
- Has SettingsMenu with activeTab state
- Has placeholder div for form content
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Integrate SettingsForm and floating chat into settings page</name>
  <files>src/app/[locale]/[workspaceSlug]/settings/page.tsx</files>
  <action>
Update settings/page.tsx to integrate SettingsForm and floating chat:

1. Import SettingsForm from ../../customer/components/Settings
2. Replace placeholder div with SettingsForm component
3. Import FloatingChatButton from ../../customer/components/FloatingChatButton
4. Add floating chat button at bottom right:
   - notificationCount: 2
   - notificationText: "Tin moi"

5. Position floating chat with CSS:
   - Position: fixed; right: 22px; bottom: 20px;
   - Use .floating-chat class matching template line 116:
   - .floating-chat { background: linear-gradient(180deg,#ef4444,#dc2626); border: 3px solid #facc15; }

Per D-06: Reuse FloatingChatButton from Phase 26. Notification count "2 Tin moi" per template.
  </action>
  <verify>
  <automated>grep -l "FloatingChatButton\|floating-chat" src/app/\[locale\]/\[workspaceSlug\]/settings/page.tsx</automated>
  </verify>
  <done>Settings page has FloatingChatButton with 2 notifications and SettingsForm integrated</done>
</task>

<task type="auto">
  <name>Task 2: Update Settings index.ts exports</name>
  <files>src/app/[locale]/customer/components/Settings/index.ts</files>
  <action>
Update Settings/index.ts to export all components:

1. Export SettingsStats
2. Export SettingsMenu
3. Export ProfileForm
4. Export ToggleRow
5. Export SettingsForm
6. Export SettingsTab type
7. Export all interface types

Create the file if not exists with all re-exports.
  </action>
  <verify>
  <automated>grep -l "export.*SettingsForm\|export.*ToggleRow" src/app/\[locale\]/customer/components/Settings/index.ts</automated>
  </verify>
  <done>All Settings components exported from index.ts</done>
</task>

<task type="auto">
  <name>Task 3: Update seed data for user settings and add verification</name>
  <files>prisma/seed.ts</files>
  <action>
Update prisma/seed.ts to add user settings seed data:

1. Add seedUserSettings() function that creates/updates:
   - User profile data for mai.phuong@anphat.vn:
     - name: "Mai Phuong"
     - email: "mai.phuong@anphat.vn"
     - phone: "+84 912 345 678"
     - title: "Head of Legal Operations"
   - Workspace membership for an-phat workspace with defaultWorkspace
   - Timezone preference: "Asia/Ho_Chi_Minh"
   - Notification preferences (3 toggles stored as JSON in User or separate table)

2. Call seedUserSettings() in main() after seedAnPhatWorkspace()

3. Note: For MVP, notification/security preferences can be stored as JSON in a new UserSettings model or in User metadata. Create UserSettings model if prisma schema needs it.

Per CLAUDE.md: All data must come from DB. No hardcoded data.
  </action>
  <verify>
  <automated>grep -l "Mai Phuong\|seedUserSettings" prisma/seed.ts</automated>
  </verify>
  <done>Seed data updated with user settings values matching template</done>
</task>

</tasks>

<verification>
- Visit /an-phat/settings and verify full page renders
- Verify 4 stat cards at top
- Verify 6 tabs in left menu
- Verify profile form with 6 fields
- Verify 3 notification toggles
- Verify 2 security toggles + password button
- Verify floating chat button with "2 Tin moi"
- Verify all data matches template layout/user-settings.html exactly
</verification>

<success_criteria>
- Settings page renders all components matching template
- All requirements CUST-SET-01 through CUST-SET-05 are covered
- Floating chat button visible with notification count
- Seed data populated with user settings
- E2E test verifies page renders (if tests exist)
</success_criteria>

<output>
After completion, create `.planning/phases/31-settings/31-03-SUMMARY.md`
</output>
