---
phase: "33"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/users/page.tsx
  - src/app/components/admin/UserStatCard.tsx
  - src/app/components/admin/UserStatCard.test.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - ADMIN-USER-01
  - ADMIN-USER-02
user_setup: []

must_haves:
  truths:
    - "User sees 4 stat cards with exact values (Total: 128, Active: 112, Workspaces: 12, Pending: 9)"
    - "User sees page header with 'User Management' title and subtitle"
    - "User sees 'Create User' button in page header"
  artifacts:
    - path: "src/app/admin/users/page.tsx"
      provides: "User management page route"
      min_lines: 50
    - path: "src/app/components/admin/UserStatCard.tsx"
      provides: "Stat card component for user stats"
      exports: ["UserStatCard"]
    - path: "src/app/globals.css"
      provides: "CSS for user management page"
  key_links:
    - from: "src/app/admin/users/page.tsx"
      to: "src/app/components/admin/UserStatCard.tsx"
      via: "import"
      pattern: "UserStatCard"
---

<objective>
Tao trang User Management voi 4 stat cards hien thi gia tri chinh xac tu template: Total Users (128), Active Users (112), Workspaces (12), Pending Invitations (9).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@layout/admin-user-management.html
@.planning/phases/33-user-management/33-CONTEXT.md
@src/app/admin/page.tsx

# Template stats structure (lines 1018-1080):
# - Total Users: blue icon, 128, "Across all workspaces"
# - Active Users: green icon, 112, "87.5% of total users"
# - Workspaces: purple icon, 12, "11 active workspaces"
# - Pending Invitations: orange icon, 9, "Awaiting acceptance"

# Admin page pattern from Phase 32 (page.tsx):
# Uses AdminStatCard component with variant, title, value, description, icon props
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/app/components/admin/AdminStatCard.tsx:
```typescript
interface AdminStatCardProps {
  variant: 'blue' | 'green' | 'orange' | 'red';
  title: string;
  value: string;
  description: string;
  icon: React.ReactNode;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create UserStatCard component</name>
  <files>src/app/components/admin/UserStatCard.tsx</files>
  <action>
Tao component UserStatCard trong thu muc src/app/components/admin/.

Su dung pattern tu AdminStatCard nhung thay doi noi dung stat de phu hop voi user management:
- Tao file moi UserStatCard.tsx (khong sua AdminStatCard.tsx vi no dung cho admin dashboard)
- Props: variant ('blue' | 'green' | 'orange' | 'purple'), title, value, description, icon
- Style giong het template: height 126px, border-radius 15px, padding 24px 22px
- Icon container: 62x62px, border-radius 13px, margin-right 18px
- Info dot o goc phai (vi tri: top 22px, right 22px, 21x21px)
- Su dung inline styles nhu Phase 32
</action>
  <verify>grep -l "UserStatCard" src/app/components/admin/UserStatCard.tsx</verify>
  <done>UserStatCard component co 4 props: variant, title, value, description, icon</done>
</task>

<task type="auto">
  <name>Task 3: Whitebox tests for UserStatCard</name>
  <files>src/app/components/admin/UserStatCard.test.tsx</files>
  <action>
Tao whitebox test cho UserStatCard component tai src/app/components/admin/UserStatCard.test.tsx.

Test cases:
1. Renders with variant='blue', title, value, description
2. Renders with variant='green'
3. Renders with variant='orange'
4. Renders with variant='purple'
5. Displays correct icon for each variant
6. Info dot visible in top-right corner

Su dung Vitest + React Testing Library:
```typescript
import { render, screen } from '@testing-library/react';
import { UserStatCard } from './UserStatCard';

describe('UserStatCard', () => {
  it('renders with blue variant', () => {
    render(<UserStatCard variant="blue" title="Total Users" value="128" description="Across all workspaces" />);
    expect(screen.getByText('128')).toBeInTheDocument();
    expect(screen.getByText('Total Users')).toBeInTheDocument();
  });
});
```
</action>
  <verify>grep -l "UserStatCard" src/app/components/admin/UserStatCard.test.tsx && grep "describe" src/app/components/admin/UserStatCard.test.tsx</verify>
  <done>UserStatCard tests pass: vitest run src/app/components/admin/UserStatCard.test.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Create users page route</name>
  <files>src/app/admin/users/page.tsx</files>
  <action>
Tao trang User Management tai src/app/admin/users/page.tsx.

Cau truc trang:
1. Page header voi:
   - h1: "User Management"
   - Subtitle: "Platform with 5 roles: customer, specialist, reviewer, coordinator_admin and super_admin."
   - Create User button (gradient green, icon users-plus)

2. Grid 4 stat cards:
   - Total Users: blue, "128", "Across all workspaces"
   - Active Users: green, "112", "87.5% of total users"
   - Workspaces: purple, "12", "11 active workspaces"
   - Pending Invitations: orange, "9", "Awaiting acceptance"

3. Icons SVG:
   - Total Users: Users icon (person + person circle)
   - Active Users: CheckCircle icon
   - Workspaces: Building icon (3 floors)
   - Pending Invitations: Mail icon (envelope)

Style padding content: 31px 36px 42px
Grid gap: 18px
</action>
  <verify>grep -l "User Management" src/app/admin/users/page.tsx && grep -l "UserStatCard" src/app/admin/users/page.tsx</verify>
  <done>Users page hien thi 4 stat cards voi gia tri dung: 128, 112, 12, 9</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server | Admin page access controlled by auth middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | User stats | accept | Stats are aggregate counts, not sensitive |
</threat_model>

<verification>
1. Page renders at /admin/users route
2. 4 stat cards visible with correct values: 128, 112, 12, 9
3. Stat card icons match variant colors
4. Info dots visible on each stat card
</verification>

<success_criteria>
- [ ] /admin/users route renders successfully
- [ ] 4 stat cards display with exact template values
- [ ] Page header with "User Management" title visible
- [ ] "Create User" button in header
</success_criteria>

<output>
After completion, create `.planning/phases/33-user-management/33-01-SUMMARY.md`
</output>
