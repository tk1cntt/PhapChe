---
phase: "33"
plan: "03"
type: execute
wave: 2
depends_on:
  - "33-01"
  - "33-02"
files_modified:
  - src/app/components/admin/UserToolbar.tsx
  - src/app/components/admin/FloatingAlertButton.tsx
  - src/app/admin/users/page.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - ADMIN-USER-05
  - ADMIN-USER-06
user_setup: []

must_haves:
  truths:
    - "User sees toolbar with search input and filter dropdowns"
    - "User sees functional search by name, email, workspace"
    - "User sees role and workspace filter dropdowns"
    - "User sees floating alert button in bottom right"
  artifacts:
    - path: "src/app/components/admin/UserToolbar.tsx"
      provides: "Toolbar with search and filters"
      exports: ["UserToolbar"]
    - path: "src/app/admin/users/page.tsx"
      provides: "Integrated user management page"
  key_links:
    - from: "src/app/admin/users/page.tsx"
      to: "src/app/components/admin/UserToolbar.tsx"
      via: "import"
      pattern: "UserToolbar"
---

<objective>
Tao UserToolbar component voi search va filters, tich hop tat ca components vao users page, tao e2e test.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@layout/admin-user-management.html
@.planning/phases/33-user-management/33-CONTEXT.md
@src/app/admin/page.tsx
@src/app/components/admin/AdminToolbar.tsx

# Toolbar structure from template (lines 1106-1163):
- Search input: width 330px, height 44px, placeholder "Search users..."
- Filters button with icon
- Role dropdown button
- Workspace dropdown button
- Refresh button (square, icon only)
- Export button with download icon
- Columns button

# Floating alert button (lines 1333-1336):
- Fixed position: right 22px, bottom 20px
- Gradient red background
- Yellow border (3px)
- "N" icon + "3 Alerts" text
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/app/components/admin/AdminToolbar.tsx:
```typescript
// Existing admin toolbar pattern - adapt for user management
interface FilterState {
  search: string;
  role?: string;
  workspace?: string;
}
```

From src/app/components/admin/FloatingAlertButton.tsx:
```typescript
// Existing floating button pattern
interface FloatingAlertButtonProps {
  alertCount: number;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create UserToolbar component</name>
  <files>src/app/components/admin/UserToolbar.tsx</files>
  <action>
Tao component UserToolbar trong src/app/components/admin/.

Interface:
```typescript
interface UserToolbarProps {
  onSearch?: (query: string) => void;
  onFilterChange?: (filters: FilterState) => void;
}
```

Structure (toolbar-card wrapper + toolbar):
- toolbar-card: border-radius 15px 15px 0 0, border-bottom none
- toolbar: display flex, justify-content space-between

Left tools:
1. Search input (330px width, 44px height):
   - Icon: search (magnifier)
   - Placeholder: "Search users..."
   - onChange callback

2. Filters button:
   - Icon: funnel/filter
   - Text: "Filters"

3. Role dropdown:
   - Text: "Role"
   - Chevron icon

4. Workspace dropdown:
   - Text: "Workspace"
   - Chevron icon

Right tools:
1. Refresh button (square 52px):
   - Icon: refresh/sync

2. Export button:
   - Icon: download
   - Text: "Export"

3. Columns button:
   - Text: "Columns"

Style buttons:
- height: 44px
- border: 1px solid var(--border)
- border-radius: 8px
- padding: 0 16px
- font-size: 14px, font-weight: 700

Add state for controlled inputs (search query, selected role, selected workspace).
</action>
  <verify>grep -l "UserToolbar" src/app/components/admin/UserToolbar.tsx && grep "Search users" src/app/components/admin/UserToolbar.tsx</verify>
  <done>UserToolbar co search input, 4 filter buttons, 3 action buttons</done>
</task>

<task type="auto">
  <name>Task 2: Integrate all components into users page</name>
  <files>src/app/admin/users/page.tsx</files>
  <action>
Cap nhat trang src/app/admin/users/page.tsx de tich hop:

1. Import components:
   - RolePill
   - UserTable
   - UserToolbar
   - FloatingAlertButton (reuse from Phase 32)

2. Add RolePills section:
   - Card with shadow, border-radius 15px
   - Section title with shield icon: "System Roles"
   - Audit note text

3. Add UserToolbar component

4. Add UserTable component

5. Add FloatingAlertButton with alertCount={3}

6. Add CSS classes to globals.css:
   - roles-card
   - section-title
   - role-pills
   - toolbar-card
   - toolbar
   - user-search
   - tool-btn
   - table-card
   - table-head, table-row
   - th, td
   - user-cell, mini-avatar
   - stack, badge

Layout order:
1. Page header
2. Stats grid
3. Roles card
4. Toolbar card
5. Table card
6. Floating button
</action>
  <verify>grep "RolePill" src/app/admin/users/page.tsx && grep "UserTable" src/app/admin/users/page.tsx && grep "UserToolbar" src/app/admin/users/page.tsx</verify>
  <done>Tat ca components duoc tich hop vao users page</done>
</task>

<task type="auto">
  <name>Task 3: Add CSS classes to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
Them cac CSS classes can thiet vao globals.css:

1. roles-card: background #fff, border 1px solid var(--border), border-radius 15px, box-shadow var(--soft-shadow), padding 24px, margin-bottom 24px

2. section-title: display flex, align-items center, gap 12px, font-size 20px, font-weight 800, margin-bottom 18px

3. audit-note: color #59687e, font-size 14px, font-weight 500, line-height 1.7

4. toolbar-card: background #fff, border 1px solid var(--border), border-radius 15px 15px 0 0, box-shadow var(--soft-shadow), padding 20px, border-bottom none

5. left-tools, right-tools: display flex, align-items center, gap 12px

6. user-search: width 330px, height 44px, border 1px solid var(--border), border-radius 8px, display flex, align-items center, gap 11px, padding 0 14px

7. table-card: background #fff, border 1px solid var(--border), border-radius 0 0 15px 15px, box-shadow var(--shadow), overflow hidden

8. table-head: background linear-gradient(#f8fafc, #f5f7fb), border-bottom 1px solid var(--border)

9. table-row: min-height 72px, border-bottom 1px solid var(--border), hover background #fbfdff

10. th: min-height 54px, padding 0 16px, color #59687e, font-size 14px, font-weight 700

11. td: display flex, align-items center, padding 0 16px, font-size 14px

12. checkbox: width 18px, height 18px, border 2px solid #cbd5e1, border-radius 4px

13. user-cell: display flex, align-items center, gap 12px

14. mini-avatar: width 38px, height 38px, border-radius 50%, flex-shrink 0

15. stack: min-width 0

16. stack strong: font-size 14px, margin-bottom 4px, white-space nowrap, overflow hidden, text-overflow ellipsis

17. stack span: font-size 12px, color #64748b

18. badge: display inline-flex, height 28px, padding 0 11px, border-radius 999px, font-size 12px, font-weight 800

19. action-link: color #087f78, font-weight 800, text-decoration none
</action>
  <verify>grep -l "roles-card" src/app/globals.css && grep -l "table-card" src/app/globals.css</verify>
  <done>globals.css chua tat ca CSS classes can thiet cho user management page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server | Search/filter queries should be sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Injection | Search input | mitigate | Input sanitized before database query |
| T-33-04 | Information Disclosure | Filter results | accept | Only admins can access user data |
</threat_model>

<verification>
1. Page renders at /admin/users with all components
2. Toolbar search input functional
3. Role and workspace dropdowns visible
4. Floating alert button in bottom right
5. All CSS classes applied correctly
</verification>

<success_criteria>
- [ ] /admin/users renders complete page
- [ ] UserToolbar visible with all buttons
- [ ] RolePills section visible with 6 roles
- [ ] UserTable with 8 users visible
- [ ] FloatingAlertButton visible
</success_criteria>

<output>
After completion, create `.planning/phases/33-user-management/33-03-SUMMARY.md`
</output>
