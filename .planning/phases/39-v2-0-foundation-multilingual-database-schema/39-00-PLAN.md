---
phase: "39"
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v2/
  - src/legacy/
  - MIGRATION-POLICY.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - RESTRUCTURE
must_haves:
  truths:
    - "src/v2/ directory created with proper route-based structure"
    - "src/legacy/ contains old code for reference (archive, not active)"
    - "src/lib/ remains source of truth for shared code"
    - "src/v2/app/ follows Next.js App Router native patterns"
    - "Migration policy documented with priority list and exit criteria"
artifacts:
  - path: "src/v2/"
    provides: "New v2 implementation directory"
    structure: "app/[locale]/{dashboard,cases,create,messages,workspace,settings,admin/*}, components/{ui,dashboard,forms,admin}, lib/"
  - path: "src/legacy/"
    provides: "Archived v1 implementation (reference only)"
    structure: "Original src/app content - DO NOT MODIFY"
  - path: "src/lib/"
    provides: "Source of truth for shared services"
    structure: "prisma.ts, security/, audit/, workflow/, documents/"
  - path: "MIGRATION-POLICY.md"
    provides: "Migration rules and exit criteria"
    sections: "Priority, Sync Policy, Exit Criteria, Deprecation Timeline"
  - path: "src/v2/README.md"
    provides: "v2 architecture documentation"
    sections: "Structure, Migration Guide, Component Patterns"
key_links:
  - from: "src/lib/"
    to: "src/v2/"
    via: "Shared services imported by v2"
    pattern: "import.*@/lib/"
  - from: "src/legacy/"
    to: "src/v2/"
    via: "Reference for migration"
    rule: "Copy patterns, do not copy code"
---

<objective>
Restructure source code to create clean v2 implementation while preserving legacy for reference.

Create new src/v2/ structure with route-based architecture, archive legacy, establish migration policy.
</objective>

<context>
@src/app/ (current structure - will become src/legacy/)
@src/lib/ (existing common code - STAYS)
@src/components/ (existing components - STAYS)

## Architectural Decision (from Advisory):

### Why src/v2/ Parallel Structure?
1. **Low Risk** - Can rollback by not deploying v2
2. **Fast Time to Value** - Placeholder pages show progress
3. **Parallel Dev** - Work on v2 while v1 still runs
4. **Incremental Migration** - Migrate page by page

### Why Route-based Structure?
- Native to Next.js App Router
- Easy migration from current [locale]/ pattern
- No extra config needed

### Critical Success Factors:
1. **Migration Priority List** - Which pages migrate first
2. **Sync Policy** - When to sync code from v1 to v2
3. **Exit Criteria** - When is "good enough to ship"
4. **Deprecation Timeline** - When to deprecate v1 completely

## Target Structure:

```
src/
├── v2/                          # NEW: Clean v2 implementation
│   ├── app/
│   │   ├── [locale]/           # i18n routing (Next.js native)
│   │   │   ├── dashboard/      # Customer dashboard
│   │   │   ├── cases/         # My cases
│   │   │   ├── create/        # Create request
│   │   │   ├── messages/       # Messages
│   │   │   ├── workspace/      # Workspace
│   │   │   ├── settings/       # Settings
│   │   │   └── admin/         # Admin portal
│   │   │       ├── dashboard/
│   │   │       ├── users/
│   │   │       ├── requests/
│   │   │       ├── vault/
│   │   │       └── audit/
│   │   ├── (auth)/sign-in/   # Auth pages
│   │   └── api/              # API routes v2
│   ├── components/            # v2-specific components
│   │   ├── ui/              # Base UI (wrappers)
│   │   ├── dashboard/        # Dashboard components
│   │   ├── forms/            # Form components
│   │   └── admin/            # Admin components
│   ├── lib/                  # v2-specific services (if needed)
│   └── README.md             # Architecture doc
├── legacy/                    # ARCHIVED: v1 code (DO NOT MODIFY)
│   └── README.md             # Explains archived content
├── lib/                      # SOURCE OF TRUTH: Shared services
│   ├── prisma.ts
│   ├── security/
│   ├── audit/
│   ├── workflow/
│   ├── documents/
│   └── i18n/
└── components/               # SOURCE OF TRUTH: Shared UI
    └── ui/
```

## Migration Priority:

| Priority | Pages | Reason |
|----------|-------|--------|
| 1 (First) | Dashboard, Auth | Core flow |
| 2 | Cases, Create Request | Main user features |
| 3 | Messages, Workspace | Secondary features |
| 4 (Last) | Admin pages | Lower priority |

## Exit Criteria for v1 Deprecation:
- All critical pages migrated and tested
- v2 passes E2E tests
- No new features in v1
</context>

<tasks>

<task type="execute">
  <name>Task 1: Create src/v2/ directory structure</name>
  <read_first>src/app/[locale]/[workspaceSlug]/DashboardClient.tsx</read_first>
  <files>src/v2/</files>
  <action>
    Create src/v2/ directory structure following Next.js App Router native patterns:

    ```
    src/v2/
    ├── app/
    │   ├── [locale]/           # i18n routing - NATIVE Next.js
    │   │   ├── dashboard/
    │   │   ├── cases/
    │   │   ├── create/
    │   │   ├── messages/
    │   │   ├── workspace/
    │   │   ├── settings/
    │   │   └── admin/
    │   │       ├── dashboard/
    │   │       ├── users/
    │   │       ├── requests/
    │   │       ├── vault/
    │   │       └── audit/
    │   ├── (auth)/
    │   │   └── sign-in/
    │   └── api/
    ├── components/
    │   ├── ui/
    │   ├── dashboard/
    │   ├── forms/
    │   └── admin/
    └── lib/
    ```

    Create all directories. Add placeholder .gitkeep files.
    DO NOT create any page files yet - that comes in later tasks.
  </action>
  <acceptance_criteria>
    - "Directory tree matches spec above exactly"
    - "find src/v2 -type d | wc -l returns expected count"
    - "No placeholder page.tsx files exist yet"
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Archive src/app/ to src/legacy/</name>
  <read_first>src/app/page.tsx</read_first>
  <files>src/legacy/</files>
  <action>
    Archive old implementation:

    1. Create src/legacy/ directory
    2. Copy entire src/app/ to src/legacy/:
       - Use COPY not MOVE initially (safer)
       - Verify all files copied
    3. Delete original src/app/ content
    4. Create src/legacy/README.md with content:
       ```
       # Legacy v1 Code (ARCHIVED)

       This directory contains the original v1 implementation.

       ## Status: ARCHIVED - DO NOT MODIFY

       This code is kept for reference only. All new development
       should happen in src/v2/.

       ## Contents:
       - app/ - Original Next.js pages and routes
       - components/ - Original UI components (now in src/components/)

       ## Migration:
       See MIGRATION-POLICY.md for migration priorities.
       ```
    5. Create src/legacy/CONTENTS.md listing all subdirectories:
       ```
       # Archived Contents

       ## src/app/
       - [locale]/[workspaceSlug]/ - Mock UI pages
       - api/ - API routes
       - customer/, specialist/, reviewer/ - Working but poor UI

       ## Migration Status
       [TBD - will be updated per page]
       ```

    IMPORTANT: Use git rm for deleted files, not filesystem delete.
    This preserves git history.
  </action>
  <acceptance_criteria>
    - "src/legacy/app/ contains identical copy of old src/app/"
    - "src/legacy/README.md exists with DO NOT MODIFY warning"
    - "src/app/ directory exists but is empty (or minimal)"
    - "git status shows src/legacy/ as new, src/app/ modified"
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Create v2 README and Architecture docs</name>
  <read_first>src/lib/prisma.ts</read_first>
  <files>src/v2/README.md</files>
  <action>
    Create v2 architecture documentation:

    1. Create src/v2/README.md:
       ```
       # v2 Implementation

       ## Architecture

       v2 follows Next.js App Router native patterns with route-based
       structure. No feature flags - clean separation from v1.

       ## Directory Structure

       ```
       src/v2/
       ├── app/           # Next.js App Router pages
       │   ├── [locale]/ # i18n routing (native)
       │   ├── (auth)/   # Auth routes
       │   └── api/      # API routes
       ├── components/   # v2-specific components
       └── lib/          # v2-specific services (if needed)
       ```

       ## Key Principles

       1. **Route-based**: Each page = one route directory
       2. **Shared code**: Import from src/lib/ and src/components/
       3. **No duplication**: Never copy from legacy, reference only
       4. **Incremental**: Migrate page by page

       ## Component Patterns

       See src/v2/docs/COMPONENTS.md

       ## Migration Guide

       See MIGRATION-POLICY.md
       ```

    2. Create src/v2/docs/ directory with:
       - COMPONENTS.md: Component patterns and usage
       - API_CONVENTIONS.md: API route conventions
       - I18N_PATTERN.md: i18n implementation pattern

    3. Create src/v2/.gitkeep files in empty dirs
  </action>
  <acceptance_criteria>
    - "src/v2/README.md exists with architecture overview"
    - "src/v2/docs/ directory exists with 3 doc files"
    - "README.md mentions src/lib/ as shared code source"
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Create MIGRATION-POLICY.md</name>
  <read_first>src/legacy/README.md</read_first>
  <files>MIGRATION-POLICY.md</files>
  <action>
    Create migration policy document:

    ```
    # Migration Policy: v1 to v2

    ## Status: IN PROGRESS

    Start Date: 2026-06-12
    Target Completion: TBD

    ## Migration Priority

    | Priority | Pages | Reason | Status |
    |----------|-------|--------|--------|
    | 1 | Dashboard | Core flow, high visibility | Pending |
    | 2 | Auth (sign-in) | Required for dashboard | Pending |
    | 3 | Cases, Create | Main user features | Pending |
    | 4 | Messages, Workspace | Secondary features | Pending |
    | 5 | Admin pages | Lower priority | Pending |

    ## Sync Policy

    ### When to SYNC (update v2 from v1):
    - When v1 has critical bug fix needed in v2
    - When new patterns are established
    - Monthly review of v1 changes

    ### When NOT to sync:
    - Routine v1 development
    - UI improvements (v2 has better UI)
    - Features not in v2 scope

    ### Sync Process:
    1. Review v1 changes
    2. Identify reusable patterns (NOT code)
    3. Implement in v2 with v2 patterns
    4. Document in v2/docs/

    ## Exit Criteria

    v1 can be deprecated when ALL:
    - [ ] All Priority 1-3 pages migrated and tested
    - [ ] E2E tests pass on v2
    - [ ] No critical bugs in v2 for 2 weeks
    - [ ] Business approves v2 as default

    ## Deprecation Timeline

    1. **Phase 1**: v2 in development, v1 still primary
    2. **Phase 2**: v2 deployed, v1 still available
    3. **Phase 3**: v2 default, v1 archived (URL redirects)
    4. **Phase 4**: v1 code deleted (after 6 months)

    ## Rules

    1. **NEVER copy code from legacy**
       - Reference patterns only
       - Rewrite with v2 conventions

    2. **ALWAYS import from shared libs**
       - src/lib/ for services
       - src/components/ for UI

    3. **DOCUMENT migrations**
       - Update this file when page migrates
       - Note any issues encountered

    4. **TEST before marking complete**
       - Run E2E tests
       - Manual verification
       - Update migration status
    ```

    Place MIGRATION-POLICY.md in project root (not in src/).
  </action>
  <acceptance_criteria>
    - "MIGRATION-POLICY.md exists in project root"
    - "Priority table has all 13 pages listed"
    - "Sync Policy section explains when/how to sync"
    - "Exit Criteria section has checkboxes"
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 5: Create shared lib re-exports</name>
  <read_first>src/lib/prisma.ts</read_first>
  <files>src/v2/lib/</files>
  <action>
    Create v2 re-exports from shared libs:

    1. Create src/v2/lib/index.ts:
       ```
       // Re-exports from shared libs
       // This file provides a clean import path for v2

       // Prisma client
       export { prisma } from '@/lib/prisma'

       // i18n utilities
       export { getLocale, setLocale } from '@/lib/i18n'

       // Auth utilities
       export { session, auth } from '@/lib/auth'

       // Types
       export type * from '@/lib/types'
       ```

    2. Create src/v2/components/ui/index.ts:
       ```
       // Re-exports from shared components

       export * from '@/components/ui'
       ```

    3. Create src/v2/components/dashboard/.gitkeep
       (v2-specific dashboard components go here)

    4. Create src/v2/components/forms/.gitkeep
       (v2-specific form components go here)

    5. Create src/v2/components/admin/.gitkeep
       (v2-specific admin components go here)

    This establishes the "bridge" between shared code and v2.
  </action>
  <acceptance_criteria>
    - "src/v2/lib/index.ts re-exports from src/lib/"
    - "src/v2/components/ui/index.ts re-exports from src/components/ui"
    - "TypeScript compiles without errors"
    - "No actual components created yet (just re-exports)"
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 6: Verify structure and commit</name>
  <read_first>src/v2/README.md</read_first>
  <files>src/v2/</files>
  <action>
    Final verification:

    1. Verify directory structure:
       ```
       src/v2/
       ├── app/          (empty dirs only)
       ├── components/
       │   ├── ui/       (has index.ts)
       │   ├── dashboard/
       │   ├── forms/
       │   └── admin/
       ├── lib/          (has index.ts)
       ├── docs/          (has 3 doc files)
       └── README.md

       src/legacy/
       └── README.md

       MIGRATION-POLICY.md (root)
       ```

    2. Run basic checks:
       - npm run typecheck (should pass with minimal code)
       - Verify no broken imports

    3. Check that src/legacy/ has old code
    4. Check that src/app/ is minimal/empty

    5. DO NOT create placeholder pages yet
       (That will be done after schema is ready)
  </action>
  <acceptance_criteria>
    - "Directory structure matches spec"
    - "npm run typecheck passes"
    - "src/legacy/ contains old app code"
    - "src/app/ is minimal (not full v1)"
    - "git status shows expected changes"
  </acceptance_criteria>
</task>

</tasks>

