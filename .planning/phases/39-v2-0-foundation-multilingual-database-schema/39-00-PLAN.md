---
phase: "39"
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v2/
  - src/legacy/
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - RESTRUCTURE
must_haves:
  truths:
    - "src/v2/ directory created with proper structure"
    - "src/legacy/ contains old code for reference"
    - "src/lib/ contains common/shared code"
    - "src/components/ contains shared UI components"
    - "src/v2/app/ follows clean architecture"
artifacts:
  - path: "src/v2/"
    provides: "New v2 implementation directory"
    structure: "app/, components/, lib/"
  - path: "src/legacy/"
    provides: "Old implementation (reference only)"
    structure: "Original src/app content"
  - path: "src/v2/app/"
    provides: "v2 App Router pages"
    structure: "[locale]/, (auth)/, api/"
  - path: "src/v2/components/"
    provides: "v2 UI components"
    structure: "ui/, dashboard/, forms/, admin/"
key_links:
  - from: "src/lib/"
    to: "src/v2/"
    via: "Shared services imported by v2"
    pattern: "import.*lib/"
---

<objective>
Restructure source code to create clean v2 implementation while preserving legacy for reference.

Create new src/v2/ structure, move common code to src/lib/, and mark old code as legacy.
</objective>

<context>
@src/app/ (current structure)
@src/lib/ (existing common code)
@src/components/ (existing components)

Current problems:
- Mixed Mock UI and Working code in src/app/
- Unclear separation between pages, components, and services
- No clear distinction between v1 and v2 implementations

Target structure:
- src/v2/ - New clean implementation
- src/legacy/ - Old code (reference only)
- src/lib/ - Common/shared code
- src/components/ - Shared UI components
</context>

<tasks>

<task type="execute">
  <name>Task 1: Create src/v2/ directory structure</name>
  <read_first>src/app/[locale]/[workspaceSlug]/DashboardClient.tsx</read_first>
  <files>src/v2/</files>
  <action>
    Create src/v2/ directory with clean structure:

    src/v2/
    ├── app/
    │   ├── (auth)/sign-in/     # Auth pages
    │   ├── [locale]/           # i18n routing
    │   │   ├── dashboard/      # Customer dashboard
    │   │   ├── cases/         # My cases
    │   │   ├── create/        # Create request
    │   │   ├── messages/       # Messages
    │   │   ├── workspace/      # Workspace
    │   │   ├── settings/       # Settings
    │   │   └── admin/         # Admin portal
    │   │       ├── dashboard/
    │   │       ├── users/
    │   │       ├── requests/
    │   │       ├── vault/
    │   │       └── audit/
    │   └── api/               # API routes v2
    ├── components/             # v2 components
    │   ├── ui/               # Base UI (Ant Design wrappers)
    │   ├── dashboard/        # Dashboard-specific
    │   ├── forms/            # Form components
    │   └── admin/            # Admin-specific
    └── lib/                  # v2 services (if different from src/lib)

    Create all directories with placeholder README files explaining purpose.
  </action>
  <acceptance_criteria>
    - "src/v2/app/ directory exists with [locale]/ structure"
    - "src/v2/components/ directory exists with ui/, dashboard/, forms/, admin/ subdirs"
    - "src/v2/lib/ directory exists for v2-specific services"
    - "find src/v2 -type d returns all expected directories"
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Create src/legacy/ directory and move old code</name>
  <read_first>src/app/[locale]/[workspaceSlug]/page.tsx</read_first>
  <files>src/legacy/</files>
  <action>
    Move old src/app/ content to src/legacy/:

    1. Create src/legacy/ directory
    2. Move entire src/app/ content to src/legacy/:
       mv src/app/* src/legacy/
       mv src/app/.* src/legacy/ 2>/dev/null || true
    3. Create src/legacy/README.md explaining:
       - This directory contains old implementation
       - Reference only - can be deleted after v2 is complete
       - Document what each subdirectory contains

    After move, src/app/ should be empty (ready for v2).
  </action>
  <acceptance_criteria>
    - "src/legacy/ directory exists with all old app content"
    - "src/app/ directory is empty (or contains only placeholder)"
    - "src/legacy/README.md explains purpose and contents"
    - "git status shows src/app/ as clean, src/legacy/ as new"
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Create placeholder pages in src/v2/app/</name>
  <read_first>src/components/ui/Layout.tsx (if exists)</read_first>
  <files>src/v2/app/[locale]/</files>
  <action>
    Create minimal placeholder pages in src/v2/app/[locale]/:

    src/v2/app/[locale]/
    ├── layout.tsx          # Root layout with providers
    ├── page.tsx           # Home page (redirect to dashboard)
    ├── dashboard/
    │   └── page.tsx      # Placeholder: "Dashboard v2"
    ├── cases/
    │   └── page.tsx      # Placeholder: "Cases v2"
    ├── create/
    │   └── page.tsx      # Placeholder: "Create Request v2"
    ├── messages/
    │   └── page.tsx      # Placeholder: "Messages v2"
    ├── workspace/
    │   └── page.tsx      # Placeholder: "Workspace v2"
    ├── settings/
    │   └── page.tsx      # Placeholder: "Settings v2"
    └── admin/
        ├── layout.tsx    # Admin layout
        └── dashboard/
            └── page.tsx  # Placeholder: "Admin Dashboard v2"

    Each placeholder page should:
    - Import from shared lib/components
    - Show minimal "Coming soon" or "v2" indicator
    - Be ready to receive real content
  </action>
  <acceptance_criteria>
    - "src/v2/app/[locale]/dashboard/page.tsx exists"
    - "src/v2/app/[locale]/admin/dashboard/page.tsx exists"
    - "All placeholder pages compile without errors"
    - "npm run build succeeds with placeholder pages"
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Verify clean import paths</name>
  <read_first>src/lib/prisma.ts</read_first>
  <files>src/v2/</files>
  <action>
    Create import verification:

    1. Create src/v2/lib/ placeholder that re-exports from src/lib/:
       // src/v2/lib/prisma.ts
       export { prisma } from '@/lib/prisma'

    2. Create src/v2/components/ui/ placeholder:
       // src/v2/components/ui/index.ts
       export { default } from '@/components/ui'

    3. Create NEXT_STEPS.md in src/v2/:
       - Document how to migrate pages from legacy to v2
       - List priority pages to migrate first
       - Document common component patterns

    4. Verify imports work:
       - Create test file importing from src/v2/
       - Run TypeScript check
  </action>
  <acceptance_criteria>
    - "src/v2/lib/prisma.ts re-exports from src/lib/"
    - "src/v2/components/ui/index.ts re-exports from src/components/ui"
    - "NEXT_STEPS.md exists with migration guide"
    - "TypeScript compilation succeeds"
  </acceptance_criteria>
</task>

</tasks>

