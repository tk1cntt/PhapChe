---
phase: 41
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v2/app/api/intake/attach-file/route.ts
  - src/v2/app/api/intake/submit/route.ts
  - src/components/create-request/ServiceTypeSelector.tsx
  - src/components/create-request/CreateRequestForm.tsx
  - src/v2/app/[locale]/create/page.tsx
autonomous: true
requirements:
  - CUST-CREATE-03
  - CUST-CREATE-04
  - CUST-CREATE-06
must_haves:
  truths:
    - "User sees MatterType labels in their locale (VI/EN/ZH/JA)"
    - "User can upload documents to draft request"
    - "User can submit request and receive confirmation"
    - "Inline errors display below invalid fields"
    - "Create page renders wizard with all 4 steps"
  artifacts:
    - path: "src/v2/app/api/intake/attach-file/route.ts"
      provides: "Document upload endpoint"
      exports: ["POST"]
    - path: "src/v2/app/api/intake/submit/route.ts"
      provides: "Request submission endpoint"
      exports: ["POST"]
    - path: "src/components/create-request/ServiceTypeSelector.tsx"
      provides: "MatterType selection with multilingual labels"
    - path: "src/components/create-request/CreateRequestForm.tsx"
      provides: "Wizard Steps 3-4 with upload and submit"
    - path: "src/v2/app/[locale]/create/page.tsx"
      provides: "Create request wizard page route"
  key_links:
    - from: "CreateRequestForm.tsx"
      to: "/api/v2/intake/attach-file"
      via: "fetch with FormData"
      pattern: "fetch.*FormData"
    - from: "CreateRequestForm.tsx"
      to: "/api/v2/intake/submit"
      via: "fetch POST with requestId"
      pattern: "fetch.*submit"
    - from: "ServiceTypeSelector.tsx"
      to: "src/lib/i18n/seed-multilingual.ts"
      via: "import SEED_MATTER_TYPES"
      pattern: "SEED_MATTER_TYPES"
---

<objective>
Connect Create Request wizard Steps 3-4 to real API endpoints and update MatterType labels to use multilingual seed data. This enables document upload and request submission to the database.
</objective>

<context>
@D:/PhapChe/.planning/phases/40-v2-create-request-wizard/40-CONTEXT.md
@D:/PhapChe/.planning/phases/40-v2-create-request-wizard/40-SPEC.md
@D:/PhapChe/src/lib/intake/upload-service.ts
@D:/PhapChe/src/lib/intake/intake-service.ts
@D:/PhapChe/src/lib/i18n/seed-multilingual.ts
@D:/PhapChe/src/lib/i18n/get-localized-content.ts
@D:/PhapChe/src/components/create-request/CreateRequestForm.tsx
@D:/PhapChe/src/components/create-request/ServiceTypeSelector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v2 API endpoints (attach-file + submit)</name>
  <files>
    - src/v2/app/api/intake/attach-file/route.ts
    - src/v2/app/api/intake/submit/route.ts
  </files>
  <action>
Create two v2 API endpoints following the legacy patterns:

**src/v2/app/api/intake/attach-file/route.ts**:
- POST handler accepting multipart FormData
- Extract `file` and `requestId` from formData
- Validate: file must be File instance, requestId required
- Call `attachIntakeFile()` from `@/lib/intake/upload-service`
- Return `{ vaultFileId, filename, size }` on success
- Handle errors: FILE_REQUIRED (400), requestId required (400), storage error (503), forbidden (403)
- Import `attachIntakeFile` from `@/lib/intake/upload-service`, `requireAppSession` from `@/lib/security/session`

**src/v2/app/api/intake/submit/route.ts**:
- POST handler accepting JSON body
- Extract `requestId` from body
- Validate: requestId required
- Call `submitIntake()` from `@/lib/intake/intake-service`
- Return `{ id, status: 'intake_submitted' }` on success
- Handle errors: requestId required (400), not draft (400), missing answers (400), forbidden (403)
- Import `submitIntake` from `@/lib/intake/intake-service`, `requireAppSession` from `@/lib/security/session`

Per D-02 (Submission Flow): Reference `src/legacy/api/intake/submit/route.ts` for error handling pattern.
</action>
  <verify>
    <automated>grep -c "export async function POST" src/v2/app/api/intake/attach-file/route.ts src/v2/app/api/intake/submit/route.ts</automated>
  </verify>
  <done>Both endpoints return proper JSON responses with error codes and handle validation</done>
</task>

<task type="auto">
  <name>Task 2: Update ServiceTypeSelector to use SEED_MATTER_TYPES</name>
  <files>
    - src/components/create-request/ServiceTypeSelector.tsx
  </files>
  <action>
Update ServiceTypeSelector to use multilingual seed data instead of hardcoded options.

**Changes:**
1. Import `SEED_MATTER_TYPES` from `@/lib/i18n/seed-multilingual.ts`
2. Import `getLocalized` from `@/lib/i18n/get-localized-content.ts`
3. Replace hardcoded `SERVICE_OPTIONS` array with dynamic mapping from SEED_MATTER_TYPES
4. Map each MatterType to ServiceOption format:
   - `id`: use the MatterType key (e.g., 'labor_contract')
   - `title`: `getLocalized(locale, matterType.label)` 
   - `description`: `getLocalized(locale, matterType.description)`
   - `tags`: Add appropriate tag based on MatterType (e.g., 'Khuyen nghi' for agency_contract)
   - `estimatedTime`: Keep localized strings from existing options

5. Ensure ServiceOption interface matches the component's expected structure

Per D-03 (MatterType Labels): Import labels from seed constants using `getLocalized()` utility.

Note: The SEED_MATTER_TYPES keys are `labor_contract`, `agency_contract`, `trademark_registration`, `unsupported`. Map `trademark_registration` to `trademark` in the UI.
</action>
  <verify>
    <automated>grep -c "SEED_MATTER_TYPES" src/components/create-request/ServiceTypeSelector.tsx</automated>
  </verify>
  <done>ServiceTypeSelector displays MatterType labels from seed data in user's locale</done>
</task>

<task type="auto">
  <name>Task 3: Update CreateRequestForm Steps 3-4 with upload and submit logic</name>
  <files>
    - src/components/create-request/CreateRequestForm.tsx
  </files>
  <action>
Update CreateRequestForm to implement real upload and submission logic for Steps 3-4.

**State additions:**
- `requestId`: string | null (created on Step 2 transition or Step 3 upload)
- `uploadedFiles`: Array<{ vaultFileId: string; filename: string; size: number }>
- `isUploading`: boolean
- `isSubmitting`: boolean
- `errors`: Record<string, string> for inline field errors

**Step 3 (Documents) implementation:**
- File input with drag-drop zone
- On file select: POST to `/api/v2/intake/attach-file` with FormData (file + requestId)
- If no requestId, create draft first via `/api/intake/create-draft`
- Show upload progress (isUploading state)
- Display list of uploaded files with filename and size
- Remove file button for each uploaded file
- Per D-01 (Upload Flow): Use direct upload via `attachIntakeFile()`

**Step 4 (Review/Submit) implementation:**
- Display summary: selected service, uploaded files count, answers summary
- "Submit Request" button
- On submit: POST to `/api/v2/intake/submit` with { requestId }
- On success: Show confirmation message, redirect to dashboard after 2 seconds
- Per D-02 (Submission Flow): Reference legacy submit for request creation pattern

**Error handling (per D-04):**
- API errors return `{ error: "ERROR_CODE", message: "Human readable" }`
- Display error message below relevant form field
- Use error code to determine which field to highlight
- Submit button disabled during submission
- Loading state shown during API calls

**Navigation logic:**
- Step 1 -> 2: Create draft intake when proceeding from service selection
- Step 2 -> 3: Pass requestId forward
- Step 3 -> 4: Require at least 1 uploaded file (or allow 0 with warning)
- Step 4 -> Submit: Validate all steps complete

Use existing styling from create-request.css. Keep the form structure intact.
</action>
  <verify>
    <automated>grep -c "uploadedFiles\|isUploading\|isSubmitting" src/components/create-request/CreateRequestForm.tsx</automated>
  </verify>
  <done>User can upload documents in Step 3 and submit request in Step 4 with inline error handling</done>
</task>

<task type="auto">
  <name>Task 4: Create page route with CreateRequestForm</name>
  <files>
    - src/v2/app/[locale]/create/page.tsx
  </files>
  <action>
Create the Create Request page route that renders the wizard.

**src/v2/app/[locale]/create/page.tsx**:
- Import `CreateRequestForm` from `@/components/create-request/CreateRequestForm`
- Import `UserLayout` from `@/components/layout/UserLayout`
- Read locale from params (for multilingual support)
- Export default function that renders UserLayout with CreateRequestForm
- Pass locale prop to CreateRequestForm for multilingual labels

```typescript
import { UserLayout } from '@/components/layout/UserLayout';
import { CreateRequestForm } from '@/components/create-request/CreateRequestForm';

interface PageProps {
  params: Promise<{ locale: string }>;
}

export default async function CreateRequestPage({ params }: PageProps) {
  const { locale } = await params;
  
  return (
    <UserLayout>
      <CreateRequestForm locale={locale} />
    </UserLayout>
  );
}
```

Reference existing page patterns from src/v2/app/[locale]/dashboard/page.tsx if it exists.
</action>
  <verify>
    <automated>grep -c "CreateRequestForm" src/v2/app/[locale]/create/page.tsx</automated>
  </verify>
  <done>Create page renders wizard at /[locale]/create route</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/v2/intake/attach-file | Untrusted file input crosses here |
| client -> /api/v2/intake/submit | Untrusted requestId crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | /api/v2/intake/attach-file | mitigate | Validate File instance, size > 0, call attachIntakeFile which validates request access |
| T-40-02 | Denial | Large file upload | mitigate | attachIntakeFile has no size limit enforcement - add check: reject > 50MB |
| T-40-03 | Tampering | /api/v2/intake/submit | mitigate | requireAppSession validates user, canAccessRequest validates ownership |
| T-40-04 | Information Disclosure | Error messages | mitigate | Return generic messages for 500 errors, specific codes for validation |
| T-40-SC | Tampering | npm/pip/cargo installs | N/A | No new packages in this plan |
</threat_model>

<verification>
## Phase-level verification

1. **Document Upload**: POST /api/v2/intake/attach-file with valid session returns vaultFileId
2. **Request Submission**: POST /api/v2/intake/submit creates IntakeSubmission record
3. **MatterType Labels**: ServiceTypeSelector displays labels from SEED_MATTER_TYPES
4. **Locale Display**: Labels change when locale prop changes
5. **Error Display**: Invalid actions show inline errors below relevant fields
6. **Create Page Route**: /[locale]/create renders CreateRequestForm with all 4 steps
</verification>

<success_criteria>
- [ ] Step 3: User can select and upload file, file appears in uploaded list
- [ ] Step 4: User can review and submit request
- [ ] Submission creates database record and shows confirmation
- [ ] MatterType labels display in VI/EN/ZH/JA based on locale prop
- [ ] Inline errors appear below fields with invalid input
- [ ] Loading states shown during API calls
- [ ] Create page renders at /[locale]/create with UserLayout wrapper
</success_criteria>

<output>
Create `.planning/phases/41-v2-create-request-wizard/41-01-SUMMARY.md` when done
</output>
