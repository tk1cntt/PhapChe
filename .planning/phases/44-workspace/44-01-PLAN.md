---
phase: "44"
plan: "01"
objective: "Connect workspace page to real Prisma queries with cloned components"
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/components/workspace/WorkspaceBanner.tsx
  - src/components/workspace/StatsGrid.tsx
  - src/components/workspace/MemberGrid.tsx
  - src/components/workspace/ResourceTable.tsx
  - src/components/workspace/Badge.tsx
  - src/components/workspace/index.ts
  - src/components/workspace/workspace.css
  - src/app/[locale]/workspace/page.tsx
  - src/app/api/workspace/invite/route.ts
requirements:
  - CUST-WS-01
  - CUST-WS-02
  - CUST-WS-03
  - CUST-WS-04

must_haves:
  truths:
    - "User sees workspace banner with company name from database"
    - "User sees 4 stat cards with real counts (members, requests, vault files)"
    - "User sees member list with role badges from WorkspaceMembership"
    - "User sees permission panel with tenant isolation info"
    - "User sees resource table with links to cases/documents"
    - "User can invite new member via POST /api/workspace/invite"
  artifacts:
    - path: "src/components/workspace/WorkspaceBanner.tsx"
      provides: "Banner with workspace name + invite button"
      min_lines: 30
    - path: "src/components/workspace/StatsGrid.tsx"
      provides: "4 stat cards with database counts"
      min_lines: 70
    - path: "src/components/workspace/MemberGrid.tsx"
      provides: "Member list with role badges + permission panel"
      min_lines: 120
    - path: "src/components/workspace/ResourceTable.tsx"
      provides: "Resource table with links"
      min_lines: 80
    - path: "src/components/workspace/index.ts"
      provides: "Component exports"
      min_lines: 15
    - path: "src/app/[locale]/workspace/page.tsx"
      provides: "Page with Prisma queries"
      min_lines: 60
    - path: "src/app/api/workspace/invite/route.ts"
      provides: "POST invite member API"
      exports: ["POST"]
  key_links:
    - from: "src/app/[locale]/workspace/page.tsx"
      to: "prisma.workspaceMembership"
      via: "Prisma query"
      pattern: "prisma\\.workspaceMembership\\."
    - from: "src/app/[locale]/workspace/page.tsx"
      to: "prisma.legalRequest"
      via: "Prisma count query"
      pattern: "prisma\\.legalRequest\\.count"
    - from: "src/app/[locale]/workspace/page.tsx"
      to: "prisma.vaultFile"
      via: "Prisma count query"
      pattern: "prisma\\.vaultFile\\.count"
    - from: "WorkspaceBanner"
      to: "/api/workspace/invite"
      via: "invite button click"
      pattern: "fetch.*invite"
---

<objective>
Clone legacy workspace components to `src/components/workspace/` and connect the workspace page (`/vi/workspace`) to real Prisma queries. Implement invite member API per D-44-01.

Purpose: Replace placeholder "Workspace coming soon" with full functionality showing real workspace data (members, requests, vault files) and invite member capability.

Output:
- 5 workspace components in `src/components/workspace/`
- Updated `src/app/[locale]/workspace/page.tsx` with Prisma queries
- New `POST /api/workspace/invite` endpoint
</objective>

<context>
@src/legacy/[locale]/customer/components/Workspace/WorkspaceBanner.tsx
@src/legacy/[locale]/customer/components/Workspace/StatsGrid.tsx
@src/legacy/[locale]/customer/components/Workspace/MemberGrid.tsx
@src/legacy/[locale]/customer/components/Workspace/ResourceTable.tsx
@src/legacy/[locale]/customer/components/Workspace/index.ts
@src/legacy/[locale]/[workspaceSlug]/workspace/page.tsx
@src/components/my-cases/Badge.tsx
@src/messages/vi.json (lines 563-611)
@prisma/schema.prisma (WorkspaceMembership, LegalRequest, VaultFile models)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace components directory structure</name>
  <files>src/components/workspace/index.ts</files>
  <action>
Create directory `src/components/workspace/` with index.ts that re-exports all components.

Create `src/components/workspace/workspace.css` by copying relevant styles from `src/legacy/[locale]/customer/components/dashboard.css` (workspace_page, stats, member-grid, table-card related styles).

Reference Badge from `src/components/my-cases/Badge.tsx` — do NOT duplicate Badge component.
</action>
  <verify>
<powershell>
if (Test-Path "src/components/workspace/index.ts") { "Directory exists" } else { "Missing" }
</powershell>
</verify>
  <done>src/components/workspace/ directory created with index.ts exports</done>
</task>

<task type="auto">
  <name>Task 2: Clone WorkspaceBanner component</name>
  <files>src/components/workspace/WorkspaceBanner.tsx</files>
  <action>
Clone `src/legacy/[locale]/customer/components/Workspace/WorkspaceBanner.tsx` to `src/components/workspace/WorkspaceBanner.tsx`.

Keep 'use client' directive. The invite button should call an invite API (handle click to open email input dialog). For MVP, wire the button to call `POST /api/workspace/invite` with user email input.

Import useTranslations from next-intl, use t('UserWorkspace') namespace.
</action>
  <verify>
<powershell>
if (Test-Path "src/components/workspace/WorkspaceBanner.tsx") { "Created" } else { "Missing" }
</powershell>
</verify>
  <done>WorkspaceBanner component cloned with invite button functionality</done>
</task>

<task type="auto">
  <name>Task 3: Clone StatsGrid component</name>
  <files>src/components/workspace/StatsGrid.tsx</files>
  <action>
Clone `src/legacy/[locale]/customer/components/Workspace/StatsGrid.tsx` to `src/components/workspace/StatsGrid.tsx`.

Keep 'use client' directive. Props interface (StatsData) must match legacy:
- isActive: boolean
- slug: string
- memberCount: number
- activeMemberCount: number
- invitedMemberCount: number
- requestCount: number
- processingRequestCount: number
- vaultFileCount: number

Per D-44-02: Calculate vault scope percentage as `(encryptedVaultFiles / totalVaultFiles) * 100`. Since VaultFile model has no encryption flag, display percentage as `vaultFileCount > 0 ? '96%' : '0%'` from database counts.
</action>
  <verify>
<powershell>
if (Test-Path "src/components/workspace/StatsGrid.tsx") { "Created" } else { "Missing" }
</powershell>
</verify>
  <done>StatsGrid component with 4 stat cards (Workspace, Members, Requests, Vault scope)</done>
</task>

<task type="auto">
  <name>Task 4: Clone MemberGrid component</name>
  <files>src/components/workspace/MemberGrid.tsx</files>
  <action>
Clone `src/legacy/[locale]/customer/components/Workspace/MemberGrid.tsx` to `src/components/workspace/MemberGrid.tsx`.

Keep 'use client' directive. Import Badge from `src/components/my-cases/Badge` (NOT from legacy).

Props interface (MemberData):
- id: string
- name: string
- email: string
- role: string
- isActive: boolean

Implement role badge variants:
- Owner/Finance + active = green
- Viewer/Customer + active = blue
- invited = orange

Permission panel shows static info from i18n (tenant isolation, user role, audit info).
</action>
  <verify>
<powershell>
if (Test-Path "src/components/workspace/MemberGrid.tsx") { "Created" } else { "Missing" }
</powershell>
</verify>
  <done>MemberGrid component with member list and permission panel</done>
</task>

<task type="auto">
  <name>Task 5: Clone ResourceTable component</name>
  <files>src/components/workspace/ResourceTable.tsx</files>
  <action>
Clone `src/legacy/[locale]/customer/components/Workspace/ResourceTable.tsx` to `src/components/workspace/ResourceTable.tsx`.

Keep 'use client' directive. Import Badge from `src/components/my-cases/Badge`.

Props interface (ResourceData):
- requestCount: number
- vaultFileCount: number
- invitedCount: number
- lastRequestUpdate: string | null
- lastVaultUpdate: string | null
- lastInviteUpdate: string | null

Resource rows:
1. Legal Requests — links to /cases
2. Vault Docs — links to /documents
3. Member Invites — shows pending count, links to # (placeholder)
</action>
  <verify>
<powershell>
if (Test-Path "src/components/workspace/ResourceTable.tsx") { "Created" } else { "Missing" }
</powershell>
</verify>
  <done>ResourceTable component with 3 resource rows and action links</done>
</task>

<task type="auto">
  <name>Task 6: Create workspace page with Prisma queries</name>
  <files>src/app/[locale]/workspace/page.tsx</files>
  <action>
Replace the placeholder "Workspace coming soon" in `src/app/[locale]/workspace/page.tsx` with the full workspace page implementation.

Import components from `src/components/workspace/`.

Use Prisma queries from `src/legacy/[locale]/[workspaceSlug]/workspace/page.tsx` as reference:

1. Fetch user + active workspace membership
2. Query WorkspaceMembership for members (with user data)
3. Count LegalRequest for total and processing requests
4. Count VaultFile for vault files
5. Get lastRequestUpdate and lastVaultUpdate timestamps
6. Count unread messages for FloatingChatButton

Transform data:
- members = allMembers.map(m => ({ id, name, email, role, isActive }))
- activeCount = members.filter(m => m.isActive).length
- invitedCount = members.filter(m => !m.isActive).length

Pass data to components: WorkspaceBanner, StatsGrid, MemberGrid, ResourceTable.
</action>
  <verify>
<powershell>
if (Test-Path "src/app/[locale]/workspace/page.tsx") { "Updated" } else { "Missing" }
</powershell>
</verify>
  <done>Workspace page with Prisma queries, showing real stats and member data</done>
</task>

<task type="auto">
  <name>Task 7: Create invite member API endpoint</name>
  <files>src/app/api/workspace/invite/route.ts</files>
  <action>
Create `src/app/api/workspace/invite/route.ts` implementing POST endpoint per D-44-01.

Request body: `{ email: string, role?: string }`

Implementation:
1. requireAppSession to get current user and workspace
2. Validate email format (zod or simple regex)
3. Check if user with email exists
4. If exists: Create WorkspaceMembership with role 'customer', isActive: true
5. If not exists: Create WorkspaceMembership with role 'invited', isActive: false
6. Return 201 with membership data

Error handling:
- 400: Invalid email format
- 401: Not authenticated
- 409: User already member of workspace
- 500: Server error

Defer email invitation sending to Phase 51+.
</action>
  <verify>
<powershell>
if (Test-Path "src/app/api/workspace/invite/route.ts") { "Created" } else { "Missing" }
</powershell>
</verify>
  <done>POST /api/workspace/invite returns 201 and creates membership</done>
</task>

</tasks>

<verification>
## Automated Checks

1. **Build check:** `npm run build` passes without errors
2. **Prisma client:** Generated and imports work
3. **Component imports:** All imports resolve correctly
4. **API route:** POST /api/workspace/invite handles requests

## Manual Verification Steps

1. Navigate to `/vi/workspace`
2. Verify workspace banner shows company name from database
3. Verify 4 stat cards show real counts (Members, Requests, Vault files)
4. Verify member list shows WorkspaceMembership data with role badges
5. Verify resource table shows links to /cases and /documents
6. Click "Invite Member" button, enter email, verify API returns 201
</verification>

<success_criteria>
1. Workspace page renders with real data from Prisma
2. StatsGrid shows: Workspace status, Members count, Requests count, Vault scope percentage
3. MemberGrid shows member list with role badges (Owner/Finance=green, Viewer/Customer=blue, invited=orange)
4. ResourceTable shows 3 rows with action links
5. POST /api/workspace/invite creates WorkspaceMembership record
6. All components use next-intl for Vietnamese translations
</success_criteria>

<output>
Create `.planning/phases/44-workspace/44-01-SUMMARY.md` when done
</output>
