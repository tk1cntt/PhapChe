---
phase: "45"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "prisma/schema.prisma"
  - "src/components/settings/SettingsMenu.tsx"
  - "src/components/settings/SettingsStats.tsx"
  - "src/components/settings/ProfileForm.tsx"
  - "src/components/settings/ToggleRow.tsx"
  - "src/components/settings/index.ts"
  - "src/components/settings/settings.css"
autonomous: true
requirements:
  - "CUST-SET-01"
  - "CUST-SET-02"
  - "CUST-SET-05"
must_haves:
  truths:
    - "User sees 6-tab settings menu with icons (Ho so ca nhan, Bao mat, Thong bao, Workspace, Ngon ngu, Audit)"
    - "User sees settings stats cards pre-filled with real data from database"
    - "User sees profile form with 6 fields (name, email, phone, title, workspace, timezone)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "UserPreferences model for notification storage"
      contains: "model UserPreferences"
    - path: "src/components/settings/SettingsMenu.tsx"
      provides: "6-tab navigation menu"
      exports: "SettingsMenu, SettingsTab, SettingsTabConfig"
    - path: "src/components/settings/SettingsStats.tsx"
      provides: "4 stats cards component"
      exports: "SettingsStats"
    - path: "src/components/settings/ProfileForm.tsx"
      provides: "6-field profile form"
      exports: "ProfileForm, ProfileFormProps"
    - path: "src/components/settings/ToggleRow.tsx"
      provides: "Reusable toggle switch component"
      exports: "ToggleRow, ToggleRowProps"
  key_links:
    - from: "prisma/schema.prisma"
      to: "UserPreferences"
      via: "prisma.$executeSchema"
      pattern: "npx prisma db push"
---

<objective>
Clone and adapt Settings components from legacy to new structure. Add UserPreferences model to Prisma schema. Create foundation components that display real data from database.

Purpose: Establish the UI foundation and data layer for settings functionality.
Output: 6 Settings components, UserPreferences model, settings CSS.
</objective>

<context>
@D:/PhapChe/prisma/schema.prisma
@D:/PhapChe/src/legacy/[locale]/customer/components/Settings/SettingsMenu.tsx
@D:/PhapChe/src/legacy/[locale]/customer/components/Settings/SettingsStats.tsx
@D:/PhapChe/src/legacy/[locale]/customer/components/Settings/ProfileForm.tsx
@D:/PhapChe/src/legacy/[locale]/customer/components/Settings/ToggleRow.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UserPreferences model to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <action>
Add UserPreferences model to prisma/schema.prisma after the User model (before line 46):

```prisma
model UserPreferences {
  id            String   @id @default(cuid())
  userId        String   @unique
  emailOnReply  Boolean  @default(true)
  slaReminder   Boolean  @default(true)
  weeklySummary Boolean  @default(false)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}
```

Add relation to User model by adding this line after line 45 (before accounts):
```
  preferences    UserPreferences?
```

Then run `npx prisma db push` to sync the schema.
  </action>
  <read_first>
prisma/schema.prisma
  </read_first>
  <acceptance_criteria>
    - UserPreferences model exists in prisma/schema.prisma with fields: id, userId, emailOnReply, slaReminder, weeklySummary
    - User model has `preferences UserPreferences?` relation
    - Database has been synced: `npx prisma db push` executed without errors
  </acceptance_criteria>
  <verify>
    <automated>grep -c "model UserPreferences" prisma/schema.prisma</automated>
  </verify>
  <done>UserPreferences model added and database synced</done>
</task>

<task type="auto">
  <name>Task 2: Clone SettingsMenu and SettingsStats components</name>
  <files>
    - "src/components/settings/SettingsMenu.tsx"
    - "src/components/settings/SettingsStats.tsx"
    - "src/components/settings/settings.css"
  </files>
  <action>
Create directory `src/components/settings/` and clone components from legacy:

1. **SettingsMenu.tsx**: Copy from legacy but:
   - Change icons from emoji to lucide-react (User, Shield, Bell, Building2, Globe, FileText)
   - Add interface `SettingsStatsProps` with: accountStatus, securityStatus, notificationCount, workspaceCount
   - Make it accept props for dynamic values instead of hardcoded

2. **SettingsStats.tsx**: Copy from legacy but:
   - Accept props for dynamic stat values instead of hardcoded
   - Props: accountStatus: string, securityStatus: string, notificationCount: number, workspaceCount: number

3. **settings.css**: Copy relevant styles from legacy Settings components or create new file with Tailwind classes

Make sure SettingsMenu.tsx exports:
- `SettingsTab` type
- `SettingsTabConfig` interface
- `SettingsMenuProps` interface
- `SettingsMenu` component
  </action>
  <read_first>
src/legacy/[locale]/customer/components/Settings/SettingsMenu.tsx
src/legacy/[locale]/customer/components/Settings/SettingsStats.tsx
  </read_first>
  <acceptance_criteria>
    - SettingsMenu.tsx exports SettingsTab type with values: 'profile' | 'security' | 'notifications' | 'workspace' | 'language' | 'audit'
    - SettingsMenu uses lucide-react icons instead of emoji
    - SettingsStats.tsx accepts props for accountStatus, securityStatus, notificationCount, workspaceCount
    - settings.css file created with basic styles
  </acceptance_criteria>
  <verify>
    <automated>grep -c "lucide-react" src/components/settings/SettingsMenu.tsx && grep -c "SettingsStatsProps" src/components/settings/SettingsStats.tsx</automated>
  </verify>
  <done>SettingsMenu and SettingsStats cloned and adapted with proper icons and props</done>
</task>

<task type="auto">
  <name>Task 3: Clone ProfileForm and ToggleRow components</name>
  <files>
    - "src/components/settings/ProfileForm.tsx"
    - "src/components/settings/ToggleRow.tsx"
    - "src/components/settings/index.ts"
  </files>
  <action>
1. **ProfileForm.tsx**: Copy from legacy but:
   - Add interface `ProfileFormProps` with: user (name, email, phone, title, timezone, workspace), workspaces array, onSave callback
   - Use actual prop values instead of hardcoded defaults
   - Make workspace and timezone fields show options from props

2. **ToggleRow.tsx**: Copy from legacy but:
   - Add `onChange` callback prop to notify parent when toggle changes
   - Call `onChange?.(!checked)` when toggle is clicked

3. **index.ts**: Create barrel exports file:
   ```typescript
   export { SettingsMenu } from './SettingsMenu';
   export type { SettingsTab, SettingsTabConfig, SettingsMenuProps } from './SettingsMenu';
   export { SettingsStats } from './SettingsStats';
   export type { SettingsStatsProps } from './SettingsStats';
   export { ProfileForm } from './ProfileForm';
   export type { ProfileFormProps } from './ProfileForm';
   export { ToggleRow } from './ToggleRow';
   export type { ToggleRowProps } from './ToggleRow';
   ```
  </action>
  <read_first>
src/legacy/[locale]/customer/components/Settings/ProfileForm.tsx
src/legacy/[locale]/customer/components/Settings/ToggleRow.tsx
  </read_first>
  <acceptance_criteria>
    - ProfileForm.tsx accepts ProfileFormProps with user and onSave
    - ProfileForm renders 6 fields: name, email, phone, title, workspace, timezone
    - ToggleRow.tsx has onChange callback prop
    - index.ts exports all components and types
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ProfileFormProps" src/components/settings/ProfileForm.tsx && grep -c "onChange" src/components/settings/ToggleRow.tsx && grep -c "export" src/components/settings/index.ts</automated>
  </verify>
  <done>ProfileForm and ToggleRow cloned with proper props and callbacks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> DB | Form submissions and toggle changes cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | UserPreferences | mitigate | User can only view own preferences via session auth |
| T-45-02 | Tampering | ProfileForm | mitigate | Server-side validation in API routes will sanitize input |
</threat_model>

<verification>
- Components compile without TypeScript errors
- Prisma schema syncs successfully
- All exports are correct
</verification>

<success_criteria>
- UserPreferences model added to schema
- 4 Settings components created (SettingsMenu, SettingsStats, ProfileForm, ToggleRow)
- Components use lucide-react icons instead of emoji
- Components accept props for dynamic data
- Barrel exports created
</success_criteria>

<output>
Create `.planning/phases/45-settings/45-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Type | File | Purpose |
|--------|------|------|---------|
| `SettingsTab` | type | SettingsMenu.tsx | Tab identifier type |
| `SettingsTabConfig` | interface | SettingsMenu.tsx | Tab configuration |
| `SettingsMenuProps` | interface | SettingsMenu.tsx | Menu component props |
| `SettingsMenu` | component | SettingsMenu.tsx | 6-tab navigation |
| `SettingsStatsProps` | interface | SettingsStats.tsx | Stats component props |
| `SettingsStats` | component | SettingsStats.tsx | 4 stat cards |
| `ProfileFormProps` | interface | ProfileForm.tsx | Form component props |
| `ProfileForm` | component | ProfileForm.tsx | 6-field profile form |
| `ToggleRowProps` | interface | ToggleRow.tsx | Toggle component props |
| `ToggleRow` | component | ToggleRow.tsx | Reusable toggle switch |
| `UserPreferences` | model | prisma/schema.prisma | Notification preferences storage |
