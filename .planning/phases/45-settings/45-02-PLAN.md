---
phase: "45"
plan: "02"
type: "execute"
wave: 2
depends_on: ["45-01"]
files_modified:
  - "src/app/api/settings/profile/route.ts"
  - "src/app/api/settings/password/route.ts"
  - "src/app/api/settings/notifications/route.ts"
  - "src/app/api/settings/language/route.ts"
  - "src/app/api/settings/audit/route.ts"
autonomous: true
requirements:
  - "CUST-SET-02"
  - "CUST-SET-03"
  - "CUST-SET-04"
must_haves:
  truths:
    - "Profile update API persists name, email, phone, title, timezone to database"
    - "Password change API validates current password before updating"
    - "Notification preferences API persists emailOnReply, slaReminder, weeklySummary"
    - "Language API updates user locale field"
    - "Audit API returns user's audit events"
  artifacts:
    - path: "src/app/api/settings/profile/route.ts"
      provides: "Profile update endpoint"
      exports: "PUT handler"
    - path: "src/app/api/settings/password/route.ts"
      provides: "Password change endpoint"
      exports: "PUT handler"
    - path: "src/app/api/settings/notifications/route.ts"
      provides: "Notification preferences endpoint"
      exports: "GET, PUT handlers"
    - path: "src/app/api/settings/language/route.ts"
      provides: "Language preference endpoint"
      exports: "PUT handler"
    - path: "src/app/api/settings/audit/route.ts"
      provides: "Audit log endpoint"
      exports: "GET handler"
  key_links:
    - from: "src/app/api/settings/profile/route.ts"
      to: "prisma.user"
      via: "prisma.user.update"
      pattern: "prisma\\.user\\.update"
    - from: "src/app/api/settings/password/route.ts"
      to: "prisma.account"
      via: "bcrypt compare + account update"
      pattern: "bcrypt\\.(compare|hash)"
---

<objective>
Create API routes for all settings endpoints. Each route uses requireAppSession() for authentication and implements proper validation.

Purpose: Enable client components to persist settings changes to the database.
Output: 5 API route files (profile, password, notifications, language, audit).
</objective>

<context>
@D:/PhapChe/src/lib/security/session.ts
@D:/PhapChe/src/app/api/intake/submit/route.ts
@D:/PhapChe/45-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile and password API routes</name>
  <files>
    - "src/app/api/settings/profile/route.ts"
    - "src/app/api/settings/password/route.ts"
  </files>
  <action>
1. **src/app/api/settings/profile/route.ts**: Create PUT handler that:
   - Calls `requireAppSession()` to get userId
   - Parses JSON body: { name, email, phone, title, timezone }
   - Validates required fields: name, email must be non-empty strings
   - Updates user via `prisma.user.update({ where: { id: userId }, data: { name, email, phone, title, timezone } })`
   - Returns { success: true, user: updatedUser }

2. **src/app/api/settings/password/route.ts**: Create PUT handler that:
   - Calls `requireAppSession()` to get userId
   - Parses JSON body: { currentPassword, newPassword }
   - Validates both passwords are provided
   - Finds account with password: `prisma.account.findFirst({ where: { userId, password: { not: null } } })`
   - Verifies current password: `await bcrypt.compare(currentPassword, account.password)`
   - If invalid, returns 400 with error
   - Hashes new password: `await bcrypt.hash(newPassword, 10)`
   - Updates account: `prisma.account.update({ where: { id: account.id }, data: { password: newHash } })`
   - Returns { success: true }

Import bcrypt from 'bcryptjs' (project standard).
  </action>
  <read_first>
src/lib/security/session.ts
src/app/api/intake/submit/route.ts
  </read_first>
  <acceptance_criteria>
    - Profile PUT returns 400 if name or email missing
    - Profile PUT returns 401 if not authenticated
    - Password PUT returns 400 if currentPassword is incorrect
    - Password PUT returns 400 if newPassword is same as current
    - Password PUT uses bcrypt for both compare and hash
  </acceptance_criteria>
  <verify>
    <automated>grep -c "requireAppSession" src/app/api/settings/profile/route.ts && grep -c "bcrypt" src/app/api/settings/password/route.ts</automated>
  </verify>
  <done>Profile and password API routes created with proper validation and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Create notifications, language, and audit API routes</name>
  <files>
    - "src/app/api/settings/notifications/route.ts"
    - "src/app/api/settings/language/route.ts"
    - "src/app/api/settings/audit/route.ts"
  </files>
  <action>
1. **src/app/api/settings/notifications/route.ts**: Create GET and PUT handlers:
   - GET: Fetch user preferences, return defaults if not found
   - PUT: Upsert preferences with emailOnReply, slaReminder, weeklySummary booleans

2. **src/app/api/settings/language/route.ts**: Create PUT handler:
   - Accept { locale } in body (valid values: 'vi', 'en', 'zh', 'ja')
   - Update user's locale field via `prisma.user.update`
   - Return { success: true, locale }

3. **src/app/api/settings/audit/route.ts**: Create GET handler:
   - Fetch audit events where actorId = userId
   - Order by createdAt desc, take 50
   - Include workspace name
   - Return { data: auditEvents[] }

All routes must:
- Call `requireAppSession()` first
- Return proper error responses with status codes
- Use try/catch with error handling pattern from intake/submit
  </action>
  <read_first>
src/app/api/intake/submit/route.ts
prisma/schema.prisma
  </read_first>
  <acceptance_criteria>
    - Notifications GET returns defaults { emailOnReply: true, slaReminder: true, weeklySummary: false } if no preferences exist
    - Notifications PUT creates or updates preferences using upsert
    - Language PUT validates locale is one of: vi, en, zh, ja
    - Audit GET returns max 50 events ordered by createdAt desc
    - All routes return 401 if not authenticated
  </acceptance_criteria>
  <verify>
    <automated>grep -c "requireAppSession" src/app/api/settings/notifications/route.ts && grep -c "requireAppSession" src/app/api/settings/language/route.ts && grep -c "requireAppSession" src/app/api/settings/audit/route.ts</automated>
  </verify>
  <done>Notifications, language, and audit API routes created with proper authentication and error handling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Routes | All routes require authenticated session |
| Database | Password hashes stored, not plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Information Disclosure | All API routes | mitigate | requireAppSession() ensures only authenticated users access their own data |
| T-45-04 | Tampering | Password change | mitigate | bcrypt compare validates current password, hash new password with salt |
| T-45-05 | Elevation | Language update | mitigate | Validate locale is one of allowed values before update |
</threat_model>

<verification>
- All API routes return proper error codes (400, 401, 500)
- Password route uses bcrypt correctly
- Session auth enforced on all routes
</verification>

<success_criteria>
- 5 API routes created: profile, password, notifications, language, audit
- All routes require authentication
- Proper validation and error handling implemented
- Password changes use bcrypt
</success_criteria>

<output>
Create `.planning/phases/45-settings/45-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Type | File | Purpose |
|--------|------|------|---------|
| `PUT /api/settings/profile` | route | profile/route.ts | Update user profile |
| `PUT /api/settings/password` | route | password/route.ts | Change user password |
| `GET /api/settings/notifications` | route | notifications/route.ts | Get notification preferences |
| `PUT /api/settings/notifications` | route | notifications/route.ts | Update notification preferences |
| `PUT /api/settings/language` | route | language/route.ts | Update user locale |
| `GET /api/settings/audit` | route | audit/route.ts | Get user's audit events |
