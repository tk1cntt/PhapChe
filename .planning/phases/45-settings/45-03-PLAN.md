---
phase: "45"
plan: "03"
type: "execute"
wave: 3
depends_on: ["45-02"]
files_modified:
  - "src/app/[locale]/settings/page.tsx"
  - "src/app/[locale]/settings/SettingsClient.tsx"
  - "src/components/settings/SecuritySettings.tsx"
  - "src/components/settings/NotificationSettings.tsx"
  - "src/components/settings/LanguageSettings.tsx"
  - "src/components/settings/AuditSettings.tsx"
  - "src/components/settings/index.ts"
autonomous: true
requirements:
  - "CUST-SET-01"
  - "CUST-SET-02"
  - "CUST-SET-03"
  - "CUST-SET-04"
  - "CUST-SET-05"
must_haves:
  truths:
    - "User sees full Settings page at /vi/settings with 6-tab navigation"
    - "User can switch between tabs (profile/security/notifications/workspace/language/audit)"
    - "User can update profile and see changes persisted to database"
    - "User can change password with current password validation"
    - "User can toggle notification preferences"
    - "User can view their audit log entries"
  artifacts:
    - path: "src/app/[locale]/settings/page.tsx"
      provides: "Server component that fetches user data"
      exports: "SettingsPage default export"
    - path: "src/app/[locale]/settings/SettingsClient.tsx"
      provides: "Client component with tab state"
      exports: "SettingsClient"
    - path: "src/components/settings/SecuritySettings.tsx"
      provides: "Password change form + 2FA toggle"
      exports: "SecuritySettings"
    - path: "src/components/settings/NotificationSettings.tsx"
      provides: "Notification toggle preferences"
      exports: "NotificationSettings"
    - path: "src/components/settings/LanguageSettings.tsx"
      provides: "Language switcher"
      exports: "LanguageSettings"
    - path: "src/components/settings/AuditSettings.tsx"
      provides: "Audit log viewer"
      exports: "AuditSettings"
  key_links:
    - from: "src/app/[locale]/settings/page.tsx"
      to: "SettingsClient.tsx"
      via: "props passing"
      pattern: "<SettingsClient"
    - from: "SettingsClient.tsx"
      to: "/api/settings/*"
      via: "fetch API calls"
      pattern: "fetch.*api/settings"
---

<objective>
Integrate all settings components into the page. Create remaining Settings components (Security, Notification, Language, Audit). Connect page to Prisma queries.

Purpose: Deliver the complete working Settings page with all tabs functional.
Output: Settings page with 6 functional tabs, API integration, and user data from database.
</objective>

<context>
@D:/PhapChe/src/app/[locale]/cases/page.tsx
@D:/PhapChe/src/components/my-cases/MyCasesClient.tsx
@D:/PhapChe/.planning/phases/45-settings/45-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Settings page server component and SettingsClient</name>
  <files>
    - "src/app/[locale]/settings/page.tsx"
    - "src/app/[locale]/settings/SettingsClient.tsx"
  </files>
  <action>
1. **src/app/[locale]/settings/page.tsx**: Rewrite to:
   - Import { requireAppSession } from '@/lib/security/session'
   - Import { prisma } from '@/lib/prisma'
   - Import UserLayout, SettingsClient
   - Fetch user data with Prisma (id, name, email, phone, title, timezone, locale, memberships)
   - Fetch stats: accountRequests count, securityEvents count, notification preferences count, workspace count
   - Pass data to SettingsClient

2. **src/app/[locale]/settings/SettingsClient.tsx**: Create client component:
   - 'use client' directive
   - Import { useState } from 'react'
   - Import { useTranslations } from 'next-intl'
   - Import all Settings components from '@/components/settings'
   - Props interface: user data, stats, workspaces
   - State: activeTab (default 'profile')
   - Render: page header, SettingsStats, settings layout with SettingsMenu + tab content
   - Implement handleSaveProfile function that calls PUT /api/settings/profile
   - Implement tab content rendering based on activeTab
   - Export SettingsClient component

Follow the pattern from src/components/my-cases/MyCasesClient.tsx for state management.
  </action>
  <read_first>
src/app/[locale]/cases/page.tsx
src/components/my-cases/MyCasesClient.tsx
  </read_first>
  <acceptance_criteria>
    - Settings page fetches user data from Prisma
    - Settings page fetches settings stats from database
    - SettingsClient manages activeTab state
    - SettingsClient renders appropriate tab content based on activeTab
  </acceptance_criteria>
  <verify>
    <automated>grep -c "requireAppSession" src/app/\[locale\]/settings/page.tsx && grep -c "SettingsClient" src/app/\[locale\]/settings/page.tsx</automated>
  </verify>
  <done>Settings page server component and SettingsClient created with proper data fetching and tab management</done>
</task>

<task type="auto">
  <name>Task 2: Create SecuritySettings and NotificationSettings components</name>
  <files>
    - "src/components/settings/SecuritySettings.tsx"
    - "src/components/settings/NotificationSettings.tsx"
  </files>
  <action>
1. **src/components/settings/SecuritySettings.tsx**: Create component:
   - Props: userId, has2FA (boolean)
   - State: currentPassword, newPassword, confirmPassword, loading, error
   - Password change form with 3 fields
   - Validate: newPassword === confirmPassword
   - Submit to PUT /api/settings/password
   - Show success/error messages
   - 2FA toggle (stubbed - just shows current status, no API call for MVP)
   - Import: Shield, Eye, EyeOff from lucide-react
   - Export: SecuritySettings

2. **src/components/settings/NotificationSettings.tsx**: Create component:
   - Props: preferences (emailOnReply, slaReminder, weeklySummary)
   - 3 ToggleRow components for each preference
   - On toggle change, call PUT /api/settings/notifications
   - Optimistic UI update (update state immediately, then call API)
   - Show loading state during API call
   - Export: NotificationSettings

Update index.ts to export these new components.
  </action>
  <read_first>
src/components/settings/ToggleRow.tsx
src/components/settings/index.ts
  </read_first>
  <acceptance_criteria>
    - SecuritySettings has password change form with 3 fields
    - SecuritySettings validates newPassword === confirmPassword
    - NotificationSettings has 3 toggle rows
    - NotificationSettings calls API on toggle change
    - Both components export properly
  </acceptance_criteria>
  <verify>
    <automated>grep -c "SecuritySettings" src/components/settings/SecuritySettings.tsx && grep -c "NotificationSettings" src/components/settings/NotificationSettings.tsx</automated>
  </verify>
  <done>SecuritySettings and NotificationSettings components created with form handling and API integration</done>
</task>

<task type="auto">
  <name>Task 3: Create LanguageSettings and AuditSettings components</name>
  <files>
    - "src/components/settings/LanguageSettings.tsx"
    - "src/components/settings/AuditSettings.tsx"
  </files>
  <action>
1. **src/components/settings/LanguageSettings.tsx**: Create component:
   - Props: currentLocale, onLocaleChange callback
   - Display language options: Vietnamese (vi), English (en), Chinese (zh), Japanese (ja)
   - Show radio buttons or selector for language selection
   - On selection, call PUT /api/settings/language
   - After API success, show success message and suggest page refresh
   - Export: LanguageSettings

2. **src/components/settings/AuditSettings.tsx**: Create component:
   - Props: userId
   - State: auditEvents (array), loading
   - useEffect to fetch GET /api/settings/audit on mount
   - Display audit events in a list/table with: action, targetType, workspace, timestamp
   - Show loading state while fetching
   - Show empty state if no events
   - Export: AuditSettings

Update index.ts to export these new components.
  </action>
  <read_first>
src/components/settings/index.ts
  </read_first>
  <acceptance_criteria>
    - LanguageSettings shows 4 language options (vi, en, zh, ja)
    - LanguageSettings calls PUT /api/settings/language on selection
    - AuditSettings fetches and displays audit events
    - AuditSettings shows empty state when no events
    - Both components export properly
  </acceptance_criteria>
  <verify>
    <automated>grep -c "LanguageSettings" src/components/settings/LanguageSettings.tsx && grep -c "AuditSettings" src/components/settings/AuditSettings.tsx</automated>
  </verify>
  <done>LanguageSettings and AuditSettings components created with proper API integration</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Form submissions cross here with user data |
| API -> DB | Queries and updates cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06 | Information Disclosure | AuditSettings | mitigate | API filters by session userId, users only see own events |
| T-45-07 | Tampering | LanguageSettings | mitigate | Server validates locale is one of allowed values |
| T-45-08 | Denial | NotificationSettings | mitigate | Simple toggle update, no heavy operations |
</threat_model>

<verification>
- Settings page loads at /vi/settings
- All 6 tabs are accessible
- Profile form submits successfully
- Password change validates properly
- Toggles persist changes
- Audit log displays events
</verification>

<success_criteria>
- Full Settings page functional at /vi/settings
- 6 tabs work: profile, security, notifications, workspace, language, audit
- Data from database displayed correctly
- Changes persist to database
- Page loads without errors
</success_criteria>

<output>
Create `.planning/phases/45-settings/45-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Type | File | Purpose |
|--------|------|------|---------|
| `SettingsPage` | component | settings/page.tsx | Server component - fetches user data |
| `SettingsClient` | component | settings/SettingsClient.tsx | Client component - manages tabs |
| `SecuritySettings` | component | SecuritySettings.tsx | Password change + 2FA |
| `NotificationSettings` | component | NotificationSettings.tsx | Notification preferences |
| `LanguageSettings` | component | LanguageSettings.tsx | Language switcher |
| `AuditSettings` | component | AuditSettings.tsx | Audit log viewer |
