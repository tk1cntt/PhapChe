---
phase: 46-admin-dashboard
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - ADMIN-DASH-REAL-01
  - ADMIN-DASH-REAL-02
  - ADMIN-DASH-REAL-03
  - ADMIN-DASH-REAL-04

must_haves:
  truths:
    - "User sees 4 stat cards with real counts from database"
    - "User sees workload panel with specialist progress bars"
    - "User sees alerts panel with real data"
    - "User sees requests table with data from LegalRequest"
    - "Admin banner shows real system status"
  artifacts:
    - path: "src/components/admin/AdminStatCard.tsx"
      provides: "Stat card with variant colors and icon"
      min_lines: 60
    - path: "src/components/admin/AdminBanner.tsx"
      provides: "System status banner with buttons"
      min_lines: 70
    - path: "src/components/admin/WorkloadPanel.tsx"
      provides: "Specialist progress panel"
      min_lines: 80
    - path: "src/components/admin/AlertPanel.tsx"
      provides: "Alerts list panel"
      min_lines: 60
    - path: "src/components/admin/WorkspacePanel.tsx"
      provides: "Featured workspaces panel"
      min_lines: 60
    - path: "src/components/admin/ApprovalPanel.tsx"
      provides: "Pending approvals panel"
      min_lines: 60
    - path: "src/components/admin/AuditTimeline.tsx"
      provides: "Recent audit events timeline"
      min_lines: 100
    - path: "src/components/admin/AdminToolbar.tsx"
      provides: "Search, filter, and export toolbar"
      min_lines: 100
    - path: "src/components/admin/AdminRequestsTable.tsx"
      provides: "Requests table with status/SLA badges"
      min_lines: 200
    - path: "src/components/admin/AdminDashboardClient.tsx"
      provides: "Client component orchestrating all panels"
      min_lines: 150
    - path: "src/app/[locale]/admin/page.tsx"
      provides: "Server component with Prisma queries"
      exports: ["AdminDashboardPage"]
  key_links:
    - from: "src/app/[locale]/admin/page.tsx"
      to: "src/components/admin/*"
      via: "props passing"
      pattern: "AdminDashboardClient"
    - from: "src/components/admin/AdminDashboardClient.tsx"
      to: "src/components/admin/*"
      via: "import"
      pattern: "import.*from.*admin"
---

<objective>
Clone 8 admin components from legacy to src/components/admin/ with Tailwind CSS conversion, then create admin page.tsx with Prisma queries for real data. Phase 46 delivers the full Admin Dashboard view with layout, stat cards, workload panel, alerts, workspaces, approvals, timeline, and request table — all connected to database.
</objective>

<execution_context>
@D:/PhapChe/.claude/gsd-core/workflows/execute-plan.md
@D:/PhapChe/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@src/legacy/[locale]/admin/page.tsx
@src/legacy/components/admin/*.tsx
@src/legacy/[locale]/admin/AdminDashboardClient.tsx
@src/components/settings/settings.css
@src/app/[locale]/settings/page.tsx
</context>

<read_first>
src/legacy/components/admin/AdminStatCard.tsx
src/legacy/components/admin/AdminBanner.tsx
src/legacy/components/admin/WorkloadPanel.tsx
src/legacy/components/admin/AlertPanel.tsx
src/legacy/components/admin/WorkspacePanel.tsx
src/legacy/components/admin/ApprovalPanel.tsx
src/legacy/components/admin/AuditTimeline.tsx
src/legacy/components/admin/AdminToolbar.tsx
src/legacy/components/admin/AdminRequestsTable.tsx
src/legacy/[locale]/admin/AdminDashboardClient.tsx
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Wave 1: Clone 8 Admin Components with Tailwind CSS</name>
  <files>
    src/components/admin/AdminStatCard.tsx
    src/components/admin/AdminBanner.tsx
    src/components/admin/WorkloadPanel.tsx
    src/components/admin/AlertPanel.tsx
    src/components/admin/WorkspacePanel.tsx
    src/components/admin/ApprovalPanel.tsx
    src/components/admin/AuditTimeline.tsx
    src/components/admin/AdminToolbar.tsx
    src/components/admin/AdminRequestsTable.tsx
    src/components/admin/admin.css
  </files>
  <behavior>
    - AdminStatCard renders with variant colors (blue/green/orange/red), icon, title, value, description
    - AdminBanner shows system status with title, description, and 2 action buttons
    - WorkloadPanel displays specialist list with avatar initials, name, role badge, progress bar
    - AlertPanel shows alert items with icon, title, description, badge
    - WorkspacePanel displays workspace items with initials, name, description, badge
    - ApprovalPanel shows pending approval items
    - AuditTimeline renders vertical timeline with dots and entries
    - AdminToolbar has search input, filter/status/workspace dropdowns, export/refresh/columns buttons
    - AdminRequestsTable displays 7-column table with request rows and status/SLA badges
  </behavior>
  <action>
Create src/components/admin/ directory and clone all 8 admin components from legacy. Convert all inline styles to Tailwind CSS classes following these mappings:

**Layout patterns:**
- `height: 126, padding: 24` -> `h-[126px] p-6`
- `border-radius: 15` -> `rounded-[15px]`
- `box-shadow: var(--soft-shadow)` -> `shadow-md`
- `display: flex, align-items: center` -> `flex items-center`
- `display: grid, grid-template-columns` -> `grid grid-cols-X`

**Color patterns:**
- `color: #2563eb` -> `text-blue-600`
- `background: #fff` -> `bg-white`
- `border: 1px solid var(--border)` -> `border border-gray-200`

**Component-specific conversions:**

1. **AdminStatCard.tsx** (per D-03, D-04):
   - Props: variant ('blue'|'green'|'orange'|'red'), title, value, description, icon (ReactNode)
   - Variant styles map to Tailwind gradient backgrounds
   - Icon container: 62x62px rounded-[13px] with gradient background
   - Stats: title (14px/600), value (30px/800), description (13px/500)

2. **AdminBanner.tsx** (per D-03, D-04):
   - Props: title, description
   - Gradient background: `bg-gradient-to-br from-white to-teal-50`
   - Shield SVG icon in green gradient container (64x64px rounded-2xl)
   - Two action buttons: outline (border) and filled (bg-teal gradient)

3. **WorkloadPanel.tsx** (per D-03, D-04):
   - Props: specialists array with initials, name, role, progress, status, count
   - Panel title with BarChart2 icon and "Xem chi tiet ->" link
   - WorkloadItem: avatar circle with initials, name/role text, progress bar (colored by status), count
   - Progress bar: h-2 rounded-full, bg-gray-200, colored fill (green/orange/red by status)

4. **AlertPanel.tsx** (per D-03, D-04):
   - Props: alerts array with icon, iconColor, title, description, badge, badgeColor
   - AlertItem: left section (icon circle + text), right badge
   - Icon circles: w-9 h-9 rounded-full, colored by iconColor

5. **WorkspacePanel.tsx** (per D-03, D-04):
   - Props: workspaces array
   - Similar structure to AlertPanel but with Building icon

6. **ApprovalPanel.tsx** (per D-03, D-04):
   - Props: approvals array
   - Similar structure to AlertPanel with CheckCircle icon

7. **AuditTimeline.tsx** (per D-03, D-04):
   - Props: entries array with title, description, time
   - Vertical timeline with absolute positioned line (left-3, w-0.5, bg-gray-200)
   - Timeline dots: absolute left-1.5 top-1 w-4 h-4 rounded-full bg-teal-600 border-4 border-teal-100
   - Content padding-left for dot space

8. **AdminToolbar.tsx** (per D-03, D-04):
   - Props: onSearch, onFilter, onExport callbacks, translations object
   - Search input: w-[370px] h-11 with search SVG icon
   - Dropdown buttons with chevron-down SVG
   - Icon buttons (refresh) w-13 h-11

9. **AdminRequestsTable.tsx** (per D-03, D-04):
   - Props: rows array with id, type, workspace, workspaceSlug, customer, customerEmail, status, statusText, assignee, assigneeRole, sla, slaText, action
   - Table header: grid grid-cols-[1.05fr_1fr_1fr_1fr_1fr_0.9fr_0.9fr] bg-gradient-to-b from-slate-50 to-slate-100
   - Table row: same grid, hover:bg-slate-50/50
   - Badge component: inline-flex items-center h-7 px-3 rounded-full text-xs font-bold with colored bg/text
   - Document icon circle for ID column

10. **admin.css** - Create shared styles for admin components if needed:
    - Panel container: bg-white border border-gray-200 rounded-[15px] shadow-md p-6
    - Panel title: flex justify-between items-center mb-4
    - Item lists with gap-3
    - Tailwind classes handle most styling

Create AdminDashboardClient.tsx to orchestrate all components, passing props from server component.
  </action>
  <verify>
    <automated>npm run build -- --filter=admin 2>&1 | head -50 || echo "Build check - inspect errors manually"</automated>
  </verify>
  <done>
All 8 components clone with Tailwind CSS, AdminDashboardClient orchestrates layout matching legacy structure (4 stat cards, banner, workload+alerts grid, 3-panel grid, toolbar, table)
  </done>
</task>

<task type="auto">
  <name>Wave 2: Create Admin Page with Prisma Queries</name>
  <files>
    src/app/[locale]/admin/page.tsx
  </files>
  <action>
Create src/app/[locale]/admin/page.tsx as server component. Import and use patterns from:

1. src/app/[locale]/settings/page.tsx (pattern reference)
2. src/legacy/[locale]/admin/page.tsx (Prisma queries to replicate)

**Implementation (per D-02, D-05):**

```typescript
import { prisma } from '@/lib/prisma';
import { requireAdminSession } from '@/lib/security/session';
import AdminDashboardClient from '@/components/admin/AdminDashboardClient';

export default async function AdminDashboardPage({ params }) {
  const { locale } = await params;
  // Verify admin session (reuse pattern from settings or legacy)
  
  // Parallel Prisma queries (same as legacy):
  const [
    totalUsers, activeUsers, invitedUsers,
    totalWorkspaces, activeWorkspaces,
    openRequests, nearSlaRequests, auditAlertsCount,
    specialistsWithWorkload,
    featuredWorkspaces,
    pendingApprovalsRaw,
    recentAuditEvents,
    recentRequests,
  ] = await Promise.all([
    // User counts
    prisma.user.count(),
    prisma.user.count({ where: { isActive: true } }),
    prisma.user.count({ where: { emailVerified: false } }),
    
    // Workspace counts
    prisma.workspace.count(),
    prisma.workspace.count({ where: { isActive: true } }),
    
    // Request counts
    prisma.legalRequest.count({ where: { status: { notIn: ['closed', 'cancelled', 'delivered'] } } }),
    prisma.legalRequest.count({ where: { slaDeadline: { lte: new Date(Date.now() + 4 * 60 * 60 * 1000), not: null }, status: { notIn: ['closed', 'cancelled', 'delivered'] } } }),
    prisma.auditEvent.count({ where: { createdAt: { gte: new Date(Date.now() - 24 * 60 * 60 * 1000) }, action: { in: ['access_denied', 'permission_change', 'unauthorized_access_attempt'] } } }),
    
    // Specialists with workload
    prisma.user.findMany({ where: { isActive: true, memberships: { some: { role: { in: ['specialist', 'reviewer', 'coordinator_admin'] } } } }, include: { memberships: { where: { role: { in: ['specialist', 'reviewer', 'coordinator_admin'] } } }, specialistRequests: true, reviewerRequests: true }, take: 5 }),
    
    // Top workspaces
    prisma.workspace.findMany({ include: { memberships: true, requests: { where: { status: { notIn: ['closed', 'cancelled'] } } } }, orderBy: { createdAt: 'desc' }, take: 3 }),
    
    // Pending approvals
    prisma.user.findMany({ where: { emailVerified: false }, take: 3 }),
    
    // Recent audit events
    prisma.auditEvent.findMany({ orderBy: { createdAt: 'desc' }, take: 5, include: { actor: { select: { name: true, id: true } }, workspace: { select: { name: true } } } }),
    
    // Recent requests
    prisma.legalRequest.findMany({ orderBy: { createdAt: 'desc' }, take: 5, include: { workspace: { select: { name: true, slug: true } }, createdBy: { select: { name: true, email: true } }, assignedSpecialist: { select: { name: true, email: true } } } }),
  ]);
```

**Transform data for client:**
- Stats object: users (total, active, invited), workspaces (total, active), nearSla, auditAlerts, openRequests
- WorkloadData: map specialists to initials (first 2 letters), role labels, progress percentage, status ('ok'|'warn'|'danger')
- WorkspaceData: map workspaces to initials, iconColor based on slug, badge
- ApprovalData: map pending users to icon/title/description/badges
- TimelineData: calculate relative time strings (phut/gio/ngay)
- RequestTableData: map LegalRequest to table format with statusColors and slaColor calculations
- AlertData: static alerts with dynamic nearSla and pendingApprovals counts

**Pass to AdminDashboardClient:**
```typescript
return (
  <AdminDashboardClient
    stats={stats}
    workloadData={workloadData}
    alertData={alertData}
    workspaceData={workspaceData}
    approvalData={approvalData}
    timelineData={timelineData}
    requestTableData={requestTableData}
    translations={...}
  />
);
```

If requireAdminSession does not exist, use requireAppSession and check for admin role in session.roles.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(Error|error|AdminDashboard)" | head -20</automated>
  </verify>
  <done>
Admin dashboard page renders at /vi/admin with real database counts, workload panel, alerts, workspaces, approvals, timeline, and request table all populated from Prisma queries
  </done>
</task>

</tasks>

<verification>
- [ ] All 8 components exist in src/components/admin/
- [ ] AdminDashboardClient.tsx orchestrates layout matching legacy
- [ ] admin/page.tsx has Prisma queries for all data
- [ ] Stat cards show real user/workspace counts
- [ ] Workload panel shows specialist progress
- [ ] Alert panel shows dynamic alerts
- [ ] Request table shows real LegalRequest data
</verification>

<success_criteria>
- Admin Dashboard accessible at /vi/admin and /en/admin
- 4 stat cards display real counts from database
- Workload panel shows specialist progress bars
- Alert panel displays system alerts
- Workspace panel shows featured workspaces
- Approval panel shows pending users
- Audit timeline shows recent events
- Request table displays recent legal requests
- All components use Tailwind CSS (no inline styles except dynamic values)
</success_criteria>

<output>
Create `.planning/phases/46-admin-dashboard/46-01-SUMMARY.md` when done
</output>
