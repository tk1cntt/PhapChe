---
phase: 73
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - prisma/seed/foundation.ts
  - prisma/seed/operations.ts
  - prisma/seed/partners.ts
  - prisma/seed/index.ts
  - prisma/seed.ts
  - prisma/seed-customer-dashboard.ts
  - prisma/seed-dashboard-activity.ts
  - prisma/seed-messages.ts
  - prisma/seed-my-cases.ts
  - prisma/seed-org-activity.ts
  - prisma/seed-partners.ts
  - prisma/seed-user-activity.ts
  - prisma/seed-user-orgs.ts
autonomous: true
requirements: [FOUND-01, FOUND-02]
---

# Plan 01: Seed Data Framework

## Objective

Tạo organized seed data framework trong `prisma/seed/` với domain-specific files, wipe+reseed strategy với transaction rollback, và realistic Vietnamese data. Consolidate 8 scattered seed files thành organized structure.

## Tasks

### Task 1.1: Create seed directory structure and index orchestrator

<read_first>
- prisma/seed.ts (existing seed entry point — must be preserved and extended)
- prisma/schema.prisma (understand all models and relationships)
- .planning/phases/73-shared-foundation/73-CONTEXT.md (Decision 1: seed by business layer)
- .planning/phases/73-shared-foundation/73-SPEC.md (requirement FOUND-01)
- .planning/phases/73-shared-foundation/73-RESEARCH.md (seed data relationships, wipe strategy)
</read_first>

<action>
Create `prisma/seed/` directory with `index.ts` orchestrator that:
1. Imports `prisma` from `../../src/lib/prisma`
2. Exports `seedAll(tx: Prisma.TransactionClient)` function
3. Calls seed functions sequentially: `seedFoundation(tx)` → `seedOperations(tx)` → `seedPartners(tx)`
4. Each domain file exports a default function accepting `Prisma.TransactionClient`
5. Returns summary object with counts of created records
</action>

<acceptance_criteria>
- `prisma/seed/index.ts` exists and exports `seedAll` function
- `prisma/seed/index.ts` imports from `./foundation`, `./operations`, `./partners`
- File uses `Prisma.TransactionClient` type for transaction parameter
- Sequential execution order: foundation → operations → partners
</acceptance_criteria>

<verify>
- `npx tsc --noEmit prisma/seed/index.ts` — TypeScript compiles without errors
- `grep -c 'import' prisma/seed/index.ts` — has 3 domain imports
</verify>

### Task 1.2: Implement foundation seed (users, orgs, workspaces)

<read_first>
- prisma/seed.ts (existing user creation pattern with `ensureUser`, `hashPassword`, Account creation)
- prisma/schema.prisma (User, Account, Organization, Tenant, Workspace, WorkspaceMembership models)
- .planning/phases/73-shared-foundation/73-CONTEXT.md (Decision 1: seed by business layer)
- .planning/phases/73-shared-foundation/73-RESEARCH.md (existing seed.ts must be preserved)
</read_first>

<action>
Create `prisma/seed/foundation.ts` with `seedFoundation(tx)` that creates:
1. **1 Tenant**: "GitNexus Platform" (platform type)
2. **3 Organizations** with Vietnamese names:
   - "Công ty Luật Hợp Danh Minh Anh" (businessType: LEGAL_FIRM, registrationNumber: '0123456789')
   - "Văn phòng Luật sư Trần & Cộng sự" (businessType: LEGAL_FIRM, registrationNumber: '9876543210')
   - "Công ty Tư vấn Pháp lý Đại Việt" (businessType: CONSULTING, registrationNumber: '5678901234')
3. **10 Users** with Vietnamese names, realistic emails, passwords via `hashPassword()`:
   - superadmin@phapche.test (Nguyễn Văn An, super_admin)
   - admin@phapche.test (Trần Thị Bình, coordinator_admin)
   - coordinator@phapche.test (Lê Văn Cường, coordinator_admin)
   - specialist1@phapche.test (Phạm Thị Dung, specialist)
   - specialist2@phapche.test (Hoàng Văn Em, specialist)
   - reviewer1@phapche.test (Vũ Thị Phương, reviewer)
   - reviewer2@phapche.test (Đặng Văn Giang, reviewer)
   - customer1@phapche.test (Bùi Thị Hương, customer)
   - customer2@phapche.test (Đỗ Văn Ích, customer)
   - customer3@phapche.test (Ngô Thị Kim, customer)
4. **5 Workspaces** with Vietnamese names: "Phòng Pháp lý ABC", "Văn phòng XYZ", etc.
5. **WorkspaceMemberships** linking users to workspaces with appropriate roles
6. All deletes happen in correct order at start (inside transaction):
   - Delete WorkspaceMembership → delete Workspace → delete Organization → delete Account → delete User → delete Tenant
7. Uses `tx` (transaction client) for ALL operations, not standalone prisma instance
</action>

<acceptance_criteria>
- `prisma/seed/foundation.ts` exports default function `seedFoundation(tx: Prisma.TransactionClient)`
- Creates exactly 10 users, 3 organizations, 1 tenant, 5 workspaces
- All user emails end with `@phapche.test`
- All passwords hashed with `hashPassword()` from `@better-auth/utils/password`
- Account records created for each user with `providerId: 'credential'`
- Wipe deletes in reverse dependency order before creating
- All CRUD uses `tx` not `prisma`
- All names are Vietnamese (not placeholder English)
</acceptance_criteria>

<verify>
- `npx tsc --noEmit prisma/seed/foundation.ts` — compiles
- `grep -c 'tx\.' prisma/seed/foundation.ts` — all operations use tx
- `grep 'hashPassword' prisma/seed/foundation.ts` — password hashing present
</verify>

### Task 1.3: Implement operations seed (requests, audit, vault, messages)

<read_first>
- prisma/schema.prisma (LegalRequest, AuditEvent, VaultFile, Message models)
- prisma/seed-messages.ts (existing message seed pattern with realistic Vietnamese content)
- .planning/phases/73-shared-foundation/73-SPEC.md (R1: 50 requests, 100 audit events, 20 vault files, 30 messages)
</read_first>

<action>
Create `prisma/seed/operations.ts` with `seedOperations(tx, context)` that creates:
1. **50 LegalRequests** with Vietnamese titles:
   - "Tư vấn hợp đồng lao động", "Soạn thảo hợp đồng thương mại", "Đăng ký nhãn hiệu"
   - Distributed across statuses: draft (10), submitted (5), in-progress (15), review (10), completed (10)
   - Realistic codes: REQ-2026-001, REQ-2026-002, etc.
2. **100 AuditEvents** with realistic actions:
   - "user.login", "request.create", "document.upload", "workflow.transition"
   - Distribution: security events (20), operations events (60), access events (20)
3. **20 VaultFiles** with Vietnamese filenames:
   - "Hợp đồng lao động Nguyễn Văn A.pdf", "Giấy phép kinh doanh.docx"
   - Realistic MIME types: application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document
4. **30 Messages** with realistic Vietnamese content:
   - "Kính gửi Quý khách, chúng tôi đã nhận được yêu cầu...", "Cảm ơn Quý khách đã sử dụng dịch vụ..."
   - Reuse patterns from existing seed-messages.ts
5. Context parameter receives IDs from foundation seed (user IDs, workspace IDs) for foreign keys
6. Wipe: delete DocumentVersion → AuditEvent → Message → VaultFile → LegalRequest (in order)
</action>

<acceptance_criteria>
- `prisma/seed/operations.ts` exports default function `seedOperations(tx, context)`
- Creates exactly 50 requests, 100 audit events, 20 vault files, 30 messages
- All request titles are Vietnamese legal document names
- All messages have Vietnamese content (formal business language)
- Foreign keys reference IDs from foundation seed context
- Wipe deletes in correct cascade order
- Uses `tx` for all operations
</acceptance_criteria>

<verify>
- `npx tsc --noEmit prisma/seed/operations.ts` — compiles
- `grep -c 'create' prisma/seed/operations.ts` — multiple create calls present
</verify>

### Task 1.4: Implement partners seed, update seed.ts, and consolidate old files

<read_first>
- prisma/seed.ts (existing main function — must preserve existing seed calls)
- prisma/schema.prisma (Partner, ServiceType models)
- prisma/seed-partners.ts (existing partner seed for reference)
</read_first>

<action>
1. Create `prisma/seed/partners.ts` with `seedPartners(tx)` that creates:
   - **5 Partners**: Vietnamese law firms with realistic names:
     - "Công ty Luật Baker McKenzie Vietnam" (CORPORATE_LAW)
     - "Văn phòng Luật sư Russin & Vecchi" (COMMERCIAL_LAW)
     - "Công ty Luật TNHH LNT & Thành viên" (REAL_ESTATE)
     - "Văn phòng Luật sư Phnom Penh & Partners" (IP_LAW)
     - "Công ty Luật Tilleke & Gibbins Vietnam" (LABOR_LAW)
   - **5 ServiceTypes**: labor_contract, trademark_registration, company_formation, compliance_review, contract_negotiation
   - Wipe: delete ServiceType → delete Partner

2. Enhance `prisma/seed/index.ts` with wipe strategy:
   ```typescript
   export async function seedAll(prisma: PrismaClient) {
     await prisma.$transaction(async (tx) => {
       // Wipe in reverse dependency order
       await tx.message.deleteMany();
       await tx.vaultFile.deleteMany();
       await tx.auditEvent.deleteMany();
       await tx.legalRequest.deleteMany();
       await tx.workspaceMembership.deleteMany();
       await tx.routingCapability.deleteMany();
       await tx.partner.deleteMany();
       await tx.serviceType.deleteMany();
       await tx.workspace.deleteMany();
       await tx.organization.deleteMany();
       await tx.account.deleteMany();
       await tx.user.deleteMany();
       
       // Seed fresh data
       await seedFoundation(tx);
       await seedPartners(tx);
       await seedOperations(tx);
     });
   }
   ```

3. Update `prisma/seed.ts` main() function to:
   - Import `seedAll` from `./seed/index`
   - Call `seedAll` inside a `prisma.$transaction()` wrapper at the beginning of main()
   - Preserve ALL existing seed calls (MatterTypes, Folders, Tags, seedMessages, An Phat workspace, Phase 16 fixtures)
   - Existing seed.ts code runs AFTER the new organized seed completes

4. Delete old scattered seed files:
   - `prisma/seed-customer-dashboard.ts`
   - `prisma/seed-dashboard-activity.ts`
   - `prisma/seed-my-cases.ts`
   - `prisma/seed-org-activity.ts`
   - `prisma/seed-partners.ts` (old version, replaced by prisma/seed/partners.ts)
   - `prisma/seed-user-activity.ts`
   - `prisma/seed-user-orgs.ts`
   - Keep `prisma/seed-messages.ts` (still imported by seed.ts)
</action>

<acceptance_criteria>
- `prisma/seed/partners.ts` exports default function `seedPartners(tx)`
- Creates exactly 5 partners with Vietnamese names and `+84` phone numbers
- `prisma/seed/index.ts` wraps all operations in `prisma.$transaction()` with wipe strategy
- Wipe order follows reverse dependency: child tables → parent tables
- If any seed function throws, transaction rolls back (no partial data in database)
- `prisma/seed.ts` imports `seedAll` from `./seed/index`
- `prisma/seed.ts` wraps new seed in `prisma.$transaction()` call
- All existing seed calls (MatterTypes, Folders, Tags, An Phat, Phase 16) still present in seed.ts
- Old seed-*.ts files deleted (except seed-messages.ts)
- `ls prisma/seed-*.ts` returns only `prisma/seed-messages.ts`
- `ls prisma/seed/*.ts` returns `foundation.ts`, `operations.ts`, `partners.ts`, `index.ts`
</acceptance_criteria>

<verify>
- `npx tsc --noEmit prisma/seed.ts` — compiles
- `ls prisma/seed-*.ts` — only seed-messages.ts remains
- `ls prisma/seed/` — has 4 files: index.ts, foundation.ts, operations.ts, partners.ts
- `grep 'seedAll' prisma/seed.ts` — new import present
- Test: Intentionally add `throw new Error('test')` in seedOperations(), run `npm run seed`
- Check: Database unchanged (query counts before/after show no change)
- Verify: Foreign key constraints satisfied (no orphaned records)
</verify>

## Verification

- `npm run seed` exits 0 and creates all required records
- Database query confirms: 10 users, 3 orgs, 5 workspaces, 50 requests, 100 audit events, 20 vault files, 30 messages, 5 partners
- Running seed twice succeeds (wipe + reseed is idempotent)
- All Vietnamese names/addresses are realistic
- No orphaned records (all foreign keys valid)

## must_haves

### truths
- `prisma/seed/index.ts` exists and exports seedAll function
- `prisma/seed/foundation.ts` creates 10 users, 3 orgs, 1 tenant, 5 workspaces
- `prisma/seed/operations.ts` creates 50 requests, 100 audit events, 20 vault files, 30 messages
- `prisma/seed/partners.ts` creates 5 partners, 5 service types
- All seed operations use Prisma transaction client (tx)
- Wipe deletes all data before inserting (no upsert in new seed files)
- Old seed-*.ts files deleted except seed-messages.ts
- All names are Vietnamese (not placeholder English)
- `npm run seed` exits 0

### prohibitions
- MUST NOT use upsert pattern in new seed files (wipe + insert only)
- MUST NOT delete or modify existing seed.ts MatterTypes/Folders/Tags/An Phat code
- MUST NOT use faker.js or random data generators (hardcoded Vietnamese data)
- MUST NOT create partial data on failure (all-or-nothing transaction)
- MUST NOT leave old seed-*.ts files in prisma/ root

## Artifacts this phase produces

- `prisma/seed/index.ts` — orchestrator with wipe + transaction
- `prisma/seed/foundation.ts` — users, orgs, tenant, workspaces
- `prisma/seed/operations.ts` — requests, audit events, vault files, messages
- `prisma/seed/partners.ts` — partners, service types
- Updated `prisma/seed.ts` — imports and calls new seed framework
