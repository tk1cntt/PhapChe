---
phase: 73
plan: 04
type: implementation
wave: 3
depends_on: [73-01, 73-02, 73-03]
files_modified:
  - src/lib/api/client.test.ts
  - src/lib/api/toast.test.ts
  - src/lib/react-query.test.ts
  - src/lib/query-keys.test.ts
  - src/hooks/useRequests.test.ts
  - src/hooks/useUsers.test.ts
  - src/hooks/useAuth.test.ts
  - src/hooks/usePermissions.test.ts
  - src/components/shared/ui/ErrorBoundary.test.tsx
  - src/components/shared/ui/LoadingSkeleton.test.tsx
  - src/components/shared/ui/EmptyState.test.tsx
autonomous: true
requirements: [FOUND-12]
---

# Plan 04: Foundation Tests

## Objective

Viết comprehensive tests cho tất cả foundation work (API client, React Query setup, hooks, shared components). Đạt 90% code coverage cho tất cả new code từ Plans 01-03.

## Tasks

### Task 4.1: Test API client retry logic

<read_first>
- src/lib/api/client.ts (enhanced with retry logic from Plan 02)
- .planning/phases/73-shared-foundation/73-SPEC.md (R3: auto-retry 3x on network errors and 5xx)
</read_first>

<action>
Create `src/lib/api/client.test.ts`:
1. Mock `fetch` globally with `vi.fn()`
2. Test retry on network error:
   - First 2 calls throw `TypeError: Failed to fetch`
   - Third call succeeds
   - Verify fetch called 3 times
   - Verify delays (use `vi.useFakeTimers()` and `vi.advanceTimersByTime()`)
3. Test retry on 502/503/504:
   - First call returns 502
   - Second call succeeds
   - Verify fetch called 2 times
4. Test NO retry on 4xx errors:
   - Call returns 400
   - Verify fetch called only 1 time
   - Verify error thrown
5. Test exponential backoff timing:
   - Verify delays are 500ms, 1000ms, 2000ms
6. Test all HTTP methods (GET, POST, PUT, PATCH, DELETE) work with retry

Use Vitest with `vi.useFakeTimers()` for timing tests.
</action>

<acceptance_criteria>
- `src/lib/api/client.test.ts` exists
- Tests retry on network errors (3 attempts)
- Tests retry on 502/503/504 responses
- Tests NO retry on 400/401/403/404/500 responses
- Tests exponential backoff timing (500/1000/2000ms)
- Tests all 5 HTTP methods
- All tests pass: `npx vitest run src/lib/api/client.test.ts`
</acceptance_criteria>

<verify>
- `npx vitest run src/lib/api/client.test.ts` — all tests pass
- `grep 'Failed to fetch' src/lib/api/client.test.ts` — network error test
- `grep '502\|503\|504' src/lib/api/client.test.ts` — 5xx retry tests
- `grep '400\|401\|403' src/lib/api/client.test.ts` — 4xx no-retry tests
</verify>

### Task 4.2: Test API client toast notifications

<read_first>
- src/lib/api/client.ts (with toast integration from Plan 02)
- src/lib/toast.ts (toast wrapper)
- .planning/phases/73-shared-foundation/73-SPEC.md (R3: toast on errors)
</read_first>

<action>
Add to `src/lib/api/client.test.ts`:
1. Mock `src/lib/toast` module with `vi.mock()`
2. Test toast on HTTP 500:
   - Call returns 500
   - Verify `toastError` called with "Lỗi máy chủ, vui lòng thử lại"
3. Test toast on HTTP 403:
   - Call returns 403
   - Verify `toastError` called with "Không có quyền truy cập"
4. Test NO toast on success (200):
   - Call succeeds
   - Verify `toastError` NOT called
5. Test 401 redirect:
   - Mock `window.location.href`
   - Call returns 401
   - Verify `window.location.href` set to `/login`
6. Test client-side guard:
   - Mock `typeof window === 'undefined'` (server-side)
   - Verify toast/redirect NOT called

Create `src/lib/toast.test.ts`:
1. Mock `react-hot-toast` with `vi.mock()`
2. Test each toast function:
   - `toastSuccess` calls `toast.success()`
   - `toastError` calls `toast.error()`
   - `toastInfo` calls `toast()`
   - `toastWarning` calls `toast()` with warning type
3. Verify message passed correctly
</action>

<acceptance_criteria>
- API client tests cover toast on 500 and 403
- API client tests verify NO toast on success
- API client tests cover 401 redirect
- API client tests verify client-side guard
- `src/lib/toast.test.ts` tests all 4 toast functions
- All tests pass: `npx vitest run src/lib/api/`
</acceptance_criteria>

<verify>
- `npx vitest run src/lib/api/client.test.ts` — toast tests pass
- `npx vitest run src/lib/toast.test.ts` — all toast tests pass
- `grep 'toastError' src/lib/api/client.test.ts` — toast integration tested
- `grep '/login' src/lib/api/client.test.ts` — redirect tested
</verify>

### Task 4.3: Test React Query setup

<read_first>
- src/lib/react-query.tsx (QueryClient and QueryProvider from Plan 02)
- src/lib/query-keys.ts (query key factories from Plan 02)
- .planning/phases/73-shared-foundation/73-SPEC.md (R5: QueryClient with defaults)
</read_first>

<action>
Create `src/lib/react-query.test.tsx`:
1. Test QueryClient configuration:
   - Verify `queryClient` exists
   - Verify `staleTime` is 5 minutes (300000ms)
   - Verify `gcTime` is 30 minutes (1800000ms)
   - Verify `retry` is 3
   - Verify `refetchOnWindowFocus` is false
2. Test QueryProvider:
   - Render `<QueryProvider><TestComponent /></QueryProvider>`
   - Verify child component renders
   - Verify `useQueryClient()` works inside provider

Create `src/lib/query-keys.test.ts`:
1. Test each domain key factory:
   - `queryKeys.users.all` returns `['users']`
   - `queryKeys.users.lists()` returns `['users', 'list']`
   - `queryKeys.users.list({ page: 1 })` returns `['users', 'list', { page: 1 }]`
   - `queryKeys.users.details()` returns `['users', 'detail']`
   - `queryKeys.users.detail('123')` returns `['users', 'detail', '123']`
2. Test all 6 domains (users, requests, workspaces, auditEvents, vaultFiles, messages)
3. Test filters object passed correctly
</action>

<acceptance_criteria>
- `src/lib/react-query.test.tsx` tests QueryClient defaults
- `src/lib/react-query.test.tsx` tests QueryProvider rendering
- `src/lib/query-keys.test.ts` tests all 6 domain factories
- All key formats verified (all, lists, list with filters, details, detail with id)
- All tests pass: `npx vitest run src/lib/react-query.test.tsx src/lib/query-keys.test.ts`
</acceptance_criteria>

<verify>
- `npx vitest run src/lib/react-query.test.tsx` — all tests pass
- `npx vitest run src/lib/query-keys.test.ts` — all tests pass
- `grep 'staleTime.*300000' src/lib/react-query.test.tsx` — staleTime verified
- `grep 'gcTime.*1800000' src/lib/react-query.test.tsx` — gcTime verified
</verify>

### Task 4.4: Test domain hooks

<read_first>
- src/hooks/useRequests.ts (refactored from Plan 02)
- src/hooks/useUsers.ts (refactored from Plan 02)
- src/hooks/useWorkspaces.ts (new from Plan 02)
- src/hooks/useVaultFiles.ts (new from Plan 02)
- src/hooks/useMessages.ts (new from Plan 02)
- .planning/phases/73-shared-foundation/73-SPEC.md (R6: domain hooks with React Query)
</read_first>

<action>
Create `src/hooks/useRequests.test.tsx`:
1. Mock `@/lib/api` with `vi.mock()` — mock `requestsApi.list()` and `requestsApi.get()`
2. Use `@testing-library/react` with `renderHook` and wrapper for QueryProvider
3. Test `useRequests()`:
   - Calls `requestsApi.list()` with correct params
   - Returns loading state, then data
   - Uses correct query key
4. Test `useRequestById(id)`:
   - Calls `requestsApi.get(id)`
   - Returns correct data
5. Test staleTime is 2 minutes (120000ms)

Create `src/hooks/useUsers.test.tsx`:
- Similar pattern for `useUsers()` and `useUserById(id)`
- Verify staleTime is 5 minutes

Create `src/hooks/useWorkspaces.test.tsx`:
- Test `useWorkspaces()` and `useWorkspaceById(id)`

Create `src/hooks/useVaultFiles.test.tsx`:
- Test `useVaultFiles()` and `useVaultFileById(id)`

Create `src/hooks/useMessages.test.tsx`:
- Test `useMessages()` and `useMessageThread()`
- Verify staleTime is 1 minute
</action>

<acceptance_criteria>
- 5 test files created (useRequests, useUsers, useWorkspaces, useVaultFiles, useMessages)
- Each hook tested with renderHook and QueryProvider wrapper
- API client mocked correctly
- Loading and success states tested
- Query keys verified
- StaleTime values verified for each hook
- All tests pass: `npx vitest run src/hooks/`
</acceptance_criteria>

<verify>
- `npx vitest run src/hooks/useRequests.test.tsx` — passes
- `npx vitest run src/hooks/useUsers.test.tsx` — passes
- `npx vitest run src/hooks/useWorkspaces.test.tsx` — passes
- `npx vitest run src/hooks/useVaultFiles.test.tsx` — passes
- `npx vitest run src/hooks/useMessages.test.tsx` — passes
- `grep 'renderHook' src/hooks/*.test.tsx` — all use renderHook
- `grep 'QueryProvider' src/hooks/*.test.tsx` — all wrapped with provider
</verify>

### Task 4.5: Test auth hooks

<read_first>
- src/hooks/useAuth.ts (from Plan 03)
- src/hooks/usePermissions.ts (from Plan 03)
- .planning/phases/73-shared-foundation/73-SPEC.md (R10: context hooks)
</read_first>

<action>
Create `src/hooks/useAuth.test.tsx`:
1. Mock `@/auth` with `vi.mock()` — mock `useSession()`
2. Test authenticated state:
   - Mock returns `{ data: { user: { id: '1', name: 'Test' } }, isPending: false }`
   - Verify `useAuth()` returns `{ user, isLoading: false, isAuthenticated: true }`
3. Test unauthenticated state:
   - Mock returns `{ data: null, isPending: false }`
   - Verify returns `{ user: null, isLoading: false, isAuthenticated: false }`
4. Test loading state:
   - Mock returns `{ data: undefined, isPending: true }`
   - Verify returns `{ user: null, isLoading: true, isAuthenticated: false }`
5. Test memoization:
   - Call hook twice, verify return object reference stable

Create `src/hooks/usePermissions.test.tsx`:
1. Mock `./useAuth` with `vi.mock()`
2. Test each role:
   - super_admin: `can('anything')` returns true
   - coordinator_admin: `can('read', 'requests')` returns true, `can('delete', 'users')` returns false
   - reviewer: `can('review', 'requests')` returns true, `can('write', 'requests')` returns false
   - specialist: `can('write', 'requests')` returns true, `can('review', 'requests')` returns false
   - customer: `can('read', 'own-requests')` returns true, `can('read', 'requests')` returns false
3. Test unauthenticated:
   - user is null
   - `can()` returns false, `cannot()` returns true
4. Test `cannot` is inverse of `can`
5. Test useCallback memoization
</action>

<acceptance_criteria>
- `src/hooks/useAuth.test.tsx` tests 3 states (authenticated, unauthenticated, loading)
- `src/hooks/usePermissions.test.tsx` tests all 5 roles
- Memoization verified for both hooks
- Unauthenticated permission checks verified
- All tests pass: `npx vitest run src/hooks/useAuth.test.tsx src/hooks/usePermissions.test.tsx`
</acceptance_criteria>

<verify>
- `npx vitest run src/hooks/useAuth.test.tsx` — passes
- `npx vitest run src/hooks/usePermissions.test.tsx` — passes
- `grep 'isAuthenticated.*true' src/hooks/useAuth.test.tsx` — authenticated test
- `grep 'super_admin' src/hooks/usePermissions.test.tsx` — role tests present
</verify>

### Task 4.6: Test shared UI components

<read_first>
- src/components/shared/ui/ErrorBoundary.tsx (from Plan 03)
- src/components/shared/ui/LoadingSkeleton.tsx (from Plan 03)
- src/components/shared/ui/EmptyState.tsx (from Plan 03)
- .planning/phases/73-shared-foundation/73-SPEC.md (R7, R8, R9: shared components)
</read_first>

<action>
Create `src/components/shared/ui/ErrorBoundary.test.tsx`:
1. Create `ThrowingComponent` that throws error on render
2. Test error catching:
   - Render `<ErrorBoundary><ThrowingComponent /></ErrorBoundary>`
   - Verify fallback UI displayed
   - Verify console.error called
3. Test onError callback:
   - Pass `onError` prop
   - Verify callback invoked with error
4. Test retry button:
   - Click "Thử lại" button
   - Verify state reset (component re-renders)
5. Test custom fallback:
   - Pass `fallback` prop
   - Verify custom fallback rendered

Create `src/components/shared/ui/LoadingSkeleton.test.tsx`:
1. Test each variant:
   - Render with `variant="card"` — verify card layout
   - Render with `variant="table-row"` — verify table row layout
   - Render with `variant="list-item"` — verify list item layout
   - Render with `variant="text-line"` — verify text line layout
2. Test count prop:
   - Render with `count={3}` — verify 3 skeletons rendered
3. Test className prop:
   - Pass additional className — verify applied
4. Test sr-only text present

Create `src/components/shared/ui/EmptyState.test.tsx`:
1. Test with all props:
   - Pass icon, title, description, action
   - Verify all rendered
2. Test without optional props:
   - Pass only title
   - Verify no description, no action button
3. Test action button:
   - Pass action with onClick
   - Click button
   - Verify onClick called
4. Test i18n integration:
   - Verify default translations used when props not provided
</action>

<acceptance_criteria>
- `src/components/shared/ui/ErrorBoundary.test.tsx` tests error catching, retry, custom fallback
- `src/components/shared/ui/LoadingSkeleton.test.tsx` tests all 4 variants, count, className
- `src/components/shared/ui/EmptyState.test.tsx` tests all props, action click, i18n
- All tests pass: `npx vitest run src/components/shared/ui/`
</acceptance_criteria>

<verify>
- `npx vitest run src/components/shared/ui/ErrorBoundary.test.tsx` — passes
- `npx vitest run src/components/shared/ui/LoadingSkeleton.test.tsx` — passes
- `npx vitest run src/components/shared/ui/EmptyState.test.tsx` — passes
- `grep 'ThrowingComponent' src/components/shared/ui/ErrorBoundary.test.tsx` — error test
- `grep 'variant=' src/components/shared/ui/LoadingSkeleton.test.tsx` — variant tests
- `grep 'onClick' src/components/shared/ui/EmptyState.test.tsx` — action test
</verify>

### Task 4.7: Verify 90% coverage

<read_first>
- All test files created in Tasks 4.1-4.6
- .planning/phases/73-shared-foundation/73-SPEC.md (R12: ≥90% coverage)
</read_first>

<action>
1. Run coverage report:
   ```bash
   npx vitest run --coverage
   ```
2. Verify coverage for each module:
   - `src/lib/api/client.ts` ≥90%
   - `src/lib/toast.ts` ≥90%
   - `src/lib/react-query.tsx` ≥90%
   - `src/lib/query-keys.ts` ≥90%
   - `src/hooks/useRequests.ts` ≥90%
   - `src/hooks/useUsers.ts` ≥90%
   - `src/hooks/useWorkspaces.ts` ≥90%
   - `src/hooks/useVaultFiles.ts` ≥90%
   - `src/hooks/useMessages.ts` ≥90%
   - `src/hooks/useAuth.ts` ≥90%
   - `src/hooks/usePermissions.ts` ≥90%
   - `src/components/shared/ui/ErrorBoundary.tsx` ≥90%
   - `src/components/shared/ui/LoadingSkeleton.tsx` ≥90%
   - `src/components/shared/ui/EmptyState.tsx` ≥90%
3. If coverage <90% for any module, add missing tests
4. Document coverage results in test output
</action>

<acceptance_criteria>
- All 14 modules have ≥90% line coverage
- Coverage report generated: `coverage/index.html`
- No uncovered critical paths (error handling, retry logic, permission checks)
- Total test count ≥50 tests
- All tests pass
</acceptance_criteria>

<verify>
- `npx vitest run --coverage` — exits 0
- Coverage report shows ≥90% for all 14 modules
- `cat coverage/coverage-summary.json | jq '.total.lines.pct'` — ≥90
</verify>

## Verification

- `npx vitest run` — all tests pass
- `npx vitest run --coverage` — ≥90% coverage for all new modules
- No test failures or errors
- Coverage report generated successfully

## must_haves

### truths
- `src/lib/api/client.test.ts` has tests for retry, toast, redirect
- `src/lib/toast.test.ts` has tests for all 4 toast functions
- `src/lib/react-query.test.tsx` has tests for QueryClient and QueryProvider
- `src/lib/query-keys.test.ts` has tests for all 6 domain factories
- 5 hook test files exist (useRequests, useUsers, useWorkspaces, useVaultFiles, useMessages)
- `src/hooks/useAuth.test.tsx` has tests for 3 states
- `src/hooks/usePermissions.test.tsx` has tests for all 5 roles
- 3 shared component test files exist (ErrorBoundary, LoadingSkeleton, EmptyState)
- All 14 modules have ≥90% line coverage
- Total test count ≥50
- All tests pass with exit code 0
- Coverage report generated at `coverage/index.html`

### prohibitions
- MUST NOT mock implementation details (test behavior, not internals)
- MUST NOT skip coverage verification (90% is mandatory)
- MUST NOT use snapshot tests for shared components (explicit assertions preferred)
- MUST NOT leave tests disabled or skipped

## Artifacts this phase produces

- `src/lib/api/client.test.ts` — API client tests (retry, toast, redirect)
- `src/lib/toast.test.ts` — toast wrapper tests
- `src/lib/react-query.test.tsx` — React Query setup tests
- `src/lib/query-keys.test.ts` — query key factory tests
- `src/hooks/useRequests.test.tsx` — requests hook tests
- `src/hooks/useUsers.test.tsx` — users hook tests
- `src/hooks/useWorkspaces.test.tsx` — workspaces hook tests
- `src/hooks/useVaultFiles.test.tsx` — vault hook tests
- `src/hooks/useMessages.test.tsx` — messages hook tests
- `src/hooks/useAuth.test.tsx` — auth hook tests
- `src/hooks/usePermissions.test.tsx` — permissions hook tests
- `src/components/shared/ui/ErrorBoundary.test.tsx` — error boundary tests
- `src/components/shared/ui/LoadingSkeleton.test.tsx` — loading skeleton tests
- `src/components/shared/ui/EmptyState.test.tsx` — empty state tests
- `coverage/index.html` — coverage report
