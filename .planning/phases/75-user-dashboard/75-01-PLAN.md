---
phase: 75
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements:
  - U-DASH-01
  - U-DASH-02
  - U-DASH-03
  - U-DASH-04
  - U-DASH-05
  - U-DASH-06
  - U-DASH-07
  - U-DASH-08
files_modified:
  - src/components/dashboard/StatCard.tsx
  - src/components/dashboard/DashboardClient.tsx
  - src/components/dashboard/dashboard.css
  - src/app/api/messages/unread-count/route.ts
user_setup: []
must_haves:
  truths:
    - User can see 4 stat cards with real counts from Prisma
    - User can click stat card and navigate to relevant page
    - Floating chat button shows unread count badge when count > 0
    - Each dashboard panel has loading skeleton during initial load
    - Each dashboard panel shows empty state when no data
    - Each dashboard panel shows error state with retry button
    - Cases table pagination works (10 items per page)
  artifacts:
    - path: src/components/dashboard/StatCard.tsx
      provides: Clickable stat cards with href prop
    - path: src/components/dashboard/DashboardClient.tsx
      provides: Floating chat badge with client-side fetch
    - path: src/app/api/messages/unread-count/route.ts
      provides: Unread message count API
  key_links:
    - from: DashboardClient.tsx
      to: api/messages/unread-count/route.ts
      via: useEffect fetches /api/messages/unread-count on mount
    - from: StatCard.tsx
      to: dashboard.css
      via: stat-card href wraps in Link, CSS cursor-pointer for hover
---

<objective>
Enhance existing User Dashboard với clickable stat cards, floating chat badge với dynamic unread count, loading/error/empty states, pagination, CSS matching, và i18n verification.
</objective>

<execution_context>
@D:/PhapChe/.claude/gsd-core/workflows/execute-plan.md
@D:/PhapChe/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-user-dashboard/75-SPEC.md
@.planning/phases/75-user-dashboard/75-CONTEXT.md
@.planning/phases/75-user-dashboard/75-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Stat Cards Clickable (U-DASH-01, U-DASH-08)</name>
  <files>
    - src/components/dashboard/StatCard.tsx
    - src/components/dashboard/dashboard.css
  </files>
  <read_first>
    - src/components/dashboard/StatCard.tsx
    - src/components/dashboard/DashboardClient.tsx
    - src/components/dashboard/dashboard.css
  </read_first>
  <action>
    1. Update StatCardProps interface to accept optional `href?: string` prop
    2. Import Next.js Link: `import Link from 'next/link'`
    3. When href provided, wrap card in Link: `<Link href={href} className="stat-card stat-card-link">{cardContent}</Link>`
    4. When no href, render plain div (current behavior)
    5. Update StatsCardGrid to pass href props:
       - totalRequests card → href="/cases"
       - inProgress card → href="/cases?status=in_progress"
       - completed card → href="/cases?status=completed"
       - vaultDocs card → href="/vault"
    6. Add CSS: `.stat-card-link { cursor: pointer; }`
    7. Per D-01 through D-05 (see 75-CONTEXT.md)
  </action>
  <verify>
    <automated>grep -c "href=" src/components/dashboard/StatCard.tsx && grep -c "stat-card-link" src/components/dashboard/dashboard.css</automated>
  </verify>
  <done>
    StatCard accepts optional href prop; when provided, card renders as Next.js Link; each stat card links to correct destination per D-02 through D-05
  </done>
</task>

<task type="auto">
  <name>Task 2: Floating Chat Badge with Dynamic Unread Count (U-DASH-07)</name>
  <files>
    - src/components/dashboard/DashboardClient.tsx
    - src/app/api/messages/unread-count/route.ts
  </files>
  <read_first>
    - src/components/dashboard/DashboardClient.tsx
    - src/app/api/messages/route.ts (if exists)
    - prisma/schema.prisma (Message model)
  </read_first>
  <action>
    1. Create directory: src/app/api/messages/unread-count/
    2. Create route.ts with GET handler:
       - Require session via requireAppSession()
       - Query: prisma.message.count({ where: { recipientId: userId, isRead: false } })
       - Return: NextResponse.json({ unreadCount: count })
       - On error: return { unreadCount: 0 } with 500 status
    3. Update DashboardClient.tsx:
       - Add useState: `const [unreadCount, setUnreadCount] = useState(0)`
       - Add useEffect: fetch('/api/messages/unread-count'), then setUnreadCount(data.unreadCount)
       - Handle error gracefully (setUnreadCount(0))
    4. Update floating chat button:
       - Conditionally render badge only when unreadCount > 0
       - Badge shows: unreadCount > 99 ? '99+' : unreadCount
       - Remove hardcoded "N"
    5. Per D-06 through D-08 (see 75-CONTEXT.md)
  </action>
  <verify>
    <automated>grep -c "unread-count" src/components/dashboard/DashboardClient.tsx && grep -c "unreadCount > 0" src/components/dashboard/DashboardClient.tsx</automated>
  </verify>
  <done>
    Floating chat badge shows unread count from API when count > 0; no badge when count = 0; initial state is 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Loading States for Each Panel (U-DASH-03 through U-DASH-06)</name>
  <files>
    - src/components/dashboard/DashboardClient.tsx
    - src/components/shared/ui/LoadingSkeleton.tsx
  </files>
  <read_first>
    - src/components/shared/ui/LoadingSkeleton.tsx
    - src/components/dashboard/DashboardClient.tsx
    - src/components/dashboard/dashboard.css
  </read_first>
  <action>
    1. Verify LoadingSkeleton from phase 73 exists with variants: card, table-row, list-item, text-line
    2. Add isLoading state to DashboardClient: `const [isLoading, setIsLoading] = useState(false)` (Note: server fetches data, so isLoading tracks client re-fetch only)
    3. Create panel-specific skeleton components within DashboardClient:
       - StatCardsSkeleton: 4 card skeletons in stats-grid layout
       - RecentCasesSkeleton: 5 list-item skeletons
       - RecentDocumentsSkeleton: 5 document-item skeletons
       - ActivityTimelineSkeleton: 5 timeline-item skeletons
    4. Each skeleton uses LoadingSkeleton with appropriate variant
    5. Panels should show skeleton when their data is loading/empty
    6. Per D-09 through D-10 (see 75-CONTEXT.md)
  </action>
  <verify>
    <automated>grep -c "Skeleton" src/components/dashboard/DashboardClient.tsx</automated>
  </verify>
  <done>
    Each panel shows skeleton during initial load; skeletons match panel dimensions
  </done>
</task>

<task type="auto">
  <name>Task 4: Empty States for Each Panel</name>
  <files>
    - src/components/dashboard/DashboardClient.tsx
    - src/components/shared/ui/EmptyState.tsx
  </files>
  <read_first>
    - src/components/shared/ui/EmptyState.tsx
    - src/components/dashboard/RecentCases.tsx
    - src/components/dashboard/RecentDocuments.tsx
    - src/components/dashboard/ActivityTimeline.tsx
  </files>
  <action>
    1. Verify EmptyState from phase 73 exists with icon, title, description, action props
    2. Import EmptyState: `import { EmptyState } from '@/components/shared/ui/EmptyState'`
    3. Update RecentCases: when cases.length === 0, show EmptyState with:
       - icon: folder icon
       - title: "Chưa có hồ sơ nào"
       - description: "Tạo hồ sơ mới để bắt đầu"
       - action: navigate to /create
    4. Update RecentDocuments: when documents.length === 0, show EmptyState with:
       - icon: document icon
       - title: "Chưa có tài liệu nào"
       - description: "Tài liệu của bạn sẽ xuất hiện ở đây"
    5. Update ActivityTimeline: when activities.length === 0, show EmptyState with:
       - icon: activity icon
       - title: "Chưa có hoạt động nào"
    6. Per D-11 through D-13 (see 75-CONTEXT.md)
  </action>
  <verify>
    <automated>grep -c "EmptyState" src/components/dashboard/DashboardClient.tsx</automated>
  </verify>
  <done>
    RecentCases shows empty message when no cases; RecentDocuments shows empty message when no documents; ActivityTimeline shows empty message when no activities
  </done>
</task>

<task type="auto">
  <name>Task 5: Error States with Retry Button</name>
  <files>
    - src/components/dashboard/DashboardClient.tsx
    - src/components/shared/ui/ErrorBoundary.tsx
  </files>
  <read_first>
    - src/components/shared/ui/ErrorBoundary.tsx
    - src/components/dashboard/RecentCases.tsx
    - src/components/dashboard/DashboardClient.tsx
  </files>
  <action>
    1. Verify ErrorBoundary from phase 73 exists with translations prop (title, retry)
    2. Import: `import { ErrorBoundary } from '@/components/shared/ui/ErrorBoundary'`
    3. Wrap each panel with ErrorBoundary component:
       - StatCardsGrid wrapped
       - RecentCases wrapped
       - DeadlineSLA wrapped
       - RecentDocuments wrapped
       - ActivityTimeline wrapped
    4. ErrorBoundary translations:
       - title: "Không thể tải dữ liệu. Vui lòng thử lại."
       - retry: "Thử lại"
    5. For client-side re-fetch: add onError callback to ErrorBoundary
    6. Per D-14 through D-16 (see 75-CONTEXT.md)
  </action>
  <verify>
    <automated>grep -c "ErrorBoundary" src/components/dashboard/DashboardClient.tsx</automated>
  </verify>
  <done>
    Panel shows error message in Vietnamese on failure; Retry button re-fetches data
  </done>
</task>

<task type="auto">
  <name>Task 6: Pagination Verification in Cases Table (U-DASH-08)</name>
  <files>
    - src/components/dashboard/CasesTable.tsx
  </files>
  <read_first>
    - src/components/dashboard/CasesTable.tsx
    - src/components/ui/Paging.tsx
  </read_first>
  <action>
    1. Verify CasesTable already has pagination implemented:
       - currentPage state (useState)
       - pageSize = 10
       - Paginated slice: cases.slice(startIndex, startIndex + pageSize)
       - Paging component from @/components/ui/Paging
    2. If pagination exists, verify it matches D-17 through D-19:
       - Client-side pagination with state
       - Page size: 10 items per page
       - Pagination controls: Previous, Next, page numbers
    3. If missing, implement pagination per spec
    4. Per D-17 through D-19 (see 75-CONTEXT.md)
  </action>
  <verify>
    <automated>grep -c "Paging" src/components/dashboard/CasesTable.tsx && grep -c "pageSize = 10" src/components/dashboard/CasesTable.tsx</automated>
  </verify>
  <done>
    CasesTable shows pagination with 10 items per page; Previous/Next buttons work correctly
  </done>
</task>

<task type="auto">
  <name>Task 7: CSS Verification and Enhancement</name>
  <files>
    - src/components/dashboard/dashboard.css
  </files>
  <read_first>
    - src/components/dashboard/dashboard.css
  </read_first>
  <action>
    1. Verify teal primary color #087f78 is used throughout CSS
    2. Verify stat card hover effects exist:
       - Current: .stat-card:hover { box-shadow, transform }
    3. Verify floating chat button CSS:
       - Current: .floating-chat with red gradient background
       - Badge: .chat-icon-wrapper with circular background
    4. Verify responsive design:
       - stats-grid: 4 → 2 → 1 columns
       - dashboard-grid: 2 → 1 columns
    5. Add cursor-pointer for stat-card-link if not exists
    6. Per D-20 through D-22 (see 75-CONTEXT.md)
  </action>
  <verify>
    <automated>grep -c "#087f78" src/components/dashboard/dashboard.css</automated>
  </verify>
  <done>
    Teal primary color applied consistently; hover effects work on stat cards; responsive grid works on mobile
  </done>
</task>

<task type="auto">
  <name>Task 8: i18n Verification</name>
  <files>
    - src/messages/vi.json
    - src/messages/en.json
    - src/messages/zh.json
    - src/messages/ja.json
  </files>
  <read_first>
    - src/messages/vi.json
    - src/messages/en.json
  </read_first>
  <action>
    1. Check existing DashboardClient namespace keys in vi.json and en.json
    2. Required keys for DashboardClient namespace:
       - greeting (with name param)
       - subtitle
       - createRequest
    3. Required keys for StatCard namespace:
       - totalRequests, totalRequestsDesc
       - inProgress, inProgressDesc
       - completed, completedDesc
       - vaultDocs, vaultDocsDesc
    4. Required keys for CasesTable namespace:
       - caseCode, requestType, status, assignee, lastUpdated, actions, viewDetails, noCases
    5. Required keys for shared.emptyState:
       - noData, create
    6. Verify all 4 locales have translations (vi/en/zh/ja)
    7. Per D-23 through D-24 (see 75-CONTEXT.md)
  </action>
  <verify>
    <automated>grep -c "DashboardClient" src/messages/vi.json && grep -c "StatCard" src/messages/vi.json</automated>
  </verify>
  <done>
    All dashboard strings have translations in all 4 locales; keys match expected namespaces
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Unread count fetched client-side; API validates session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Information Disclosure | /api/messages/unread-count | mitigate | requireAppSession() ensures user can only query own messages |
| T-75-02 | Tampering | StatCard href navigation | accept | Standard Next.js Link navigation; URL params are filtered by server |
| T-75-SC | Tampering | npm/pip/cargo installs | N/A | No new packages in this phase |
</threat_model>

<verification>
1. Run `npm run dev` and navigate to `/vi/dashboard`
2. Click each stat card and verify navigation to correct destination
3. Check floating chat badge appears when unreadCount > 0
4. Verify loading skeletons appear (if data loads slowly)
5. Test empty states by verifying component handles empty arrays
6. Test pagination in cases table by viewing more than 10 cases
7. Verify CSS teal color (#087f78) is applied to timeline dots and buttons
8. Switch language and verify all translations load correctly
</verification>

<success_criteria>
- Stat cards show real counts from Prisma (totalRequests, inProgress, completed, vaultDocs)
- Click "Tổng hồ sơ" stat card → navigate to `/cases`
- Click "Đang xử lý" stat card → navigate to `/cases?status=in_progress`
- Floating chat button shows unread count badge when count > 0
- Each panel shows loading skeleton when appropriate
- Each panel shows empty state when no data
- Each panel shows error state with retry button
- Cases table pagination works (10 per page)
- All dashboard strings have i18n translations
</success_criteria>

<output>
Create `.planning/phases/75-user-dashboard/75-01-SUMMARY.md` when done
</output>
