---
phase: 85
plan: "02"
type: execute
wave: 2
depends_on:
  - "85-01"
files_modified:
  - prisma/schema.prisma
  - prisma/schema.sql
autonomous: true
requirements:
  - DB-06
  - DB-07
  - DB-08
  - DB-09

must_haves:
  truths:
    - "LegalRequest has matterTypeId FK for type-safe matter type reference"
    - "RequestAssignment has isCurrent and endedAt for source of truth pattern"
    - "VaultFile renamed to VaultItem with fileId FK for 2-tier architecture"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Updated schema with new columns"
      min_lines: 50
    - path: "prisma/schema.sql"
      provides: "SQL export with new columns"
      min_lines: 30
    - path: "prisma/migrations/YYYYMMDDHHMMSS_expand_columns/"
      provides: "Migration for new columns"
  key_links:
    - from: "LegalRequest"
      to: "MatterType"
      via: "matterTypeId FK"
    - from: "RequestAssignment"
      to: "self"
      via: "isCurrent flag + partial unique index"
    - from: "VaultItem"
      to: "File"
      via: "fileId FK"
---

<objective>
Expand schema with new columns for improved data model. This is BACKWARD COMPATIBLE - old code continues to work.

Per D-04: Add matterTypeId FK to LegalRequest
Per D-01: Add isCurrent/endedAt to RequestAssignment with partial unique index
Per D-02: Add fileId to VaultFile (rename to VaultItem) for 2-tier vault
</objective>

<execution_context>
@D:/PhapChe/.claude/gsd-core/workflows/execute-plan.md
@D:/PhapChe/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@prisma/schema.prisma
@prisma/DB_IMPROVEMENT_PLAN.md
@.planning/phases/85-database-schema-improvement/85-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add matterTypeId FK to LegalRequest</name>
  <files>prisma/schema.prisma, prisma/schema.sql</files>
  <read_first>
    - prisma/schema.prisma (lines 244-288: LegalRequest model)
    - prisma/schema.prisma (lines 290-316: MatterType model)
  </read_first>
  <action>
    Add matterTypeId FK field to LegalRequest model.

    In prisma/schema.prisma, add to LegalRequest model (after line 251):
    ```
    matterTypeId    String?
    matterType      MatterType? @relation(fields: [matterTypeId], references: [id])
    ```

    Keep existing matterType String? field for backward compatibility during migration.

    Add index for query performance:
    ```
    @@index([matterTypeId])
    ```

    Note: MatterType has composite unique constraint @@unique([workspaceId, key]). This FK references MatterType.id directly, not the composite key. The FK should be to MatterType.id for cleaner reference.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate --no-engine</automated>
  </verify>
  <acceptance_criteria>
    - LegalRequest.matterTypeId field exists (nullable)
    - LegalRequest.matterType relation exists
    - Index on matterTypeId exists
    - prisma validate passes
  </acceptance_criteria>
  <done>LegalRequest has matterTypeId FK field to MatterType.id</done>
</task>

<task type="auto">
  <name>Task 2: Add isCurrent and endedAt to RequestAssignment</name>
  <files>prisma/schema.prisma, prisma/schema.sql</files>
  <read_first>
    - prisma/schema.prisma (lines 336-359: RequestAssignment model)
  </read_first>
  <action>
    Add isCurrent and endedAt fields to RequestAssignment model for source of truth pattern (D-01).

    In prisma/schema.prisma, update RequestAssignment model (after line 344):
    ```
    isCurrent       Boolean   @default(true)
    endedAt        DateTime?
    ```

    Add index for current assignments query:
    ```
    @@index([requestId, kind, isCurrent])
    @@index([isCurrent])
    ```

    Note: Partial unique index on (requestId, kind) WHERE isCurrent = true will be created via raw SQL migration, as Prisma does not support partial indexes directly.

    Create raw SQL migration file:
    ```sql
    CREATE UNIQUE INDEX uniq_current_assignment
    ON RequestAssignment(requestId, kind)
    WHERE isCurrent = 1;
    ```
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate --no-engine</automated>
  </verify>
  <acceptance_criteria>
    - RequestAssignment.isCurrent field exists with @default(true)
    - RequestAssignment.endedAt field exists (nullable DateTime)
    - Index on (requestId, kind, isCurrent) exists
    - Partial unique index created via migration
    - prisma validate passes
  </acceptance_criteria>
  <done>RequestAssignment has isCurrent and endedAt fields with proper indexes</done>
</task>

<task type="auto">
  <name>Task 3: Add fileId to VaultFile and rename to VaultItem</name>
  <files>prisma/schema.prisma, prisma/schema.sql</files>
  <read_first>
    - prisma/schema.prisma (lines 502-533: VaultFile model)
    - prisma/schema.prisma (lines 670-699: File model)
  </read_first>
  <action>
    Add fileId FK to VaultFile model for 2-tier vault architecture (D-02).

    In prisma/schema.prisma:
    1. Rename model VaultFile to VaultItem (line 502)
    2. Add fileId field and relation (after line 519):
    ```
    fileId          String?
    file            File?      @relation(fields: [fileId], references: [id])
    ```

    3. Update all inverse relation names:
    - VaultFile -> VaultItem
    - vaultFiles -> vaultItems

    4. Update Workspace.vaultFiles relation to vaultItems
    5. Update VaultFileFolder.vaultFile to VaultItem
    6. Update VaultFileTag.vaultFile to VaultItem
    7. Update User.vaultFiles to vaultItems
    8. Update LegalRequest.vaultFiles to vaultItems

    Keep existing columns (workspaceId, requestId, filename, storageKey, size, contentType) for backward compatibility during migration. These will be dropped in Wave 4.

    Add index:
    ```
    @@index([fileId])
    ```

    Add inverse relation to File model:
    ```
    vaultItems      VaultItem[]
    ```
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate --no-engine</automated>
  </verify>
  <acceptance_criteria>
    - Model renamed from VaultFile to VaultItem
    - VaultItem.fileId field exists (nullable)
    - VaultItem.file relation exists
    - All inverse relations updated
    - Index on fileId exists
    - prisma validate passes
  </acceptance_criteria>
  <done>VaultFile renamed to VaultItem with fileId FK added</done>
</task>

<task type="auto">
  <name>Task 4: Update VaultFileFolder and VaultFileTag for VaultItem rename</name>
  <files>prisma/schema.prisma, prisma/schema.sql</files>
  <read_first>
    - prisma/schema.prisma (lines 573-593: VaultFileFolder and VaultFileTag models)
  </read_first>
  <action>
    Update VaultFileFolder and VaultFileTag models for VaultItem rename.

    In prisma/schema.prisma:

    1. Rename VaultFileFolder model to VaultItemFolder
    2. Update VaultFileFolder.vaultFile relation to VaultItemFolder.vaultItem:
    ```
    model VaultItemFolder {
      vaultItemId   String
      folderId      String
      createdAt     DateTime   @default(now())
      vaultItem     VaultItem  @relation(fields: [vaultItemId], references: [id])
      folder        Folder     @relation(fields: [folderId], references: [id])

      @@id([vaultItemId, folderId])
      @@index([folderId])
    }
    ```

    3. Rename VaultFileTag model to VaultItemTag
    4. Update relation to VaultItem:
    ```
    model VaultItemTag {
      vaultItemId   String
      tagId         String
      createdAt     DateTime   @default(now())
      vaultItem     VaultItem   @relation(fields: [vaultItemId], references: [id])
      tag           Tag         @relation(fields: [tagId], references: [id])

      @@id([vaultItemId, tagId])
      @@index([tagId])
    }
    ```

    5. Update Folder.vaultFileFolders relation to VaultItemFolder
    6. Update Tag.vaultFileTags relation to VaultItemTag
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate --no-engine</automated>
  </verify>
  <acceptance_criteria>
    - VaultFileFolder renamed to VaultItemFolder
    - VaultFileTag renamed to VaultItemTag
    - Both models reference VaultItem
    - prisma validate passes
  </acceptance_criteria>
  <done>VaultFileFolder and VaultFileTag renamed to VaultItemFolder and VaultItemTag</done>
</task>

<task type="auto">
  <name>Task 5: Update inverse relations across all models</name>
  <files>prisma/schema.prisma, prisma/schema.sql</files>
  <read_first>
    - prisma/schema.prisma (full file for relation mapping)
  </read_first>
  <action>
    Update all inverse relations throughout the schema to reference the renamed VaultItem model.

    Search and replace in prisma/schema.prisma:
    - `vaultFiles: VaultFile[]` -> `vaultItems: VaultItem[]` (in Workspace, LegalRequest, User)
    - `vaultFileFolders: VaultFileFolder[]` -> `vaultItemFolders: VaultItemFolder[]` (in Folder)
    - `vaultFileTags: VaultFileTag[]` -> `vaultItemTags: VaultItemTag[]` (in Tag)

    Ensure all relation field names are consistent.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma generate --no-engine</automated>
  </verify>
  <acceptance_criteria>
    - All vaultFiles references updated to vaultItems
    - All vaultFileFolders references updated to vaultItemFolders
    - All vaultFileTags references updated to vaultItemTags
    - prisma validate passes
  </acceptance_criteria>
  <done>All inverse relations updated for VaultItem rename</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {DB} | New nullable columns are backward compatible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04 | Tampering | New FK columns | mitigate | Nullable columns allow gradual migration |
| T-85-05 | Integrity | Partial unique index | mitigate | Prevents duplicate current assignments |
</threat_model>

<verification>
1. Run `npx prisma validate` - schema must be valid
2. Run `npx prisma generate --no-engine` - client must generate
3. Run `npx prisma migrate dev --name expand_columns` - migration created
4. Verify all relations are properly linked
</verification>

<success_criteria>
LegalRequest.matterTypeId FK added and indexed.
RequestAssignment.isCurrent and endedAt added with partial unique index.
VaultFile renamed to VaultItem with fileId FK added.
VaultFileFolder and VaultFileTag renamed.
All inverse relations updated.
Prisma schema validates and generates successfully.
</success_criteria>

<output>
Create `.planning/phases/85-database-schema-improvement/85-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New Fields Added
- `LegalRequest.matterTypeId String?`
- `LegalRequest.matterType MatterType? @relation`
- `RequestAssignment.isCurrent Boolean @default(true)`
- `RequestAssignment.endedAt DateTime?`
- `VaultItem.fileId String?`
- `VaultItem.file File? @relation`

### Model Renames
- `VaultFile` -> `VaultItem`
- `VaultFileFolder` -> `VaultItemFolder`
- `VaultFileTag` -> `VaultItemTag`

### New Indexes
- `LegalRequest.matterTypeId`
- `RequestAssignment.requestId, kind, isCurrent`
- `RequestAssignment.isCurrent`
- `VaultItem.fileId`

### New Partial Unique Index (via SQL)
- `uniq_current_assignment (requestId, kind) WHERE isCurrent = 1`

### Migration Files
- `prisma/migrations/YYYYMMDDHHMMSS_expand_columns/`
- `prisma/migrations/YYYYMMDDHHMMSS_rename_vaultfile_to_vaultitem/`
- `prisma/migrations/YYYYMMDDHHMMSS_add_partial_unique_index/`
