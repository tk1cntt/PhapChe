---
phase: 260527-9ud-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/admin/components/admin-shell.tsx
  - src/app/admin/components/ui.tsx
autonomous: false
requirements:
  - QUICK-260527-9UD
must_haves:
  truths:
    - "Admin pages look less bare and more intentionally laid out without adding product behavior."
    - "Navigation, cards, tables, buttons, and page headers share consistent spacing, borders, shadows, and focus states."
    - "Layout remains responsive for mobile and desktop admin pages."
  artifacts:
    - path: "src/app/globals.css"
      provides: "Global background, typography smoothing, selection, and form/table defaults"
      contains: "body"
    - path: "src/app/admin/components/admin-shell.tsx"
      provides: "Improved admin frame with header, nav, sidebar, and main content spacing"
      exports: ["AdminShell"]
    - path: "src/app/admin/components/ui.tsx"
      provides: "Polished shared UI primitives used by admin pages"
      exports: ["Button", "Badge", "Card", "Table", "PageHeader"]
  key_links:
    - from: "src/app/admin/users/page.tsx"
      to: "src/app/admin/components/ui.tsx"
      via: "shared admin primitives import"
      pattern: "Badge, Button, Card, PageHeader, Table"
    - from: "src/app/admin/requests/page.tsx"
      to: "src/app/admin/components/admin-shell.tsx"
      via: "AdminShell wrapper"
      pattern: "<AdminShell>"
---

<objective>
Cải thiện CSS/layout cho admin foundation đang quá thô.

Purpose: Làm UI nhìn gọn, chuyên nghiệp hơn mà không đổi feature, dữ liệu, workflow, permissions, hay business logic.
Output: Global CSS polish, admin shell polish, shared UI primitive polish.
</objective>

<execution_context>
@D:/Tradingview/.claude/get-shit-done/workflows/execute-plan.md
@D:/Tradingview/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/app/globals.css
@src/app/admin/components/admin-shell.tsx
@src/app/admin/components/ui.tsx
@src/app/admin/users/page.tsx
@src/app/admin/requests/page.tsx

<interfaces>
Shared admin contracts already exist. Keep exports and prop shapes unchanged.

From src/app/admin/components/admin-shell.tsx:
```typescript
export function AdminShell({ children }: { children: ReactNode })
```

From src/app/admin/components/ui.tsx:
```typescript
export function Button({ children, variant = 'primary', type = 'button', disabled = false }: ButtonProps)
export function Badge({ children, tone = 'neutral' }: { children: ReactNode; tone?: keyof typeof badgeStyles })
export function Card({ children, className = '' }: { children: ReactNode; className?: string })
export function Table({ headers, children }: { headers: string[]; children: ReactNode })
export function PageHeader({ title, description, action }: { title: string; description: string; action?: ReactNode })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish global CSS foundation</name>
  <files>src/app/globals.css</files>
  <action>Improve only visual foundation: add font smoothing, subtle background gradient on body, default line-height, selection color, and safe global focus/form/table baseline if needed. Keep existing CSS variables. Do not add new dependencies, animations, routes, feature flags, or business logic.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Global admin pages keep same content but have better typography/background baseline and no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Polish admin shell layout</name>
  <files>src/app/admin/components/admin-shell.tsx</files>
  <action>Refine AdminShell visual layout only: improve header hierarchy, sidebar card treatment, page container spacing, sticky nav polish, active-looking hover/focus states, and main content width. Keep navItems unchanged and keep Vietnamese labels. Do not add route awareness, icons, auth logic, or new admin features.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Admin shell looks structured on desktop and mobile; existing children render unchanged; no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 3: Polish shared UI primitives</name>
  <files>src/app/admin/components/ui.tsx</files>
  <action>Refine Button, Badge, Card, Table, and PageHeader classes only: consistent radius, shadow, border contrast, padding, table row readability, button states, and heading rhythm. Preserve component names, props, variants, tones, and exports. Do not change page data or introduce interactive behavior.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Existing admin pages using shared primitives look more polished while all current imports and props continue working.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CSS/layout polish for existing admin foundation only.</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Open `/admin/users`, `/admin/workspaces`, `/admin/requests`, and `/admin/audit`.
    3. Confirm pages look less bare, spacing is consistent, tables/cards/buttons are readable, and no feature behavior changed.
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu UI ổn, hoặc mô tả chỗ còn xấu.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> admin UI | Visual rendering only; no new data mutation or input trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260527-9UD-01 | T | src/app/admin/components/admin-shell.tsx | accept | CSS/layout-only change; no server state, auth, or workflow logic modified. |
| T-260527-9UD-02 | I | src/app/admin/components/ui.tsx | mitigate | Preserve existing content rendering; do not add hidden fields, external assets, or client-side data fetches. |
</threat_model>

<verification>
Run `npm run typecheck`. Then perform human visual check across admin pages because layout quality requires visual review.
</verification>

<success_criteria>
- Only CSS/className/layout polish changes.
- No business logic, routes, data arrays, workflow rules, permissions, or database code changed.
- Admin pages look materially less bare and remain responsive.
- `npm run typecheck` passes.
</success_criteria>

<output>
After completion, create `.planning/quick/260527-9ud-c-n-b-sung-th-m-css-layout-x-u-qu/260527-9ud-SUMMARY.md`
</output>
