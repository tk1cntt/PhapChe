---
phase: quick
id: 260605-ju6
type: execute
wave: 1
description: "Capture screenshots of all feature pages and fix any errors"
autonomous: true
files_modified:
  - "scripts/screenshot-all.mjs"
  - ".planning/quick/260605-ju6-capture-screenshots/screenshots/*.png"
  - ".planning/quick/260605-ju6-capture-screenshots/SCREENSHOT-REPORT.md"
---

<objective>
Take screenshots of every page route in the application. Start the dev server, navigate to each page, capture the rendered output with timestamps. Report any runtime errors, missing pages, or rendering issues. Generate a consolidated report at the end.
</objective>

<must_haves>
  <truths>
    - truth: "Screenshots captured for all defined routes"
    - truth: "No render errors detected during screenshot capture (or errors logged for fixing)"
    - truth: "SCREENSHOT-REPORT.md generated with page-by-page results"
  </truths>
  <artifacts>
    - artifact: "scripts/screenshot-all.mjs"
    - artifact: "SCREENSHOT-REPORT.md"
  </artifacts>
  <key_links>
    - link: "SCREENSHOT-REPORT.md -> scripts/screenshot-all.mjs (report references script)"
  </key_links>
</must_haves>

<tasks>
  <task>
    <type>execute</type>
    <files>
      <file>scripts/screenshot-all.mjs</file>
    </files>
    <action>
      Create a Playwright script at `scripts/screenshot-all.mjs` that:
      1. Starts Next.js dev server on port 3000 (with timeout)
      2. Waits for server ready
      3. Visits ALL these routes and screenshots each:
         - `/` (home/landing)
         - `/intake` (intake form)
         - `/admin/audit`
         - `/admin/users`
         - `/admin/workspaces`
         - `/admin/requests`
         - `/admin/ops`
         - `/admin/ops/[requestId]` (use a placeholder ID if no real data)
         - `/admin/routing`
         - `/admin/templates`
         - `/admin/templates/[templateId]`
         - `/admin/vault`
         - `/specialist/requests`
         - `/specialist/requests/[requestId]`
         - `/reviewer/requests`
         - `/customer/requests/[requestId]`
         - `/requests/[requestId]`
      4. Each screenshot: captures full page, includes route name in filename
      5. Captures console errors and logs them
      6. On failure (500, 404, crash): logs the error, captures what's on screen
      7. After all pages: generates SCREENSHOT-REPORT.md with:
         - Summary table (Route | Status | Screenshot | Notes)
         - Any errors found
         - Timestamps
      8. Shuts down dev server

      Use `@playwright/test` `chromium.launch()` with `headless: true`.
      Write screenshots to `.planning/quick/260605-ju6-capture-screenshots/screenshots/`.
    </action>
    <verify>
      - SCREENSHOT-REPORT.md exists
      - At least 10 screenshot PNGs exist
      - Report covers all routes listed above
    </verify>
    <acceptance_criteria>
      <criterion>scripts/screenshot-all.mjs file exists</criterion>
      <criterion>SCREENSHOT-REPORT.md lists all routes with status</criterion>
      <criterion>At least 10 .png files in screenshots/ directory</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  - Run `ls .planning/quick/260605-ju6-capture-screenshots/screenshots/*.png | wc -l` to confirm screenshot count
  - Check SCREENSHOT-REPORT.md exists
</verification>
