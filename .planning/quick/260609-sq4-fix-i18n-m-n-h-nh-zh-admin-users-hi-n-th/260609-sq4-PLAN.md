---
gsd_quick: true
quick_id: 260609-sq4
slug: fix-i18n-m-n-h-nh-zh-admin-users-hi-n-th
description: Fix i18n: Màn hình /zh/admin/users hiển thị tiếng Việt không dấu. Dịch đầy đủ các ngôn ngữ
date: 2026-06-09
status: planned
must_haves:
  - translations:
      - vi.json has AdminUsers section with all user management labels
      - zh.json has AdminUsers section translated to Chinese
      - en.json has AdminUsers section translated to English
      - ja.json has AdminUsers section translated to Japanese
  - component:
      - UsersPageClient.tsx uses useTranslations hook instead of hardcoded text
  - verified:
      - All 4 language files have consistent AdminUsers keys
      - Vietnamese text has proper diacritics (không có "Khach hang" mà phải là "Khách hàng")
---

# Quick Plan: fix-i18n-admin-users

## Task 1: Add missing i18n keys for admin users page

**files:**
- `src/messages/vi.json`
- `src/messages/zh.json`
- `src/messages/en.json`
- `src/messages/ja.json`

**action:**
Add `AdminUsers` section to all locale files with keys: pageTitle, pageDescription, systemRoles, roleChangeNote, createUserButton, and per-role labels (customer, specialist, reviewer, coordinator_admin, super_admin).

**verify:**
- All 4 files have AdminUsers section with same keys
- Vietnamese text has proper diacritics (e.g., "Khách hàng", "Điều phối viên", "Tạo người dùng")
- Chinese text properly translated (e.g., "用户管理", "客户", "专员")

**done:**
- vi.json, zh.json, en.json, ja.json all have consistent AdminUsers keys

---

## Task 2: Update UsersPageClient to use i18n

**files:**
- `src/app/admin/users/UsersPageClient.tsx`

**action:**
1. Import `useTranslations` from `@/i18n`
2. Replace hardcoded text with `t('AdminUsers.xxx')` calls
3. Replace role label array with translations

**verify:**
- Component uses `const t = useTranslations()` hook
- All hardcoded Vietnamese text replaced with translation keys
- Role labels come from `t('AdminUsers.role_customer')` etc.

**done:**
- UsersPageClient.tsx no longer has hardcoded Vietnamese text without diacritics

---

## Task 3: Verify all locales are complete and consistent

**files:**
- All 4 locale files

**action:**
Check that each locale file has:
1. All existing keys preserved
2. AdminUsers section with all required keys
3. Vietnamese has proper diacritics (no "Khach hang", "Dieu phoi vien")
4. Each language properly translated (not copied from Vietnamese)

**verify:**
- grep for common Vietnamese diacritic errors in vi.json
- Verify zh.json has Chinese characters, not Vietnamese
- Verify ja.json has Japanese characters

**done:**
- All 4 locale files are complete and consistent
