---
gsd_quick: true
quick_id: 260609-t1h
slug: fix-3-issues-1-zh-admin-users-still-show
description: Fix 3 issues: 1) /zh/admin/users still shows Vietnamese without diacritics, 2) Create demo data, 3) Admin dashboard left menu not displayed
date: 2026-06-09
status: planned
must_haves:
  - AdminUsersTable.tsx uses useTranslations for all column headers
  - API returns translated status labels via i18n
  - Admin layout has NextIntlClientProvider for [locale]/admin routes
  - Demo data is seedable via Prisma seed script
---

# Quick Plan: fix-3-admin-issues

## Task 1: Fix AdminUsersTable.tsx to use i18n

**files:**
- `src/app/admin/users/AdminUsersTable.tsx`
- `src/messages/vi.json` (needs TableUsers section)

**action:**
1. Import `useTranslations` from `next-intl`
2. Add `TableUsers` section to locale files with keys: name, email, role, workspace, status, active, inactive
3. Replace hardcoded Vietnamese text with translation keys:
   - "Ten" → t('TableUsers.name')
   - "Email" → t('TableUsers.email')
   - "Vai tro" → t('TableUsers.role')
   - "Workspace" → t('TableUsers.workspace')
   - "Trang thai" → t('TableUsers.status')
   - Role labels → t('AdminUsers.role_xxx')
   - Status labels → t('TableUsers.active') / t('TableUsers.inactive')

**verify:**
- grep for "Khach hang", "Ten", "Vai tro" in AdminUsersTable.tsx returns nothing
- Table columns use t() function

**done:**
- AdminUsersTable.tsx uses useTranslations hook

---

## Task 2: Create [locale]/admin/layout.tsx with i18n provider

**files:**
- `src/app/[locale]/admin/layout.tsx` (new file)

**action:**
1. Create layout that wraps admin routes with NextIntlClientProvider
2. Import and render the existing admin layout
3. Pass children through

**verify:**
- File exists at src/app/[locale]/admin/layout.tsx
- Contains NextIntlClientProvider wrapping children

**done:**
- [locale]/admin routes have i18n context

---

## Task 3: Create demo data seed script for users

**files:**
- `prisma/seed.ts` (update if exists) or `scripts/seed-users.ts`

**action:**
1. Create seed script that creates demo users with various roles
2. Use prisma client to insert test data
3. Include users: admin, specialist, reviewer, customer

**verify:**
- Script runs without errors: `npx tsx prisma/seed.ts` or similar
- API /api/users returns demo data

**done:**
- Demo user data is seedable

---

## Task 4: Update API to use translation-ready status

**files:**
- `src/app/api/users/route.ts`

**action:**
1. Return status as translation key instead of hardcoded text
2. Change "Dang hoat dong" → "active"
3. Change "Vo hieu hoa" → "inactive"

**verify:**
- API returns "active"/"inactive" instead of Vietnamese

**done:**
- API status is i18n-ready
