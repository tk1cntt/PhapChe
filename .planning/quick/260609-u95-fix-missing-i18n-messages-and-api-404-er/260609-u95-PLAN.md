---
description: Fix missing i18n messages and API 404 errors
status: in-progress
must_haves:
  - src/app/admin/audit/page.tsx wrapped in [locale]/admin/ with i18n context
  - src/app/admin/requests/page.tsx wrapped in [locale]/admin/ with i18n context
  - API calls updated to include locale prefix /{locale}/api/...
  - No IntlError messages in console
  - API calls return 200 instead of 404
---

## Tasks

### Task 1: Add i18n context to admin pages

**Problem:** Admin pages at `src/app/admin/` are outside `[locale]` folder, so `useTranslations()` fails.

**Solution:** 
1. Create `src/app/[locale]/admin/audit/page.tsx` - same content as current, but inside locale context
2. Create `src/app/[locale]/admin/requests/page.tsx` - same content as current
3. Create `src/app/[locale]/admin/layout.tsx` - to provide page structure
4. Create `src/app/[locale]/admin/page.tsx` - redirect or overview page
5. Delete old `src/app/admin/` folder

**verify:** `npm run build` passes, IntlError gone from console

### Task 2: Fix API calls to include locale prefix

**Problem:** Frontend calls `/api/ops` and `/api/users` but middleware rewrites to `/en/api/ops`.

**Solution:**
1. Update `src/app/[locale]/admin/ops/OpsPageClient.tsx` - fetch `/{locale}/api/ops`
2. Update `src/app/[locale]/admin/users/UsersPageClient.tsx` - fetch `/{locale}/api/users`

**verify:** Network calls to `/en/api/ops` return 200

### Task 3: Create e2e test for admin i18n and API

**verify:** Test passes showing no IntlError and API returns data

---

## Files to modify
- `src/app/[locale]/admin/audit/page.tsx` (new)
- `src/app/[locale]/admin/requests/page.tsx` (new)
- `src/app/[locale]/admin/ops/OpsPageClient.tsx`
- `src/app/[locale]/admin/users/UsersPageClient.tsx`

## Files to delete
- `src/app/admin/` (entire folder, moved to [locale])

## Done criteria
- [ ] No `IntlError: MISSING_MESSAGE` in console
- [ ] Admin audit page loads with correct translations
- [ ] Admin requests page loads with correct translations
- [ ] API calls return 200 with data
