---
gsd_quick: true
quick_id: 260613-34v
description: Form /create CSS styling and file upload JSON error fix
status: in_progress
created: 2026-06-12
tasks: 2
must_haves:
  - Fixed API endpoint path in CreateRequestForm.tsx
  - Added CSS styles for step 3 (documents) and step 4 (review)
  - Test file upload works correctly
---

# Quick Plan: Form /create CSS Styling and File Upload Fix

## Issues Identified

1. **JSON Error**: Frontend calls `/api/v2/intake/attach-file` but API is at `/api/intake/attach-file`
2. **Missing CSS**: Step 3 (documents) and Step 4 (review) elements lack CSS styles

---

## Task 1: Fix API Endpoint Path

**File:** `src/components/create-request/CreateRequestForm.tsx`

**Action:**
- Change line 98: `/api/v2/intake/attach-file` → `/api/intake/attach-file`
- Verify `/api/intake/submit` path (line 150) is correct

**Verify:** Search for both API calls, confirm paths match actual routes

---

## Task 2: Add Missing CSS Styles

**File:** `src/components/create-request/create-request.css`

**Action:** Add CSS for:
- `.upload-zone` - Upload dropzone styling
- `.documents-step` - Step 3 container
- `.uploaded-files-list` - File list container
- `.review-step` - Step 4 container
- `.flex`, `.items-center`, `.justify-between`, `.gap-2`, `.gap-3` - Utility classes (or replace with proper classes)

**Verify:** CSS file contains all required styles

---

## Task 3: Update API Submit Path (if needed)

**File:** `src/components/create-request/CreateRequestForm.tsx`

**Action:**
- Check if `/api/v2/intake/submit` exists or should be `/api/intake/submit`

**Verify:** API path matches actual route
