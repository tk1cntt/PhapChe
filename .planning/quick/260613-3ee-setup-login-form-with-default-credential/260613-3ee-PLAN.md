---
gsd_quick: true
quick_id: 260613-3ee
description: Setup login form with default credentials and auth redirect
status: in_progress
created: 2026-06-12
tasks: 3
must_haves:
  - Middleware passes returnUrl when redirecting to login
  - SignInForm reads returnUrl and redirects correctly after login
  - Login form pre-fills default credentials
---

# Quick Plan: Login Form Default Credentials + Auth Redirect

## Current State

- Middleware redirects unauthenticated users to `/vi/sign-in` (hardcoded)
- SignInForm always redirects to `/vi/dashboard` after login
- No returnUrl handling

## Task 1: Update Middleware to Pass returnUrl

**File:** `src/middleware.ts`

**Action:**
- Change redirect to include `returnUrl` query param
- Use `encodeURIComponent` for safe URL encoding
- Example: `/vi/sign-in?returnUrl=/vi/dashboard`

**Verify:** Check redirect URL format

---

## Task 2: Update SignInForm to Handle returnUrl

**File:** `src/components/auth/SignInForm.tsx`

**Action:**
- Read `searchParams` from URL to get `returnUrl`
- After successful login, redirect to `returnUrl` if present, else `/vi/dashboard`

**Verify:** Login redirects to correct page

---

## Task 3: Add Default Credentials to Login Form

**File:** `src/components/auth/SignInForm.tsx`

**Action:**
- Add default email/password values for demo (pre-fill inputs)
- Use placeholder values that work with seed data

**Verify:** Form inputs are pre-filled on load
