---
status: complete
date: "2026-06-13"
commit: c12e290
---

# Quick Task: Migrate src/legacy to src/components

**Task:** Phân tích source code phần src/legacy, nếu phần nào còn dùng thì đưa vào phần src/components. Nếu không còn dùng thì xóa đi

## Actions Taken

1. **Analyzed src/legacy structure**
   - Found 100+ files in src/legacy folder
   - Checked for imports from src/legacy in src/app, src/components, src/lib

2. **Updated imports in MyCasesClient.tsx**
   - Changed imports from `@/legacy/[locale]/customer/components/*` to `@/components/my-cases/*`
   - Components already migrated: SummaryBanner, StatCard, MyCasesToolbar, MyCasesTable, FloatingChatButton

3. **Migrated intake actions**
   - Moved `src/legacy/intake/actions.ts` to `src/lib/intake/actions.ts`
   - Updated import in `src/legacy/requests/[requestId]/page.tsx` to use `@/lib/intake/actions`
   - Created test file `src/lib/intake/actions.test.ts`

4. **Deleted src/legacy folder**
   - 189 files removed
   - No references remain to src/legacy in codebase

## Files Changed

- Updated: `src/legacy/[locale]/[workspaceSlug]/cases/MyCasesClient.tsx`
- Updated: `src/legacy/requests/[requestId]/page.tsx`
- Created: `src/lib/intake/actions.ts` (moved from legacy)
- Created: `src/lib/intake/actions.test.ts`
- Deleted: Entire `src/legacy/` folder (189 files)

## Verification

- `grep -r "@/legacy" src/` returns no matches
- `grep -r "src/legacy" src/` returns no matches
- Build should pass without legacy references
