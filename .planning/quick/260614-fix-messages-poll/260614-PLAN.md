---
status: in-progress
date: 2026-06-14
must_haves:
  - "Fix poll API không xóa thread-list khi nhận messages mới"
  - "Thêm API endpoint fetch messages theo requestId"
  - "Khi click thread → fetch và hiển thị messages trong msg-list"
  - "Sample messages được link với legalRequestId trong DB"
---

# Quick Task: fix-messages-poll

## Tasks

### Task 1: Fix poll logic không xóa thread-list
**files:** src/components/messages/MessagesClient.tsx
**action:** |
  1. Đọc MessagesClient.tsx, tìm poll handler (line ~59-60)
  2. Sửa `setThreads(data.threads)` thành MERGE logic:
     ```tsx
     setThreads(prev => {
       const newThreads = data.threads.filter(t => !prev.find(e => e.id === t.id));
       return [...newThreads, ...prev];
     })
     ```
  3. Đảm bảo poll chỉ THÊM threads mới, không xóa threads cũ
**verify:** |
  - Kiểm tra http://localhost:3000/vi/messages
  - Sau 10s poll, thread-list không bị xóa
**done:** Thread-list giữ nguyên sau poll, threads mới được thêm vào đầu danh sách

### Task 2: Tạo API endpoint fetch messages theo requestId
**files:** src/app/api/messages/[requestId]/route.ts
**action:** |
  1. Tạo GET /api/messages/[requestId]/route.ts
  2. Prisma query: `prisma.message.findMany({ where: { legalRequestId: params.requestId }, orderBy: { createdAt: 'asc' } })`
  3. Response format: `{ messages: [...] }`
**verify:** |
  - Test GET /api/messages/{requestId} trả về messages
**done:** API endpoint hoạt động, trả về messages array

### Task 3: Wire click handler để fetch và hiển thị messages
**files:** src/components/messages/MessagesClient.tsx
**action:** |
  1. Trong handleSelectThread, gọi GET /api/messages/{threadId}
  2. Set messages state với data trả về
  3. Hiển thị messages trong msg-list panel
**verify:** |
  - Click vào thread → msg-list hiển thị nội dung
**done:** Click vào thread hiển thị >=1 message trong msg-list

### Task 4: Seed messages link với legalRequestId
**files:** prisma/seed-messages.ts
**action:** |
  1. Kiểm tra các requests đang có trong workspace
  2. Update/Create messages với legalRequestId field
  3. Sample 3-5 messages cho mỗi thread
**verify:** |
  - Query DB: `SELECT legalRequestId, COUNT(*) FROM Message GROUP BY legalRequestId`
**done:** DB có messages cho ít nhất 3 threads trong workspace

---

## Verification Criteria
- [ ] Thread-list không bị xóa sau khi poll
- [ ] Click thread hiển thị nội dung messages
- [ ] Messages mới được append vào thread đang active
- [ ] DB có sample messages cho các threads
