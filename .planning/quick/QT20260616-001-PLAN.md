---
slug: admin-user-detail
quick_id: QT20260616-001
mode: quick-full
status: in-progress
date: 2026-06-16
commit: 
must_haves:
  - "/vi/admin/users/[id] page with user activity dashboard"
  - "Link from /vi/admin/users table to user detail page"
  - "User activity feed with timestamps"
  - "User stats panel (organizations, workspaces, open cases, partners, documents, risk)"
  - "Sidebar with profile, status, partners, permissions, SLA/risk, timeline"
  - "Sample data seeding for all users"
---

# Quick Task: Admin User Detail Page & Sample Data

## Task Description

Build `/vi/admin/users/[id]` page based on mock UI `layout/user-activity.html`. Add link from `/vi/admin/users` table. Seed sample data for all users.

## Tasks

### Task 1: Create User Detail API Endpoint

**Files:**
- `src/app/api/admin/users/[id]/route.ts`

**Action:**
Create API endpoint `/api/admin/users/[id]` that returns:
- User details with membership info
- Activity feed from audit_events table
- Stats (orgs, workspaces, open cases, partners, documents)
- Related partners from assignments
- Permissions from memberships
- SLA/risk metrics

**Verify:** API returns user data with all fields needed for UI

**Done:** When endpoint returns complete user profile with activity feed

---

### Task 2: Create User Detail Page

**Files:**
- `src/app/[locale]/admin/users/[id]/page.tsx`
- `src/components/admin/UserActivityClient.tsx`
- `src/components/admin/user-activity-client.css`

**Action:**
Create page matching mock UI layout:
- Hero section with user avatar, name, status badge, health card
- 6 stat cards (Organizations, Workspaces, Open cases, Partners, Documents, Risk)
- Tabs for filtering (All, Hồ sơ, Organization, Workspace, Partner, Tài liệu, Permission, Risk)
- Activity feed panel with feed items
- Requests table panel
- Org/Workspace access panel
- Documents panel
- Sidebar: Profile card, User status, Related partners, Permission scope, SLA & Risk, Timeline

**Verify:** Page renders with all mock UI sections

**Done:** When page displays user detail matching mock UI layout

---

### Task 3: Add Link from Users Table

**Files:**
- `src/components/admin/UserTable.tsx`

**Action:**
Update action column in UserTable to link to `/vi/admin/users/[id]`

**Verify:** Clicking action opens user detail page

**Done:** When user can click from list to detail

---

### Task 4: Seed Sample Data for All Users

**Files:**
- `prisma/seed-user-activity.ts`

**Action:**
Seed for each user:
- Audit events (activity feed)
- Request assignments
- Document uploads
- Partner interactions
- Workspace memberships with varied roles

**Verify:** Users have rich activity data

**Done:** When all users have sample activity in database
