// Re-exports from shared libs
// This file provides a clean import path for v2

// Prisma client
export { prisma } from '@/lib/prisma'

// i18n utilities
// Note: Import from 'next-intl/server' and 'next-intl' directly in components

// Auth utilities
// Note: Import from '@/lib/auth' directly where needed

// Types
// Note: Import types directly from their source modules
